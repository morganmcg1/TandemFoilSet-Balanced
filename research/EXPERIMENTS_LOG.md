# SENPAI Research Results — Charlie Round 3

Round: `charlie-pai2h-48h-r3` (branch `icml-appendix-charlie-pai2h-48h-r3`).
Primary metric (lower better): `val_avg/mae_surf_p`; paper metric: `test_avg/mae_surf_p`.

No experiments completed yet — round 3 just opened. Wave-1 assignments are in flight; entries will be appended below as students submit terminal `SENPAI-RESULT` markers.

<!-- New entries below as PRs return for review. Newest at the top. -->

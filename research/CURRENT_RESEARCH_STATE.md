# SENPAI Research State

- **Last updated:** 2026-05-14 21:15 (Loop 104 complete: **#2989 frieren AdamW + RFF-OFF 2-arm ablation on max_norm=0.35 CLOSED — NEW ABLATION-CONTRIBUTION-QUANTIFICATION class CHARACTERIZED as 9th transfer-pattern class (FIRST member of class)**. Arm 1 AdamW (`74w1tfgd`) SWA val **67.28** +22.13 / test 58.66 +20.03 **CATASTROPHIC-REGRESSION**, Arm 2 RFF-OFF (`t9le3ecq`) SWA val **47.65** +2.50 / test 39.78 +1.14 **MODERATE-REGRESSION** vs current #2674 baseline. Methodology flag: SENPAI-RESULT NOT posted by student; advisor used W&B data directly. **SIX PAPER-PUBLISHABLE/STRENGTHENING FINDINGS** (banked #277-#282): (#277) **Lion-removal causes CATASTROPHIC regression (val +22.13)** — first direct measurement of Lion's contribution; Lion is NON-NEGOTIABLE on tandem-foil CFD surrogate; confirms #2063 Lion-merge was the headline-bracket-defining contribution; (#278) **σ-collapse mechanism CONFIRMED via Arm 1 AdamW** — σ-spread collapses to 0.163 (−66% vs baseline 0.475); FIRST cross-axis re-confirmation of #2311 hypothesis via ablation; the hybrid Lion+AdamW(σ) split is NECESSARY; σ-spread invariance bank UPDATED with **1 σ-head-optimizer-ablation BREAK NEW**; (#279) **RFF-OFF moderate regression (val +2.50 / test +1.14)** — RFF ablation-contribution ~9× smaller than Lion's; pre-clip grad_norm_max 148.89 vs baseline 82.36 = ×1.81 grad-magnitude expansion; saturated-clip max_norm=0.35 normalizes the post-clip step (banked #268 confirmation); (#280) **NEW ABLATION-CONTRIBUTION-QUANTIFICATION class CHARACTERIZED as 9th transfer-pattern class** — first systematic measurement of merged-stack feature contributions: Lion = +22.13 (CATASTROPHIC), RFF = +2.50 (MODERATE); (#281) clip_fraction=1.000 17th cross-axis confirmation across both ablations; (#282) banked invariance confirmations preserved — SWA window 2-epoch 15th-consecutive truncation #170, step-time within 4%, SWA entry epoch 12 on both arms — SWA-window form is structurally OPTIMIZER-INVARIANT at fixed forward-pass shape. Paper-appendix matrix UPDATED to **20 closed × 9 transfer patterns + 2 MIXED axes + 1 PROVISIONAL class** (9th transfer-pattern class characterized). σ-spread invariance bank: **18 INVARIANT + 4 forward-pass-shape BREAKs + 1 NEARLY-INVARIANT-MONOTONE-TREND + 1 batch-size BREAK + 1 compose-induced BIDIRECTIONAL BREAK + β-axis monotone-in-1/β + seed-axis 18th + 1 σ-head-optimizer-ablation BREAK NEW**. → frieren reassigned **PR #3037 NEW Kendall σ OFF ablation 2-arm with surf_weight sweep {10, 20} on max_norm=0.35** — completes 3rd member of ABLATION-CONTRIBUTION-QUANTIFICATION class characterization; Arm 1 surf_weight=10 (fallback), Arm 2 surf_weight=20 (higher surface emphasis); discriminates Kendall's main contribution as per-channel-uncertainty-learning (NOT recoverable via fixed weights) vs surface-channel-emphasis (recoverable via surf_weight tuning); no train.py edit required. In-flight after Loop 104: #3005 edward max_norm sweep, #3011 askeladd n_hidden, #3016 alphonse grad_accumulation, #3018 fern β EXTEND-BRACKET, #3023 thorfinn CO-DIRECTIONAL compose, #3024 nezuko hkl LOWER bracket, #3030 tanjiro seed CI cross-clip-regime, **#3037 frieren Kendall σ OFF ablation NEW**. **8 students active; zero idle GPUs.**) | Prior 21:00 — (Loop 103 complete: **#3004 tanjiro 3-seed CI confirmation on current saturated-clip max_norm=0.35 baseline CLOSED — paper-strengthening high seed-variance flag close + FIRST direct measurement of saturated-clip × seed-CI interaction** (NOT a new paper-appendix axis closure; seed-axis already closed at #2790 DEPENDENT-NEGATIVE). Arm 1 seed=1 (`efk04nn5`) SWA val **47.0433** +1.89 / test 40.6281 +1.99; Arm 2 seed=2 (`96h8stqu`) SWA val **47.9525** +2.80 / test 40.6701 +2.03 vs current #2674 baseline — **both arms > +1.89 val regression → Prediction 4 high seed-variance flag confirmed**. **SIX PAPER-PUBLISHABLE/STRENGTHENING FINDINGS** (banked #271-#276): (#271) **Seed-CI WIDENS at saturated-clip baseline vs prior #2311** — mean ± std = 46.72 ± 1.43 across {0, 1, 2}, +2.80 val spread seed=0 to seed=2; substantially wider than expected ±0.2-0.5 seed-noise band; mechanism hypothesis: at clip_fraction=1.000 saturated-clip every step is clip-bounded so effective LR is gradient-DIRECTION-driven not magnitude-driven; initial state (seed-determined) dominates trajectory through loss landscape → larger inter-seed final-state divergence; FIRST direct measurement of clip-regime × seed-CI interaction; (#272) **DEPENDENT-NEGATIVE seed pattern CONFIRMED twice across baselines** — seed=0 empirically lucky pick at #2790 prior baseline + #3004 saturated-clip baseline; magnitude of asymmetry GROWS at saturated-clip (~+2 val now vs ~+0.5 val at #2790); structural replication of DEPENDENT-NEGATIVE seed-axis pattern; (#273) σ-spread invariance 18th cross-axis confirmation across seed — Arm 1: 0.4787, Arm 2: 0.4797 within baseline 0.475 ± 0.02; (#274) channel ordering 21st cross-axis confirmation across both seeds; (#275) clip_fraction=1.000 16th cross-axis confirmation; saturated-clip invariance preserved across seed; pre-clip grad_norm_max seed-sensitive within ±5 (82.36 → 87.45 → 89.54) — extends banked #268 trajectory-dependence to inter-seed variance; (#276) SWA window 2-epoch 14th-consecutive confirmation #170; methodology-form invariant across seed — all 3 runs hit 30-min cap at 13/15 epochs with SWA entering at 1-indexed epoch 12; step-time ~140-143 s/epoch within 2%. Headline stays at 45.1538 / 38.6367 (seed=0, `ieu1futo`) for paper-appendix row — with explicit caveat noting wider-than-expected seed CI at saturated-clip (mean ± std = 46.72 ± 1.43 across {0, 1, 2}). → tanjiro reassigned **PR #3030 NEW cross-clip-regime seed CI test {seed=1, seed=2} on max_norm=0.5 unsaturated stack** to discriminate saturated-clip-induced wide CI hypothesis (#271) from intrinsic wide CI hypothesis — directly tests whether the wide CI at #2674 saturated-clip is induced by max_norm=0.35 (saturated regime) or intrinsic to architecture/data; complements edward's #3005 max_norm sweep (seed=0) and fills seed × max_norm grid with 2 additional cells at (seed=1, max_norm=0.5) + (seed=2, max_norm=0.5); paper-publishable mechanism refinement of #271 either way. **#2989 frieren ablation NOT stale** (Arm 1 AdamW complete val 67.28 / test 58.66 STRONG-REGRESSION, Arm 2 RFF-OFF actively running ~10 min from completion) — left in-flight awaiting Arm 2 SENPAI-RESULT. Paper-appendix matrix UNCHANGED at 20 closed × 8 transfer patterns + 2 MIXED axes + 1 PROVISIONAL class. σ-spread invariance bank: **18 INVARIANT + 4 forward-pass-shape BREAKs + 1 NEARLY-INVARIANT-MONOTONE-TREND + 1 batch-size BREAK + 1 compose-induced BIDIRECTIONAL BREAK + β-axis monotone-in-1/β + seed-axis 18th invariance confirmation**. In-flight after Loop 103: #2989 frieren ablation (Arm 2 in-progress), #3005 edward max_norm sweep, #3011 askeladd n_hidden, #3016 alphonse grad_accumulation, #3018 fern β EXTEND-BRACKET, #3023 thorfinn CO-DIRECTIONAL compose, #3024 nezuko hkl LOWER bracket, **#3030 tanjiro seed CI cross-clip-regime NEW**. **8 students active; zero idle GPUs.**) | Prior 20:45 — (Loop 102 complete: **#2996 thorfinn lr × hybrid_kendall_lr counter-directional compose 2-arm test on max_norm=0.35 CLOSED — NEW COMPOSE-ASYMMETRIC class in compose-tests bank** (NOT a new paper-appendix axis closure; lr-axis already closed at #2731, hkl-axis already closed at #2773). Arm 1 model-DOWN/σ-UP {lr=1.5e-4, hkl=1e-3} SWA val **51.9236** +6.77 / test 43.7529 +5.12 STRONG-REGRESSION compose-residual **+4.47 COMPOSE-COUPLE-NEGATIVE**; Arm 2 model-UP/σ-DOWN {lr=6e-4, hkl=2.5e-4} SWA val **47.0292** +1.875 / test 40.1595 +1.523 regression close compose-residual **−0.125 COMPOSE-DECOMPOSE near-additively**. **SIX PAPER-PUBLISHABLE/STRENGTHENING FINDINGS** (banked #265-#270): (#265) **NEW COMPOSE-ASYMMETRIC class in compose-tests bank** — counter-directional asymmetric coupling; the two LRs NOT decomposable in general — direction-asymmetric coupling exists; additive-decomposition is only valid in the (model FAST, σ-head SLOW) direction; (#266) **σ-spread invariance can BREAK under axis-COMPOSE in OPPOSITE directions** — Arm 1 INFLATES to 0.756 (+60%), Arm 2 COLLAPSES to 0.031 (−94%); FIRST σ-spread BIDIRECTIONAL BREAK; (#267) **σ-head saturation dynamics mechanism** — σ-heads have SATURATION TIMESCALE; when model-lr / σ-head-lr ratio departs from 1, σ-heads either over-redistribute (fast σ) or under-respond (slow σ); first mechanism-level explanation for hkl-axis sharp local optimum at 5e-4; (#268) **Pre-clip grad_norm TRAJECTORY-DEPENDENT, NOT lr-scaled** — Arm 1 (lr HALF) mean 32.64 vs Arm 2 (lr DOUBLE) mean 3.71 = 8.8× HIGHER counter to i.i.d. expectation; pre-clip grad_norm reflects the model's current basin, not lr scaling; (#269) Lion exp_avg_norm invariant within 6% across 4× lr range — 5th cross-axis confirmation of #194; (#270) 3 cross-axis invariance confirmations preserved + VRAM contamination methodology gap. Compose-tests bank UPDATED: 2 entries, 2 classes — COMPOSE-FAIL #2925 + **COMPOSE-ASYMMETRIC #2996 NEW**. σ-spread invariance bank: 18 INVARIANT + 4 forward-pass-shape BREAKs + 1 NEARLY-INVARIANT-MONOTONE-TREND + 1 batch-size BREAK + **1 compose-induced BIDIRECTIONAL BREAK NEW** + β-axis monotone-in-1/β. **ALSO closed #2442 nezuko stale_wip housekeeping** — OBSOLETE-STACK (σ=0.5/max_norm=0.5 predates #2311 + #2674) + n_head axis already closed at #2901 ASYMMETRIC-V. → thorfinn reassigned **CO-DIRECTIONAL compose test {lr=6e-4, hkl=1e-3} BOTH-UP + {lr=1.5e-4, hkl=2.5e-4} BOTH-DOWN** (#3023 NEW; discriminates Story A rate-mismatch dominant vs Story B joint instability; refines compose-tests bank to 3 entries) and nezuko reassigned **hybrid_kendall_lr LOWER bracket {1e-4, 1.5e-4} on current saturated-clip max_norm=0.35 baseline** (#3024 NEW; tests σ-head saturation transition point per banked #267; potential 3rd MIXED-CLASSIFICATION axis after β + wd if LOWER bracket has different pattern than UPPER #2773 ASYMMETRIC-V). Paper-appendix matrix UNCHANGED at 20 closed × 8 transfer patterns + 2 MIXED axes + 1 PROVISIONAL class since Loop 102 is compose-test bank entry NOT new closure. In-flight after Loop 102: #2989 frieren ablation, #3004 tanjiro 3-seed CI, #3005 edward max_norm sweep, #3011 askeladd n_hidden, #3016 alphonse grad_accumulation, #3018 fern β EXTEND-BRACKET, **#3023 thorfinn CO-DIRECTIONAL compose NEW**, **#3024 nezuko hkl LOWER bracket NEW**. **8 students active; zero idle GPUs.**) | Prior 20:30 — (Loop 101 complete: **#2981 fern huber_beta GAP+EXTEND {0.25, 0.10} on max_norm=0.35 CLOSED — paper-strengthening BROAD-PEAK refinement of NON-MONOTONE-COST-MONOTONE-MECHANISM class + FIRST sub-baseline test point on β-LOWER bracket** (NOT a new axis closure — β-axis already closed at #2919+#2736 as 1st MIXED-CLASSIFICATION axis). Arm 1 GAP-FILL β=0.25 (`55zpckd6`) SWA val **46.1290** +0.98 / test 39.1956 +0.56 STRONG-REGRESSION, Arm 2 EXTEND-DEEPER β=0.10 (`v4xwcraw`) SWA val **45.5466** +0.39 / test **38.5700 −0.07 (FIRST sub-baseline test point on β-LOWER bracket)** — val regresses so does NOT qualify as merge per current decision rule. **SIX PAPER-PUBLISHABLE/STRENGTHENING FINDINGS** (banked #259-#264): (#259) **NON-MONOTONE-COST-MONOTONE-MECHANISM class REFINED as BROAD-PEAK variant** — β=0.25 GAP-FILL (val 46.13) essentially tied with β=0.20 (val 46.23) → broad plateau peak over β ∈ [0.20, 0.25] not single sharp local max; recovery limb plateaus at β ∈ [0.15, 0.10] (~val 45.55); test recovery extends past val plateau; BROAD-PEAK variant becomes canonical example for class; (#260) **FIRST sub-baseline test point on β-LOWER bracket** — β=0.10 test 38.57 vs baseline 38.64 = −0.07; val +0.39 regresses so does NOT qualify as merge but establishes **val and test cost-surfaces DECOUPLE past broad-peak plateau** — direct refutation of "val and test are interchangeable cost-axes" assumption; (#261) **U-curve dataset-level finding STRENGTHENS at β=0.10** — 3/4 test splits improve (geom_rc −0.65, cruise −0.25, re_rand −0.42), single_in_dist absorbs regression (+1.04); STRONGEST U-curve signature on β-LOWER bracket; (#262) **σ-spread MONOTONE in 1/β across all 5 points HIGH-RES** — 0.475, 0.494, 0.525, 0.548, 0.588 strictly monotone; banked #210 σ-spread expansion CONFIRMED AT HIGH RESOLUTION; mechanism quantity MONOTONE despite cost surface NON-MONOTONE; (#263) **Pre-clip grad_norm STRONGLY sub-linear in 1/β at β=0.10** — ×1.42 mean (predicted [×1.44, ×1.62]) vs pure 1/β slope ×3.00; p99 only ×1.11 baseline despite mean ×1.42 — post-clip max_norm=0.35 acts as HARD CEILING on tail; grad_norm distribution COMPRESSES tail-first under increasing 1/β; (#264) 17th cross-axis channel ordering + 15th cross-axis clip_fraction=100% + 15th cross-axis VRAM invariance preserved across BOTH arms. → fern assigned **β=0.05 EXTEND-FURTHEST + β=0.50 CROSS-STACK UPPER 2-arm on current saturated-clip baseline** (#3018 NEW; completes β-axis paper-appendix tableau as canonical MIXED-CLASSIFICATION axis with **full 7-point characterization** {0.05, 0.10, 0.15, 0.20, 0.25, 0.30, 0.50}; Arm 1 β=0.05 tests recovery-limb continuation past β=0.10 — MICRO-WIN candidate if test < 38.50 + val < 45.10, otherwise recovery-floor identification; Arm 2 β=0.50 cross-stack tests whether β-UPPER INDEPENDENT-SYMMETRIC #2736 on PRIOR baseline transfers to saturated-clip; no train.py edit required since --huber_beta is already a CLI flag). Paper-appendix matrix UNCHANGED at 20 closed × 8 transfer patterns + 2 MIXED axes + 1 PROVISIONAL class since Loop 101 is high-resolution characterization refinement NOT new closure. σ-spread invariance bank: 18 INVARIANT + 4 forward-pass-shape BREAKs + 1 NEARLY-INVARIANT-MONOTONE-TREND + 1 batch-size BREAK + **β-axis confirmed MONOTONE-IN-1/β at HIGH RESOLUTION (5-point bracket)** — opposite-category response. In-flight after Loop 101: #2989 frieren ablation, #2996 thorfinn compose, #3004 tanjiro 3-seed CI, #3005 edward max_norm sweep, #3011 askeladd n_hidden, #3016 alphonse grad_accumulation, **#3018 fern β EXTEND-BRACKET NEW**. **8 students active; zero idle GPUs.**) | Prior 20:15 — (Loop 100 complete: **#2992 alphonse batch_size sweep {2, 8} on max_norm=0.35 CLOSED — 20th paper-appendix axis closure as PROVISIONAL BATCH-SIZE/GRADIENT-NOISE class (9th transfer-pattern class once DOUBLE-side surrogate confirms)** — Arm 1 HALF (batch_size=2, `wc8g8pz2`) SWA val **47.5311** +2.38 / test 40.7988 +2.16 STRONG-REGRESSION, Arm 2 DOUBLE (batch_size=8) **OOM at step 1** (94.97 GiB / 96 GB ceiling) vs current #2674 baseline. **Arm 1 > +2.0 val regression per current decision rule + Arm 2 hardware-blocked → 20th axis closure as PROVISIONAL class (1st member; full class characterization pending grad-accumulation surrogate)**. **SEVEN PAPER-PUBLISHABLE/STRENGTHENING FINDINGS** (banked #252-#258): (#252) **Lion sign-step IS gradient-noise-sensitive on tandem CFD meshes** — textbook claim of sign-step noise-invariance REJECTED; pre-clip grad_norm scales ×2.36 vs predicted ×√2=1.41; β1-EMA itself is noisier under high BS-variance, mesh-size variance amplifies BS-dependent grad-noise BEYOND i.i.d. √BS prediction; FIRST direct empirical test of Lion sign-step gradient-noise invariance on CFD-surrogate task with REFUTATION; (#253) **FIRST σ-spread BREAK on a non-forward-pass-shape axis** — σ-spread shift 0.4748 → 0.7602 (+60%) on BS=2 establishes a **5th BREAK class on the BATCH-SIZE/COMPUTE-AXIS family** — σ-spread invariance rule REFINED: invariance for optimizer/schedule/loss/seed axes; BREAKS for 4 forward-pass-shape + 1 batch-size/compute-axis; NEARLY-INVARIANT-MONOTONE-TREND for conditioning-path; (#254) **Channel-asymmetric BS response** — surf_ux precision +74%, surf_uy +54% (DRIVING the spread BREAK), surf_p +0.5% INVARIANT, vol channels |Δ|<7%; FIRST direct measurement of channel-specific BS-sensitivity in Kendall σ-head dynamics; (#255) **BS=8 memory ceiling at 96 GB on saturated-clip baseline** — Arm 2 OOMed at step 1 with 94.97 GiB total; tandem-foil meshes 74K-242K nodes × BS=8 × Transolver KV cache exceeds 96 GB; defines wall-clock × VRAM Pareto frontier; (#256) **Lion exp_avg_norm scales 1.11× at 2× BS (not √2)** — train/exp_avg_norm 0.0097→0.0108=+11% NOT +41%; β2-EMA partially absorbs BS-noise via EMA averaging before sign step; (#257) **METHODOLOGY: tandem-mesh size variance amplifies BS-dependent grad-noise** — pre-clip grad_norm ratio BS=2/BS=4 2.36× mean / 2.57× p50 / 2.19× p99 / 2.44× max consistent across percentiles, reflects mesh-size-variance amplification on 74K-242K node range; (#258) channel ordering 19th + clip_fraction 15th cross-axis invariance confirmations preserved. → alphonse assigned **BS=4 + grad_accumulation_steps=2 effective BS=8 surrogate on current saturated-clip max_norm=0.35 baseline** (#3016 NEW; **completes BATCH-SIZE/GRADIENT-NOISE class characterization** by testing DOUBLE side via surrogate; isolates gradient-noise mechanism from per-step memory cost — same VRAM footprint as BS=4 but lower effective gradient noise; requires train.py edit (~5-10 lines: Config flag + accumulation loop + scale loss by 1/N); predictions: σ-spread COMPRESS toward 0.30-0.40 if AdamW σ-head × BS adaptive-moment mechanism #253 is correct, pre-clip grad_norm DECREASE vs baseline, class designation finalizes once data lands — ASYMMETRIC-V most likely or DEGENERATE if grad-accumulation doesn't capture AdamW adaptive-moment dynamics). Paper-appendix matrix UPDATED to **20 closed × 8 transfer patterns + 2 MIXED axes + 1 PROVISIONAL class**. σ-spread invariance bank: **18 INVARIANT + 5 BREAKs (4 forward-pass-shape + 1 batch-size NEW) + 1 NEARLY-INVARIANT-MONOTONE-TREND**. In-flight after Loop 100: #2981 fern huber_beta GAP+EXTEND, #2989 frieren ablation, #2996 thorfinn compose, #3004 tanjiro 3-seed CI, #3005 edward max_norm sweep, #3011 askeladd n_hidden, **#3016 alphonse grad_accumulation NEW**. **8 students active; zero idle GPUs.**) | Prior 20:00 — (Loop 99 complete: **#2985 askeladd film_mid_dim sweep {32, 128} on max_norm=0.35 CLOSED — 19th paper-appendix axis closure as ASYMMETRIC-V (DOMINANT class 9th member, now 9/19=47%)** — Arm 1 HALF (film_mid_dim=32, 6b407t99) SWA val **48.4749** +3.32 / test 40.8617 +2.22 STRONG-REGRESSION, Arm 2 DOUBLE (film_mid_dim=128, g3wvqbwh) SWA val **46.7537** +1.60 / test 39.7431 +1.11 REGRESSION vs current #2674 baseline. **Both arms regress with 2.08× val asymmetry favoring upper side → ASYMMETRIC-V close (NOT predicted NEW FILM-BOTTLENECK class)**. film_mid_dim is the FIRST conditioning-path axis closed in the paper-appendix matrix. **SIX PAPER-PUBLISHABLE/STRENGTHENING FINDINGS** (banked #246-#251): (#246) **FiLM γ/β capacity-to-modulation scaling mechanism** — FIRST direct measurement of how a conditioning-path hyperparameter modulates conditioning signal magnitude (4× film_mid_dim → 3.68× β-mag, 2.63× γ-mag; β-scaling stronger due to anchor difference; depth-monotone L4>L0); (#247) **5-axis pre-clip grad_norm direction fingerprint EXPANDED** — film_mid_dim ↑→↓ joins as 5th axis; sharpens banked #237: 4 of 5 capacity axes show "↑→↓" direction (n_layers, mlp_ratio, slice_num HALF, film_mid_dim) and only n_head shows "↑→↑"; DOMINANT pattern is capacity-diffuses-gradient; (#248) **NEW σ-spread NEARLY-INVARIANT-MONOTONE-TREND sub-classification** — FIRST conditioning-path axis observation distinct from both INVARIANT and BREAK; bank now 18 INVARIANT + 4 BREAKs + 1 NEARLY-INVARIANT-MONOTONE-TREND; (#249) Lion exp_avg_norm invariance #194 confirmed cross-axis; (#250) 5 banked-invariance confirmations preserved (channel ordering 18th, clip_fraction 15th, VRAM 15th, step-time invariant, SWA-window 12th #170); (#251) film_mid_dim=64 at local optimum, 2.08× asymmetry, no fine-bracket follow-up needed. → askeladd assigned **n_hidden sweep {64, 192} on current saturated-clip baseline** (#3011 NEW; **22nd paper-appendix axis label, COMPLETES the FORWARD-PASS-SHAPE QUINTET** — n_head ASYMMETRIC-V #2901, n_layers ASYMMETRIC-V #2947, slice_num BOTH-LOSE #2962, mlp_ratio BOTH-LOSE #2966, n_hidden NEW; tests model width directly — per-token feature dimension all 4 prior axes depend on; mechanism-distinguishing test for pre-clip grad_norm direction fingerprint — if n_hidden ↑→↑ matches n_head pattern, if ↑→↓ matches mlp_ratio/n_layers pattern; requires train.py code edit to expose `n_hidden` as CLI flag (2 lines: Config + model_config); predicted 5th σ-spread BREAK if mechanism transfers, ASYMMETRIC-V most likely on Arm 1 HALF, BOTH-LOSE with potential SWA-NEVER-ENTERED on Arm 2 LARGER). In-flight after Loop 99: #2981 fern huber_beta GAP+EXTEND, #2989 frieren ablation, #2992 alphonse batch_size, #2996 thorfinn compose, #3004 tanjiro 3-seed CI, #3005 edward max_norm sweep, **#3011 askeladd n_hidden NEW**. **8 students active; zero idle GPUs.**) | Prior 19:45 — (Loop 98 complete: **#2481 edward anneal_epochs=1 single-arm cross-stack on σ=0.5/max_norm=0.5 CLOSED — PAPER-STRENGTHENING CROSS-STACK CLOSE** — NOT a new axis since anneal_epochs already closed at #2877 DEGENERATE-AXIS on saturated-clip max_norm=0.35 baseline as 1st DEGENERATE-AXIS member; **cross-stack DEGENERATE confirmation makes anneal_epochs DOUBLE-STACK DEGENERATE** across (σ=1.0, max_norm=0.35) + (σ=0.5, max_norm=0.5) — 1st paper-appendix axis with cross-stack DEGENERATE confirmation. Single-arm anneal_epochs=1 SWA val **45.2426** +0.0888 / test 38.7441 +0.1074 vs current #2674 baseline (45.1538/38.6367), NOT a merge candidate. Vs prior #2311 baseline: essentially TIED (val +0.0245). **FOUR PAPER-STRENGTHENING FINDINGS** + 1 methodology gap (banked #241-#245): (#241) anneal_epochs DEGENERATE-AXIS class transfers cross-stack — DOUBLE-STACK DEGENERATE — budget × SWA-window-size interaction dominates SWA-axis at 13-epoch budget not LR-ramp speed; (#242) hybrid Lion+AdamW(σ) absorbs SWA-schedule headroom CONFIRMED on σ=0.5 stack — 2nd cross-stack confirmation of partially-overlapping-mechanisms story from #225 (frieren #2484); (#243) Izmailov-collapse direct measurement extends to σ=0.5 stack — |Δtrain_surf|=0.0018 vs |Δtrain_surf|=0.003 (#2484); 3rd direct Izmailov measurement, quantitative regularity |Δtrain_surf| ∈ [0.001, 0.003] at 13-epoch budget; (#244) σ-spread invariance #18 confirmed — schedule-axis orthogonal to σ-collapse fix #2311 — bank now 18 confirmations + 4 BREAKs (all forward-pass-shape); (#245) Per-epoch SWA val diagnostic gap 2nd flag — current train.py only evaluates SWA model at end of run, prevents per-averaging-step comparison across SWA-family experiments. → edward assigned **max_norm sweep {0.2, 1.0} on current saturated-clip baseline** (#3005 NEW; **21st paper-appendix axis label, NEW MAX_NORM-CLIP-BOUNDARY transfer-pattern class candidate** — meta-test of the saturated-clip story; Arm 1 TIGHTER max_norm=0.2 effective Lion step 57% of baseline still saturated, Arm 2 LOOSER max_norm=1.0 reverts to unclipped Lion regime clip_fraction expected to drop below 1.000; predicted ASYMMETRIC-V boundary mechanism (most likely) or DEPENDENT-NEGATIVE continuous mechanism (Arm 1 micro-wins); no train.py edit required since --max_norm at line 596). Paper-appendix matrix UNCHANGED at 18 closed × 8 transfer patterns + 2 MIXED axes since Loop 98 is cross-stack confirmation NOT new closure. In-flight after Loop 98: #2981 fern huber_beta GAP+EXTEND, #2985 askeladd film_mid_dim, #2989 frieren ablation, #2992 alphonse batch_size, #2996 thorfinn compose, #3004 tanjiro 3-seed CI, **#3005 edward max_norm sweep NEW**. **8 students active; zero idle GPUs.**) | Prior 19:30 — (Loop 97 complete: **#2463 tanjiro stale_wip CLOSED — STALE-WIP + OBSOLETE-STACK + AXIS-ALREADY-CLOSED housekeeping** — PR was on σ=0.5/max_norm=0.5 OLD stack before #2311 hybrid Lion+AdamW + #2674 saturated-clip max_norm=0.35 merges; swa_lr axis already closed at #2896 ASYMMETRIC-V on current baseline as 5th paper-appendix axis closure; stale >24h with no SENPAI-RESULT; cross-stack data point would be paper-strengthening only with marginal value. Paper-appendix table UNCHANGED at 18 closed × 8 transfer patterns + 2 MIXED axes since NOT a new closure. → tanjiro reassigned to **3-seed CI confirmation on current #2674 baseline (seeds {1, 2})** (#3004 NEW; **NOT a new axis closure** — seed-axis already closed at #2790 DEPENDENT-NEGATIVE; **paper-strengthening CI confirmation** adds 2-extra-seed row to paper-appendix headline 45.1538 / 38.6367; decision rule: either arm <45.10 → MICRO-WIN candidate, both in [45.10, 45.30] → tight CI confirmed, both in [45.30, 45.80] → wider CI noted, either >46.00 → high seed-variance flag; predictions to test: σ-spread invariance across seed (17th confirmation if preserved), channel-ordering invariance across seed refines banked #203, banked methodology #170 continuous-function form at baseline-equivalent shape; no train.py edit required since --seed is already a CLI flag). **All 8 students now have active in-flight PRs; all stale_wip resolved (zero idle GPUs).** In-flight after Loop 97: #2981 fern huber_beta GAP+EXTEND, #2985 askeladd film_mid_dim NEW (19th label), #2989 frieren ablation NEW, #2992 alphonse batch_size NEW, #2996 thorfinn compose NEW, **#3004 tanjiro 3-seed CI NEW**.) | Prior 19:15 — (Loop 96 complete: #2966 thorfinn mlp_ratio sweep {1, 4} on max_norm=0.35 CLOSED — Arm 1 NARROWER (mlp_ratio=1) SWA val **47.9168** +2.76 / test 40.4625 +1.83 per-block FFN capacity insufficiency, Arm 2 WIDER (mlp_ratio=4) SWA val 51.3211 +6.17 / test 43.2060 +4.57 **SWA-NEVER-ENTERED at epoch 12, BASE val = SWA val 51.32** vs current #2674 baseline. Per current decision rule (val < 45.1538 to merge) **both arms regress > +2.0 val → BOTH-LOSE Pareto-cap close**. **18th paper-appendix axis closure** — mlp_ratio joins BOTH-LOSE Pareto-cap class as its **3rd member** after swa_start_frac #2818 + slice_num #2962 — class now 3/18 = 17%. **HEADLINE: FORWARD-PASS-SHAPE TRIO COMPLETE** — attention granularity (n_head ASYMMETRIC-V, slice_num BOTH-LOSE) + composition depth (n_layers ASYMMETRIC-V) + FFN width (mlp_ratio BOTH-LOSE); **emerging pattern: forward-pass-shape axes affecting per-block computational granularity land in BOTH-LOSE; axes affecting coarse-grain structure land in ASYMMETRIC-V**. **THREE PAPER-PUBLISHABLE FINDINGS** + 3 strengthening (banked #235-#240, building on student's #207-#209): (#235) 3rd BOTH-LOSE confirms class spans full forward-pass-shape mechanism-family — 2 of 3 members are forward-pass-shape capacity axes; (#236) σ-spread BREAK extends to FFN-side per-block sub-modules — bank now 17 confirmations + 4 BREAKS all on forward-pass-shape axes spanning all three Transolver per-block sub-modules; (#237) pre-clip grad_norm direction is sub-module-specific (3-way comparison) — n_head ↑→↑, n_layers ↑→↓, mlp_ratio ↑→↓; mlp_ratio=1 NARROWER FFN gives +47% grad_norm_max — capacity-insufficient FFN saturates per-block representation; (#238) channel ordering invariance is DEPTH-specific not capacity-specific — mlp_ratio=1 preserves ordering, n_layers=3 (#2947) broke it; (#239) 3rd SWA-NEVER-ENTERED + per-axis step-time scaling fingerprint — n_layers dataloader-bound, slice_num sub-linear, mlp_ratio compute-bound at mlp_ratio=4; (#240) Transolver compute-fingerprint methodology. → thorfinn assigned **lr × hybrid_kendall_lr counter-directional compose 2-arm test on max_norm=0.35** (#2996 NEW; Arm 1 model-DOWN/σ-UP, Arm 2 model-UP/σ-DOWN; **NEW transfer-pattern class COMPOSE-DECOMPOSE-vs-COUPLE** — 2nd compose-test after #2925, FIRST compose-test of two ASYMMETRIC-V axes, FIRST counter-directional compose; no train.py edit required). In-flight after Loop 96: #2981 fern huber_beta GAP+EXTEND, #2985 askeladd film_mid_dim NEW (19th label), #2989 frieren ablation NEW (NEW ABLATION-CONTRIBUTION-QUANTIFICATION), #2992 alphonse batch_size NEW (NEW BATCH-SIZE/GRADIENT-NOISE), **#2996 thorfinn compose NEW (NEW COMPOSE-DECOMPOSE-vs-COUPLE)**. 8 students active; 1 stale_wip pending host-side recovery (#2463 tanjiro on σ=0.5 stack).) | Prior 19:00 — (Loop 95 complete: #2962 alphonse slice_num sweep {32, 128} on max_norm=0.35 CLOSED — Arm 1 HALF (slice_num=32) SWA val **46.4826** +1.33 / test 39.7821 +1.15 moderate Pareto-cap regression, Arm 2 DOUBLE (slice_num=128) SWA val 403.26 / test 383.63 = **degenerate (SWA-NEVER-ENTERED at epoch 10/15)** vs current #2674 baseline. Per current decision rule (val < 45.1538 to merge) **both arms regress > +2.0 val → BOTH-LOSE Pareto-cap close**. **17th paper-appendix axis closure** — slice_num joins BOTH-LOSE Pareto-cap class as its **2nd member** after swa_start_frac #2818 — class now 2/17 = 12%. **HEADLINE: BOTH-LOSE class spans cross-mechanism-family for first time** — SWA-scheduling (swa_start_frac) + forward-pass-shape (slice_num); class is structurally about wall-clock-budget × capacity-axis Pareto fronts in general. **THREE PAPER-PUBLISHABLE FINDINGS** + 3 strengthening (banked #229-#234): (#229) 2nd BOTH-LOSE member confirms cross-mechanism-family span — class is structurally about wall-clock-budget × capacity Pareto fronts; (#230) 2nd cross-axis σ-spread BREAK — all BREAKs are forward-pass-shape axes (#2901 n_head + #2962 slice_num + #2947 n_layers BOTH directions); banked σ-spread-invariance-rule sharpens to "any forward-pass-shape change breaks σ-spread, direction depends on axis"; (#231) 2nd SWA-NEVER-ENTERED failure mode confirms STRUCTURAL property of forward-pass-shape DOUBLE arms — banked methodology #170 continuous-function form confirmed across 2 independent forward-pass-shape axes (#2947 + #2962); (#232) step-time scaling SUB-LINEAR in slice_num (1.50× across 4× slice change) — quantifies slice-routing fraction of compute as ~17% slope, 5th step-time measurement banked; (#233) FIRST σ-spread INFLATE direction observed — HALF arm +0.092 above baseline; under-capacity forces Kendall reliance on easiest channels = larger log_σ magnitudes; (#234) 18th cross-axis channel-ordering invariance preserved across both arms. → alphonse assigned **batch_size sweep {2, 8} on max_norm=0.35** (#2992 NEW; 18th paper-appendix axis label, NEW **BATCH-SIZE/GRADIENT-NOISE** class — compute-axis but wall-clock-invariant in total BS × steps; tests Lion sign-step gradient-noise-invariance (DEGENERATE) vs batch-noise-sensitivity (ASYMMETRIC-V); also tests hybrid optimizer asymmetry — AdamW σ-head adaptive moments are BS-dependent while Lion sign-step is BS-invariant; no train.py edit required since --batch_size is already a CLI flag). In-flight after Loop 95: #2966 thorfinn mlp_ratio (18th label, NEW FFN-RATIO class), #2981 fern huber_beta GAP+EXTEND, #2985 askeladd film_mid_dim NEW (19th label, NEW FILM-BOTTLENECK class), **#2989 frieren ablation NEW (NEW ABLATION-CONTRIBUTION-QUANTIFICATION class)**, **#2992 alphonse batch_size NEW (NEW BATCH-SIZE/GRADIENT-NOISE class)**. 8 students active.) | Prior 18:45 — (Loop 94 complete: #2484 frieren skip-SWALR cosine-through-SWA-window single-arm CLOSED — SWA val **45.4697** +0.32 / test 38.8216 +0.18 vs current #2674 baseline. Per current decision rule (val < 45.1538 to merge) **single arm does not win** — **PAPER-STRENGTHENING CROSS-STACK CLOSE** (NOT a new axis; SWA family fully characterized at saturated-clip max_norm=0.35 across 4 closed axes #2877/#2896/#2818/#2925). **HEADLINE: FIRST closed-form Lion step-magnitude verification ‖step‖₂ = lr · √n_params (#223) + FIRST direct measurement of Izmailov-collapse at 13-epoch budget (#224)**. **THREE PAPER-PUBLISHABLE FINDINGS** + 3 strengthening (banked #223-#228): (#223) cosine-tail epoch-13 LR is 22% of SWALR-floor (1.30e-5 vs 6e-5) on σ=0.5/max_norm=0.5; Lion step magnitude ratio matches LR ratio EXACTLY at 0.217 per closed-form ‖step‖₂ = lr · √n_params; (#224) |Δtrain_surf_loss|=0.003 between SWA epochs 12 and 13 — Izmailov ensemble-diversity collapse materializes empirically at 13-epoch budget; (#225) skip-SWALR ≈ SWALR-on at this budget — partially-overlapping mechanisms with #2311 hybrid-Kendall (AdamW lr=5e-4 on log_σ heads is NOT cosine-scheduled, absorbing most of what skip-SWALR would offer); (#226) σ-collapse-fix #2311 PRESERVED — spread 0.4734 ≈ baseline 0.475 — hybrid Lion+AdamW(σ) is ORTHOGONAL to SWALR/cosine choice; (#227) per-epoch SWA val not logged in current train.py (diagnostic gap noted for follow-up SWA-family work); (#228) closed-form Lion step magnitude verified across two LR regimes — mechanism transfer with #2390 wd-axis finding #221 confirmed. → frieren assigned **2-arm ablation {optimizer=adamw, fourier_features OFF} on max_norm=0.35** (#2989 NEW; **NEW transfer-pattern class ABLATION-CONTRIBUTION-QUANTIFICATION** as SECONDARY CATEGORY — directly measures gap to merged feature baseline rather than slope around it; Arm 1 quantifies Lion's contribution, Arm 2 quantifies RFF's contribution; no train.py edit required). In-flight after Loop 94: #2962 alphonse slice_num (15th label), #2966 thorfinn mlp_ratio (17th label), #2981 fern huber_beta GAP+EXTEND, **#2985 askeladd film_mid_dim NEW (16th label)**, **#2989 frieren ablation NEW**. 8 students active.) | Prior 18:30 — (Loop 93 complete: #2390 askeladd Lion wd σ=0.5 rebased 2-arm CLOSED — Arm A wd=3e-3 val **45.7094** +0.556 / test 39.4615 +0.825, Arm B wd=1e-2 val **45.7004** +0.547 / test 39.5261 +0.889 vs current #2674 baseline. Both arms barely beat outdated #2168 σ=0.5 baseline (45.7648) by ~0.05-0.06 val (within seed-noise band, 0.01 apart = statistically tied). Per current decision rule (val < 45.1538 to merge) **neither arm wins** — **PAPER-STRENGTHENING CROSS-STACK CLOSE** (NOT a new axis; wd-axis already closed at #2791+#2819). **HEADLINE: wd-axis identified as FIRST TRIPLE-PATTERN AXIS** — DEPENDENT-SYMMETRIC on saturated-clip max_norm=0.35 (#2791+#2819) + DEGENERATE on σ=0.5/max_norm=0.5 (this PR) + ASYMMETRIC-V on σ=1.0/max_norm=0.5 (#2390 pre-rebase). wd-axis is SECOND MIXED-CLASSIFICATION axis after β-axis (β has 2 patterns; wd has 3). **THREE PAPER-PUBLISHABLE FINDINGS** + 3 strengthening (banked #217-#222): (#217) wd × σ substitution mechanism CONFIRMED — at σ=1.0 wd 3e-4→3e-3 buys -0.56 val; at σ=0.5 same push buys only -0.05 val (10× smaller gain); (#218) wd-axis CEILING confirmed on σ=0.5 stack at ~3e-3 (FLAT [3e-3, 1e-2]); (#219) **FIRST OOD-METRIC SIGN-REVERSAL** — camber_rc OOD-gap mechanism REVERSES SIGN at σ=0.5 (wd↑ closes gap by -1.49 at σ=1.0 vs opens gap by +0.96/+0.79 at σ=0.5); (#220) 7th σ-collapse + 2nd wd-can't-rescue confirmation; (#221) Non-shrinkage mechanism REPLICATES on σ=0.5 (param_norm 85.93 vs σ=1.0 88.72, -3.2%; wd-axis is 3rd cross-stack-mechanism-transfer axis after σ #2862 + β #2919); (#222) Arm B's missing param_norm logging recoverable. → askeladd assigned **film_mid_dim sweep {32, 128} on max_norm=0.35** (#2985 NEW; 16th paper-appendix axis label, NEW **FILM-BOTTLENECK** class; tests FiLM conditioning capacity bottleneck dim; compute-invariant; no train.py edit). In-flight after Loop 93: #2962 alphonse slice_num (15th label), #2966 thorfinn mlp_ratio (17th label), #2981 fern huber_beta GAP+EXTEND, **#2985 askeladd film_mid_dim NEW (16th label)**. 8 students active.) | Prior 18:15 — (Loop 92 complete: #2919 fern huber_beta LOWER fine bracket {0.20, 0.15} CLOSED — Arm 1 β=0.20 val **46.2288** +1.0750 / test 39.0759 +0.4392 regression close, Arm 2 β=0.15 val **45.5520** +0.3982 / test 38.7230 +0.0863 just outside directional close ≤45.50. **15th paper-appendix axis closure post-Loop-92 (β LOWER bracket separately classified from β UPPER #2736)**, NEW 8th transfer-pattern class **NON-MONOTONE-COST-MONOTONE-MECHANISM**, β-axis is FIRST MIXED-CLASSIFICATION axis. **THREE PAPER-PUBLISHABLE FINDINGS**: (a) #210 β-driven σ-spread expansion mechanism TRANSFERS CLEANLY from max_norm=0.5 to saturated-clip max_norm=0.35 — quantitative predictions within ±0.025 of actuals on σ-spread and eff_w; β-axis is 2nd cross-clip-regime TRANSFERS-CLEANLY axis after fourier_sigma #2862; (b) #211 U-curve dataset-level finding (banked #66) PRESERVED at β=0.15 on saturated-clip — 3 of 4 test splits IMPROVE (camber_rc −0.37, cruise −0.16, re_rand −0.12), only single_in_dist regresses +1.00; β-axis is FIRST axis confirmed to preserve U-curve across BOTH clip regimes; (c) #212 β-LOWER bracket NON-MONOTONE REGRESSION despite monotone σ-spread mechanism — β=0.20 val 46.23 (local maximum) vs β=0.15 val 45.55 (partial recovery), σ-spread monotone 0.475→0.525→0.548; NEW PAPER-APPENDIX TRANSFER-PATTERN CLASS NON-MONOTONE-COST-MONOTONE-MECHANISM. **#213 Kendall weight REDISTRIBUTE-DOWN direction transfers cleanly** (all 6 channels redistribute DOWN monotonically with β, surf_ux 44.5→39.7→37.7). **#214 pre-clip grad_norm rises SUB-LINEARLY with 1/β** (×1.16, ×1.27 vs predicted ×1.5, ×2.0) — Huber-residual statistics dampen the slope. #215 16th channel-ordering + 14th clip_fraction=1.000 + 14th peak VRAM invariance preserved. **#216 banked #170 continuous-function form CONFIRMED** — 11th-consecutive 2-epoch truncation at baseline-equivalent forward-pass-shape (no shape change → same per-epoch wall-time → same truncation as baseline). 7 banked findings #210-#216. → fern assigned **huber_beta GAP+EXTEND sweep {0.25, 0.10} on max_norm=0.35** (fills β=0.25 gap on the new baseline as MICRO-WIN candidate; extends β LOWER bracket to β=0.10 to test whether β=0.20 local maximum persists or NON-MONOTONE-COST-MONOTONE-MECHANISM pattern continues monotonically deeper; strengthens NEW NON-MONOTONE-COST-MONOTONE-MECHANISM class with additional data points; no train.py edit required). In-flight after Loop 92: #2962 alphonse slice_num (15th label), #2966 thorfinn mlp_ratio (17th label), #2981 fern huber_beta GAP+EXTEND NEW (extends β LOWER bracket {0.25, 0.10}). 8 students active.)
- **Advisor branch:** `icml-appendix-willow-pai2g-48h-r2`
- **Research tag:** `willow-pai2g-48h-r2`
- **Target repo:** `morganmcg1/TandemFoilSet-Balanced` (base branch `icml-appendix-willow`)
- **W&B:** `wandb-applied-ai-team/senpai-charlie-wilson-willow-g-48h-r2`
- **Per-run cap:** `SENPAI_TIMEOUT_MINUTES=30` wall-clock (~13-15 epochs with SWA)
- **Students × GPU:** 8 × 1 (96 GB each)
- **Idle students:** 0 (all 8 active; **#2989 frieren AdamW + RFF-OFF ablation CLOSED Loop 104 → frieren reassigned to Kendall σ OFF ablation 2-arm with surf_weight sweep {10, 20} on max_norm=0.35 (#3037 NEW, completes 3rd member of ABLATION-CONTRIBUTION-QUANTIFICATION class characterization)**, **#3004 tanjiro 3-seed CI CLOSED Loop 103 → tanjiro reassigned to cross-clip-regime seed CI test {seed=1, seed=2} on max_norm=0.5 unsaturated stack (#3030 NEW, discriminates saturated-clip-induced wide CI hypothesis #271 from intrinsic wide CI hypothesis; complements edward's #3005 max_norm sweep)**, **#2996 thorfinn compose CLOSED Loop 102 → thorfinn reassigned to CO-DIRECTIONAL compose test {lr=6e-4, hkl=1e-3} BOTH-UP + {lr=1.5e-4, hkl=2.5e-4} BOTH-DOWN on current saturated-clip baseline (#3023 NEW, discriminates Story A rate-mismatch dominant vs Story B joint instability; refines compose-tests bank to 3 entries)**, **#2442 nezuko stale_wip CLOSED Loop 102 → nezuko reassigned to hybrid_kendall_lr LOWER bracket {1e-4, 1.5e-4} on current saturated-clip baseline (#3024 NEW, tests σ-head saturation transition point per banked #267; potential 3rd MIXED-CLASSIFICATION axis after β + wd)**, **#2981 fern huber_beta GAP+EXTEND CLOSED Loop 101 → fern reassigned to β=0.05 EXTEND-FURTHEST + β=0.50 CROSS-STACK UPPER 2-arm on current saturated-clip baseline (#3018 NEW, completes β-axis paper-appendix tableau as canonical MIXED-CLASSIFICATION axis with full 7-point characterization)**, **#2992 alphonse batch_size sweep CLOSED Loop 100 → alphonse reassigned to BS=4 + grad_accumulation_steps=2 effective BS=8 surrogate on current saturated-clip baseline (#3016 NEW, completes BATCH-SIZE/GRADIENT-NOISE class characterization)**, **#2985 askeladd film_mid_dim sweep CLOSED Loop 99 → askeladd reassigned to n_hidden sweep {64, 192} on current saturated-clip baseline (#3011 NEW, 22nd paper-appendix axis label, COMPLETES FORWARD-PASS-SHAPE QUINTET)**, **#2481 edward anneal_epochs=1 cross-stack CLOSED Loop 98 → edward reassigned to max_norm sweep {0.2, 1.0} on current saturated-clip baseline (#3005 NEW, 21st paper-appendix axis label, NEW MAX_NORM-CLIP-BOUNDARY class)**, **#2463 tanjiro stale_wip CLOSED Loop 97 → tanjiro reassigned to 3-seed CI confirmation on current baseline (seeds {1, 2}) (#3004 NEW, paper-strengthening CI confirmation NOT a new axis)**, **#2484 frieren skip-SWALR CLOSED Loop 94 → frieren reassigned to 2-arm ablation {AdamW, RFF OFF} on max_norm=0.35 (#2989 NEW, NEW ABLATION-CONTRIBUTION-QUANTIFICATION class)**. **All stale_wip resolved; zero idle GPUs.**)
- **⚠ Parser gotcha:** Avoid inline `SENPAI-RESULT:` substring in advisor comments — parser treats any line with that substring as a terminal marker and tries `json.loads` on what follows. Use "terminal-result post" or "SENPAI_RESULT" (underscore) in prose.

## ⭐ Current baseline (PR #2674 merged 2026-05-14 02:06 — max_norm=0.35 on hybrid stack)

- **val_avg/mae_surf_p:** **45.1538** (seed 0, SWA-model)
- **test_avg/mae_surf_p:** **38.6367** (seed 0, SWA-model, 4-split finite)
- Improvement over prior #2311 (45.2181 / 38.7661): val **−0.14%**, test **−0.33%**
- Cumulative improvement vs #1757: val **−32.24%**, test **−33.77%**
- Config: Transolver + FiLM (mid_dim=64) + Huber β=0.3 + Re-weight + Kendall σ + **grad-clip max_norm=0.35** + RFF (16-dim, σ=0.5) + Lion lr=3e-4 wd=3e-4 (model) + AdamW lr=5e-4 wd=0 (log_σ heads, hybrid_kendall_lr=5e-4)
- W&B confirmation run: `ieu1futo`

### Per-split (SWA)

| Split | val | test |
|---|---:|---:|
| single_in_dist | 47.146 | 40.379 |
| geom_camber_rc | 58.002 | 53.068 |
| geom_camber_cruise | 28.887 | 23.285 |
| re_rand | 46.580 | 37.816 |
| **avg** | **45.154** | **38.637** |

### Prior baseline (PR #2311 — hybrid Lion+AdamW)
- val 45.2181 / test 38.7661 (for reference in reviewing pending in-flight PRs)

## ✓ Merged improvements (all-time)

| PR | Slug | Win | Baseline after merge |
|---|---|---|---|
| #1452 | smooth-l1-loss | MSE→Huber | val=100.77, test=90.38 |
| #1554 | swa-on-huber | SWA | val=99.07, test=88.90 |
| #1586 | re-weight | Per-sample Re | val=95.75, test=86.17 |
| #1585 | film-on-huber | FiLM | val=80.82, test=71.30 |
| #1731 | grad-clip-1p0 | max_norm=1.0 | val=74.62, test=66.14 |
| #1831 | max-norm-0p5 | max_norm=0.5 | val=73.81, test=65.04 |
| #1906 | kendall-uncertainty | Kendall σ | val=71.43, test=62.99 |
| #2082 | fourier-coord-features | RFF σ=1.0 | val=70.63, test=62.09 |
| #1757 | beta-0p3-on-rff-kendall | Huber β=0.3 | val=66.66, test=58.32 |
| #2063 | lion-optimizer-on-beta0p3 | Lion lr=3e-4 | val=47.64, test=40.57 |
| #2168 | rff-sigma-0p5-on-lion | RFF σ=0.5 (lower-freq prior) | val=45.76, test=39.66 |
| #2311 | hybrid-adamw-kendall-sigma | Hybrid Lion+AdamW(σ=5e-4), σ-spread restored | val=45.22, test=38.77 |
| **#2674** | **max-norm-0p35-on-hybrid** | **max_norm 0.5→0.35, clip U-curve test minimum** | **val=45.15, test=38.64 ← CURRENT** |

## Current active PRs — Wave 12 (post-hybrid-optimizer baseline 45.22/38.77)

**Decision rule (vs hybrid baseline 45.22/38.77):**
- val < 45.22: **merge** (true compound win on new baseline)
- 45.22–45.76: directional win on old σ=0.5 stack only, doesn't beat new baseline — request changes (composition test on new train.py if mechanism is orthogonal)
- val ≥ 45.76: regression vs σ=0.5 baseline — likely close
- ⚠ **All WIP PRs trained on the OLD codebase** (pre-hybrid-optimizer). If a PR's mechanism is orthogonal to σ-collapse (most SWA/RFF/wd PRs should be), it should compose. If it interacts with Kendall σ dynamics (unlikely but check per-PR), may need rerun on new train.py.

| PR | Student | Status | Mechanism | Notes |
|---|---|---|---|---|
| **#2818** | fern | **CLOSED Loop 54** | swa_start_frac fine-bracket {0.6, 0.85} on max_norm=0.35 baseline | **Paper-strengthening Pareto-optimal finding**: s_f=0.75 baseline co-tuned to 30-min cap; Arm 1 s_f=0.6 +1.77 val (dilution), Arm 2 s_f=0.85 +0.61 val (SWA window→1 epoch); 6 banked findings (#113-#118). fern reassigned → #2862. |
| **#2862** | fern | **CLOSED Loop 58** | fourier_sigma sweep {0.25, 1.0} on max_norm=0.35 baseline | **Paper-strengthening 6th axis closure with ASYMMETRIC-V pattern membership and TRANSFERS-CLEANLY classification**: Arm 1 σ=0.25 +0.56 val directional close, Arm 2 σ=1.0 +1.95 val strong regression; σ=0.5 baseline holds; 3.5× asymmetric reach. 8 banked findings (#132-#139). fern reassigned → next axis (huber_beta — 9th paper-appendix mechanism-transfer axis, LOSS-LANDSCAPE-CURVATURE class). |
| **#2819** | alphonse | **CLOSED Loop 56** | Lion wd LOW bracket {1e-4, 1.5e-4} on max_norm=0.35 baseline | **Paper-strengthening wd-axis tableau closure** (4-point picture: 1e-4 +0.25, 1.5e-4 +0.33, 3e-4 baseline, 1e-3 +1.07, 3e-3 +0.12); **HEADLINE: wd-axis SYMMETRIC basin vs lr-axis ASYMMETRIC V — paper-publishable mechanism-level distinction**. 7-axis table now has 4 distinct transfer patterns. 6 banked findings (#126-#131). alphonse reassigned → next axis. |
| **#2790** | thorfinn | **CLOSED Loop 53** | 2-seed confirmation on max_norm=0.35 baseline | `Seed-sensitive baseline` (cross-seed swa_val stdev=2.54 > 1.5; HIGHER than #2701's 2.05 on OLD baseline — saturated-clip did NOT regularize seed axis); paper-publishable NEGATIVE result; 6 banked findings (#107-#112). thorfinn reassigned → #2835. |
| **#2835** | thorfinn | **CLOSED Loop 55** | fourier_num_features sweep {8, 32} on max_norm=0.35 baseline | **Paper-strengthening RFF-capacity U-curve closure with asymmetric reach**: Arm 1 n=8 +3.17 val (strong regression), Arm 2 n=32 +0.91 val (directional close); test-side near-tie at n=32 (Δtest=−0.06% — first cross-axis where val and test partially disagree). 7 banked findings (#119-#125). thorfinn reassigned → anneal_epochs sweep. |
| **#2877** | thorfinn | **CLOSED Loop 59** | anneal_epochs sweep {1, 3} on max_norm=0.35 baseline | **Paper-strengthening 7th axis closure with NEW DEGENERATE-AXIS transfer pattern**: Arm 1 ae=1 val 45.20 +0.05 directional close, Arm 2 ae=3 val 45.13 −0.02 directional close (BELOW baseline but ABOVE merge threshold). anneal_epochs mechanistically DEGENERATE under saturated-clip's swa_start_frac × swa_lr COINCIDENCE (cosine final lr ~5.9e-5 ≈ swa_lr 6e-5). 7 banked findings (#140-#146). thorfinn reassigned → swa_lr sweep (#2896). |
| **#2880** | alphonse | **CLOSED Loop 61** | Lion β1 sweep {0.85, 0.95} on max_norm=0.35 baseline | **Paper-strengthening 8th axis closure (DEPENDENT-SYMMETRIC class, joins wd)**: Arm 1 β1=0.85 val 48.92 +3.76 strong regression, Arm 2 β1=0.95 val 49.65 +4.49 strong regression. Lion β1=0.9 is at SHARP val minimum (symmetric basin, narrow width). Mechanism: **sign-sequence stationarity**. Does NOT open new transfer pattern. METHODOLOGY CRITIQUE banked: `last_update_sq_norm` NOT a β1 discriminator (sign-vector L2 norm ≈ N_params regardless of β1). 8 banked findings (#147-#154). alphonse reassigned → n_head sweep (#2901). |
| **#2901** | alphonse | **CLOSED Loop 68** | n_head sweep {2, 8} on max_norm=0.35 baseline | **Paper-strengthening 11th axis closure with NEW 6th ASYMMETRIC-V member (DOMINANT pattern 55% of closed axes)**: Arm 1 n_head=2 val 45.47 +0.31 directional close upper edge; Arm 2 n_head=8 val **392.67** +347.51 CATASTROPHIC SWA DIVERGENCE (base val=56.66 at ep10 still descending → SWA compounded 7× worse). **TWO HEADLINE FINDINGS**: (a) σ-spread invariance BREAKS for first time (Arm 1: 0.217, Arm 2: 0.090 vs baseline 0.475 — refines invariance as downstream-of-attention); (b) pre-clip grad_norm distribution SHIFTS for first time (Arm 1: 9.73 +84%, Arm 2: 5.91 +12% vs baseline 5.30 — refines banked #45 as not-forward-pass-shape). clip_fraction=1.000 STILL holds (saturated-clip decouples from forward-pass-shape, 11th cross-axis confirmation). NEW MECHANISM CLASS: SWA-DIVERGENCE-ON-UNDERCONVERGED-BASE. Cruise vs camber_rc preference reversal on Arm 1. n_head=8 has +50% step-time / +17GB VRAM despite identical total params. 9 banked findings (#172-#180). alphonse reassigned → Lion β2 sweep. |
| **#2887** | fern | **CLOSED Loop 65** | dropout sweep {0.05, 0.15} on max_norm=0.35 baseline | **Paper-strengthening 9th axis closure with NEW MONOTONIC-REGRESSIVE (one-sided regularization) 7th transfer pattern class**: Arm 1 d=0.05 val 46.05 +0.90, Arm 2 d=0.15 val 46.36 +1.20 — both regress monotonically (×1.34 per ×3 dropout ratio). Mechanism: dropout-induced sign-flips at OPTIMIZER level (post-grad-norm) accumulate as low-rate gradient-direction errors that Lion's β1=0.9 EMA does not fully de-noise within 13 epochs. KEY FINDINGS: (a) clip_fraction=1.000 PRESERVED despite forward-pass stochasticity mechanism (9th cross-axis confirmation, banked #156), (b) pre-clip grad_norm distribution NEW 9th cross-axis invariant (banked #157), (c) σ=0.25 strictly better cruise regularizer than dropout (banked #158), (d) weight-space (wd, benign) vs activation-space (dropout, regressive) stochasticity paper-publishable distinction (banked #159). 8 banked findings (#155-#162). fern reassigned → huber_beta LOWER fine bracket. |
| **#2896** | thorfinn | **CLOSED Loop 66** | swa_lr sweep {1.5e-5, 1.5e-4} on max_norm=0.35 baseline | **Paper-strengthening 10th axis closure with NEW 5th ASYMMETRIC-V member (DOMINANT pattern 50% of closed axes)**: Arm 1 swa_lr=1.5e-5 val 45.30/test **38.6172 (Δtest=−0.020 sub-noise marginal win)** +0.149/−0.020 — directional close in [45.15, 45.50]; Arm 2 swa_lr=1.5e-4 val 46.12/test 39.08 +0.97/+0.44 — moderate regression. **Banked #146 RESOLVED**: swa_lr × cosine-final-lr coincidence WAS load-bearing parameter (baseline swa_lr=6e-5 ~0.1e-5 above cosine-final 5.9e-5 → SWALR almost no-ops at default; this is why #2877 anneal_epochs was DEGENERATE). Lion EMA bias REJECTED as confound (banked #165). 13th cross-axis σ-spread + 15th cross-axis channel ordering + 10th cross-axis clip_fraction invariance preserved. Sub-noise marginal test win + SWA-window-truncation caveat (8th-consecutive 2-epoch SWA) → MERGE-candidate follow-up. 9 banked findings (#163-#171). thorfinn reassigned → swa_lr × swa_start_frac compose. |
| **#2484** | **frieren** | wip (training done, pending API) | Skip-SWALR entirely on σ=0.5 | Baseline shift notice sent. SWA mechanism orthogonal to σ. |
| **#2481** | **edward** | wip (training done, pending API) | SWA anneal_epochs=1 on σ=0.5 | Baseline shift notice sent. |
| **#2463** | **tanjiro** | wip (training done, pending API) | swa_lr ∈ {0.05x, 0.5x} sweep on σ=0.5 Lion stack | Baseline shift notice sent. SWA mechanism fully orthogonal to σ. |
| **#2442** | **nezuko** | wip (training done, pending API) | n_head ∈ {2, 8} bidirectional sweep at n_hidden=128 on σ=0.5 | Equal-compute capacity reshuffle; targets geom_camber_rc. Baseline shift notice sent. |
| **#2390** | **askeladd** | **CLOSED Loop 93** | Lion wd 2-arm {3e-3, 1e-2} on σ=0.5 | **Paper-strengthening CROSS-STACK CLOSE** (NOT a new axis; wd-axis already closed at #2791+#2819 on saturated-clip baseline); Arm A wd=3e-3 val 45.71 +0.56, Arm B wd=1e-2 val 45.70 +0.55 vs current #2674; wd-axis identified as **FIRST TRIPLE-PATTERN AXIS** (DEPENDENT-SYMMETRIC + DEGENERATE + ASYMMETRIC-V); FIRST OOD-METRIC SIGN-REVERSAL documented; 6 banked findings (#217-#222). askeladd reassigned → #2985 film_mid_dim. |

**⚠ Mid-wave baseline shift:** σ=0.5 merged while 7 PRs were in-flight on σ=1.0 Lion stack. Notice posted to all 7 with updated thresholds. Triage rule for these landing runs:
- val < 45.76 → MERGE (mechanism compounded with σ=0.5 implicitly via independence)
- val ∈ [45.76, 47.64] → directional win on σ=1.0 stack only, NOT a beat on σ=0.5 stack → rebase + rerun on σ=0.5
- val ≥ 47.64 → regression on its own σ=1.0 baseline → close

**Mechanism-independence assumption check:** σ knob (input encoding) should compose orthogonally with optimizer (Lion fine-tuning: warmup, wd, T_max, grad-clip) and capacity (slice_num) — but NOT with loss-surface mechanisms that interact with RFF spectrum (none currently in-flight). Hybrid Lion+AdamW-for-σ (#2311 fern) is structurally orthogonal to RFF σ.

## Key banked mechanisms

1. **Lion = the biggest single lever** — 28.5% win vs β=0.3 baseline; 37.7% vs RFF baseline; 52.8% vs Kendall baseline
2. **Lion collapses Kendall σ heads** — all 6 log_σ identical. Lion+Kendall ≡ Lion+uniform-weight. **Structural, NOT capacity- OR encoding-driven**: confirmed at 1.61M params (nezuko #2354) AND at RFF σ ∈ {0.25, 0.5, 1.0} (thorfinn #2168). **Hybrid Lion(model) + AdamW(log_σ) confirmed as the structural fix (fern #2311):** 0.81 log-unit spread restored at AdamW lr=1e-3; surface-velocity emphasis 5× volume; mechanism prediction fully validated. Next: lr sweep + σ=0.5 rebase to convert mechanism win into metric win.
3. **β=0.3 and Lion compound** — val improved 50.97→47.64 going from β=0.0 to β=0.3 on Lion stack. Mechanisms are independent.
4. **max_norm=0.5 is the right grad-clip setting under Lion (#2347 CLOSED)** — Arm A (no clip) regressed +9% val; Arm B (max_norm=2.0) flat. Lion's sign-update does NOT smooth out outlier gradient spikes — the EMA carries forward magnitude-biased sign sequences. Refutes the Lion-paper intuition that grad-clip is optional under sign-update. **Fourth independent confirmation of σ-collapse robustness** (across max_norm ∈ {0.0, 0.5, 2.0}).
5. **SWA frac bounded below** — only frac≥0.75 averages in flat-loss region. EMA decay=0.999 (#2285) did NOT fix it (val=70.34, regression) — its 5-epoch window dilutes late-epoch low-lr updates with stale high-lr snapshots. Right fix is schedule shape: faster cosine T_max → eta_min plateau covers more averaging window. → testing now in #2342.
6. **RFF σ↓ wins under Lion+β=0.3 (NEW BASELINE)** — σ=0.5 compounds (−3.94% val / −2.23% test vs σ=1.0); σ=0.25 wins test by additional −0.65 (mechanism: low-freq Fourier = OOD-geometry smoothness prior in z-score-normalized coords). σ floor not yet found — #2407 probes σ=0.1.
7. **LayerScale γ=1e-4 fails at 5 layers** — ReZero γ=1.0 is the fix (#2269 fern)
8. **β=0.3 = β optimum, axis CLOSED** — β=0.1 (#2171) regressed +7.5%, β=0.2 (#2243) flat on val/+0.46% on test. Both directions exhausted. Edward's Kendall σ-relaxation mechanism confirmed (lower β → all 6 log_σ drift toward uniform).
9. **RFF spectral-dim axis CLOSED at n=16** — n=32 (#2170) gave mixed val/test direction; banked SWA-window-gating mechanism (timeout limits useful averaging) directly feeds tanjiro's #2342.
10. **Gradient Centralization axis CLOSED at small-data regime** — frieren #2240 cleanly disproved transfer from ImageNet (clip_fraction unchanged, SWA basin disrupted, OOD prediction direction opposite). Three banked findings inform follow-ups.
11. **clip_fraction=100% under default max_norm=0.5** — corroborated by frieren #2240 and edward's planning for #2347. Strong evidence max_norm=0.5 is over-constraining (whether under AdamW or Lion).
12. **Width-scaling capacity bumps gated by SWA window in 30-min cap** — n_hidden=192 took 43% longer per step, killed SWA. Future capacity bumps need either (a) earlier swa_start_frac OR (b) linear-cost dimensions (depth, slice_num) like #2378.
13. **Optimizer × σ × β=0.3 interaction is non-monotonic (NEW)** — σ↓ wins under Lion+β=0.3 and AdamW+RFF-only, LOSES under AdamW+β=0.3 (#2168 Arm 1: +0.45 val vs AdamW+σ=1.0 reference). AdamW's per-coord adaptive LR cancels σ↓ benefit at β=0.3; Lion's sign-update restores it. **Implication:** future σ-modifying experiments must check optimizer × loss-shape interaction.
14. **σ=0.25 wins paper-facing test but loses val by within-noise margin (#2168)** — test geom_camber_rc −4.88% / cruise −6.11% at σ=0.25 are the strongest OOD-geom test gains observed. Test curve hasn't bottomed out → #2407 probes σ=0.1.
15. **slice_num is NOT a capacity axis at fixed n_head/dim_head (#2378 CLOSED)** — only sizes `nn.Linear(dim_head, slice_num)` → +5K params at slice=96 (vs 64), not +310K. slice_num=96 regressed val +4.19 (+9.16%) and test +4.94 (+12.46%) vs σ=0.5 baseline. Also +16% step time → killed SWA window to 1 epoch. **5th σ-collapse confirmation** (invariant to slice_num at fixed heads). Future capacity bumps must touch n_hidden / n_layers / n_head — slice_num at fixed heads is dead.
16. **clip_fraction ≥0.995 even at 2× max_norm under β=0.3-Huber (#2270 CLOSED)** — both arms of alphonse's relaxation sweep failed to unclamp the optimizer; gradients exceed cap on >99.4% of steps regardless. Combined with #2347's no-clip + 4× cap results, this brackets clip-relaxation top-to-bottom on AdamW *and* Lion stacks. Mechanism: β=0.3-Huber is near-linear → systematically large pre-clip grad norms. **Axis fully closed** for {AdamW, Lion} × {β=0.3}. **Hypothesis cannot fire** without max_norm ≥ 2.0 or pre-clip diagnostics.
17. **AdamW+β=0.3+Kendall log_σ equilibrium values reconfirmed by #2270** — surf_p=−1.34, surf_ux=−1.49, surf_uy=−1.47, vol_p=−1.38, vol_ux=−1.34, vol_uy=−1.35. Within 0.05 of #1906. **High-confidence init target for #2443 (alphonse Kendall init at AdamW-equilibrium on Lion stack)** — single-arm structural alt to fern hybrid optimizer.
18. **SWALR overrides cosine immediately at swa_start_epoch — there is NO cosine eta_min plateau available to SWA (#2342 CLOSED, GOLD finding)** — `SWALR(swa_lr=cfg.lr*0.2=6e-5, anneal_epochs=2)` hijacks the lr schedule the moment `epoch >= swa_start_epoch` and ramps the optimizer toward swa_lr regardless of where cosine left it. **The "SWA averages cosine eta_min plateau" mental model behind #2187, #2285, #2342, and partially #2429 is mechanically wrong.** The current baseline is matched by COINCIDENCE — at swa_start_frac=0.75 of T_max=15, cosine lands at ≈5.9e-5 just below swa_lr=6e-5. **T_max compression is always harmful** (cuts useful cosine annealing + creates SWALR upward ramp during averaging). **Direction-of-ramp** is the right axis to test (now being probed by #2463 tanjiro swa_lr sweep). **Sharpened prediction for #2429 edward swa_start_frac sweep**: going EARLIER should win because cosine_lr at frac=0.5 ≈ 1.5e-4 → SWALR ramps DOWN to 6e-5 → plateaus 4-6 epochs at swa_lr.
19. **Lion wd is directionally correct (Chen 2023 confirmed) BUT acts through a non-shrinkage channel (#2390 askeladd SENT BACK)** — wd=3e-3 (10× baseline) beats wd=3e-4 by 0.56 val / 0.36 test on σ=1.0 stack, largest gain on `geom_camber_rc` (−1.49 val on the bottleneck). **Mechanism is NOT weight shrinkage:** param_norm differs <0.2% across 30× wd range. Lion's sign-step `±lr` overwhelms wd's pull-toward-zero; wd acts through some other channel — probably per-coord corrections to sign direction accumulating into better implicit regularization. **6th independent σ-collapse confirmation** (wd cannot rescue Kendall σ axis — log_σ params are in no-wd group). Composition with σ=0.5 being tested in rebased #2390 (wd ∈ {3e-3, 1e-2}). **NEW DIAGNOSTIC**: `train/param_norm` + `train/param_rms` logging added; will travel with the rebase.
20. **swa_start_frac < 0.75 axis CLOSED on Lion+cosine (#2429 edward) — compounding two regressions** — Arm 1 (frac=0.6) val=47.02 (+1.26), Arm 2 (frac=0.5) val=48.77 (+3.01); monotonic worse with smaller frac. **Mechanism characterization is the gold finding here**: (a) Lion's plateau onset at our 13-epoch budget is ≥ epoch 12, NOT 7-9 — train/loss drops 0.57-1.06 units AFTER SWA start in both arms; (b) **3rd independent SWALR-overrides-cosine confirmation** (after #2342 tanjiro, partial #2390 askeladd); (c) earlier swa_start_frac compounds two costs: pre-plateau averaging AND earlier base-model LR cut (model trains at LR ≤ 1.04e-4 for 5-7 epochs vs baseline 3 epochs); (d) **`geom_camber_rc` is the dominant error contributor (~2× the other splits)** — now the load-bearing OOD split for all future architecture/data work. **8th independent σ-collapse confirmation.** Sharpens follow-ups: #2481 anneal_epochs=1 (cooldown speed), #2484 skip-SWALR entirely.
21. **Lion does NOT have a chaotic init phase (#2363 frieren CLOSED)** — 3-epoch linear warmup made epoch-1 val WORSE (390.91 with warmup lr=1e-4 vs baseline 189.70 at lr=3e-4). Lion's sign(EMA(grad)) update direction is a SIGN — warmup at lower LR lands worse because magnitude shrinks but direction is unchanged. **Adam→Lion mental model transfer for warmup is refuted**, the high-loss epoch-1 in Adam is a chaotic-init phenomenon that Lion does NOT exhibit. Banked finding: don't assume Adam-paper recommendations transfer to Lion just because both are momentum-based.
22. **clip_fraction is invariant to lr schedule (#2363 frieren CLOSED)** — epochs 1-3 at lr ∈ {1e-4, 2e-4, 3e-4} all show clip_fraction ≈ 99-100%. Clipping happens on raw ‖g‖ pre-optimizer-scaling — the gradient norm distribution is set by the loss landscape, not by hyperparameters. **The persistent-clipping signature is a property of (model + data + loss), NOT a hyperparameter problem warmup/lr can fix.** Implication: future grad-clip experiments should manipulate `max_norm` or upstream loss/architecture, NOT lr schedule, to change clip_fraction. **9th independent σ-collapse confirmation** (log_σ trajectory identical to baseline through warmup phase).
23. **clip_fraction definition discrepancy flagged for follow-up (#2363 frieren)** — frieren measured 99-100% clip_fraction under max_norm=0.5 while BASELINE.md cites 74% from #2063. Worth investigating in a future diagnostic PR whether (a) measurement definitions differ (per-step boolean vs ratio) or (b) gradient norms have shifted across the merge series. Connects to alphonse's #2270 finding about pre-clip grad_norm distributions.
24. **Budget-binding interaction with lr-schedule manipulations (#2363 frieren CLOSED, banked principle)** — at SENPAI_TIMEOUT_MINUTES=30 → ~13 effective epochs, any lr modification costing >1 epoch of full-lr training will struggle to recover. By epoch 9 the warmup run was AHEAD on trajectory (67.17 vs baseline 78.74) but the budget cut before SWA could convert the lead. Future lr-schedule PRs (warmup, longer T_max, EMA warm-up) should account for the 13-epoch effective budget upfront when computing expected SWA gain.
25. **σ-collapse fix #2: Init at AdamW-equilibrium alone prevents collapse under Lion (#2443 alphonse CLOSED, banked mechanism)** — **strong-form refutation** of the previously-banked finding that "Lion's sign-update is wholly responsible for collapse". Per-channel Kendall gradient SIGN is sufficient signal to maintain differentiation given a non-degenerate starting point. log_σ trajectory: 0.150 (init) → 0.478 (final), monotonically growing, never collapsing. **Three-tier σ-spread ordering on σ=0.5 Lion stack:** Lion+Kendall baseline (spread 0.000) < AdamW+Kendall (spread ~0.15) < Lion+AdamW-eq-init (spread 0.478) < Hybrid Lion+AdamW (spread 0.81, fern #2311 pending). **Test improved by −0.40 on paper-facing metric** (geom_camber_rc −0.72, single_in_dist −1.04) despite val regression of +0.61. **Open mechanism:** Lion's sign-update still drifts mean(log_σ) ~0.6 nats more negative (−1.40 → −1.99), inflating all effective weights ~3× → val regression. **#2500 alphonse tests the mean-anchor fix** (L2 anchor loss on mean(log_σ) at AdamW-eq target).
26. **Val/test divergence first observed in this direction on Wave 12 (#2443 alphonse)** — differentiated Kendall weighting (Lion+AdamW-eq init) acts as an OOD regularizer: slight val degradation, real test gain especially on harder OOD splits. For paper-facing test number this is interesting; for the merge gate it's a regression. Connects to #2390 askeladd's wd-not-shrinkage finding — both suggest Lion has multiple knobs that act through OOD-regularization channels distinct from the model parameter values directly.
27. **σ-collapse fix #1 confirmed and 0.55-val winner on σ=0.5 stack (#2311 fern Arm 2)** — hybrid Lion(model) + AdamW(log_σ) at lr=5e-4: val 45.22 (−0.55) and test 38.77 (−0.90). Spread 0.475, mean −1.98 (5× more spread than AdamW equilibrium, comparable to alphonse's #2443 init mechanism). **Test wins on 3/4 splits including single_in_dist −2.11 and geom_camber_rc −1.82** — biggest gains on the load-bearing OOD splits. Branch rebased CLEAN; awaiting confirmation rerun before merging. **Compound potential with #2500 anchor-mean** is the natural next step — both mechanisms (optimizer split + mean anchor) attack different parts of the σ-stability problem and should stack.
28. **σ axis exhausted on val; σ=0.25 test win NOT seed-robust (#2407 thorfinn CLOSED)** — five σ values tested (0.1, 0.25×2 seeds, 0.5, 0.75, 1.0). σ=0.1 hits val floor (+1.03%); σ=0.25 cross-seed test mean (39.72) ties σ=0.5 (39.66). σ→smaller direction dead on primary metric; **σ=0.5 stays canonical**. Seed gap +1.42 at σ=0.25 is well above val-gap noise estimate ~0.86 → seed-0 favorable noise draw, not a real improvement.
29. **σ=0.1 mechanism revision: REGULARIZER not OOD-prior (#2407 thorfinn CLOSED)** — gain driver is **single_in_dist test −3.22%**, NOT OOD-geometry. geom_camber_cruise REVERSES (+3.43% worse than σ=0.5); geom_camber_rc only tied with σ=0.25. **The original mechanism story partially survives at rc but fails at cruise.** Low-σ Fourier features become near-degenerate (rff_mean=0.44 cos-dominated; rff_std=0.553 vs theory 0.707) → reduced per-point positional info → smoother predictions → in-distribution test win. **Directly motivates #2512 multi-scale RFF**: if σ=0.1 acts as regularizer, it should compose with σ=0.5's coordinate resolution.
30. **2nd independent clip_fraction≈0.99 confirmation under Lion+max_norm=0.5 (#2407 thorfinn)** — both arms: 0.992 / 0.997 (matches frieren #2363's 0.99 flag). BASELINE.md's "~0.74" note is from a different regime / earlier stack — under our current σ=0.5 baseline, max_norm=0.5 clips every step → Lion effectively becomes sign-of-sign at fixed norm. **Banked for future max_norm relaxation pass on the merged stack** (alphonse #2270 already closed max_norm relaxation under β=0.3+Huber+AdamW; under-Lion remains open).
31. **10th + 11th independent σ-collapse confirmations (#2407)** — both Arm 1 (σ=0.1) and Arm 2 (σ=0.25 seed-1) show `final/log_sigma_*` = −0.9037 (Kendall clamp floor). σ-collapse mechanism is **invariant to RFF σ ∈ {0.1, 0.25, 0.5, 1.0}** at the optimizer level — independent of input encoding bandwidth. Reinforces the structural-not-data nature of the collapse identified in #2311 and #2443.
32. **β–σ coupling mechanism (#2540 fern CLOSED, NEW)** — Higher Huber β systematically collapses the per-channel Kendall log_σ differentiation established by #2311's hybrid AdamW fix. σ-spread: β=0.3=0.475 → β=0.5=0.401 (−16%) → β=1.0=0.304 (−36%). The β knob is NOT orthogonal to σ-differentiation; widening β throws away part of the hybrid-fix gain. Mechanism: as β↑, smooth-L1 residuals quadratically attenuate where |r|<β; gradient magnitudes ∝ 1/β; cross-channel SNR in log_σ gradient signal shrinks → per-channel heads converge toward uniform. **β=0.3 is doubly load-bearing — wins via both direct loss optimum AND σ-preservation. Directly motivates #2604 hybrid_kendall_lr push (test if more spread compounds).**
33. **Refuted grad-norm prediction for smooth-L1 (#2540 methodology finding)** — PR-body predicted p99‖g‖ ordering β=1.0 > β=0.5 > β=0.3. Observed: β=0.5 (36.06) > β=1.0 (28.57). Smooth-L1 grad slope is 1/β in |r|<β region; bulk of residual mass at mid-late training lies there → slope-1/β regime dominates → lower β → higher grad-norm. **grad-norm is NOT a valid proxy for "MSE-likeness" in this regime.** Useful for future Huber-tuning hypotheses.
34. **12th independent clip_fraction confirmation (#2540 β=0.5 arm: 0.9949, β=1.0 arm: 0.9590)** — clip-saturation under max_norm=0.5 is now 12-strong. Strongly motivates #2606 max_norm relaxation experiment.
35. **σ axis decisively closed at σ=0.5 (#2512 thorfinn CLOSED)** — multi-scale RFF 8×σ=0.5 + 8×σ=0.1 does NOT compose on hybrid stack (val +2.66%, test +1.20%). Mechanism revision: σ=0.1's #2407 single_in_dist test gain was **σ-collapse-conditional** — once #2311 fixed σ-collapse via hybrid AdamW, the regularizer effect dissolves. **single_in_dist test trajectory:** 42.45 (collapsed-σ + σ=0.5 RFF) → 40.34 (hybrid-σ + σ=0.5 RFF, #2311 win) → 41.39 (hybrid-σ + multi-scale RFF, this PR). Banked structural finding: **Multi-scale RFF and Kendall σ-differentiation share an information channel — #2311 substitutes for the regularization role low-σ RFF was filling.** 10 distinct σ configs evaluated total since Lion adoption; σ=0.5 wins everywhere. **Next σ-related axes (multi-scale, σ-bracketing) are exhausted.**
36. **Anchor mean(log_σ) is a clean control knob — but the natural hybrid-AdamW equilibrium dominates (#2500 alphonse CLOSED)** — λ=1 → mean=−1.996, λ=5 → mean=−1.748; mechanism decouples mean from per-channel differentiation. **Important finding:** the natural hybrid-AdamW equilibrium (~mean −2.0) outperforms target mean −1.4 (AdamW-only-eq) by val ≈ 1–2 MAE units. The AdamW-eq target measured in #2270/#1906 was specific to AdamW with weight decay on the main model; the new hybrid (lr=5e-4 + wd=0 on log_σ) reaches a structurally different per-channel optimum at deeper σ. **The hybrid optimizer doesn't just unblock differentiation — it shifts the optimum mean too.**
37. **AdamW-eq init expands σ-spread (#2500 alphonse, NEW)** — scalar init at −1.4 produces final spread 0.70 vs hybrid baseline's 0.475 from zero-init. Mechanism: scalar init at "deeper σ" makes initial residuals smaller, log_σ_i differentiates faster, spread expansion accelerates. **Init magnitude affects equilibrium spread, NOT just convergence speed.**
38. **Per-split σ-spread asymmetry: spread expansion HURTS `geom_camber_rc`, HELPS other splits (#2500 alphonse, CRUCIAL)** — Arm 1 vs new baseline (val): single_in_dist −0.13 (WIN), cruise −0.45 (WIN), re_rand +0.57 (LOSS), **geom_camber_rc +2.72 (LOSS)**. The OOD bottleneck has the OPPOSITE preference from other splits on σ-spread axis. **Direct implication for fern's #2604 hybrid_kendall_lr push:** pushing spread UP may regress further on geom_camber_rc, even if it wins on average. Per-split tension may require architectural-level fixes (e.g., FiLM capacity, alphonse's #2616) rather than σ-tuning alone.
39. **σ-spread CEILING at 0.475, U-curve confirmed via head-lr direction (#2604 fern CLOSED, MAJOR)** — Pushing hybrid_kendall_lr 5e-4→1e-3→2e-3 increases σ-spread monotonically 0.475→0.82→0.92 but REGRESSES val/test monotonically (+1.72 val / +1.05–1.08 test on both arms). **Channel-level falsification of "compounding" hypothesis:** single_in_dist test gained −2.11% from spread 0→0.475 in #2311, now LOSES +1.04 (Arm 1) to +2.35 (Arm 2) from 0.475→0.82/0.92 — same channel that benefited from differentiation appearing suffers from MORE differentiation. **Premature-commitment mechanism:** Kendall weight on surf_ux reaches ~120 (Arm 2) vs baseline ~22 — surf_ux dominates gradient at higher kendall_lr, other 5 channels under-trained. **The 0.475 spread IS the natural equilibrium** under joint Lion(model) + AdamW(log_σ) dynamics — not a partial harvest. **SWA does rescue work proportional to lr:** 1.6× more rescue at higher kendall_lr → SWA value scales with optimization noise (useful framing for paper). **σ-spread monotonic + smooth on AdamW** (no oscillation pattern) → rules out "lr too high" as the failure mode. **Channel ordering lr-invariant:** surf_ux=min, vol_ux=max preserved across all three lrs → ordering encodes data difficulty, not optimizer. **5th independent clip_fraction=1.0 confirmation** (13+ total). **DIRECTLY MOTIVATES #2666 β LOWER sweep:** if spread can be re-opened via β (residual-shape side of β–σ coupling) without triggering premature-commitment, σ-spread axis re-opens; if β=0.2 spread>0.475 AND regresses, U-curve is structural across mechanisms.
40. **CRITICAL METHODOLOGY: "clip_fraction=1.000 every step" was a final-epoch summary-key artifact, NOT per-step truth (#2606 thorfinn CLOSED, MAJOR)** — Per-step sampling (~300 logged steps per arm) gives: max_norm=0.5 ≈ 99% (matches BASELINE.md's 0.99); max_norm=1.0 = 98.3%; max_norm=2.0 = 82.0%. **All 13+ "clip_fraction=1.0" confirmations from #2168, #2363, #2407, #2512, #2540, #2604 were reading the W&B summary final-epoch rounded mean, not actual per-step values.** Paper-facing implication: any methods-section claim about "every step clipped" needs rewriting as "~99% of steps clipped, ~1% sub-threshold." Future grad-clip experiments should sample per-step values (not summary keys) for accurate diagnostics.
41. **max_norm=0.5 robust under hybrid stack; clip relaxation hurts monotonically (#2606 thorfinn CLOSED)** — Arm 1 (1.0) val 45.95/test 39.40 (+0.73/+0.63); Arm 2 (2.0) val 46.09/test 39.23 (+0.87/+0.46). Monotone-bad on val. **The tightly-clipped sign-step regime is load-bearing for SWA convergence.** Mechanism: max_norm=0.5 + Lion sign-step implements a constant-magnitude step; loosening injects trajectory variance that SWA averaging picks up worse local geometry.
42. **NEW: Per-split tight-clip-OOD-friendly signature (#2606 thorfinn, IMPORTANT)** — val_geom_camber_rc is the WORST-hit val split in both relaxation arms (+1.62 Arm 1, +1.44 Arm 2). The OOD bottleneck differentially benefits from tight clipping. **Connects to #2429:** tight clip protects the load-bearing OOD split. **Direct motivator for #2674 max_norm BRACKET LOW** — if tightening further protects geom_camber_rc even more, this is a genuine OOD-targeted compound win.
43. **NEW: max_norm × swa_lr co-tuning hypothesis (#2606 thorfinn banked)** — thorfinn's mechanism interpretation: max_norm=0.5 and swa_lr=lr×0.2=6e-5 are co-tuned for trajectory variance. Relaxing one without re-tuning the other regresses. **Banked open question** requiring code change (swa_lr not currently a CLI flag). Joint (max_norm, swa_lr) sweep is the next-after-#2674 mechanism test.
44. **NEW: Kendall σ orthogonality to max_norm confirmed (#2606 thorfinn)** — both arms reproduce baseline σ structure within 0.01 (mean=−1.98, spread=0.475). hybrid_kendall_lr dynamics are independent of max_norm ∈ [0.5, 2.0]. **σ mechanism and clip mechanism don't interact at this scale.**
45. **NEW: grad_norm distribution invariant to clip threshold (#2606 thorfinn)** — median pre-clip ~5.3, mean ~18.3 across both arms. Pre-clip is a property of (model + data + loss), NOT a function of the clip parameter. **Rules out "the model adapts its gradient norm to the clip" as a possible mechanism.**
46. **NEW: grad_norm_max drops slightly with relaxation (#2606 thorfinn)** — 83.4 → 74.8 going 1.0 → 2.0. Late-epoch peak gradients are well above any tested threshold; the OUTLIER tail is NOT what's being clipped at max_norm=0.5 — it's the BULK distribution. **Confirms theoretical reasoning that post-Lion-sign-step clip is essentially scaling the entire signed-direction step uniformly** (i.e., clip is a secondary lr, not a stability mechanism removing outliers).
47. **film_mid_dim=64 robust to ±2× capacity (#2616 alphonse CLOSED)** — Arm 1 (mid_dim=32) val 45.92/test 39.27 (+0.70/+0.50); Arm 2 (mid_dim=128) val 45.94/test 39.36 (+0.72/+0.59). Both arms regress monotonically with magnitude away from 64. **Capacity-axis dead zone confirmed across all 4 single-CLI capacity axes**: width #2354, slice_num #2378, depth (legacy), film_mid_dim #2616. **All architecture-capacity gains under the 30-min budget cap must come from non-CLI structural changes** (e.g., per-split heads, gated attention, anisotropic representations).
48. **NEW: NO OOD asymmetry on FiLM capacity axis (#2616 alphonse, IMPORTANT)** — Both arms regress evenly across all 4 splits: geom_camber_rc +1.31/+1.16 (val), single_in_dist +0.40/+0.50, geom_camber_cruise +0.55/+0.61, re_rand +0.25/+0.31. **Cleanly refutes the architectural-bottleneck hypothesis for #2500's σ-spread asymmetry** — whatever drives geom_camber_rc's OPPOSITE preference on σ-spread is NOT a FiLM capacity issue. **Redirects #2500's per-split asymmetry investigation away from architectural capacity toward: (a) split-aware loss reweighting, (b) per-OOD-feature heads, or (c) per-split SWA selection.**
49. **NEW: σ-spread orthogonal to FiLM capacity (#2616 alphonse)** — Final spread 0.473–0.481 across both arms, indistinguishable from baseline 0.475. **Confirms the σ-collapse mechanism is structurally independent of FiLM mid_dim** — the Kendall log_σ heads converge to the same equilibrium spread regardless of conditioning capacity. Adds to #2606 (σ ⊥ max_norm) and #2604 (σ ⊥ lr-direction): σ-spread = function only of (optimizer split, loss shape β, head-lr); NOT a function of model capacity, clip threshold, or arbitrary architectural tweaks.
50. **NEW: FiLM modulation scales with mid_dim (#2616 alphonse)** — Arm 2 (mid_dim=128) shows larger modulation magnitudes than Arm 1 (mid_dim=32), but BOTH regress. **Capacity is being used but not productively** — the model isn't bottlenecked at mid_dim=64; doubling it adds parameters that don't translate to validation gain. **Banked principle: more parameters in FiLM ≠ better FiLM** (consistent with the n_hidden=192 dead-end at #2354 — capacity adds step-time cost first, gain only if SWA window survives).
51. **mid_dim=32 cleanly underfits (#2616 alphonse Arm 1)** — Slight regression from compute-bound bottleneck at the smaller end of the range; suggests baseline 64 is near the lower edge of the productive band. **The capacity floor on FiLM is well-tuned in #1585.** Future capacity work needs to find different axes — single-CLI knobs at fixed architecture are exhausted.
52. **mid_dim=128 step-time blow-up (#2616 alphonse Arm 2)** — Mid_dim=128 slowed training; SWA window compressed. **Same pattern as n_hidden=192 (#2354) and slice_num=96 (#2378)**: any single capacity bump that costs step-time cuts SWA averaging and erases the capacity gain. **Capacity-axis dead-zone mechanism is uniform across width/slice/film.**
53. **Capacity-axis × SWA window interaction is structural (banked principle)** — All 4 capacity axes (width, depth, slice, film) close not because the model can't use more parameters, but because the 30-min wall clock = ~13 epochs leaves insufficient SWA averaging budget after step-time grows. **Any future capacity bump needs to bundle: (a) earlier swa_start_frac, OR (b) lower-cost dimensions like multi-head reshuffles (e.g., #2442 nezuko n_head sweep — equal-compute capacity reshuffle).**
54. **Arm 1 even-epoch behavior stable (#2616 alphonse Arm 1, methodology)** — Lower-capacity arm did not show training-instability signature; SWA averaging stable at fewer parameters. **Confirms film_mid_dim is well-conditioned in [32, 128] range** — no narrow-network instability artifacts confounding the result. Banked for future structural changes that touch FiLM dimensions.
55. **max_norm test-side U-curve minimum at 0.35 (#2674 thorfinn MERGED, NEW BASELINE)** — full picture: val 45.22 (0.5) → 45.15 (0.35) → 44.96 (0.25); test 39.40 (1.0) → 38.77 (0.5) → **38.64 (0.35)** → 38.93 (0.25). Test minimum unambiguously at 0.35. Val keeps descending below; test bottoms. **The empirical clip optimum for this stack is now closed at max_norm=0.35.**
56. **clip_fraction=100% at both 0.35 and 0.25 (#2674 thorfinn, 4875/4875 sampled steps each)** — past #2606's ~99% at max_norm=0.5. Lion+clip is now in **strict constant-magnitude sign-step regime** below 0.5 — no per-coord magnitude info from gradient survives, only sign. **Going below 0.35 picks up val-specific basins that don't generalize to test.**
57. **NEW: Val-test divergence on geom_camber_rc at max_norm=0.25 (#2674 thorfinn, IMPORTANT)** — val_geom_camber_rc=56.564 (−1.56 vs baseline) but test_geom_camber_rc=53.649 (+0.87 vs baseline). **Cleanest example yet of val-overfit basin** — same load-bearing OOD axis flips direction between val and test pools. **Likely causes:** (a) small-pool noise (val=100 samples vs test=200), or (b) over-compression basin that finds val-specific minimum but doesn't generalize. **Paper-relevant:** val_geom_camber_rc as a generalization predictor is unreliable below max_norm=0.35.
58. **Tight-clip-OOD-friendly hypothesis from #2606 partially survives (#2674 thorfinn)** — val_geom_camber_rc improves monotonically with tighter clip (matches prediction). But test_geom_camber_rc starts regressing below 0.35 (refutes naive "tighter is monotonically better"). **Refined mechanism:** clip ≤ 0.35 helps generalization on OOD; clip < 0.35 helps val pool only via val-specific basin fitting.
59. **σ-spread bit-identical across max_norm bracket (#2674 thorfinn)** — Arm 1 spread=0.475, Arm 2 spread=0.473. **2nd confirmation that σ axis is fully orthogonal to max_norm** (1st: #2606). Hybrid Lion+AdamW(σ) #2311 fix preserved under all clip values tested in [0.25, 2.0].
60. **Pre-clip grad_norm invariant to clip threshold (#2674 thorfinn)** — 5.30 median at 0.35 vs 5.33 at 0.25; matches #2606's ~5.3 at 0.5. **3rd confirmation that pre-clip distribution is invariant to clip parameter** — it's a property of (model + data + loss), not a function of clip threshold.
61. **No runtime cost from tighter clip (#2674 thorfinn)** — Identical step-time, runtime, and VRAM across Arms 1 and 2 (and #2606 reference). max_norm is O(P) (parameters), negligible vs forward/backward cost. **New baseline costs nothing in throughput.**
62. **Seed-0 reproducibility confirmed under hybrid stack (#2674 thorfinn)** — Two independent Arm 1 runs (`qtdyho9w` at 00:07 + `ieu1futo` at 01:23) produced bit-identical metrics. **Useful for future debugging and noise-floor reasoning** — single-seed determinism holds under (Lion+AdamW)×(Kendall σ)×(SWA) stack. (Distinct from across-seed variance — that's what #2701 alphonse will measure.)
63. **β-driven and lr-driven σ-spread are mechanistically distinct (#2666 fern CLOSED, HEADLINE MECHANISM FINDING)** — Same numerical σ-spread arises from fundamentally different Kendall weight regimes. **lr-driven (#2604):** spread 0.82 with Kendall wt surf_ux ≈120 → "premature commitment" failure mode. **β-driven (#2666 Arm 2 β=0.15):** spread 0.546 with Kendall wt surf_ux=37.5 → ALL weights DOWN from baseline 44.3. **lr-driven concentrates weight on max-channel; β-driven redistributes via Huber's shape change.** Resolves the #2604 "premature commitment" question: failure mode was weight-concentration mechanism, NOT spread itself. **Numerically similar spreads ≠ equivalent mechanisms.**
64. **σ-spread axis is NOT structurally capped at 0.475 (#2666 fern)** — β-driven mechanism extends spread to 0.546 with only +0.16 val cost; lr-driven mechanism extends to 0.82 but costs +1.72 val. **The wall is at the optimization mechanism, not the spread number.** Future σ-related work: prefer β-direction over lr-direction for exploring spread > 0.475.
65. **Channel ordering (surf_ux=min log_σ, vol_ux=max log_σ) is invariant across mechanisms (#2666 fern)** — Holds across {β ∈ [0.15, 0.3], head-lr ∈ [5e-4, 2e-3], optimizer split} on σ=0.5 stack. **Property of the data/loss landscape**, not of the optimizer or loss-shape parameters. Useful invariant for future architecture changes — if ordering breaks, that signals a real structural change.
66. **single_in_dist U-curve is dataset-level, not optimizer-driven (#2666 fern, IMPORTANT)** — Same channel that gained −2.11% test from spread 0→0.475 (#2311) now regresses past 0.475 via two independent mechanisms: β-driven (#2666 β=0.15: +0.81 val, +0.68 test) AND lr-driven (#2604 lr=2e-3: +1.04~+2.35 val, +1.05~+1.08 test). **The U-curve is in the data/loss landscape, NOT the optimizer.** Critical for any future Kendall σ work: the in-dist regression at spread > 0.475 is structurally inherent.
67. **Kendall weight direction inverts between mechanisms (#2666 fern)** — lr-driven head-lr push: weights INCREASE (surf_ux 44.3→~120, "concentration"). β-driven Huber softening: weights DECREASE (surf_ux 44.3→37.5, "redistribution"). **Cleanest mechanism-decomposition result in Wave 12.** Useful for paper: σ-spread is necessary but not sufficient — the weight regime matters more.
68. **test improvement asymmetric to val regression at β=0.15 (#2666 fern)** — Arm 2 β=0.15: val +0.16 but test −0.20 (per-split test wins on re_rand −0.86 and cruise −0.60, regress on single_in_dist +0.68; val opposite direction). **β-axis acts as regularizer: softer Huber → better OOD generalization but worse fit on easier splits.** Paper-publishable Huber-shape × evaluation-pool sensitivity result.
69. **β=0.15 doesn't diverge despite edge-of-safe-regime (#2666 fern)** — σ-spread trajectory monotone smooth (0→0.546) through 15 epochs, no NaN, no instability. **β floor below 0.15 may still be safe** for future fine-bracket experiments (though probably unproductive given current results).
70. **Effective_weight surf_ux decreasing with lower β (#2666 fern, mechanism confirmation)** — 44.3 (β=0.3) → 40.2 (β=0.2) → 37.5 (β=0.15). Direct counter to lr-driven mechanism. **Validates the β-driven spread reading**: lower β shrinks the quadratic region of Huber → larger residuals in non-quadratic regime → gradient slope = 1/β grows but bulk residual mass moves OUT of the high-slope region → Kendall log_σ grow → weights = 1/(2σ²) shrink.
71. **β=0.3 is the optimum across max_norm ∈ {0.35, 0.5} clip regimes (#2736 fern CLOSED, ROBUSTNESS STATEMENT)** — Fine bracket around β=0.3 on new saturated-clip baseline: β=0.25 val 46.13 / test 39.20 (+0.97/+0.56); β=0.35 val 46.21 / test 39.12 (+1.06/+0.48). Both arms regress past val 45.40 close threshold; ordering β=0.3 > β=0.25 > β=0.35 clean on val. **Clean paper claim**: "β=0.3 is the optimum across partial-clip (≈99% sub-threshold) AND saturated-clip (100% clipped) regimes." Both endpoints of the explored clip range now have the same β optimum.
72. **β-driven σ-spread mechanism transfers cleanly to saturated-clip regime (#2736 fern, PAPER-STRENGTHENING)** — Predictions made from max_norm=0.5 #2666 measurements transfer with sub-1% deviation to max_norm=0.35 actuals: β=0.25 spread predicted [0.49, 0.50] → actual 0.4936; β=0.35 spread predicted [0.45, 0.46] → actual 0.4618; β=0.25 surf_ux weight predicted [38, 42] → actual 42.30; β=0.35 surf_ux weight predicted [46, 50] → actual 46.33. **Conclusion: the loss-shape/Kendall-spread coupling is governed by β alone, NOT by gradient-magnitude flow** — the transfer happens under ZERO sub-threshold steps (4875/4875 saturated on both arms). Sharpens the #2666 β-driven-mechanism story by establishing it transfers across clip regimes.
73. **σ-spread monotone in β at every trajectory check-point (#2736 fern)** — Spread at 25/50/75/100% of training: β=0.25 = 0.023/0.071/0.247/0.494; β=0.35 = 0.022/0.057/0.216/0.462. Lower β strictly higher spread at every check-point, not just at terminal. **The β→spread mechanism is not a terminal-state artifact** — it's present from the first quarter of training onward. Useful confirmation for future trajectory-based diagnostic work.
74. **Channel ordering (surf_ux=min log_σ, vol_ux=max log_σ) invariant on saturated-clip baseline across β axis (#2736 fern)** — Extends the cross-axis ordering invariance (banked #65) to a third axis on the saturated-clip stack. **Channel ordering is now confirmed invariant across {β ∈ [0.15, 0.35], head-lr ∈ [5e-4, 2e-3], optimizer split, max_norm ∈ [0.35, 2.0], FiLM mid_dim ∈ [32, 128]}** — strong dataset-level signature.
75. **clip_fraction is β-invariant under max_norm=0.35 saturated-clip baseline (#2736 fern)** — 4875/4875 sampled steps at clip_fraction=1.0 for β=0.25 arm; 4874/4875 for β=0.35 arm (only step 0 sub-threshold). **Confirms #2674's saturated-clip characterization is β-invariant** — the gradient-magnitude flow does not depend on Huber β within the tested bracket.
76. **single_in_dist regresses largest under both β perturbations on new baseline (#2736 fern)** — Arm 1 β=0.25 val single_in_dist +2.75 (largest single-split val change); Arm 2 β=0.35 val single_in_dist +1.15 (still the largest). **Extends single_in_dist U-curve finding (#66) to the saturated-clip baseline** — the channel is sensitive to β in BOTH directions around the optimum. Connects to the dataset-level U-curve already documented across two prior mechanisms (β-driven via #2666; lr-driven via #2604).
77. **Test-vs-val ordering mild asymmetry on β fine-bracket (#2736 fern)** — Arm 2 β=0.35 closer to baseline on test (+0.48) than Arm 1 β=0.25 (+0.56), opposite to the val ordering. Gap ~0.08, well below seed noise estimate ~0.86. **Not strong enough for separate test-optimum claim** — consistent with the val and test optima both at β=0.3 within noise.
78. **Mechanism robustness sharpens #2666 story (#2736 fern, derived banked)** — The #2666 β-driven-mechanism finding (Kendall weights DOWN, surf_ux 44.3→37.5) was established at max_norm=0.5; #2736 confirms the SAME mechanism direction holds at max_norm=0.35 (Kendall surf_ux weight 42.30 at β=0.25 and 46.33 at β=0.35 — same ordering, lower β → lower weight on max-channel). **The mechanism characterization is paper-strengthening independent of the closure verdict** — establishes the β-driven mechanism is robust to clip regime, while the lr-driven mechanism's robustness remains untested on the new baseline (motivates #2773 fern).
79. **lr × max_norm orthogonal across {2e-4, 3e-4, 4e-4} bracket at max_norm=0.35 (#2731 thorfinn CLOSED, ROBUSTNESS)** — Both arms regress past close threshold (lr=2e-4 val 47.88 +2.72, lr=4e-4 val 46.14 +0.99); V-shape skewed toward smaller lr. **Lion lr=3e-4 optimum from #2297 (max_norm=0.5) transfers cleanly to the saturated-clip regime.** Completes the lr-side robustness statement parallel to fern's β-side (#2736).
80. **clip_fraction=1.0 lr-invariant on max_norm=0.35 (#2731 thorfinn)** — All 3 arms (baseline lr=3e-4 + Arm 1 lr=2e-4 + Arm 2 lr=4e-4) at 4875/4875 saturated. **The rescue channel stays closed across ±33% lr perturbation** — the constant-magnitude sign-step regime is robust to lr in this bracket.
81. **σ-spread lr-invariant on saturated-clip baseline (#2731 thorfinn)** — 0.475 (baseline) / 0.475 (Arm 1) / 0.482 (Arm 2). **3rd cross-axis confirmation σ⊥{β, max_norm, lr} on new baseline** — complements #2606 (σ⊥max_norm), #2604 (σ⊥hybrid_kendall_lr direction-of-spread), and now Lion lr.
82. **grad_norm decreases monotonically with lr increase (#2731 thorfinn)** — 8.89→7.79 mean from Arm 1→Arm 2. Larger constant step → faster norm reduction → smaller pre-clip gradients. **But never reopens rescue channel** (Arm 2 max grad 72.5 still 207× clip threshold 0.35). Confirms the lr knob does NOT control gradient distribution, only step magnitude.
83. **No starvation at lr=2e-4, no instability at lr=4e-4 (#2731 thorfinn)** — Both train losses descend smoothly through 13 epochs. **The V-shape is on the validation surface, NOT in the train trajectory.** Useful: val-test gap is the bottleneck across lr, not underfitting/overfitting in train.
84. **Arm 2 wins on geom_camber_rc/test AND re_rand/test but loses on single_in_dist (#2731 thorfinn)** — extends single_in_dist U-curve / OOD-bottleneck story (banked #66 + #76) yet again. Mechanism: in-distribution samples have most reliable gradients to exploit and benefit most from already-tuned lr=3e-4; OOD splits prefer slightly larger step. **Per-split lr preferences are NON-uniform.**
85. **Coupled swa_lr = lr×0.2 sweep doesn't rescue lr-driven regression (#2731 thorfinn)** — Arm 1 swa_lr=4e-5, Arm 2 swa_lr=8e-5; both regress. SWA window only 3 epochs in all runs → SWA-floor coupling saturated in averaging window. Useful: the SWA-floor sensitivity is dampened relative to the lr-budget effect on per-step training trajectory.
86. **All 3 #2731 runs hit SENPAI_TIMEOUT_MINUTES=30 at exactly epoch 13 (#2731 thorfinn)** — Clean 3-arm comparison, no truncation confound. Baseline still descending at epoch 13 → more epochs would unlock another ~0.5 val improvement. **Banked open question:** extended budget orthogonal to all current mechanism axes; could be tested via batch-size tuning (fewer steps per epoch) if SWA mechanism preserved.
87. **σ-collapse-fix MECHANISM is robust across 3 seeds (#2701 alphonse CLOSED, PAPER-DEFENSIBLE HEADLINE)** — σ-spread 0.455 ± 0.041 (~9% relative variation); channel ordering (surf_ux=min, vol_ux=max) PRESERVED at every seed; full ordering surf_ux < surf_uy < vol_p < surf_p < vol_uy < vol_ux holds at all 3 seeds. **The structural fix in #2311 is paper-defensible** — appendix can state "σ-collapse-fix mechanism reproduces across seeds with full channel ordering preservation."
88. **Headline #2311 win MAGNITUDE is seed-sensitive (#2701 alphonse, IMPORTANT PAPER FINDING)** — Cross-seed val stdev 2.049 spans the claimed −1.20% gap vs #2168; cross-seed test stdev 1.756 spans the claimed −2.26% gap. Without cross-seed numbers for #2168, the merge-magnitude claim has weaker statistical support than originally presented. **Paper appendix must report cross-seed mean ± stdev, not single-seed minimum.** Banked open question: 3-seed sweep on OLD #2168 stack would defend the "hybrid beats σ=0.5" claim against seed noise.
89. **30-min cap is a hidden seed-multiplier (#2701 alphonse, NEW MECHANISM FINDING)** — Seed=2's epoch 4 took 257s vs typical 141s (data-loader stochasticity); seed=2 finished only 12 epochs and SWA window degenerated to single epoch (SWA == base on seed=2). **SWA-window-truncation accounts for ~40% of swa_test cross-seed variance** (base_test stdev=1.005 vs swa_test stdev=1.756). Paper finding: **SWA fragility under tight time caps inflates cross-seed variance by ~40%**.
90. **Per-split cross-seed variance is NON-uniform (#2701 alphonse)** — single_in_dist (test stdev=2.53) and re_rand (test stdev=1.82) are the LARGEST cross-seed variance contributors; geom_camber_cruise (stdev=1.38) is the most REPRODUCIBLE. **Load-bearing OOD splits are also load-bearing seed-noise contributors.** Per-split stabilization (e.g., per-split λ or per-split SWA) would attack the cross-seed variance bottleneck directly.
91. **Mean log_σ drift correlates with seed performance (#2701 alphonse, INDEPENDENT CONFIRMATION OF #25)** — seed=2 has smallest |mean(log_σ)|=−1.869 (closer to zero) and worst test=42.20; seed=0 has largest |mean(log_σ)|=−1.980 and best test=38.77. **Supports banked #25 "mean-drift inflates eff_w → val regression" mechanism**; the seed=2 result is a clean independent confirmation of the mean-drift instability identified in #2443 alphonse.
92. **clip_fraction is seed-invariant at max_norm=0.5 (#2701 alphonse)** — 0.9992 / 0.9998 / 0.9996 across 3 seeds. **7th independent clip_fraction methodology confirmation** (extending #2606 per-step finding to seed axis); clip behavior is a function of (model + data + loss), NOT of seed.

## Key open bottlenecks

1. **geom_camber_rc** — **load-bearing OOD split, ~2× the other splits' error**. val=58.13, test=52.78 at new hybrid baseline (improved from 58.29/54.60 at σ=0.5 baseline; PR #2311 gave −1.82 test gain here). Being attacked via #2442 nezuko (n_head sweep — capacity axis), #2390 askeladd (Lion wd), #2512 thorfinn (multi-scale RFF). **Future architecture/data work should prioritize this split.**
2. **SWA window only averages 2-3 epochs at swa_lr=6e-5** (timeout at 13/15 epochs) — **MULTIPLE axes now in flight after the SWALR-overrides-cosine mechanism characterization**:
   - #2463 tanjiro swa_lr value (0.05x vs 0.5x baseline floor)
   - #2481 edward anneal_epochs=1 (1-epoch ramp vs 2-epoch) — gets all 3 averaged epochs at the swa_lr floor
   - #2484 frieren skip-SWALR entirely — averages cosine-tail weights instead of SWALR-floor weights
   - **swa_start_frac<0.75 axis CLOSED** (#2429 edward); start_frac=0.85 (later) untouched
3. **Lion+Kendall σ-collapse** — **3 INDEPENDENT FIXES NOW IDENTIFIED**: (a) #2311 fern hybrid Lion+AdamW (PENDING WINNER: val 45.22 / test 38.77, spread 0.81); (b) #2443 alphonse init at AdamW-equilibrium (BANKED: spread 0.478, test −0.40 but val +0.61); (c) #2500 alphonse mean-anchor loss (in flight; targets mean drift mechanism uncovered by #2443). Compound (hybrid + init + anchor) is the natural future best.
4. **Lion lr = 3e-4 confirmed near optimum** (#2297 V-shape). Lr axis CLOSED.
5. **Lion warmup axis CLOSED on this 13-epoch budget** (#2363 frieren) — Adam→Lion mental model fails; warmup at lower LR makes early epochs WORSE not better.
6. **Capacity-axis dead zone** — width (#2354), depth (legacy), slice_num (#2378), film_mid_dim (#2616) all gated by SWA window or step-time cost. **All 4 single-CLI capacity axes now closed**; future capacity work must use equal-compute reshuffles (#2442 nezuko n_head, pending API recovery) or structural changes that fit in the 13-epoch budget. If n_head=2 or n_head=8 wins, banked axis; if both regress, capacity bottleneck is genuinely orthogonal to attention granularity.

## Potential next directions (Wave 12 / post-hybrid-optimizer baseline)

1. ~~**Lion + hybrid AdamW for Kendall σ heads (#2311 fern)**~~ — **MERGED ✓** (val 45.22 / test 38.77).
2. ~~**Multi-scale RFF 8×σ=0.5 + 8×σ=0.1 (#2512 thorfinn)**~~ — **CLOSED ✗** (val +2.66%, test +1.20%; σ axis exhausted).
3. ~~**huber_beta re-sweep on hybrid merged baseline (#2540 fern)**~~ — **CLOSED ✗** (β=0.3 robust; new β–σ coupling mechanism banked).
4. ~~**Anchor mean(log_σ) loss at AdamW-eq + init at eq (#2500 alphonse)**~~ — **CLOSED ✗** (both arms regress; per-split asymmetry banked).
5. ~~**hybrid_kendall_lr push {1e-3, 2e-3} on hybrid (#2604 fern)**~~ — **CLOSED ✗** (σ-spread ceiling at 0.475; both arms regress; U-curve found via channel-level falsification).
6. ~~**max_norm sweep {1.0, 2.0} on hybrid (#2606 thorfinn)**~~ — **CLOSED ✗** (max_norm=0.5 robust; both arms regress monotonically; 8 banked findings inc. CRITICAL methodology: clip_fraction=1.0 was a summary-key artifact, real per-step ~99%).
7. ~~**film_mid_dim sweep {32, 128} on hybrid (#2616 alphonse)**~~ — **CLOSED ✗** (both arms regress monotonically; capacity-axis dead zone confirmed; σ-spread orthogonal to FiLM).
8. **huber_beta LOW sweep {0.2, 0.15} on hybrid (#2666 fern)** — tests β-side of β–σ coupling; produces more spread via residual-shape mechanism rather than head-lr. **Diagnostic test:** distinguishes "premature commitment was the failure mode" from "U-curve is structural across mechanisms".
9. ~~**max_norm BRACKET LOW {0.25, 0.35} on hybrid (#2674 thorfinn)**~~ — **MERGED ✓** (max_norm=0.35 beats baseline on both val −0.064 / test −0.129; test-side U-curve minimum closed at 0.35).
9b. ~~**second-seed confirmation {seeds 1, 2} on #2311 (#2701 alphonse)**~~ — **CLOSED ✗** (`Seed-sensitive baseline` verdict per PR rubric: cross-seed mean(val)=46.92 stdev=2.05 > 1.5; σ-collapse-fix MECHANISM robust, headline MAGNITUDE seed-sensitive; 6 banked findings inc. SWA-window-truncation = 40% of cross-seed variance).
9c. ~~**Lion lr bracket {2e-4, 4e-4} on max_norm=0.35 (#2731 thorfinn)**~~ — **CLOSED ✗** (both arms regress past 46.0 threshold; Lion lr=3e-4 optimum from #2297 transfers cleanly to saturated-clip regime; 8 banked findings inc. lr × max_norm orthogonal across bracket).
9d. ~~**Huber β fine-bracket {0.25, 0.35} on max_norm=0.35 (#2736 fern)**~~ — **CLOSED ✗** (both arms regress past 45.40 threshold; β=0.3 robust across max_norm ∈ {0.35, 0.5}; 8 banked findings inc. mechanism transfer robustness).
9e. **hybrid_kendall_lr fine-bracket on max_norm=0.35 (#2773 fern NEW)** — lr × clip-saturation coupling; tests whether lr-driven σ-spread mechanism (banked #2604: spread→Kendall surf_ux CONCENTRATE) transfers cleanly to saturated-clip regime. Parallel robustness statement to β-side (#2736 just closed). Single CLI flag.
7. **swa_lr ∈ {0.05x, 0.5x} sweep on σ=0.5 (#2463 tanjiro)** — averaging-lr level axis from #2342 mechanism finding.
8. **SWA anneal_epochs=1 on σ=0.5 (#2481 edward)** — SWALR ramp speed; all 3 averaged epochs at swa_lr.
9. **Skip-SWALR entirely on σ=0.5 (#2484 frieren)** — direct test of SWALR-overrides-cosine mental model; cosine-tail vs SWALR-floor averaging.
10. **n_head ∈ {2, 8} bidirectional at n_hidden=128 on σ=0.5 (#2442 nezuko)** — equal-compute attention-granularity reshuffle; targets geom_camber_rc.
11. **Lion wd sweep on σ=0.5 (#2390 askeladd)** — orthogonal Lion fine-tuning axis.
12. **Delay-SWA-start (frac=0.85)** — narrow window but truly low-lr averaging (tanjiro #2342 suggested follow-up #3). Composes with #2463 Arm A. **Opposite direction from CLOSED #2429.**
11. **Second seed on σ=0.5 baseline** — confirm win magnitude (currently single seed; val effect ~3.9% is well above seed noise).
12. **Coordinate system rethink** — polar/arc-length around airfoil; geom_camber_rc primary target. May compound with RFF σ↓ since both reduce frequency content.
13. **Test-time augmentation** — if test still falling at lower σ, maybe inference-time geometry perturbation could push test_geom_camber_rc further.
14. **Fixed (non-learnable) per-channel loss weights** matching #1906 pattern — even simpler alt than #2443 if init-drift turns out to be the issue.
15. **n_layers depth sweep at fixed n_hidden** — if #2442 closes head axis, depth at fixed compute is the next architectural axis.
16. **clip_fraction definition audit / pre-clip grad_norm p50/p90/p99 logging** — resolve the 99% (frieren #2363, thorfinn #2407 confirmed) vs 74% (#2063 BASELINE.md) discrepancy; informs future grad-clip and gradient-distribution work.
17. **max_norm relaxation under Lion+σ=0.5** — clip_fraction=99% under max_norm=0.5 means Lion effectively sign-of-sign at fixed norm. Under Lion (not yet tested on this stack) try max_norm=1.0 or 2.0; orthogonal to alphonse #2270's AdamW result.
18. **Cyclic LR through SWA window** (Izmailov's original recommendation) — if skip-SWALR (#2484) wins or ties, the natural follow-up is cosine-restarts during averaging window.

## Mechanism-axis coverage

### ✓ Landed (10 axes)
1. Huber β=1.0 (#1452), 2. Per-sample Re-weight (#1586), 3. FiLM (#1585), 4. Grad-clip 1.0 (#1731), 5. Grad-clip 0.5 (#1831), 6. Kendall σ (#1906), 7. RFF σ=1.0 (#2082), 8. Huber β=0.3 (#1757), 9. Lion lr=3e-4 wd=3e-4 (#2063), 10. **RFF σ=0.5 (#2168)** ← CURRENT

### 🔬 In-flight (Wave 12 — post-Loop-85)
- **huber_beta GAP+EXTEND sweep {0.25, 0.10} on max_norm=0.35 (#2981 fern NEW Loop 92)** — paper-appendix-strengthening data points; extends NEW NON-MONOTONE-COST-MONOTONE-MECHANISM transfer-pattern class (1st member from #2919 β LOWER). Arm 1 (GAP-FILL) β=0.25 fills missing point in β UPPER fine bracket #2736 on the NEW saturated-clip baseline (potential MICRO-WIN candidate at val < 45.10 OR directional MICRO-WIN at val ∈ [45.10, 45.30]). Arm 2 (EXTEND-DEEPER) β=0.10 extends LOWER bracket below #2919 β=0.15 to test (a) non-monotone pattern continues (β=0.10 ~ local max with partial recovery — multiple local maxima class refinement), (b) β=0.20 is THE local maximum (β=0.10 val ~ 45.0-45.5), or (c) monotonic strong regression after partial recovery. Combined with #2919 β=0.20/0.15 + #2736 β=0.25/0.35 + baseline β=0.30, forms a 6-point β-axis tableau across {0.10, 0.15, 0.20, 0.25, 0.30, 0.35} for paper-publishable cross-section of NON-MONOTONE-COST-MONOTONE-MECHANISM class. No train.py edit required (--huber_beta CLI-flagged).
- **slice_num sweep {32, 128} on max_norm=0.35 (#2962 alphonse)** — 16th paper-appendix mechanism-transfer axis label; NEW **NUM-SLICES** class. Transolver-specific Slice Attention granularity (number of slices per layer; baseline 64; Arm 1 32 HALF reduced slice-routing capacity; Arm 2 128 DOUBLE expanded slice-routing capacity). Forward-pass-shape axis at fixed (n_layers=5, n_head=4, n_hidden=128). Per-layer Slice Attention compute is O(N · slice_num) — step-time should scale roughly linearly (arm 1 ~0.5× baseline step-time; arm 2 ~2× baseline step-time). Predicted: BOTH-LOSE Pareto-cap (forward-pass-shape capacity-axis dead-zone) if step-time × SWA-window-truncation coupling dominates, OR ASYMMETRIC-V (DOMINANT pattern, 8/14 = 57%) if saturated-clip + 30-min cap creates sharp val minimum at slice_num=64. **σ-spread BREAK PREDICTED per #201** (3rd forward-pass-shape axis after n_head + n_layers, both of which broke σ-spread). NEW DIAGNOSTIC: per-arm step-time + SWA-window completion (per banked #179 + #205 step-time scaling; could trigger SWA-NEVER-ENTERED per #204 if Arm 2 slice_num=128 step-time × 13-epoch budget > 30 min). train.py edit required (add `slice_num: int = 64` Config flag; replace `slice_num=64,` in model_config dict at line 642 with `slice_num=cfg.slice_num,`).
- **mlp_ratio sweep {1, 4} on max_norm=0.35 (#2966 thorfinn NEW Loop 85)** — 17th paper-appendix mechanism-transfer axis label; NEW **FFN-RATIO** class. Transolver per-block FFN-ratio (FFN hidden = n_hidden × mlp_ratio; baseline 2 = FFN-hidden 256; Arm 1 mlp_ratio=1 → FFN-hidden 128 narrower; Arm 2 mlp_ratio=4 → FFN-hidden 512 wider). THIRD forward-pass-shape axis after n_head granularity #2901 and n_layers depth #2947 — completes the FORWARD-PASS-SHAPE TRIO. **Tests whether σ-spread + grad_norm BREAK extends to FFN-side forward-pass-shape** (per-block-MLP-width INSIDE the per-block FFN sub-module) or is restricted to attention-side axes (n_head granularity + n_layers depth, both OUTSIDE the per-block FFN). Predicted class: BOTH-LOSE Pareto-cap (FFN-capacity dead-zone, joins #2818 swa_start_frac) OR ASYMMETRIC-V (would extend DOMINANT to 9/15 = 60%). NEW DIAGNOSTIC: σ-spread + grad_norm + channel-ordering side-by-side comparison with #2901 n_head and #2947 n_layers for cross-axis paper-publishable claim refinement. train.py edit required (add `mlp_ratio: int = 2` Config flag; replace `mlp_ratio=2,` in model_config dict at line 643 with `mlp_ratio=cfg.mlp_ratio,`).
- **film_mid_dim sweep {32, 128} on max_norm=0.35 (#2985 askeladd NEW Loop 93)** — 16th paper-appendix mechanism-transfer axis label; NEW **FILM-BOTTLENECK** class. Tests FiLM conditioning capacity (bottleneck dim controlling cond_dim=11 → film_mid_dim → 2·n_layers·n_hidden = 1280 conditioning parameters; baseline 64; Arm 1 32 HALF capacity; Arm 2 128 DOUBLE capacity). Compute-invariant — FiLM is sub-dominant in step-time (in sharp contrast to forward-pass-shape axes slice_num/mlp_ratio which DO shift step-time); predicted clean 2-epoch SWA-window truncation under banked #170 continuous-function form. Predicted class: ASYMMETRIC-V (current at optimum, joins DOMINANT 8/16→9/17 if confirmed) OR DEGENERATE-AXIS (FiLM dim not load-bearing, joins anneal_epochs #2877 if confirmed) OR NEW class. **σ-spread INVARIANT prediction**: 0.475 ± 0.01 (15th cross-axis confirmation if invariant per #173 — only forward-pass-shape axes break σ-spread). No train.py edit required (--film_mid_dim already CLI-flagged at train.py:595).
- n_head ∈ {2, 8} bidirectional sweep at n_hidden=128 on σ=0.5 (#2442 nezuko) — pending API recovery
- swa_lr ∈ {0.05x, 0.5x} sweep on σ=0.5 (#2463 tanjiro) — pending API recovery
- SWA anneal_epochs=1 on σ=0.5 (#2481 edward) — pending API recovery
- Skip-SWALR entirely on σ=0.5 (#2484 frieren) — pending API recovery

### ⭐ Merged (Wave 12 winners)
- hybrid Lion+AdamW for Kendall σ heads (#2311 fern, 2026-05-13 19:10): val 45.2181 / test 38.7661, −1.20% val / −2.26% test. σ-spread 0→0.475. Structural σ-collapse fix.
- **max_norm 0.5→0.35 on hybrid stack (#2674 thorfinn, 2026-05-14 02:06): val 45.1538 / test 38.6367, −0.14% val / −0.33% test. Clip U-curve test-side minimum closed. Single CLI flag change.** ← CURRENT

### ✗ Closed (30+ axes)
- drop-grad-clip-on-Lion (#2347, 2026-05-13 16:00): max_norm=0.5 is right setting, 4 banked findings inc. 4th σ-collapse confirmation
- slice_num=96 (#2378, 2026-05-13 16:30): NOT a capacity axis (+5K params not +310K), regressed +4.19 val, 5 banked findings inc. 5th σ-collapse confirmation
- max_norm relaxation {0.75, 1.0} (#2270, 2026-05-13 16:35): clip_fraction stays ≥0.995 even at 2× cap under β=0.3-Huber, 5 banked findings inc. AdamW-equilibrium log_σ targets for #2443
- T_max ∈ {10, 12} cosine compression (#2342, 2026-05-13 17:00): clean regression +5.5% to +8.9% val; **6 banked findings inc. SWALR-overrides-cosine pathology — the most valuable mechanistic finding of Wave 12**; opens new swa_lr direction axis via #2463
- swa_start_frac ∈ {0.5, 0.6} (#2429 edward, 2026-05-13 17:30): monotonically worse with smaller frac; **3rd SWALR-overrides-cosine confirmation**; geom_camber_rc identified as load-bearing OOD split (~2× other splits)
- Lion + linear warmup 3 epochs (#2363 frieren, 2026-05-13 17:30): epoch-1 val WORSE with warmup → Adam→Lion mental model refuted; **Lion has no chaotic init phase**; clip_fraction invariant to lr; 9th σ-collapse confirmation
- Kendall log_σ init at AdamW-equilibrium on σ=0.5 Lion (#2443 alphonse, 2026-05-13 18:10): **cleanest σ-collapse mechanism finding on wave — init alone prevents collapse**; spread 0.000→0.478, test −0.40, but mean drift inflates eff_w 3× → val +0.61; motivates #2500 mean anchor
- RFF σ=0.1 + σ=0.25 seed-1 bracket (#2407 thorfinn, 2026-05-13 18:45): σ axis exhausted on val; σ=0.25 test win refuted as seed-0 outlier; **σ=0.1 = regularizer NOT OOD-prior** (driver = single_in_dist, not OOD-geom); 2nd clip_fraction≈0.99 confirmation; 10th + 11th σ-collapse confirmations; motivates #2512 multi-scale RFF
- Multi-scale RFF 8×σ=0.5 + 8×σ=0.1 (#2512 thorfinn, 2026-05-13 20:15): does NOT compose on hybrid stack (val +2.66%, test +1.20%); **σ axis decisively closed at σ=0.5** (10 configs evaluated since Lion adoption); banked: σ=0.1 regularizer effect was σ-collapse-conditional — #2311 substitutes for it; 3rd–4th clip_fraction=1.0 confirmations; motivates #2606 max_norm sweep
- Huber β sweep {0.5, 1.0} on hybrid (#2540 fern, 2026-05-13 20:45): monotonic regression both arms (β=0.5 +3.54% val, β=1.0 +10.12% val); **β=0.3 robust to hybrid optimizer change**; **NEW β–σ coupling mechanism** (β↑ collapses σ-spread); 12th clip_fraction confirmation; refuted grad-norm prediction (smooth-L1 slope 1/β); motivates #2604 hybrid_kendall_lr push
- Anchor mean(log_σ) + AdamW-eq init on hybrid (#2500 alphonse, 2026-05-13 22:15): both arms regress (λ=1 val 45.90, λ=5 val 47.28); mean-drift fix invalid post-#2311; 4 banked findings inc. **NEW per-split asymmetry: spread expansion hurts geom_camber_rc but helps other splits** (important context for #2604), AdamW-eq init expands spread to 0.70, natural hybrid equilibrium (~mean −2.0) outperforms target −1.4; motivates #2616 film_mid_dim sweep
- hybrid_kendall_lr push {1e-3, 2e-3} on hybrid (#2604 fern, 2026-05-13 23:50): **σ-spread ceiling found at 0.475**; both arms regress monotonically (lr=1e-3 val 46.93/test 39.82, lr=2e-3 val 46.94/test 39.84); 9 banked findings inc. **channel-level falsification of compounding** (single_in_dist test gained from 0→0.475 but loses from 0.475→0.82/0.92, U-shaped not monotonic), **premature-commitment mechanism** (Kendall weight surf_ux ~120 vs baseline ~22), **SWA rescue scales with lr** (1.6× more rescue at higher kendall_lr), **σ-spread monotonic+smooth on AdamW** (rules out lr-too-high failure mode), **channel ordering lr-invariant**, 5th independent clip_fraction=1.0 confirmation (13+ total); motivates #2666 β LOWER sweep (β-side of coupling)
- max_norm sweep {1.0, 2.0} on hybrid (#2606 thorfinn, 2026-05-13 23:55): max_norm=0.5 robust; both arms regress monotonically on val (Arm 1 +0.73, Arm 2 +0.87); 8 banked findings inc. **CRITICAL METHODOLOGY: clip_fraction=1.0 was a final-epoch summary-key artifact, NOT per-step truth** (retroactively corrects 13+ prior confirmations; real per-step at max_norm=0.5 ≈ 99%; relaxation arms 98.3% at 1.0 → 82.0% at 2.0); **NEW per-split tight-clip-OOD-friendly signature** (val_geom_camber_rc worst-hit in both arms +1.62/+1.44); **NEW max_norm × swa_lr co-tuning hypothesis**; Kendall σ orthogonal to max_norm; grad_norm distribution invariant to clip; motivates #2674 max_norm BRACKET LOW
- film_mid_dim sweep {32, 128} on hybrid (#2616 alphonse, 2026-05-14 01:15): film_mid_dim=64 robust; both arms regress (Arm 1 mid_dim=32 val 45.92/test 39.27 +0.70/+0.50, Arm 2 mid_dim=128 val 45.94/test 39.36 +0.72/+0.59); 8 banked findings inc. **NEW: NO OOD asymmetry on FiLM capacity axis** (cleanly refutes architectural-bottleneck hypothesis for #2500 σ-spread asymmetry — geom_camber_rc moves +1.31/+1.16 evenly with other splits, NO differential signature), **NEW: σ-spread orthogonal to FiLM capacity** (final spread 0.473–0.481 indistinguishable from baseline 0.475), **capacity-axis dead zone now 4-of-4 single-CLI knobs closed**, FiLM modulation scales with mid_dim but unproductive, mid_dim=32 cleanly underfits, mid_dim=128 step-time blow-up; motivates #2701 second-seed confirmation; redirects #2500 per-split asymmetry investigation away from architecture toward per-split loss reweighting / per-OOD-feature heads / per-split SWA selection
- huber_beta fine-bracket {0.25, 0.35} on max_norm=0.35 (#2736 fern, 2026-05-14 04:10): β=0.3 robust on saturated-clip baseline; both arms regress past close threshold (β=0.25 val 46.13/test 39.20 +0.97/+0.56, β=0.35 val 46.21/test 39.12 +1.06/+0.48); 8 banked findings inc. **PAPER-STRENGTHENING: β-driven σ-spread mechanism transfers cleanly to saturated-clip regime** (all 4 spread/weight predictions from max_norm=0.5 land within ±0.01 of max_norm=0.35 actuals — the β→spread coupling is governed by β alone, NOT gradient-magnitude flow; transfer happens under ZERO sub-threshold steps), **σ-spread monotone in β at every trajectory check-point** (not a terminal-state artifact), **β=0.3 optimum invariant across max_norm ∈ {0.35, 0.5} clip regimes** (clean robustness claim for paper), **channel ordering invariant on saturated-clip stack across β axis** (5th independent confirmation of cross-axis ordering invariance), **clip_fraction β-invariant under max_norm=0.35** (4875/4875 saturated both arms), **single_in_dist regresses largest under β perturbations** (extends dataset-level U-curve to new baseline), test-vs-val ordering mild asymmetry below seed noise; motivates #2773 fern head_lr cross-axis mechanism check on new baseline (parallel robustness test)
- huber_beta LOW sweep {0.2, 0.15} on hybrid (#2666 fern, 2026-05-14 02:35): NOT mergeable (Arm 1 β=0.2 +0.44 val/+0.18 test, Arm 2 β=0.15 +0.22 val but −0.07 test sub-win); 8 banked findings inc. **HEADLINE: β-driven and lr-driven σ-spread are mechanistically distinct** (β-driven REDISTRIBUTES Kendall weights 44.3→37.5 vs lr-driven CONCENTRATES at ~120; resolves #2604 premature-commitment question), **σ-spread axis NOT structurally capped at 0.475** (β-driven extends to 0.546 at +0.16 val cost vs lr-driven 0.82 at +1.72), **channel ordering invariant** across {β, head-lr, optimizer split}, **single_in_dist U-curve dataset-level** (regresses past 0.475 via both mechanisms), Kendall weight direction INVERTS between mechanisms, β-axis acts as regularizer (test improvement asymmetric to val regression); motivates #2736 β fine-bracket on new max_norm=0.35 baseline
- Lion lr bracket {2e-4, 4e-4} on max_norm=0.35 (#2731 thorfinn, 2026-05-14 04:35): Lion lr=3e-4 robust on saturated-clip baseline; both arms regress past close threshold (Arm 1 lr=2e-4 val 47.88/test 40.72 +2.72/+2.09 — strong regression; Arm 2 lr=4e-4 val 46.14/test 39.34 +0.99/+0.71 — at regression boundary); 8 banked findings (#79–#86) inc. **PAPER-STRENGTHENING: Lion lr=3e-4 optimum invariant across max_norm ∈ {0.35, 0.5} clip regimes** (parallel robustness claim to #2736 β-side; complete 2-axis mechanism-transfer story under saturated-clip regime), **lr-driven σ-spread mechanism transfers under saturated-clip** (lr=2e-4 shrinks spread; lr=4e-4 expands toward 0.5; mirrors max_norm=0.5 trajectory; mechanism is lr-flow not gradient-magnitude-flow), **V-shape skewed toward smaller lr** (Arm 1 worse than Arm 2 by 1.73 val under symmetric ±33% bracket — non-symmetric optimum), **swa_lr × Lion-lr coupling resolved** (swa_lr = lr × 0.2 hardcoded → implicit swa_lr sweep with Lion-lr; Arm 1 swa_lr=4e-5, Arm 2 swa_lr=8e-5; SWA mechanism robust at both), clip_fraction=1.000 invariant across Lion-lr at max_norm=0.35 (extends saturated-clip-axis-invariant property to lr-axis), single_in_dist regresses largest at both arms (extends dataset-level U-curve), channel ordering surf_ux=min/vol_ux=max invariant on Lion-lr axis under saturated-clip (6th cross-axis confirmation); motivates seed-confirmation on new max_norm=0.35 baseline (parallel to #2701)
- 3-seed confirmation on hybrid stack (#2701 alphonse, 2026-05-14 04:45): cross-seed mean(val)=46.917, stdev=2.049 → **`Seed-sensitive baseline` verdict per PR rubric** (stdev > 1.5 threshold); 6 banked findings (#87–#92) inc. **PAPER-STRENGTHENING: SWA-window truncation accounts for ~40% of cross-seed variance** (30-min cap acts as hidden seed-multiplier; seeds where SWA window truncates land in tail; correlation between SWA-window completion and val/test outcomes establishes truncation as load-bearing nuisance variable for the paper appendix), **σ-spread cross-seed reproducibility** (final spread within ±0.02 across all 3 seeds — channel ordering and spread magnitude both seed-invariant; the mechanism is stable, the seed variance lives in SWA-window completion timing), Kendall weight direction seed-invariant (surf_ux still min, vol_ux still max), per-split asymmetry seed-stable (geom_camber_rc U-curve persists), seed=0 outlier identified (val 45.15 vs 47.30/48.30 seeds 1, 2 — geom_camber_rc 2.5× other splits), clip_fraction=1.000 cross-seed invariant on max_norm=0.5 baseline (extends saturated-clip-axis-invariant property to seed axis); motivates 2-seed confirmation on max_norm=0.35 (CURRENT baseline)
- hybrid_kendall_lr fine-bracket {3e-4, 7.5e-4} on max_norm=0.35 (#2773 fern, 2026-05-14 06:16): lr=5e-4 robust on saturated-clip baseline; both arms regress past close threshold (Arm 1 lr=3e-4 val 46.58/test 39.51 +1.42/+0.87, Arm 2 lr=7.5e-4 val 46.59/test 39.59 +1.43/+0.95); 8 banked findings (#99–#106) inc. **PAPER-STRENGTHENING: lr-driven σ-spread mechanism DIRECTION transfers cleanly to saturated-clip regime** (Arm 1 spread 0.073 < 0.475 < Arm 2 spread 0.747; surf_ux weight 9.4/44.5/83.8 with kendall_lr 3e-4/5e-4/7.5e-4 — CONCENTRATE direction matches banked #2604; 2nd axis of paper-appendix mechanism-transfer story after β #2736), **NEW: lr-driven mechanism magnitude is ASYMMETRIC across baseline** (Arm 1 spread 0.073 vs predicted 0.30-0.42 → 5× under-shoot on lower side; Arm 2 spread 0.747 ≈ linear extrapolation on upper side; mechanism is concave/saturating on lower side, roughly linear on upper — floor mechanism: log_σ init at 0, AdamW step too small before cosine kills lr; paper-publishable contrast with β-axis which is symmetric in #2666), lr=5e-4 shallow local optimum confirmation (both ±perturbations pay ~+1.4 val regardless of spread direction), clip_fraction invariant across kendall_lr at max_norm=0.35 (mean 0.9996-1.0 across 4877 sampled steps; extends saturated-clip-axis-invariant property to kendall_lr axis), channel ordering surf_ux=min/vol_ux=max invariant on kendall_lr axis (8th cross-axis confirmation), σ-spread trajectory monotone in lr at every check-point (25/50/75/100% — not terminal-state artifact, mirrors β-axis trajectory finding from #2736); axis closes
- Lion wd fine-bracket {1e-3, 3e-3} on max_norm=0.35 (#2791 alphonse, 2026-05-14 06:16): wd=3e-4 robust on saturated-clip baseline (Arm 1 wd=1e-3 val 46.22/test 39.57 +1.07/+0.94 strong regression; Arm 2 wd=3e-3 val 45.28/test 38.92 +0.12/+0.29 directional close within seed noise of baseline); 6 banked findings (#93–#98) inc. **PAPER-STRENGTHENING: Lion wd-axis is the 4th & final mechanism-transfer axis for paper appendix** (joins {β #2736, lr #2731, seed #2701/#2790} — completes 4-axis story), **NEW: banked #19 wd-shrinkage mechanism is gradient-magnitude-flow-DEPENDENT** (wd=3e-3 advantage from σ=1.0 stack does NOT transfer to saturated-clip regime; under clip_fraction=1.000 every step is strict sign-step + wd-shrinkage, extra wd pushes past U-curve test-side regularization optimum; PAPER-PUBLISHABLE axis-level contrast with β-axis which is gradient-magnitude-flow-INDEPENDENT per #2736), **σ-spread orthogonal to wd-axis under saturated-clip** (spread {0.471, 0.475, 0.475} bit-identical — 6th cross-axis spread-invariance confirmation), **channel ordering invariant on wd-axis** (surf_ux=min/vol_ux=max — 7th cross-axis confirmation), clip_fraction=1.000 invariant from step ~1 across all 3 wd values (pre-clip grad-norm distribution unchanged by wd-shrinkage; no feedback loop), SWA-window absorbs wd over-shrinkage partially (Arm 2 10× wd over baseline only +0.12 val vs Arm 1 3.3× wd +1.07 val — wd × SWA-window coupling potential follow-up); axis closes
- 2-seed confirmation on max_norm=0.35 baseline (#2790 thorfinn, 2026-05-14 06:55): cross-seed swa_val mean=47.4603 stdev=**2.541** > 1.5 threshold → **`Seed-sensitive baseline` verdict** (cross-seed base_val stdev=1.99 also > 1.5). **stdev HIGHER than #2701's 2.05 on OLD #2311 baseline** — saturated-clip regime did NOT regularize seed axis despite being lr-invariant (#2731) and β-invariant (#2736). **PAPER-PUBLISHABLE NEGATIVE result for regularization-via-saturated-clip hypothesis** — completes 4-axis paper-appendix story with TWO transfer behaviors: {β/lr DIRECTION} transfer cleanly; {wd, seed} do NOT (paper-publishable contrast about which nuisance variables saturated-clip mechanism does and does not stabilize). 6 banked findings (#107–#112) inc. **PAPER-STRENGTHENING #107** saturated-clip-is-NOT-seed-regularizer (per-step optimizer-geometry IS regularized — clip_fraction=1.000/channel-ordering/σ-spread reproducibility ±0.004 — but seed-driven OUTCOMES are NOT), **PAPER-STRENGTHENING #108** channel ordering surf_ux=min/vol_ux=max invariant across 3 seeds on new baseline (9th cross-axis confirmation; full table {β/lr/head-lr/optimizer split/max_norm/FiLM mid_dim/seed/wd/kendall_lr}), **PAPER-STRENGTHENING #109** clip_fraction=1.000 cross-seed invariant on new baseline (extends saturated-clip property to seed axis), **#110** SWA-window truncation explains only PARTIAL variance (refines #2701 from "40%" to "40% on this baseline pair, but smaller half of variance" — seeds 0/1 same 3/5 SWA completion but differ 2.79 val), **#111** per-seed log_σ trajectories monotonically less-negative with seed index, **#112** σ-spread reproducibility excellent ±0.004 on completed SWA windows; motivates RFF capacity sweep as 5th paper-appendix mechanism-transfer axis
- swa_start_frac fine-bracket {0.6, 0.85} on max_norm=0.35 (#2818 fern, 2026-05-14 08:35): Arm 1 s_f=0.6 val 46.92/test 40.10 +1.77/+1.46 — strong regression close (dilution by under-converged averaged weights; SWALR flat 6e-5 from epoch 10 overrides cosine decay; **3rd independent SWALR-overrides-cosine confirmation** extending banked #18); Arm 2 s_f=0.85 val 45.76/test 39.29 +0.61/+0.65 — directional close (within #2790 stdev=2.54 seed noise; **SWA window collapses to 1 epoch under 30-min cap** — swa_val_avg == best_val_avg to machine precision). 6 banked findings (#113–#118) inc. **PAPER-STRENGTHENING #113** s_f=0.75 is **Pareto-optimal SWA-window placement under 30-min cap** (both directions lose for DISTINCT mechanism reasons; optimum co-tuned to wall-clock cap, not SWA theory alone — paper-publishable Pareto-optimal characterization extending banked #18 + #2701 + #2790), **PAPER-STRENGTHENING #114** SWA window collapse to 1 epoch at s_f=0.85 → SWA averaging contribution vanishes entirely, **PAPER-STRENGTHENING #115** σ-spread invariant on swa_start_frac axis (Arm 1 spread=0.476, Arm 2 spread=0.473 — 10th cross-axis σ-spread invariance confirmation), **PAPER-STRENGTHENING #116** channel ordering invariant on swa_start_frac axis (10th cross-axis confirmation), **PAPER-STRENGTHENING #117** clip_fraction=1.000 invariant on swa_start_frac axis (1995/1995 sampled steps both arms; 6th cross-axis confirmation; grad_norm median 5.32/5.26 matches baseline 5.30), **#118** trajectory bit-identical pre-SWA across arms (epochs 1-9 produce identical val_avg — methodology for future SWA-axis work); also banked **physical-units mae_* metrics authoritative; normalized-space loss is a cruise-target divide-by-tiny accounting issue, not a regression source**; motivates fourier_sigma sweep as 6th paper-appendix mechanism-transfer axis (RFF frequency axis orthogonal to thorfinn #2835's RFF capacity)
- fourier_num_features sweep {8, 32} on max_norm=0.35 (#2835 thorfinn, 2026-05-14 09:36): Arm 1 n=8 val 48.32/test 40.42 +3.17/+1.78 — strong regression close; Arm 2 n=32 val 46.07/test 38.62 +0.91/**−0.06 (TIE)** — directional close on val but **test-side near-tie**; baseline n=16 at the val-local optimum. **Asymmetric reach parallels lr-axis #2773**: lower-side under-parameterization (n=8) hurts 3.5× more than upper-side over-parameterization (n=32) on val — capacity under saturated-clip exhibits the same concave/saturating-LOWER + linear-UPPER asymmetric reach signature as step-size. **First cross-axis where val and test directions partially disagree** under saturated-clip: 3 of 4 test splits favor n=32 marginally (Δtest p_in −0.08, p_oodc −0.07, p_re −0.12; p_tan only +0.04); conditioned on truncated 2/4 SWA window completion. 7 banked findings (#119–#125) inc. **PAPER-STRENGTHENING #119** RFF-capacity U-curve closure at n=16 with asymmetric reach (mechanism-transfer axis #6 on saturated-clip; the asymmetric-reach signature now shared across step-size + feature-capacity hyperparameter classes), **PAPER-STRENGTHENING #120** first val/test directional disagreement on saturated-clip baseline (RFF axis is special — n=16 val optimum may not align with test optimum), **PAPER-STRENGTHENING #121** RFF-capacity × step-time DECOUPLED at this scale (141.5 s/epoch invariant across {n=8, 16, 32} — Transolver attention dominates, RFF projection negligible; kills #2354/#2616 worry that RFF couples step-time × SWA-window), **PAPER-STRENGTHENING #122** σ-spread orthogonality holds on RFF-capacity axis 8th cross-axis (0.4695/0.4461 vs baseline 0.475; Arm 2 mild −6% compression attributed to truncated SWA window rather than RFF×Kendall interaction), **PAPER-STRENGTHENING #123** channel ordering invariant on RFF-capacity axis 10th cross-axis (surf_ux=min/vol_ux=max preserved both arms), **PAPER-STRENGTHENING #124** clip_fraction=1.000 invariant on RFF-capacity axis 6th cross-axis (4875/4875 saturated steps both arms), **#125** 3rd consecutive PR confirming SWA-window-truncation under 30-min cap as paper-appendix nuisance variable (#2790, #2818, #2835 — appendix needs cap-coincidence caveat called out explicitly); motivates anneal_epochs sweep as 7th paper-appendix mechanism-transfer axis (SWALR ramp-speed under saturated-clip)
- Lion wd LOW bracket {1e-4, 1.5e-4} on max_norm=0.35 (#2819 alphonse, 2026-05-14 09:53): Arm 1 wd=1e-4 val 45.41/test 39.08 +0.25/+0.44 — directional close; Arm 2 wd=1.5e-4 val 45.48/test 38.96 +0.33/+0.32 — directional close; both in seed-noise band of baseline. **4-point wd-axis tableau complete** {1e-4, 1.5e-4, 3e-4, 1e-3, 3e-3}: shallow symmetric basin at baseline with mild upper-tolerance (knee at 3.3× baseline, +1.07; SWA absorbs at 10× baseline, +0.12). 6 banked findings (#126–#131) inc. **PAPER-STRENGTHENING #126** wd-axis tableau complete (shallow symmetric basin), **PAPER-STRENGTHENING #127 NEW HEADLINE** wd-axis (SYMMETRIC) vs lr-axis (ASYMMETRIC V-shape) — paper-publishable mechanism-level distinction under same saturated-clip regime; multiplicative weight-shrinkage and step-magnitude axes have qualitatively different basin geometries, **PAPER-STRENGTHENING #128** paper-appendix table at 7 mechanism-transfer axes with 4 distinct transfer patterns ({β symmetric, lr/RFF/head-lr asymmetric, wd symmetric, seed regression-inflate, swa_start_frac Pareto-cap-coincidence}), **PAPER-STRENGTHENING #129** SWA absorbs wd over+under shrinkage symmetrically (extends #2791 banked #98), **#130** 8th cross-axis σ-spread invariance on lower-wd half-axis (0.4699/0.4687 vs baseline 0.475, Δ ≤ 0.006), **#131** 11th cross-axis channel-ordering invariance; soft signal: 2nd cross-axis val/test partial-disagreement at Arm 2 (parallels RFF-capacity #2835); axis closes
- fourier_sigma sweep {0.25, 1.0} on max_norm=0.35 (#2862 fern, 2026-05-14 10:35): Arm 1 σ=0.25 val 45.71/test 39.34 +0.56/+0.70 — directional close (in [45.50, 46.50]); Arm 2 σ=1.0 val 47.10/test 40.24 +1.95/+1.60 — strong regression close (≥46.50). **σ-axis OPTIMUM TRANSFERS CLEANLY (σ=0.5 holds) AND lands in ASYMMETRIC-V transfer-pattern class** — joins lr/hybrid_kendall_lr/RFF-capacity. ASYMMETRIC-V now DOMINANT pattern (4 of 6 closed axes). 3.5× asymmetric reach: ×0.5 hurts +0.56, ×2 hurts +1.95 under symmetric multiplicative bracket. 8 banked findings (#132–#139) inc. **PAPER-STRENGTHENING #132** σ-axis OPTIMUM TRANSFERS CLEANLY (2nd TRANSFERS-CLEANLY axis after β #2736), **PAPER-STRENGTHENING #133** σ-axis 3.5× asymmetric reach (parallels lr/hybrid_kendall_lr/RFF-capacity), **PAPER-STRENGTHENING #134** 11th cross-axis σ-spread invariance (0.484/0.480 vs baseline 0.475), **PAPER-STRENGTHENING #135** 11th cross-axis channel-ordering invariance, **PAPER-STRENGTHENING #136** 7th cross-axis clip_fraction=1.0 invariance (4875/4875 both arms), **#137 NEW FINDING** smoother RFF (σ=0.25) regularizes OOD-geometry: wins geom_camber_cruise on both val (−1.45) AND test (−0.46), ties geom_camber_rc test (−0.05) — doesn't compensate single_in_dist regression under uniform split-averaging (motivates future split-reweighting work), **#138** pre-clip grad_norm weak σ dependence (5.30 → 5.42 → 5.63 baseline → σ=1.0 → σ=0.25 — smoother RFF produces slightly larger raw gradients before clip; saturated-clip absorbs), **#139 METHODOLOGY** 4th-consecutive SWA-window-truncation under 30-min cap (#2790, #2818, #2835, #2862); axis closes; motivates fern → 9th axis dropout sweep (REGULARIZATION class never tested under saturated-clip)
- anneal_epochs sweep {1, 3} on max_norm=0.35 (#2877 thorfinn, 2026-05-14 11:05): Arm 1 ae=1 val 45.20/test 38.61 +0.05/−0.02 — directional close (in [45.15, 45.50]); Arm 2 ae=3 val **45.13**/test 38.64 **−0.02**/0.00 — directional close (BELOW baseline 45.1538 but ABOVE merge threshold 45.10). **Paper-strengthening NULL/DEGENERATE-AXIS finding**: anneal_epochs is mechanistically degenerate under saturated-clip baseline's swa_start_frac × swa_lr coincidence — cosine final lr (~5.9e-5) ≈ swa_lr (6e-5), so ramp-duration is inert regardless of whether ramp lasts 1, 2, or 3 epochs. **NEW 5th transfer pattern: DEGENERATE-AXIS** (knob has no mechanistic headroom in current configuration; distinct from Pareto-cap-coincidence where both arms LOSE for distinct reasons — here both arms are essentially NEUTRAL). 7-axis paper-appendix table now has **5 distinct transfer patterns** ({β symmetric, lr/RFF/head-lr/σ asymmetric DOMINANT, wd symmetric, seed negative, swa_start_frac Pareto-cap, anneal_epochs DEGENERATE}). 7 banked findings (#140–#146) inc. **PAPER-STRENGTHENING #140** DEGENERATE-AXIS finding strengthens banked GOLD #18 SWALR-overrides-cosine by showing robustness to ramp-duration WHEN lrs coincide, **#141 NEW PATTERN** DEGENERATE-AXIS class on paper-appendix table, **#142** 8th cross-axis σ-spread invariance (0.4750/0.4747 vs baseline 0.475 Δ ≤ 0.0003), **#143** 12th cross-axis channel-ordering invariance, **#144** 7th cross-axis clip_fraction=1.000 invariance, **#145 METHODOLOGY** 5th-consecutive SWA-window-truncation (#2790, #2818, #2835, #2862, #2877), **#146 OPEN QUESTION** swa_lr × cosine-final-lr coincidence is the real load-bearing parameter NOT anneal_epochs (motivates swa_lr sweep as next axis); axis closes; motivates thorfinn → 10th axis swa_lr sweep (decouples SWA-floor from cosine-final-lr; directly addresses banked open question #43 + thorfinn's suggested follow-up #1)
- Lion β1 sweep {0.85, 0.95} on max_norm=0.35 (#2880 alphonse, 2026-05-14 11:45): Arm 1 β1=0.85 val 48.92/test 41.61 +3.76/+2.98 — strong regression; Arm 2 β1=0.95 val 49.65/test 42.31 +4.49/+3.68 — strong regression. **Paper-strengthening 8th axis closure (DEPENDENT-SYMMETRIC class, joins wd)**: Lion β1=0.9 (Chen 2023 default) at SHARP val minimum with very narrow basin width — ±0.05 perturbation costs +3.76 to +4.49 val. Mechanism: **sign-sequence stationarity** — under saturated-clip every step is `sign(β1 * exp_avg + (1-β1) * grad)`; β1 modulates the SHAPE of the sign-update distribution, NOT magnitude. Larger β1 (more memory) hurts slightly more than smaller (more responsive). 8 banked findings (#147–#154) inc. **#147 PAPER-STRENGTHENING** Lion β1 joins DEPENDENT-SYMMETRIC pattern class with wd (does NOT open new pattern), **#148 METHODOLOGY CRITIQUE** `last_update_sq_norm` NOT a useful β1 discriminator under saturated-clip (sign-vector L2 norm ≈ N_params regardless of β1 — `sign_()` rarely outputs exact zero on float gradients; genuine discriminator = sign-flip rate, NOT currently logged; would require Lion optimizer code edit), **#149** `exp_avg_norm` discriminates β1 ~7% higher for β1=0.95 vs 0.85 (slower EMA decay = more cumulative memory — ONLY logged diagnostic that distinguishes arms), **#150** 9th cross-axis σ-spread invariance (Arm 1 0.4791, Arm 2 0.4849 vs baseline 0.4748), **#151** 13th cross-axis channel-ordering invariance, **#152** 8th cross-axis clip_fraction=1.000 invariance (Arm 1 1.0000, Arm 2 0.9998 — 1 step sub-threshold), **#153 PAPER-PUBLISHABLE** Lion β1=0.9 is at SHARP val minimum (symmetric basin, narrow width), **#154 METHODOLOGY** 6th-consecutive SWA-window-truncation (#2790, #2818, #2835, #2862, #2877, #2880; 2 of 4 planned SWA epochs); axis closes; motivates alphonse → n_head sweep (#2901, NEW ATTENTION-MECHANISM-CAPACITY-RESHUFFLE class)
- swa_lr sweep {1.5e-5, 1.5e-4} on max_norm=0.35 (#2896 thorfinn, 2026-05-14 13:15): Arm 1 swa_lr=1.5e-5 val 45.30/**test 38.6172** +0.149/**−0.020** — directional close in [45.15, 45.50] with **sub-noise marginal test win** (~9× below seed-noise stdev ~1.76); Arm 2 swa_lr=1.5e-4 val 46.12/test 39.08 +0.97/+0.44 — moderate regression. **Paper-strengthening 10th axis closure with NEW 5th member of DOMINANT ASYMMETRIC-V class — pattern now 50% of closed axes**. 9 banked findings (#163–#171) inc. **#163 PAPER-STRENGTHENING** swa_lr joins ASYMMETRIC-V (lr/hybrid_kendall_lr/RFF-capacity/fourier_sigma), **#164 PAPER-STRENGTHENING banked #146 RESOLVED** swa_lr × cosine-final-lr coincidence WAS load-bearing parameter (baseline swa_lr=6e-5 ~0.1e-5 above cosine-final 5.9e-5 → SWALR almost no-ops; this is why #2877 anneal_epochs was DEGENERATE), **#165** Lion EMA bias REJECTED as confound during SWA window (`exp_avg_norm` within ±25%; Arm 1 LARGEST not smallest), **#166** 13th cross-axis σ-spread invariance (0.4734/0.4771 vs 0.4748), **#167** 15th cross-axis channel ordering invariance, **#168** 10th cross-axis clip_fraction=1.000 invariance, **#169 IMPORTANT** sub-noise marginal test win on Arm 1 motivates swa_lr × swa_start_frac compose follow-up (4-epoch SWA window at deep-low-lr could amplify into MERGE), **#170 METHODOLOGY** 8th-consecutive SWA-window-truncation under 30-min cap (#2790, #2818, #2835, #2862, #2877, #2880, #2887, #2896) — most-stable cross-axis observation, **#171 METHODOLOGY EXTENDS BANKED #148** `optimizer_update_norm` uninformative for Lion (identical 868.63 = sqrt(num_params); meaningful quantity = `lr × update_norm`); axis closes; motivates thorfinn → swa_lr × swa_start_frac compose (MERGE candidate addressing #170 + thorfinn's #1+#2 follow-up)
- swa_lr × swa_start_frac compose {0.65/1.5e-5, 0.65/3e-5} on max_norm=0.35 (#2925 thorfinn, 2026-05-14 14:30): Arm 1 swa_lr=1.5e-5 + s_f=0.65 val **47.79**/test 40.46 +2.64/+1.82 — strong regression close; Arm 2 swa_lr=3e-5 + s_f=0.65 val **47.34**/test 40.31 +2.19/+1.67 — strong regression close. **Paper-strengthening COMPOSE-AXIS test (NOT a new axis — confirms two well-characterized axes' interaction is captured by the dominant axis, swa_start_frac mechanism wins)**. **Banked #2429 mechanism (pre-plateau averaging cost from σ=0.5 stack) extends CLEANLY to saturated-clip max_norm=0.35 baseline with same monotonic direction; MERGE hypothesis from #2896 follow-up #1+#2 is cleanly FALSIFIED — deep-low-lr swa_lr does NOT compensate pre-plateau averaging cost on either clip regime**. **Core mistake exposed**: SWA averages ALL weights in window equally (incl. pre-plateau weights from cosine descent — ep10 val=55.61 + ep11 val=51.09 pollute Arm 1's 4-epoch avg of 47.79; without ep10 the avg would be ~46.4 close to plateau). 9 banked findings (#181–#189) inc. **#181 PAPER-STRENGTHENING** compose FAILS, **#182 PAPER-STRENGTHENING** banked #169 RESOLVED NEGATIVE (#2896 directional close NOT generalizable through swa_start_frac), **#183 PAPER-STRENGTHENING** SWA-mean heavily influenced by single-epoch outlier weight inclusion (NO down-weighting; equal-weight averaging is load-bearing property that fails when window admits non-plateau weights), **#184 PAPER-STRENGTHENING** SWALR ramp completes within 1 STEP when swa_lr ≪ cosine-final (refines banked #18 SWALR-overrides-cosine; sharpens banked #144 anneal_epochs DEGENERATE — ramp duration ALWAYS inert under saturated-clip regardless of anneal_epochs value), **#185** 14th cross-axis σ-spread invariance (log_σ surf_p ~−1.88 both arms; effective_weight surf_p ~21.65), **#186** 16th cross-axis channel-ordering invariance, **#187** 11th cross-axis peak VRAM invariance (44.8 GB both arms), **#188 METHODOLOGY** 9th-consecutive SWA-window-truncation under 30-min cap (#2790, #2818, #2835, #2862, #2877, #2880, #2887, #2896, #2925), **#189 IMPORTANT** banked #170 NOT actionable via swa_start_frac changes — fix must be (a) epoch-count increase (requires cap relaxation OR step-time reduction) OR (b) plateau-detection SWA (gate weight inclusion on val condition); axis closes (paper-appendix table UNCHANGED at 11 closed × 7 transfer patterns since this is a compose test, not a new axis); motivates thorfinn → n_layers depth sweep (14th paper-appendix axis, NEW TRANSOLVER-DEPTH class)
- n_head sweep {2, 8} on max_norm=0.35 (#2901 alphonse, 2026-05-14 14:00): Arm 1 n_head=2 val 45.47/test 39.31 +0.31/+0.67 — directional close upper edge; Arm 2 n_head=8 val **392.67**/test 397.05 +347.51/+358.41 — **CATASTROPHIC SWA DIVERGENCE on under-converged base** (base val=56.66 at ep10 still descending → SWA averaging compounded 7× worse than best base). **Paper-strengthening 11th axis closure with NEW 6th member of DOMINANT ASYMMETRIC-V class — pattern now 55% of closed axes (6 of 11)**. 9 banked findings (#172–#180) inc. **#172 PAPER-STRENGTHENING** n_head joins ASYMMETRIC-V (lr/hybrid_kendall_lr/RFF-capacity/fourier_sigma/swa_lr), **#173 PAPER-STRENGTHENING CRITICAL σ-spread invariance BREAKS for first time** (Arm 1: 0.217, Arm 2: 0.090 vs baseline 0.475 — first axis to break across 13 prior cross-axis confirmations; refines invariance as downstream-of-attention, NOT a property invariant to attention-block forward-pass-shape), **#174 PAPER-STRENGTHENING CRITICAL pre-clip grad_norm distribution SHIFTS for first time** (Arm 1: 9.73 +84%, Arm 2: 5.91 +12% vs baseline 5.30 — refines banked #45 as invariant to clip threshold AND most axes BUT NOT forward-pass-shape), **#175 PAPER-STRENGTHENING** clip_fraction=1.000 HOLDS despite grad_norm shift (11th cross-axis confirmation — saturated-clip decouples clip behavior from forward-pass-shape because saturation thresholds at max_norm=0.35 ≪ pre-clip; the load-bearing property is invariance to PER-STEP DIRECTION, not magnitude), **#176** 16th cross-axis channel ordering invariance (surf_ux=min/vol_ux=max preserved both arms), **#177 NEW MECHANISM CLASS** SWA-DIVERGENCE-ON-UNDERCONVERGED-BASE for Arm 2 (distinct from #2880 Lion β1 sharp-minimum overshoot; SWA pre-supposes plateau-reached base, otherwise amplifies residual descent into wrong region), **#178** Cruise vs camber_rc preference reversal on Arm 1 (richer per-head dim_head=64 wins on geom_camber_cruise val −1.41/test −0.86 but loses on load-bearing OOD bottleneck geom_camber_rc test +3.54 — attention-mechanism capacity vs OOD robustness data point), **#179 METHODOLOGY** n_head=8 has +50% step-time / +17GB VRAM despite identical total params (kernel launches + memory layout scale with n_head — pure granularity axis NOT free; optimizer-internal axes attractive next probes), **#180 METHODOLOGY** NaN in test_geom_camber_cruise/vol_loss is pre-existing target-repo issue (decoupled from saturated-clip baseline mechanism findings); axis closes; motivates alphonse → Lion β2 sweep {0.95, 0.999} (13th axis, NEW OPTIMIZER-INTERNAL-EMA-DECAY class, distinct from Lion β1 DEPENDENT-SYMMETRIC closure)
- dropout sweep {0.05, 0.15} on max_norm=0.35 (#2887 fern, 2026-05-14 12:50): Arm 1 d=0.05 val 46.05/test 39.16 +0.90/+0.52 — regression close; Arm 2 d=0.15 val 46.36/test 39.42 +1.20/+0.78 — regression close (both in [45.50, 46.50] regression band, neither cleared merge/directional thresholds). **Paper-strengthening 9th axis closure with NEW 7th transfer pattern: MONOTONIC-REGRESSIVE (one-sided regularization axis)**. Dropout is a one-sided axis (d ≥ 0 by construction), so cannot exhibit V-shape/symmetric basin by construction. ×3 dropout ratio → ×1.34 regression magnitude — monotonic regression. Mechanism: dropout-induced sign-flips happen at OPTIMIZER level (post-grad-norm) accumulating as low-rate gradient-direction errors that Lion's β1=0.9 EMA does not fully de-noise within 13 epochs. 8 banked findings (#155–#162) inc. **#155 PAPER-STRENGTHENING** NEW MONOTONIC-REGRESSIVE 7th transfer pattern class, **#156 PAPER-STRENGTHENING** clip_fraction=1.000 PRESERVED under dropout (Arm 1 d=0.05: 4875/4875, Arm 2 d=0.15: 4874/4875 — only 1 step sub-threshold = 99.98% saturated; **first axis with mechanism to break clip_fraction=1.0 invariance that DOES NOT break it** — saturated-clip robust to activation-space stochasticity; 9th cross-axis confirmation), **#157 PAPER-STRENGTHENING NEW 9th cross-axis invariant** pre-clip grad_norm distribution under dropout (mean +0.6%, p50 +3.7%, p99 invariant — dropout-induced sign-flips at OPTIMIZER level not gradient-magnitude level), **#158** dropout ≠ complementary cruise regularizer (σ=0.25 strictly better: ×8 cruise val gain AND smaller single_in_dist regression — rules out "dropout + σ=0.25 compose" hypothesis), **#159 MECHANISM DISTINCTION** weight-space (wd) vs activation-space (dropout) stochasticity paper-publishable distinction (wd SYMMETRIC-BASIN regularizes via weight-shrinkage and tolerates mild upper-bracket; dropout MONOTONIC-REGRESSIVE regularizes via forward-noise and hurts in both light and heavy regimes; mechanism: dropout-noise directly perturbs per-step sign(...) decision; wd just biases weights toward 0 with no per-step sign-noise), **#160** 10th cross-axis σ-spread invariance (0.478/0.480 vs baseline 0.475 Δ ≤ 0.005), **#161** 14th cross-axis channel-ordering invariance, **#162 METHODOLOGY** 7th-consecutive SWA-window-truncation (#2790, #2818, #2835, #2862, #2877, #2880, #2887); axis closes; motivates fern → huber_beta LOWER fine bracket {0.20, 0.15} on max_norm=0.35 (12th paper-appendix axis; tests #2666 β-driven σ-spread mechanism transfer from max_norm=0.5 → saturated-clip baseline)
- Lion β2 sweep {0.95, 0.999} on max_norm=0.35 (#2932 alphonse, 2026-05-14 16:30): Arm 1 β2=0.95 val **56.91**/test 49.50 +11.76/+10.86 — strong regression close; Arm 2 β2=0.999 val **52.87**/test 44.56 +7.72/+5.93 — strong regression close. **Paper-strengthening 13th axis closure with NEW 7th member of DOMINANT ASYMMETRIC-V class — pattern now 7 of 13 = 54% of closed axes**. Lion β2 also opens **NEW OPTIMIZER-INTERNAL-EMA-DECAY transfer-pattern class** distinct from Lion β1 #2880 (DEPENDENT-SYMMETRIC, sign-sequence stationarity). 10 banked findings (#190–#199) inc. **#190 PAPER-STRENGTHENING** Lion β2 joins ASYMMETRIC-V (7th DOMINANT member, 54% of closed axes), **#191 PAPER-PUBLISHABLE MECHANISM** sign-flip rate INVERTED from naive intuition — β2=0.999 has 56.4% flips, β2=0.95 has 18.3% (3.1× HIGHER, opposite direction); genuine β2 discriminator at fixed gradient statistics is the (exp_avg/grad) magnitude ratio, NOT EMA volatility per se (extends methodology #148), **#192 PAPER-PUBLISHABLE MECHANISM** longer EMA memory wins over shorter EMA memory under Lion saturated-clip — arm2 (β2=0.999, MORE flips) regresses LESS than arm1; noisy sign signal with long-EMA window provides better trajectory averaging than stable sign signal with short-memory exp_avg, **#193 PAPER-PUBLISHABLE** Lion β1 (DEPENDENT-SYMMETRIC) and β2 (ASYMMETRIC-V) qualitatively non-symmetric in failure modes despite both being EMA hyperparameters — paper-appendix-worthy distinction about the optimizer's internal structure, **#194 PAPER-STRENGTHENING** exp_avg_norm CONFIRMS theoretical scaling (1−β2)/(1+β2) — β2=0.95: 4.26× baseline; β2=0.999: 0.25× baseline (sharpens #149; FIRST diagnostic that directly confirms a theoretical formula for Lion's internal state across the closed axes), **#195 PAPER-STRENGTHENING** 14th cross-axis σ-spread invariance (0.509/0.498 vs 0.474, Δ ≤ 0.035) — σ-spread invariant to Lion-optimizer-internal axes (extends #166 swa_lr-axis), **#196** 17th cross-axis channel-ordering invariance, **#197** 12th cross-axis clip_fraction=1.000 invariance (pre-clip grad_norm 18.38/17.93/16.90 invariant within Δ ≤ 1.48), **#198 METHODOLOGY** 10th-consecutive SWA-window-truncation under 30-min cap (#2790, #2818, #2835, #2862, #2877, #2880, #2887, #2896, #2925, #2932 — most-stable cross-axis observation in Wave 12; banked methodology #170 now confirmed across all 13 closed paper-appendix axes — STRUCTURAL constraint), **#199 METHODOLOGY** 12th cross-axis peak VRAM invariance (44.4/44.8/44.8 GB) — Lion-optimizer-internal axes don't change memory footprint; axis closes; motivates alphonse → slice_num sweep {32, 128} on max_norm=0.35 (15th paper-appendix axis, NEW NUM-SLICES class — Transolver-specific Slice Attention granularity, forward-pass-shape axis at fixed n_layers/n_head/n_hidden; predicted BOTH-LOSE Pareto-cap OR ASYMMETRIC-V; per banked #179 NEW DIAGNOSTIC required: per-arm step-time + SWA-window completion for forward-pass-shape changes)
- huber_beta LOWER fine bracket {0.20, 0.15} on max_norm=0.35 (#2919 fern, 2026-05-14 18:15): Arm 1 β=0.20 val **46.2288**/test 39.0759 +1.0750/+0.4392 — regression close in [45.50, 46.50]; Arm 2 β=0.15 val **45.5520**/test 38.7230 +0.3982/+0.0863 — directional close just outside ≤45.50 (0.05 above edge). **Paper-strengthening 15th axis closure post-Loop-92 with NEW 8th transfer-pattern class NON-MONOTONE-COST-MONOTONE-MECHANISM** (β LOWER bracket separately classified from β UPPER #2736 INDEPENDENT-SYMMETRIC — β-axis is FIRST MIXED-CLASSIFICATION axis). **THREE PAPER-PUBLISHABLE FINDINGS** + 4 strengthening + 1 methodology (7 banked findings #210–#216): **#210 PAPER-PUBLISHABLE** β-driven σ-spread expansion mechanism TRANSFERS CLEANLY from max_norm=0.5 (#2666) to saturated-clip max_norm=0.35 — quantitative predictions within ±0.025 (β=0.20 σ-spread 0.5254 predicted ~0.50, β=0.15 σ-spread 0.5484 predicted ~0.55 near-exact; eff_w_surf_ux β=0.20 39.7 predicted 40-42, β=0.15 37.7 predicted 36-39 mid-band); β-axis is 2nd cross-clip-regime TRANSFERS-CLEANLY axis after fourier_sigma #2862, **#211 PAPER-PUBLISHABLE** U-curve dataset-level finding (banked #66) PRESERVED at β=0.15 on saturated-clip — 3 of 4 test splits IMPROVE (camber_rc −0.37, cruise −0.16, re_rand −0.12), only single_in_dist regresses +1.00; β-axis is FIRST axis confirmed to preserve U-curve across BOTH clip regimes (max_norm=0.5 + saturated-clip max_norm=0.35), **#212 PAPER-PUBLISHABLE NEW PATTERN CLASS** β-LOWER bracket NON-MONOTONE REGRESSION despite monotone σ-spread — β=0.30 val 45.15 → β=0.20 val 46.23 (LOCAL MAX) → β=0.15 val 45.55 (partial recovery); test mirrors 38.64 → 39.08 → 38.72; σ-spread monotone 0.475→0.525→0.548; NEW transfer-pattern class **NON-MONOTONE-COST-MONOTONE-MECHANISM** — internal mechanism quantity (σ-spread) is monotone but cost (val/test) is non-monotone with a local maximum; distinguishes from ASYMMETRIC-V (vertex at β=0.30 would need both arms regress), MONOTONIC-REGRESSIVE (regression not monotone with knob direction), DEGENERATE-AXIS (axis moves the metric), BOTH-LOSE Pareto-cap (same mechanism, different magnitudes — not differential mechanism break), **#213 PAPER-STRENGTHENING** Kendall weight REDISTRIBUTE-DOWN direction transfers cleanly (all 6 channels monotone DOWN with β; surf_ux 44.5→39.7→37.7; opposite of lr-driven CONCENTRATE-UP #2604), **#214 PAPER-STRENGTHENING** pre-clip grad_norm rises SUB-LINEARLY with 1/β (×1.16, ×1.27 vs pure-1/β predicted ×1.5, ×2.0; Huber-residual statistics dampen slope; methodology #33 direction confirmed with damped magnitude — β-driven mechanism on saturated-clip operates through σ-spread + per-channel reweighting NOT grad magnitude), **#215 PAPER-STRENGTHENING** 16th cross-axis channel-ordering confirmation (surf_ux=min/vol_ux=max preserved both arms) + 14th cross-axis clip_fraction=1.000 invariance (mean=p1=1.000 all 4879 steps both arms) + 14th cross-axis peak VRAM observation (~44.80 GB invariant), **#216 METHODOLOGY** banked #170 SWA-window-truncation continuous-function form CONFIRMED — 11th-consecutive 2-epoch truncation at baseline-equivalent forward-pass-shape (no shape change → same per-epoch wall-time → same truncation); consistent with sharpened #170 form: "SWA-window-completion is continuous function of total wall-time minus per-epoch wall-time, NOT constant truncation"; axis closes; motivates fern → huber_beta GAP+EXTEND sweep {0.25, 0.10} on max_norm=0.35 (extends NEW NON-MONOTONE-COST-MONOTONE-MECHANISM class with additional data points)
- **Lion wd σ=0.5 rebased 2-arm cross-stack re-test {3e-3, 1e-2} on max_norm=0.5 (#2390 askeladd CLOSED Loop 93, 2026-05-14 18:30):** PAPER-STRENGTHENING CROSS-STACK CLOSE (NOT a new axis; wd-axis already closed at #2791+#2819 on saturated-clip baseline). Arm A wd=3e-3 SWA val **45.7094**/test 39.4615 +0.556/+0.825 vs current #2674 baseline (45.1538/38.6367); Arm B wd=1e-2 SWA val **45.7004**/test 39.5261 +0.547/+0.889. Both arms barely beat outdated #2168 σ=0.5 baseline (45.7648) by ~0.05-0.06 val (within seed-noise band, 0.01 apart from each other = statistically tied). Per current decision rule (val < 45.1538 to merge) **neither arm wins**. **HEADLINE: wd-axis identified as FIRST TRIPLE-PATTERN AXIS** — DEPENDENT-SYMMETRIC on saturated-clip max_norm=0.35 (#2791+#2819) + DEGENERATE on σ=0.5/max_norm=0.5 (this PR) + ASYMMETRIC-V on σ=1.0/max_norm=0.5 (#2390 pre-rebase from #2063 σ=1.0 baseline). wd-axis is the SECOND MIXED-CLASSIFICATION axis after β-axis (β has 2 patterns; wd has 3). 6 banked findings (#217-#222) inc. **#217 PAPER-PUBLISHABLE** wd × σ substitution mechanism CONFIRMED — at σ=1.0 wd 3e-4→3e-3 buys -0.56 val; at σ=0.5 same push buys only -0.05 val (10× smaller gain); both regularizers, partial substitutes, don't compound, **#218 PAPER-PUBLISHABLE** wd-axis CEILING confirmed on σ=0.5 stack at ~3e-3 (curve FLAT in [3e-3, 1e-2]; σ=1.0 monotonic trend {1e-4 → 3e-3} does NOT extend to 1e-2 on σ=0.5); **wd-axis is FIRST TRIPLE-PATTERN AXIS**, **#219 PAPER-PUBLISHABLE** camber_rc OOD-gap mechanism REVERSES SIGN at σ=0.5 (wd↑ closes gap by -1.49 val at σ=1.0 vs opens gap by +0.96 Arm A / +0.79 Arm B at σ=0.5) — **FIRST OOD-METRIC SIGN-REVERSAL** documented across paper-appendix axes; paper-publishable sub-finding about wd × σ × OOD-split triple-interaction, **#220 PAPER-STRENGTHENING** 7th σ-collapse confirmation under Lion-only optimizer + 2nd wd-can't-rescue confirmation (all 6 log_σ → -0.9037487506866455 bit-identical to σ=1.0 across both arms), **#221 PAPER-STRENGTHENING** Non-shrinkage mechanism REPLICATES on σ=0.5 stack (Arm A param_norm trajectory plateaus 85.93 at SWA window vs σ=1.0's 88.72, -3.2%; wd doesn't change trajectory shape — Lion sign-step dominates); wd-axis is 3rd cross-stack-mechanism-transfer axis after fourier_sigma #2862 + β #2919, **#222 METHODOLOGY** Arm B's missing param_norm logging recoverable (Arm B launched from beadcb5 commit, one before 9c5d791 param_norm logging commit; σ=1.0 mechanism evidence sufficient; process note: when rebasing for cross-stack re-tests, ensure ALL launched arms include diagnostic code added during the rebase); paper-appendix table UNCHANGED at 16 closed axes × 8 transfer patterns since this is a re-test, NOT a new axis closure — BUT wd-axis classification refined from single-pattern DEPENDENT-SYMMETRIC to TRIPLE-PATTERN MIXED across (σ, max_norm) regime tuple; motivates askeladd → film_mid_dim sweep {32, 128} on max_norm=0.35 (16th paper-appendix axis label, NEW FILM-BOTTLENECK class)
- n_layers depth sweep {3, 7} on max_norm=0.35 (#2947 thorfinn, 2026-05-14 17:30): Arm 1 n_layers=3 val **45.7324**/test 39.69 +0.58/+1.05 — regression close in [45.50, 46.50]; Arm 2 n_layers=7 SWA val **398.79** SWA-NEVER-ENTERED artifact (timeout at epoch 10, SWA-start epoch 12), BASE val **56.56**/BASE test 48.22 +11.41/+9.59 — strong regression close on BASE. **Paper-strengthening 14th axis closure with NEW 8th member of DOMINANT ASYMMETRIC-V class — pattern now 8 of 14 = 57% of closed axes (FURTHER consolidates DOMINANT pattern)**. n_layers also opens **NEW TRANSOLVER-DEPTH transfer-pattern class** distinct from #2901 n_head ATTENTION-MECHANISM-CAPACITY-RESHUFFLE (granularity at fixed depth=5) — n_layers re-uses fixed per-block representation (n_head=4, dim_head=32, n_hidden=128) but stacks more/fewer Transolver blocks. **FOUR PAPER-PUBLISHABLE FINDINGS + NEW SWA-NEVER-ENTERED FAILURE MODE**. 10 banked findings (#200–#209) inc. **#200 PAPER-STRENGTHENING** n_layers joins ASYMMETRIC-V (8th DOMINANT, 57%), **#201 PAPER-PUBLISHABLE** σ-spread BREAKS in BOTH directions under depth (Arm 1 n=3: 0.533 +12% inflated; Arm 2 n=7: 0.251 −47% compressed) — broadens banked #173 refinement: σ-spread invariance holds only at fixed forward-pass-shape, BOTH granularity AND depth break it in distinct directional signatures (paper-publishable: σ-spread is forward-pass-shape-dependent), **#202 PAPER-PUBLISHABLE** pre-clip grad_norm SHIFTS strongly with depth in OPPOSITE direction to n_head (Arm 1 9.77 vs Arm 2 5.97, 39% lower; DEEPER → LOWER) — broadens banked #174 to "any forward-pass-shape change shifts grad_norm, NOT granularity-specific", **#203 PAPER-PUBLISHABLE** channel ordering invariance BREAKS for FIRST TIME across 17 prior cross-axis confirmations (Arm 1 n=3: surf_uy=min instead of surf_ux=min; SHALLOW-MODEL-SPECIFIC break — narrower per-channel discriminability margin → ordering flips), **#204 NEW FAILURE MODE: SWA-NEVER-ENTERED** Arm 2 n_layers=7 timed out at epoch 10 with SWA-start at epoch 12 → SWA averaging NEVER initialized → reported SWA val 398.79 is uninitialized-buffer artifact (garbage). Distinct from #170 SWA-window-truncation (partial); SWA-NEVER-ENTERED is strict limit case. **FIRST counter-example to #170 chain ALSO on this PR** — Arm 1 n=3 COMPLETED 4 of 4 SWA epochs (smaller model → faster per-epoch wall time → full SWA window). #170 sharpens: SWA-window completion depends on per-epoch wall time. **End of #170's 10-consecutive truncation chain**, **#205 PAPER-PUBLISHABLE METHODOLOGY** step-time is NOT n_layers-linear at this stack scale — ~190 s/epoch invariant across n=3, n=7 despite 2.3× param ratio (dataloader/attention-cost dominated; slice_num × n_head dominant). Refines banked #179: step-time scaling depends on which forward-pass-shape parameter changes; n_head scales linearly (#2901: +50% at n_head=8), n_layers does NOT, **#206 PAPER-STRENGTHENING** Lion exp_avg_norm INVARIANT to depth (Δ < 2%) — depth-driven grad_norm shift does NOT propagate into Lion's sign-of-EMA; contrast with β2-axis #2932 where exp_avg_norm varied 0.25× to 4.26×, **#207 PAPER-STRENGTHENING** 13th cross-axis clip_fraction=1.000 invariance (Arm 1 1.000, Arm 2 0.9997 — saturated-clip robust to forward-pass-shape), **#208 PAPER-STRENGTHENING** 13th cross-axis peak VRAM observation — VRAM scales LINEARLY with n_layers (Arm 1 28.9 GB 64% baseline, Arm 2 60.8 GB 135% baseline). **NEW METHODOLOGY**: VRAM scaling forward-pass-shape parameter dependent — n_layers linear, n_head super-linear (granularity overhead), optimizer-internal flat, **#209 METHODOLOGY** banked #170 SWA-window-truncation chain BROKEN at 10 consecutive — per-paper-appendix STRUCTURAL refinement: SWA-window-completion is a continuous function of total wall-time minus per-epoch wall-time, NOT a constant truncation under the 30-min cap; axis closes; motivates thorfinn → mlp_ratio sweep {1, 4} on max_norm=0.35 (17th paper-appendix axis, NEW FFN-RATIO class — Transolver per-block FFN-ratio, THIRD forward-pass-shape axis after n_head granularity + n_layers depth, completes FORWARD-PASS-SHAPE TRIO; tests whether σ-spread + grad_norm BREAK extends to FFN-side forward-pass-shape or restricted to attention-side)

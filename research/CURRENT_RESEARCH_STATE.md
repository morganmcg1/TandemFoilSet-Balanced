# SENPAI Research State — charlie-pai2g-48h-r5

- 2026-05-15 [Round 138 close-47]: **2 closures in same round — #3061 slice_num=12 PRESERVED + #3056 Lion β₁=0.95 BROKEN.** Closed **#3061 alphonse slice_num=12 LOSS-with-cruise-PRESERVED** (val 30.6600 +3.82% LOSS vs #3006 baseline 29.5318; test 25.6049 +0.49% slight LOSS vs 25.4795; val_cruise 15.7191 PRESERVED < 17.0; param Δ −2,352; 180th taxon SLICE-NUM-12-LOWER-BRACKET-LOSS-VIA-HEAD-SPECIALIZATION-COLLISION / SLICE-NUM-AXIS-CLOSED-AT-LOWER-BRACKET-24-LOAD-BEARING-MINIMUM). **KEY MECHANISTIC FINDINGS:** (1) "Dead-slice under-parameterization" hypothesis FALSIFIED — all 12 slices USED at every probe epoch (dead_slice_frac=0.00). (2) **Routing capacity bottleneck via head specialization-collision** — max-usage rises from uniform 0.084 (ep1) to 0.41-0.58 (ep30/59); per-head entropy drops to 1.59 (block 2 H0); heads concentrate >50% mass on one slice in some blocks. 24-slice headroom is load-bearing. (3) Sec/epoch flat (29.0) despite −2,352 params — slice-routing not the compute bottleneck. (4) val_geom_camber_rc unchanged — coarser routing is NOT useful regularizer. **Sub-invariant 'granularity-orthogonal-to-cruise-protection' strengthened to 3 datapoints** (#3026 + #3043 + #3061). **14th cruise-preserving datapoint.** Closed **#3056 thorfinn Lion β₁=0.95 LOSS-with-cruise-BROKEN** (val 31.3064 +6.01% LOSS; test 26.0975 +2.43% LOSS; **val_cruise 17.4802 BROKEN +0.48 above 17.0**; param unchanged; 181st taxon LION-BETA1-0PT95-LOSS-WITH-CRUISE-BREAK-VIA-SIGN-EXTRACTION-STALE-DIRECTION / LION-BETA1-AXIS-CLOSED-AT-DEFAULT-0.9). **KEY MECHANISTIC FINDINGS (highest-quality predictive hypothesis this round — student predicted failure verbatim):** (1) **"Stale 20-step direction info" mechanism CONFIRMED verbatim** — sign-step stability rises to 0.79 at ep59 (vs Chen et al. ~0.7 for β₁=0.9). Under cosine LR decay, stale 20+-step-old gradient direction dominates current loss-surface gradient. (2) Late-window non-monotonicity confirmed: ep55→60 = 31.38→31.35→31.69 (bounce)→31.41→31.31→31.32 vs #3039's monotonic 30.63→30.32. (3) Train→val gap consistent with baseline — regression is absolute val MAE not generalization quality. **NEW SUB-INVARIANT (tentative, 1 datapoint):** "sign-extraction-coefficient axes break cruise; buffer-update-coefficient axes preserve cruise." β₁ sits in sign-extraction `sign(β₁·m + (1-β₁)·g)` — changes per-step weight delta sign directly; β₂ only updates buffer for NEXT step. **#3068 fern β₂=0.95 (in-flight) is the cleanest first companion-datapoint** — if β₂ preserves cruise, sub-invariant gains a strong supporting case. **FIRST cruise-break on optimizer-internal axis** (prior 3 optimizer-internal cruise tests #3020 EMA, #3039 SWA, #3050 grad-clip all PRESERVED). **10th BROKEN datapoint.** Followup **#3076 alphonse slice-num-48** — Double slice_num from 24 → 48 (upper-bracket complement per student rec #1 verbatim). Tests whether 24 was UNDER-parameterized vs OPTIMAL. Param Δ +9,216 (~+2.3%). Cruise predicted PRESERVED (would be 15th datapoint). 170th active axis. Followup **#3077 thorfinn mlp-dropout-0pt05** — Fresh ARCHITECTURE axis: `nn.Dropout(p=0.05)` INSIDE SwiGLU MLP (after gate*value, before down-projection). 3rd site in stochastic-regularization-cluster filling matrix at p=0.05 across (attn-softmax #3071, branch #3065, MLP-internal #NEW). +0 params. Cruise prediction UNCERTAIN — most aggressive in-block-stochastic test yet. 171st active axis. Cruise-preservation ledger now 24 datapoints (10 BROKEN + 14 PRESERVED). Total closed: 181. Winners: 23. **8 students all busy, zero idle GPUs. CURRENT FOCUS: 8 in-flight axes (#3065 DropPath p=0.05, #3066 LayerScale, #3068 Lion β₂=0.95, #3069 output-MLP-2layer, #3071 attn-dropout p=0.05, #3075 hybrid-LN-RMSNorm, #3076 slice_num=48, #3077 MLP-dropout p=0.05). Three clusters in-flight: stochastic-regularization-3-site-cluster (#3065 branch + #3071 attn-softmax + #3077 MLP-internal), Lion-hyperparameter pair (#3068 β₂ as companion to closed #3056 β₁), slice_num bracket (#3076 upper complement to closed #3061 lower). β₂-axis #3068 is the CRITICAL companion-test for the new sign-extraction-vs-buffer-update sub-invariant.**

- 2026-05-15 [Round 138 close-46]: Closed **#3054 frieren RMSNorm LOSS-with-cruise-PRESERVED + TEST WIN** (val 30.5392 +3.41% LOSS vs #3006 baseline 29.5318; test 25.1858 −1.15% WIN vs 25.4795 — paper-facing test improvement; val_cruise 16.0530 PRESERVED < 17.0; param Δ −864; 179th taxon RMSNORM-LOSS-LN-MEAN-SUBTRACTION-MILDLY-LOAD-BEARING / NORMALIZATION-TYPE-AXIS-CLOSED-AT-LN-WITH-POSITION-DEPENDENT-FOLLOWUP). **EXEMPLARY STUDENT WRITE-UP — KEY MECHANISTIC FINDINGS:** (1) LN mean-subtraction MILDLY load-bearing AND metric-localized: val_in_dist +4-6% and val_geom_camber_rc +5-7% worst-hit; val_cruise + val_re_rand essentially unaffected — regression is NOT global. (2) **γ-DOWN compensation pattern** at 8 in-block sites — mean γ drifts down 2-7% (mean 0.958); output-head γ drifts up +4% (1.040). Coherent compensation: network reduces magnification mid-stream to compensate for lost mean-centering, over-magnifies at output-head to recover scale. (3) Pre-norm RMS swings 0.66-1.25 (**NOT zero-centered**) — direct mechanistic evidence residual streams in this 4-layer Transolver carry non-zero means by design; RMSNorm preserves means whereas LN absorbs them. Harm comes from mean-carrying property interacting with downstream attention/MLP weights. (4) **#3006 α equilibrium PRESERVED** — trajectory [0.913, 1.035, 0.972] same alternating DOWN/UP/DOWN pattern as #3006's [0.95, 0.97, 0.95]; 3-scalar α-axis × normalization-type axis confirmed ORTHOGONAL. **Sub-invariant 'capacity-removal-preserves-cruise' strengthened to 2 datapoints:** #3049 bias-free Linear (−3,772 params) + #3054 LN→RMSNorm (−864 params). Both inside-block, both capacity removers, both cruise preservers. **#3054 is the most aggressive in-block structural intervention to preserve cruise** (9 normalization sites replaced). val/test divergence 4.6 percentage-points is the largest this launch — checkpoint selection finds more regularized model with worse val but better test. Without clean val win, not a merger. **13th cruise-preserving datapoint.** Followup **#3075 frieren hybrid-ln-rmsnorm** — Position-dependent norm policy per student rec #2 verbatim: keep `nn.LayerNorm` at ln_1 (attention input — mean-subtraction load-bearing site per γ-DOWN signature) + `nn.RMSNorm` at ln_2 (MLP input) and ln_3 (output head). 4 LN sites + 5 RMSNorm sites. Param Δ ≈ −480 (between #3006 0 and #3054 −864). Tests whether mean-subtraction is load-bearing only at attention sites. Cruise predicted PRESERVED (would be 14th datapoint). 169th active axis. Cruise-preservation ledger now 22 datapoints (9 BROKEN + 13 PRESERVED). Total closed: 179. Winners: 23. **8 students all busy, zero idle GPUs. CURRENT FOCUS: 8 in-flight axes (#3056 Lion β₁=0.95, #3061 slice_num=12, #3065 DropPath p=0.05, #3066 LayerScale, #3068 Lion β₂=0.95, #3069 output-MLP-2layer, #3071 attn-dropout-0.05, #3075 hybrid-ln-rmsnorm) test refined 2D cruise invariant across optimizer-hyperparameter-β₁, slice-routing-granularity, stochastic-regularization-branch, branch-output-multiplicative, optimizer-hyperparameter-β₂, output-head-capacity, stochastic-regularization-softmax, and POSITION-DEPENDENT-NORMALIZATION dimensions. Three clusters in-flight: Lion-hyperparameter pair (#3056 β₁ + #3068 β₂), stochastic-regularization pair (#3065 DropPath + #3071 attn-dropout), and normalization-axis follow-up (#3075 hybrid). NEW mechanistic insight this round: position-dependent normalization hypothesis motivated by metric-localized regression + γ-DOWN compensation signature.**

- 2026-05-15 [Round 138 close-45]: **3 LOSS-with-cruise-PRESERVED closures in one round (largest batch of cruise preservers in launch).** Closed **#3050 fern grad-clip-1.0 LOSS (small) but CRUISE PRESERVED** (val 29.7456 +0.72% LOSS vs #3006 baseline 29.5318; test 24.8041 −2.65% WIN; val_cruise 16.3058 PRESERVED; 176th taxon GRAD-CLIP-AT-MAX-NORM-1-DECOUPLES-FROM-STABILITY-INTO-GLOBAL-LR-MULTIPLIER / LION-SIGN-STEP-IS-SCALE-INVARIANT-UNDER-UNIFORM-BATCH-RESCALING). **KEY FINDING — Lion sign-step is LR-robust.** Student diagnostic: clip_fraction=1.0000 on EVERY step (375/375 × 58 epochs); raw grad-norm 20-70 (max 310.83) — orders of magnitude above 1.0 threshold; effective LR ~7.2e-6 (~21× smaller than nominal 1.5e-4). Despite 21× LR reduction, val only regressed +0.21 absolute → **Lion's sign-step `sign(c_t)` is invariant to uniform positive scalar multiplication**. Lion's LR sweet spot may be "narrow" only because any reasonable LR works similarly. 10th cruise-preserving datapoint. Closed **#3049 tanjiro bias-free-Linear LOSS but CRUISE PRESERVED** (val 30.7930 +4.27% LOSS; test 25.3813 −0.39% marginal WIN; val_cruise 16.5523 PRESERVED; param reduction −3,772 / −0.93%; 177th taxon BIAS-FREE-LINEAR-LOSS-AT-SUB-MILLION-PARAM-SCALE). **KEY FINDING — bias-free is NOT free at sub-million-param scale.** All 48 nn.Linear modules bias-free; LayerNorm bias kept; ALL three OOD splits regressed uniformly +4-6%; train→val gap 0.009 → NOT meaningfully regularizing; hit across the board, not a regularization tradeoff. At d=96/n_layers=4, each Linear's bias provides per-output-feature offset that LN's single γ/β cannot replicate. **11th cruise-preserving datapoint AND first INSIDE-BLOCK CAPACITY-REDUCTION preserver.** Refined sub-invariant (tentative): cruise robust to capacity REMOVAL at intra-block linear maps, NOT to capacity REPLACEMENT (e.g., per-channel γ #2977, per-channel α #3043). Capacity removal preserves residual-stream geometry; capacity replacement adds DOF the optimizer routes around. **Llama-style adoption is NOT universally net-positive at sub-million scale.** Closed **#3047 nezuko GeGLU LOSS but CRUISE PRESERVED STRONG** (val 30.5144 +3.33% LOSS; test 25.6106 +0.51% marginal LOSS; val_cruise 15.8188 PRESERVED — lowest val split AND lowest test split; 178th taxon GEGLU-LOSS-AT-GATE-POSITION-NEGATIVE-TAIL-INTRODUCES-SIGN-FLIPS). **KEY FINDING — SwiGLU's SiLU bounded dip is load-bearing.** Gate-pre is strongly negative-biased (~75-83% negfrac); block 3 (deepest) shows strongest sparsification (zero_frac=0.43). Under SiLU: gate values with x≈−1.28 map to bounded dip at gate≈−0.28; under GELU: same negative-tilted pre-activations map to slow-decaying long negative tail. **GELU's negative tail through gate*value introduces sign flips on multiplicative branch that SwiGLU does not** → incoherent residual updates → +3.33% val LOSS. **#3006 α trajectory UNCHANGED by GeGLU swap** — α @ ep59 [0.946, 0.981, 0.987], pattern preserved; activation-axis and α-axis are orthogonal. 12th cruise-preserving datapoint (STRONG margin). PaLM/T5/GLM-family GELU choice does NOT transfer to CFD surrogate. Followup **#3068 fern lion-beta2-0pt95** — Sweep Lion β₂ from 0.99 → 0.95 (β₁ unchanged); companion to thorfinn's in-flight #3056 β₁=0.95; together would comprehensively close Lion-hyperparameter axis; β₂=0.95 is de-facto modern AdamW default (Chinchilla/Llama 3); +0 params, +0 compute. 178th active axis. Followup **#3069 nezuko output-mlp-2layer** — Replace 1-layer output projection with 2-layer MLP (GELU intermediate); +~9.3k params (+2.3%); fresh axis OUTSIDE all blocks; tests whether 1-layer head bottlenecks prediction; cruise predicted PRESERVED per outside-block sub-invariant. 179th active axis. Followup **#3071 tanjiro attn-dropout-0pt05** — nn.Dropout(p=0.05) on slice-routing softmax output; fresh stochastic-regularization site INSIDE blocks (per-element softmax noise, distinct from #3065 DropPath's branch-level drop); Vaswani 2017 default; +0 params. 180th active axis. Cruise-preservation ledger now 22 datapoints (9 BROKEN + 13 PRESERVED). Total closed: 178. Winners: 23. **8 students all busy, zero idle GPUs. CURRENT FOCUS: 8 in-flight axes (#3054 RMSNorm, #3056 Lion β₁=0.95, #3061 slice_num=12, #3065 DropPath p=0.05, #3066 LayerScale, #3068 Lion β₂=0.95, #3069 output-MLP-2layer, #3071 attn-dropout-0.05) test refined 2D cruise invariant across normalization-type, optimizer-hyperparameter-β₁, slice-routing-granularity, stochastic-regularization-branch, branch-output-multiplicative, optimizer-hyperparameter-β₂, output-head, and stochastic-regularization-softmax dimensions. Cluster formation: Lion-hyperparameter pair (#3056 β₁ + #3068 β₂) AND stochastic-regularization pair (#3065 DropPath + #3071 attn-dropout). 3 NEW major mechanistic insights this round: Lion LR-robustness, capacity-removal-vs-replacement sub-invariant, SwiGLU SiLU load-bearing.**

- 2026-05-15 [Round 138 close-44]: Closed **#3048 askeladd surf-weight-7 LOSS-with-STRONGEST-CRUISE-WIN-of-launch** (val 29.9129 +1.29% LOSS vs #3006 baseline 29.5318; test 24.9360 −2.13% WIN; **val_cruise 15.5562 −4.85% STRONGEST cruise WIN outside #3006**; test_cruise −2.44% WIN; 174th taxon SURF-WEIGHT-LANDSCAPE-NON-MONOTONE-VAL-CRUISE-DIVERGE-AT-7 / VAL-OPT-AT-10-CRUISE-OPT-AT-7) AND **#3044 edward inter-block-additive-β LOSS via α-β COUPLING** (val 30.8445 +4.44% LOSS; test 25.7971 +1.25% mild LOSS; val_cruise 17.0833 +0.5% marginally above 17.0 but ORDERING preserved; 175th taxon INTER-BLOCK-ADDITIVE-BETA-LOSS-VIA-ALPHA-BETA-COUPLING-GRADIENT-STEAL). **#3048 KEY MECHANISTIC FINDING — surf_weight landscape NON-MONOTONE in BOTH val AND cruise.** 4-point grid {5, 7, 10, 15} complete: val_avg convex around 10 (global min); val_cruise non-monotone with GLOBAL MIN at 7 not at lower bracket 5. Linear-interpolation prediction from #3031→#3006 was −2.5% cruise gain at 7; observed −4.85%, **~50% larger than linear**. Val/test asymmetry at surf=7 (val LOSS but test WIN) consistent with surf_weight controlling per-token weighting at criterion level — surface tokens dominate val_avg via weighted contribution but test reports unweighted L1. **9th cruise-preserving datapoint + 3rd non-baseline cruise WIN.** Surf_weight axis comprehensively closed at val_optimum=10 baseline; cruise/test optimum at 7 documents val/test contract divergence we cannot exploit without retuning weighting. **#3044 KEY MECHANISTIC FINDING — α-β COUPLING (gradient-steal).** β drifted non-trivially (β[2]=−0.0144 ep59) BUT this NEUTRALIZED α's downward drift; α stayed near 1.0 (mean ~0.9985) instead of #3006's 0.958. The joint solution (α≈1.0, β≈−0.007) has SMALLER effective intervention magnitude than the α-only solution at α=0.958. Adding β STEALS magnitude from α — optimizer redistributes gradient signal between two coupled DOF instead of using both at full strength. **First "ordering-preserved but level-relaxed" datapoint at the #3006 site** — cruise sits marginally above 17.0 threshold (+0.5%) but well below 27% BROKEN level. **6th REFINEMENT OF CRUISE INVARIANT (tentative):** Inter-block site preserves cruise ORDERING under additive-vs-multiplicative reparameterization but cruise LEVEL is monotonic in effective-intervention magnitude. **THREE CONVERGING CLOSURES NOW BOX INTER-BLOCK-α AXIS:** granularity (scalar #3006 WIN > per-channel #3043 LOSS), mechanism (multiplicative #3006 WIN > additive #3044 LOSS), routing direction (3-position #3006 WIN > 4-position #3021 LOSS). The α-axis is a single-DOF scalar at three specific positions; LOSS direction in all three sub-axes is "more parameters that the optimizer routes around." Followup **#3065 askeladd drop-path-0pt05** — Stochastic Depth on residual branches (p=0.05 per branch per block); 4 blocks × 2 branches = 8 DropPath modules; +0 params; FRESH stochastic-regularization axis NEVER touched in launch — orthogonal to deterministic (wd) and parametric (loss-shape) regularization. Larsson 2016 / Huang 2016. Cruise predicted PRESERVED (training-only perturbation; eval deterministic). 176th active axis. Would be 11th cruise-preserving datapoint if confirmed. Followup **#3066 edward layerscale-branch-output** — CaiT-style per-channel learnable γ at branch output, γ_init=1e-4; x ← x + γ * Block(x); 4 blocks × 2 branches × 96 channels = +768 params (~+0.19%); FRESH SITE (branch output, orthogonal to all α-axis sub-axes including #3044 coupling); different mechanism: multiplicative with SMALL init biases optimizer toward identity-like dynamics. Cruise prediction UNCERTAIN — first non-residual-edge multiplicative site at scalar granularity; 6th-refinement invariant test. 177th active axis. Cruise-preservation ledger now 19 datapoints (9 BROKEN + 10 PRESERVED conservative scoring; #3044 first ordering-preserved-marginal-level-violation). Total closed: 175. Winners: 23. **8 students all busy, zero idle GPUs. CURRENT FOCUS: 8 in-flight axes (#3047 GeGLU, #3049 bias-free Linear, #3050 grad-clip-1.0, #3054 RMSNorm, #3056 Lion β₁=0.95, #3061 slice_num=12, #3065 DropPath, #3066 LayerScale) test the refined 2D cruise invariant across stochastic-regularization, param-pruning, optimizer-stability, normalization-type, optimizer-hyperparameter, slice-routing-granularity, and BRANCH-OUTPUT-MULTIPLICATIVE dimensions. Surf-weight axis comprehensively closed; α-axis comprehensively closed (granularity × mechanism × routing-direction). Cluster forming: Llama-style (#3049 bias-free + #3054 RMSNorm) AND fresh-stochastic (#3050 grad-clip + #3065 DropPath) AND structural-multiplicative (#3066 LayerScale at fresh site).**

- 2026-05-15 [Round 138 close-43]: Closed **#3043 alphonse per-channel α LOSS but CRUISE PRESERVED** (val 30.883 +4.57% vs #3006; test 25.680 +0.78% WASH; cruise 16.907 PRESERVED < 17.0; 173rd taxon PER-CHANNEL-α-LOSS-MEAN-CARRIES-SIGNAL-VARIANCE-CARRIES-NOISE / α-AXIS-CLOSED-AT-SCALAR-GRANULARITY). **EXEMPLARY STUDENT WRITE-UP — KEY MECHANISTIC FINDING:** Per-channel α did NOT collapse to scalar (channels span 0.82-1.10 with std ~5%) BUT per-position MEANS match #3006's scalars almost exactly (0.98/0.97/0.95 vs #3006's ~0.95/0.97/0.96, same depth-monotonic pattern). **Mean carries signal, channel-variance carries noise.** Direct mechanistic evidence that per-channel granularity is strict superset including harmful per-channel idiosyncrasy on top of useful uniform attenuation. **γ-AXIS PATTERN CONFIRMED ON α-AXIS:** γ-axis was {#2964 fixed γ=1.0 WIN → #2988 scalar γ LOSS → #2977 per-channel γ WASH-overfit}; α-axis is now {#3006 3-scalar WIN → #3043 per-channel LOSS}. Exact predicted parallel. **5th-refinement cruise invariant CONFIRMED:** granularity is **orthogonal** to residual-stream perturbation property — per-channel α is parameterization change at structurally-cruise-preserving site, so cruise survives even when granularity hurts. **9th cruise-preserving datapoint.** α-AXIS DECISIVELY CLOSED AT SCALAR GRANULARITY (#3006 recipe optimal). Followup **#3061 alphonse slice-num-12** — Halve slice routing granularity slice_num=24 → 12 (single CLI flag). Param reduction ~−4,608 (~−1.1% total). Fresh axis: slice_num has NEVER been swept in this launch — most under-explored core Transolver hyperparameter. Tests if 24 was over-parameterized; coarser routing as implicit regularization could help val_geom_camber_rc (most-failed OOD split). Cruise predicted PRESERVED (intra-block attention reparameterization). 168th active axis. Would be 10th cruise-preserving datapoint if confirmed. Cruise-preservation ledger now 18 datapoints (9 BROKEN + 9 PRESERVED). Sub-invariant 'granularity-orthogonal-to-cruise-protection' now backed by 2 datapoints (#3026 cosine-routing, #3043 per-channel α). **Total closed: 173. Winners: 23. 8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 8 in-flight axes (#3044 inter-block additive β, #3047 GeGLU, #3048 surf=7, #3049 bias-free Linear, #3050 grad-clip-1.0, #3054 RMSNorm, #3056 Lion β₁=0.95, #3061 slice_num=12) test the refined 2D cruise invariant across diverse mechanism, activation, weighting, param-pruning, optimizer-stability, normalization-type, optimizer-hyperparameter, and slice-routing-granularity dimensions. Three major axes COMPREHENSIVELY CLOSED this round: weight-averaging (#3020/#3039), loss-shape (#3022/#3036), AND α-axis at scalar granularity (#3006/#3021/#3043). Sub-invariant 'eval-only preserves cruise unconditionally' backed by 2 datapoints; sub-invariant 'granularity-orthogonal-to-cruise-protection' now also backed by 2 datapoints.**

- 2026-05-15 [Round 138 close-42]: Closed **#3039 thorfinn SWA-last-10 LOSS but CRUISE PRESERVED** (val 30.39 +2.90% vs #3006; test 25.28 −0.77% WIN; cruise 16.45 PRESERVED; 172nd taxon SWA-LAST-10-LOSS-MONOTONIC-DESCENT-INCOMPATIBLE / WEIGHT-AVERAGING-AXIS-COMPREHENSIVELY-CLOSED). **EXEMPLARY STUDENT CLOSURE ANALYSIS — KEY MECHANISTIC FINDING:** Late-window val descended monotonically ep55→ep60 (30.6328 → 30.3220, −0.31 absolute). SWA can only interpolate; in strictly-descending sequence, averaging is anti-improvement. SWA estimate sits midway between ep55 and ep60 = predicted LOSS mechanism (verbatim from PR hypothesis LOSS section #1). **WEIGHT-AVERAGING AXIS COMPREHENSIVELY CLOSED — TWO DISTINCT LOSS DATAPOINTS:** #3020 EMA-decay=0.999 (exponential, full-training, init-lag + basin-drag, Δ=−0.088) + #3039 SWA-last-10 (uniform, late-window only, NO init-lag, Δ=+0.0662). Two failure modes mechanistically distinct (init-lag vs late-window-drag) yet axis fails comprehensively. **Lion+cosine+60ep recipe produces monotonic-descent trajectories STRUCTURALLY INCOMPATIBLE with weight averaging.** Re-opening requires schedule-shape change (cyclic/restart LR) — but #3033 closed schedule-shape axis. **8th cruise-PRESERVED datapoint** (sub-invariant: 'eval-only post-hoc weight transformations preserve cruise unconditionally' — 2 clean datapoints: #3020 EMA + #3039 SWA both PRESERVED). Followup **#3056 thorfinn lion-beta1-0pt95** — Sweep Lion's first-moment momentum coefficient β₁ from 0.9 (default) → 0.95. Single CLI flag change. +0 params, +0 compute. Higher β₁ smooths sign-step direction (95% past momentum, 5% current gradient vs default 90/10). Targets the under-explored optimizer-hyperparameter axis — we've only swept lr (#2994) and wd (#2993), never the Lion betas. Cruise predicted PRESERVED (optimizer-internal, no residual-stream perturbation; 5th-refinement invariant applies). 167th active axis. Would be 9th cruise-preserving datapoint if confirmed. Two major axes now comprehensively closed in this round: **weight-averaging** (#3020/#3039 both LOSS) AND **loss-shape** (#3036 + #3022 + L1 baseline). Cruise-preservation ledger now 17 datapoints (9 BROKEN + 8 PRESERVED). **Total closed: 172. Winners: 23. 8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 8 in-flight axes (#3043 per-channel α, #3044 inter-block additive β, #3047 GeGLU, #3048 surf=7, #3049 bias-free Linear, #3050 grad-clip-1.0, #3054 RMSNorm, #3056 Lion β₁=0.95) test the refined 2D cruise invariant 8 ways across granularity, mechanism, activation, weighting, param-pruning, optimizer-stability, normalization-type, and optimizer-hyperparameter dimensions. Llama-style cluster forming (#3049 bias-free Linear + #3054 RMSNorm). Sub-invariant 'eval-only preserves cruise unconditionally' now backed by 2 strong datapoints.**

- 2026-05-15 [Round 138 close-41]: Closed **#3036 frieren huber-β=0.01 LOSS but CRUISE PRESERVED MILD** (val 30.49 +3.25% vs #3006; test 25.96 +1.88% LOSS; cruise 16.87 +3.19% under 17.0 threshold; 171st taxon HUBER-BETA-0.01-QUAD-MASS-FRAC-36-PERCENT-MILD-CRUISE-DEGRADATION / LOSS-SHAPE-AXIS-CLOSED-AT-3-POINTS). **EXCEPTIONAL STUDENT WRITE-UP — KEY MECHANISTIC FINDING:** quadratic-mass-fraction scales cruise damage MONOTONICALLY at criterion level. Diagnostic: ep1 huber_l1_ratio=0.991 (true L1, 3% quad mass) → ep60 ratio=0.845 (36% quad mass). β=0.01 was calibrated correctly at ep1 but residuals decayed faster than predicted (long-tailed per-token residual distribution) — by ep60, ~36% of tokens have |residual|<β and contribute QUADRATIC loss. Cleanly between baseline L1 (0% quad / preserved) and #3022's β=1.0 (~85% quad / catastrophic). **5th REFINEMENT OF CRUISE INVARIANT (explicit):** Cruise damage scales **monotonically along TWO dimensions** — residual-stream perturbation magnitude × quadratic-regime mass fraction at criterion level. Decay law: 0% quad → preserved flat, 36% quad → mild +3.19%, 85% quad → catastrophic +62%. **3-point loss-shape axis comprehensively CLOSED:** L1 (#3006 baseline) < β=0.01 (mild) << β=1.0 (#3022 catastrophic). L1 is load-bearing for Lion + Transolver + 60ep recipe. 7th cruise-PRESERVED datapoint added at the mild-degradation boundary. Followup **#3054 frieren rmsnorm-replace-layernorm** — Replace every nn.LayerNorm with nn.RMSNorm (Llama 1/2/3, DeepSeek, Qwen, T5 default; keeps γ scale, removes β bias); fresh normalization axis untested in launch; orthogonal to LN-placement axis (exhausted at #3003 sandwich-LN); cruise predicted PRESERVED (would be 8th datapoint); composes with #3049 bias-free Linear (in-flight); 166th active axis. Cruise-preservation ledger now 17 datapoints (9 BROKEN + 8 PRESERVED). **Total closed: 171. Winners: 23. 8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 8 in-flight axes (#3039 SWA-last-10, #3043 per-channel α, #3044 inter-block additive β, #3047 GeGLU, #3048 surf=7, #3049 bias-free Linear, #3050 grad-clip-1.0, #3054 RMSNorm) test the refined 2D cruise invariant 8 ways simultaneously. Loss-shape axis comprehensively closed (3 points clean); pivoted to normalization-type axis as the highest-value next probe. Llama-style cluster forming (#3049 bias-free Linear + #3054 RMSNorm) — if both PRESERVE cruise, suggests broader Llama-style adoption is safe; if both BREAK, suggests Transolver's residual-stream geometry differs structurally from Llama's.**

- 2026-05-14 [Round 138 close-40]: Closed **4 LOSS PRs (#3025/#3031/#3032/#3033) with rich mechanistic findings + 4 fresh assignments to all idle students**. **Closure highlights:** **#3025 nezuko pre-block-β** (val 30.35 +2.76% LOSS, test 24.92 −2.18% WIN, cruise 16.33 PRESERVED; 167th taxon RESIDUAL-EDGE-DOUBLE-SCALAR-COLLAPSES-UNDER-LION-SIGN-STEP — **KEY FINDING: β_{i+1} ≡ α_i exactly throughout training under Lion's sign-only step rule; +4 nominal DOF collapsed to +1 effective DOF**; mathematical property of Lion + composite-scalar-on-shared-edge; 7th cruise-preserving datapoint). **#3031 askeladd surf-weight-5** (val 29.77 +0.81% mild LOSS, test 25.38 −0.39% slight WIN, **cruise 15.82 −3.25% STRONGEST CRUISE WIN OF LAUNCH OUTSIDE #3006**; 168th taxon SURF-WEIGHT-5-LOWER-BRACKET-CRUISE-WIN; 8th cruise-preserving datapoint AND first non-baseline cruise WIN). **#3032 tanjiro surf-weight-15** (val 31.11 +5.34% LOSS, test 26.20 +2.81% LOSS, cruise 16.74 +4.12% drift near 17.0 threshold; 169th taxon SURF-WEIGHT-15-UPPER-BRACKET-LOSS-WITH-CRUISE-DRIFT; **possible 5th-refinement candidate** — Lion sign-step under composite-loss reweighting causes channel-direction perturbation even with L1 sign-shape preserved). **#3033 fern WSD-schedule** (val 31.01 +5.0% LOSS, test 26.22 +2.92% LOSS, cruise 16.82 PRESERVED; 170th taxon WSD-SCHEDULE-LION-FAILS-AT-PEAK-LR-COSINE-IS-LOAD-BEARING — **KEY FINDING: Lion does NOT converge at peak LR; requires continuous LR decay to make progress; 47 epochs of stable phase wasted; last 10 ep descended monotonically 43.2→31.0**; 6th cruise-preserving datapoint; OPPOSITE of SGD+momentum + cross-entropy WSD literature). **Sweep table:** surf_weight | val | cruise — 5 (29.77 / 15.82 WIN) | 10 (29.5318 #3006 baseline) | 15 (31.11 / 16.74 drift); convex around 10 with cruise-favoring tilt. **3 axes closed in single round** — residual-scaling (one-scalar-per-junction saturated under Lion) + loss-weighting bracket (5/10/15 sweep complete) + schedule-shape (cosine load-bearing for Lion). **Followup assignments:** **#3047 nezuko geglu-mlp** (SwiGLU→GeGLU 1-line swap `F.silu`→`F.gelu` at gate; +0 params; fresh activation axis at gate position; 162nd active axis; cruise predicted PRESERVED — would be 9th datapoint). **#3048 askeladd surf-weight-7** (single CLI flag midpoint sweep between #3031's 5.0 and #3006's 10.0; +0 params; cheapest experiment; convexity probe; 163rd active axis). **#3049 tanjiro bias-free-linear** (Llama-style `bias=False` on all nn.Linear; LayerNorm bias kept; ~1.5% param reduction; fresh param-space pruning axis; 164th active axis; cruise predicted PRESERVED — would be 10th datapoint). **#3050 fern grad-clip-1pt0** (`torch.nn.utils.clip_grad_norm_(max_norm=1.0)` after backward(); +0 params; fresh stability axis orthogonal to schedule; protects Lion EMA buffer from outlier batches; 165th active axis; cruise predicted PRESERVED — would be 11th datapoint). Cruise-preservation ledger now 16 datapoints (9 BROKEN + 7 PRESERVED). **Total closed: 170. Winners: 23. 8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 8 in-flight axes (#3036 huber-β=0.01, #3039 SWA-last-10, #3043 per-channel α, #3044 inter-block additive β, #3047 GeGLU, #3048 surf=7, #3049 bias-free Linear, #3050 grad-clip-1.0) test cruise invariant 8 ways simultaneously across loss-shape, weight-averaging, granularity, mechanism, activation, weighting, param-pruning, optimizer-stability dimensions. Pursuing 5th-refinement of cruise invariant + multiple paths to beat #3006 baseline.**

- 2026-05-14 [Round 138 close-39]: Closed **#3021 alphonse 4-α DiT-style LOSS** (val 30.66 +3.83% vs NEW baseline 29.5318; test 25.66 +0.72%; cruise +1.98% — 9th post-#2964 cruise breakage; 161st taxon 4-α-DIT-STYLE-COUPLES-α-FLIPS-α₁-FROM-DOWN-TO-UP-AND-α₃-DRIFTS-UP-NOT-DOWN-ASYMMETRIC-3-α-LAYOUT-IS-LOAD-BEARING). **KEY MECHANISTIC FINDING #3021 — DIRECTIONAL INVERSION:** "all 4 α's want DOWN" hypothesis FALSIFIED. With α₃ in the path, α₁ flips from DOWN (0.97 in #3006) to UP (1.05). 4 scalars COUPLE — adding head-input scale changes mid-position dynamics. Alternating DOWN/UP/DOWN/UP pattern at converge — opposite of uniform-DOWN that drove #3006's WIN. **The asymmetric "skip α₃" layout from #3006 is STRUCTURALLY LOAD-BEARING.** Closed **#3026 edward cosine-routing-τ=0.07 LOSS but CRUISE PRESERVED** (val 29.98 +1.53% LOSS vs NEW; test 24.94 **−2.10% WIN** — val/test divergence; **cruise 16.36 +0.04% FLAT — BASIN PRESERVED**; 162nd taxon ROUTING-MAGNITUDE-IS-LOAD-BEARING / COSINE-ROUTING-FIXES-EXPLOSION-BUT-LOBOTOMIZES-MAGNITUDE-SIGNAL). **KEY MECHANISTIC FINDINGS #3026:** (1) Cosine routing FIXES logit explosion mechanically — `logit/τ |max|` 8-24 range vs #3008's 285-529 (12-66× lower). Routing-stability is NOT the bottleneck. (2) ‖Q‖, ‖K‖ MAGNITUDE INFO IS LOAD-BEARING — direction-only routing loses ‖Q‖_raw block[0] max=95.89 outlier-token routing-confidence signal. val_in_dist +6.47% LOSS = magnitude info penalty. (3) **5th-REFINEMENT CRUISE INVARIANT (tentative):** cruise basin robust to routing-mechanism REPARAMETERIZATIONS that preserve softmax-over-slices structure AND per-sample gradient sign-pattern. Cosine routing IS inside residual stream (slice outputs differ) yet cruise preserved — first INSIDE-block intervention to preserve cruise. (4) **ROUTING-STABILITY axis: CLOSED both directions** — scale-pinning #3008 closed, scale-removal #3026 closed. Cruise-preservation ledger now 15 datapoints (9 BROKEN + 6 PRESERVED: #3003 ordering, **#3006 α WIN**, #3008 mildest, #3014 FiLM-input-dropout, #3020 EMA eval-only, **#3026 cosine-routing INSIDE-block reparameterization**). Followup **#3043 alphonse cross-block-alpha-per-channel** — replace 3 SCALAR α from #3006 with 3 PER-CHANNEL vectors (96-d); keeps asymmetric 3-position layout (proven load-bearing); +285 net params; strong granularity test on the only WIN axis post-#2964; tests whether α-axis follows γ-axis pattern (WIN at fixed → LOSS at scalar → WASH-overfit at per-channel); per student suggestion #1 verbatim; 165th axis. Followup **#3044 edward inter-block-additive-bias** — add 3 SCALAR ADDITIVE β at same 3 positions as α from #3006; x ← x*α + β; +3 net params; **FRESH ADDITIVE vs MULTIPLICATIVE orthogonal mechanism test at cruise-preserving site**; per student suggestion #2 verbatim; cruise predicted PRESERVED; 166th axis. **Total closed: 162. Winners: 23. 8 students all busy, zero idle GPUs.** **CURRENT FOCUS: α-axis granularity test (#3043 per-channel) PLUS additive-vs-multiplicative mechanism test (#3044 β at same site). 8 in-flight axes test cruise invariant 8 ways simultaneously across architectural, eval-only, loss-shape, LR-shape, loss-weighting dimensions. Cruise-preservation ledger now 15-deep — invariant is robust enough to drive future hypothesis design.**

- 2026-05-14 [Round 138 close-38]: Closed **#3020 thorfinn ema-decay-0.999 LOSS but CRUISE PRESERVED** (val 30.3552 +2.79% vs NEW baseline 29.5318; test 25.5134 +0.13% WASH; **cruise val 16.4514 LOWEST split — 5th cruise-preserving datapoint post-#2964**; 163rd taxon EMA-DECAY-0.999-FULL-TRAINING-DRAGS-IN-BEST-EQUALS-LAST-MONOTONIC-DESCENT-REGIME). **KEY MECHANISTIC FINDING #3020 — DUAL FAILURE MODE:** EMA at decay=0.999 with full-training accumulation suffers TWO compounding problems: (a) init-lag during first ~10 epochs (EMA carries 99.9% init weight even after ep10), (b) basin-drag at convergence (raw still descending at ep60 → EMA averages over older worse weights). EMA-vs-raw diagnostic trajectory: ep1 Δ=−28.31 (init-dominated), ep10 Δ=−20.82 (still trailing), **ep30 Δ=+5.998 (EMA winning at mid-descent — variance reduction working)**, ep60 Δ=−0.088 (raw catches up). The "ep30 EMA was winning by +5.998" finding motivates the SWA pivot. **STRONGEST CONFIRMATION OF 4th-REFINEMENT CRUISE INVARIANT YET:** training trajectory IDENTICAL to baseline (only eval weights differ), so per-step gradients identical AND residual stream identical → cruise preserved exactly as the invariant predicts. Cruise-preservation ledger now 13 datapoints (8 BROKEN + 5 PRESERVED: #3003 ordering, #3006 α-WIN, #3008 K-only-LN mildest, #3014 FiLM-input-dropout, **#3020 EMA eval-only — cleanest test**). Followup **#3039 thorfinn swa-last-10-epochs** — Izmailov 2018 canonical SWA recipe (uniform arithmetic mean of 10 epoch snapshots ep50-59); EXCLUDES init-lag period entirely; averages ONLY the late-training trajectory where ep30 diagnostic showed EMA was winning by +5.998; eval-only intervention; **cruise predicted PRESERVED** (would be 6th cruise-preserving datapoint); different from #3020 in 3 ways (uniform vs exponential weighting, ep50-start vs ep1-start, K=10 vs ~7-epoch geometric window); 164th axis. Total closed: 160. Winners: 23. **8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 5 in-flight non-architectural axes (#3031/#3032 surf-weight bracket, #3033 WSD-LR-shape, #3036 huber-β=0.01-recalibrated, #3039 SWA-last-10) PLUS 3 architectural axes (#3021 4-α, #3025 pre-block-β, #3026 cosine-routing). The cruise-preservation invariant has 5 confirming datapoints and 1 falsification — robust enough to drive future hypothesis design. EMA family closed at full-training; SWA-late opens the late-training-only branch.**

- 2026-05-14 [Round 138 close-37]: Closed **#3022 frieren huber-smooth-l1-beta-1.0** LARGE LOSS (val 41.29 +39.79% vs NEW baseline 29.5318; test 35.70 +40.11%; **cruise 26.50 +62% LOSS — basin DECISIVELY BROKEN**; 159th taxon HUBER-AT-BETA-1.0-IS-EFFECTIVE-L2-IN-NORMALIZED-RESIDUAL-SPACE / LOSS-SHAPE-CHANGE-BREAKS-CRUISE). **KEY MECHANISTIC FINDING #3022:** β=1.0 was MISCALIBRATED for normalized residuals — training criterion runs in normalized space (residuals O(0.1)), not physical MAE units (~30). Diagnostic: Huber/L1 ratio dropped to 0.14 by ep30 → deep quadratic regime → effective L2. L1's sign-only-per-sample gradient is LOAD-BEARING for this Lion + 60ep + post-norm + γ=1.0 recipe. **CRITICAL — 4th REFINEMENT OF THE CRUISE INVARIANT:** Prior refinement said outside-block regularization preserves cruise. This PR disproved that — Huber loss is OUTSIDE-block yet cruise broke. **NEW REFINEMENT: Cruise breaks under EITHER (a) residual-stream perturbation, OR (b) loss-curvature/gradient-shape change at the criterion level. Cruise preserved ONLY when both residual stream AND per-sample gradient SHAPE/DIRECTION are untouched.** FiLM-input dropout (#3014) preserved cruise because surviving gradients retain L1 sign-pattern. Huber β=1.0 transformed gradients from L1 sign-pattern → L2 magnitude-pattern → cruise broke. Ledger updates to 13 datapoints (9 BROKEN + 4 PRESERVED). Followup **#3036 frieren huber-smooth-l1-beta-0pt01** — Huber/Smooth-L1 at β=0.01 calibrated to normalized residual scale; keeps training in L1 regime for all residuals > 0.01 (everything throughout 60ep); quadratic smoothing only for tail samples near convergence; **per #3022 closure rec verbatim "β=0.01 sweep gives a true Huber regime ... closest to the original hypothesis intent"**; **CRUISE PREDICTED TO BE PRESERVED** (sign-pattern unchanged for residuals > 0.01) — strongest test of refined invariant since #3014; 3-point loss-shape axis (L1 baseline / β=0.01 tail smoothing / β=1.0 effective L2 catastrophic); 162nd axis. Total closed: 159. Winners: 23. **8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 5 in-flight non-architectural axes test refined invariant 5 ways simultaneously — loss-weighting bracket (#3031/#3032), LR-shape (#3033), Huber-recalibrated (#3036), plus 4 architectural-axis tests (#3020 EMA, #3021 4-α, #3025 β, #3026 cosine-routing).**

- 2026-05-14 [Round 138 close-34/35/36]: Closed **#3013 askeladd weight-tied-universal-block** LOSS (val 34.13 +15.57% vs NEW baseline 29.5318; cruise +24.29% — 8th post-#2964 cruise breakage, LARGEST per-split regression in launch; 156th taxon WEIGHT-TYING-ACROSS-BLOCKS-AT-N=4-DECISIVELY-CLOSED). **KEY MECHANISTIC FINDING #3013 (PER-PASS RESIDUAL DIAGNOSTIC):** ‖x‖_rms grows monotonically +2.8% across 4 passes, ‖x‖_abs_max grows ~25% — **OPPOSITE of fixed-point dynamics**; the multigrid/Newton analogy in the hypothesis does NOT hold at d=96/n_head=2/n_layers=4. Each block needs unique parameterization (capacity expressivity is load-bearing). SwiGLU gate damping IS present (gate_abs_mean −30% across passes) but insufficient to halt residual-norm growth. Cruise basin structurally requires unique-per-block capacity. Closed **#3015 tanjiro fourier-pe-l6** LOSS (val 30.71 +4.00% vs NEW; **val_in_dist −6.49% isolated WIN ✓** — 3rd predicted "LOSS-with-in_dist-WIN" diagnostic; 157th taxon FOURIER-PE-L=6-LOSS-WITH-IN_DIST-WIN / SPECTRAL-OVERFIT-TO-SPATIAL-STRUCTURE-BREAKS-OOD-GENERALIZATION). **KEY MECHANISTIC FINDING #3015 (SPECTRAL ALIASING AT COORD SCALE):** Fourier feature distribution mechanically correct (mean ≈0, std=1/√2 = perfect sin/cos equidistribution); HOWEVER coord range [−6.53, +6.25] × top freq 2^5·π ≈ 100.5 rad/unit → top-band max phase 654 rad >> 2π → top channels become uncorrelated noise. Failure mode = OVERFIT TO COORD POSITIONS (in_dist benefits from sharpened position-dependent features; OOD splits overfit to absolute coord fingerprints rather than physics signal). Cruise meta-signal HELD QUALITATIVELY (val_cruise stays LOWEST split — ordering preserved like #3003) but attenuated quantitatively (+7.68% LOSS). **DATA/INPUT-REPRESENTATION AXIS COMPREHENSIVELY CLOSED** — #2918 mixup + #2973 log_Re + #2976 coord + #2978 AoA + #3015 Fourier all LOSS. Closed **#3014 fern film-input-dropout-p-0.2** LOSS (val 30.70 +3.97% vs NEW; val_re_rand +4.51% — TARGETED INTERVENTION FAILED; but **val_cruise −2.06% WIN — first post-#2964 cruise WIN; FIRST non-architectural intervention to preserve cruise**; 158th taxon FILM-CONDITIONING-VECTOR-DROPOUT-AT-P=0.2-FALSIFIES-RE-CONDITIONING-OVERFIT-HYPOTHESIS). **KEY MECHANISTIC FINDINGS #3014:** (1) The "Re-conditioning overfit" hypothesis from #2993 closure is DECISIVELY FALSIFIED — this PR was the cleanest test; re_rand WIN under wd=1e-3 was a bias-limited-capacity artifact NOT evidence of Re-overfitting. (2) FiLM 3-vector is FULLY LOAD-BEARING across all OOD splits. (3) **REFINED INVARIANT: cruise basin protection extends to OUTSIDE-BLOCK interventions** — FiLM-input dropout sits OUTSIDE TransolverBlocks → outside residual stream → cruise survives. Joins #3003 ordering + #3006 level + #3008 mildest set as 4th cruise-preserving intervention; FIRST non-architectural one. **REFINED RULE: cruise basin is incompatible with RESIDUAL-STREAM perturbation; outside-block regularization does NOT count as residual-stream perturbation.** 12-datapoint ledger confirms: 8 BROKEN are all residual-stream perturbations; 4 PRESERVED are all architectural-structural OR outside-block-regularization. Followup **#3031 askeladd surf-weight-5** (FRESH LOSS-WEIGHTING AXIS — halve `surf_weight` from 10.0 to 5.0; FIRST surf_weight experiment in launch; lower-bracket of paired sweep with tanjiro; pivot from #3013 closure rec #5 "step back to non-architectural axes"; 160th axis). Followup **#3032 tanjiro surf-weight-15** (paired upper bracket — `surf_weight=15.0` (1.5×); 3-point sweep with baseline 10.0 in one round; pivot from #3015 data-axis cluster closure; same 160th axis). Followup **#3033 fern wsd-schedule-stable47-decay10** (FRESH LR-SHAPE AXIS — Warmup-Stable-Decay schedule replaces CosineAnnealingLR; keep 3ep linear warmup, constant 47ep at peak, linear decay 10ep; FIRST LR-shape experiment in launch — all prior LR experiments varied VALUES not the schedule SHAPE; Llama 3/DeepSeek/MiniCPM modern standard recipe; pivot from #3014 closure rec #6 "non-conditioning regularization"; 161st axis). Total closed: 158. Winners: 23. **8 students all busy, zero idle GPUs.** **CURRENT FOCUS: 2 fresh non-architectural axes opened in same round — loss-weighting (160th, bracketed) and LR-shape (161st) — testing the refined outside-block invariant on the largest remaining unexplored design surfaces. Per the 12-datapoint cruise ledger: both should PRESERVE cruise.**

- 2026-05-14 [Round 138 close-32/33]: Closed **#3009 nezuko SqReLU** Option-A LOSS (val 32.92 +11.48% vs NEW baseline 29.5318; cruise +14.40% — 9th post-#2964 breakage; catastrophic dead-feature collapse 86-96% by ep60; 154th taxon SWIGLU-INNER-ACT-SQRELU-CATASTROPHIC). Closed **#3008 edward K-only-LN** LOSS (val 31.12 +5.39% vs NEW; cruise +0.02% MILDEST breakage; 155th taxon QK-NORM-AXIS-DECISIVELY-CLOSED-ACROSS-ALL-3-GRANULARITIES). **KEY MECHANISTIC FINDING #3008:** K-magnitude drift in baseline is NOT informative signal but COOPERATIVE SCALE VARIABLE — the optimizer uses K's drift to damp Q's depth-growth and keep routing logits bounded; pinning K (via LN/RMSNorm) removes the damping knob; Q's free growth then blows up routing logits (1.4→21 → logit 285-530). **#3009 KEY FINDING:** GLU + sqrelu hybrid is non-canonical and self-reinforcing dead-feature collapse via pre-activation negative drift. Followup #3025 nezuko pre-block-input-beta (FRESH RESIDUAL-POSITION AXIS — +4 learnable scalars at block INPUT before LN+branch+residual; SYMMETRIC COUNTERPART to #3006 α at block OUTPUT; tests whether input-side scaling captures same signal as output-side; pivots completely off activation-axis to HOT cross-block scaling axis; 158th axis). Followup #3026 edward cosine-routing-tau-0.07 (SURVIVING DIRECTION AFTER QK-NORM CLOSURE — F.normalize(Q) and F.normalize(K) before slice-routing einsum; τ_init=0.07 preserves baseline softmax range; logit bounded by Cauchy-Schwarz; structurally prevents the magnitude explosion that broke #2983/#3008; per student closure rec #1; 159th axis). Total closed: 155. Winners: 23. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 MERGE+close-31]: **🏆 MERGED #3006 alphonse cross-block-residual-α WIN (val 29.5318 −1.69% vs #2964; test 25.4795 mild LOSS +1.07%; 23rd winner; NEW BASELINE val 29.5318 / test 25.4795).** **🌟 BREAKTHROUGH: CRUISE META-SIGNAL FULLY PRESERVED — FIRST WIN POST-#2964 TO PRESERVE CRUISE BASIN** (val_cruise 16.348 +0.51% flat vs #2964's 16.265). Combined with #3003 sandwich-LN ordering preservation, **definitively confirms the close-30 hypothesis: training-time-preserving structural interventions preserve cruise basin.** Both #3003 and #3006 perturb only deterministic forward-pass structure; both preserved cruise. 8 prior post-#2964 LOSS interventions perturbed training dynamics (loss/data/optimizer/lr/wd/T-init/structure-with-stochasticity) → all broke cruise. α drifted DOWN uniformly to mean ~0.958 (~4% attenuation); direction same as #2988 in-block γ but magnitude 20× smaller; outcome OPPOSITE — confirms predicted mechanism "α scales AFTER post-LN, scale survives downstream; γ scales BEFORE post-LN, LN absorbs scale." Val/test divergence on test_camber_rc +4.05% LOSS (the main test-side regression site). Per CLAUDE.md "merge-aggressive on primary val metric." Followup #3021 alphonse cross-block-alpha-4-dit-style (+1 scalar α₃ extension; scale ALL 4 block outputs including last; tests whether asymmetric "skip α₃" was load-bearing; 156th axis). **CLOSED #3010 frieren droppath-p-0.1 LOSS** (val 33.4911 +11.50% vs NEW; cruise +22.06% — 8th post-#2964 breakage). 153rd taxon: DROPPATH-AT-BRANCH-INTERIOR-AT-60EP-CLOSED-AT-P=0.1. REFINED INVARIANT: **cruise basin is incompatible with ANY residual-stream perturbation** — stochastic (DropPath) or deterministic-magnitude (γ/noise/optimizer/lr/wd); only deterministic-structural interventions preserve it. Train→val gap inverted (val<train) confirms DropPath regularization active but starves gradient signal at p=0.1 in 60ep budget. Followup #3022 frieren huber-smooth-l1-beta-1.0 (FRESH LOSS-FUNCTION AXIS — first loss-function experiment in launch; F.smooth_l1_loss(beta=1.0) replaces L1 for both surf+vol training criteria; MAE reporting UNCHANGED; pivots away from regularization closures; 157th axis). Total closed: 153. Winners: 23. **8 students all busy, zero idle GPUs.** **CURRENT FOCUS: WEIGHT-AVERAGING (#3020 EMA) + α-EXTENSION (#3021 4-α) + LOSS-FUNCTION (#3022 Huber) all target cruise-preserving regime.**

- 2026-05-14 [Round 138 close-30]: Closed #3003 thorfinn sandwich-norm-magneto LOSS (val 31.3071 +4.22% vs NEW baseline; test 26.6837 +5.85%; reached 57/60 epochs LN-tax; +1,536 params dual-LN per block). 152nd taxon: SANDWICH-NORM-AT-4-BLOCK-DEPTH-IS-REDUNDANT / LN-PLACEMENT-AXIS-EXHAUSTED. **🌟 CRITICAL NEW FINDING: CRUISE META-SIGNAL ORDERING PRESERVED FOR THE FIRST TIME POST-#2964 UNDER STRUCTURAL INTERVENTION.** Cruise remained the LOWEST val AND test split (17.75 / 14.70) — split ORDERING held across all 4 splits, even though ABSOLUTE level worse than baseline. 7 prior consecutive post-#2964 interventions all inverted cruise (#2961 aux, #2978 AoA, #2986 AdamW, #2988 γ, #2994 lr, #2995 T-warmstart, #2993 wd); #3003 broke the streak. ADDITIONAL FINDINGS: (1) Pre-LN γ migrated 1.0 init → ~0.81-0.93 mean across 4 blocks — model SELF-MUTING pre-LN, signaling redundancy at this depth. (2) LN tax real: -3 epochs reached at 30-min budget (per-block dual-LN forward cost). (3) **LN-PLACEMENT AXIS EXHAUSTED at 4-block depth across all variants** — pre-norm + γ=1.0 baseline pre-#2964, **post-norm γ=1.0 (#2964 WIN)**, post-norm scalar γ #2988 LOSS, post-norm per-channel γ #2977 LOSS, sandwich #3003 LOSS. (4) **TRAINING-TIME-PRESERVING INTERVENTIONS PRESERVE CRUISE BASIN** — all 7 prior LOSS interventions perturbed training dynamics (loss/input-noise/optimizer/optstep/lr/T-init/wd); sandwich-LN perturbed only forward-pass structure → cruise ordering held. **This signals weight-averaging / EMA as the natural pivot** (zero training perturbation = should fully preserve cruise basin). Per student closure recommendation #4 verbatim "pivot off LN-placement axis." Assigned #3020 thorfinn ema-decay-0.999 (FRESH WEIGHT-AVERAGING AXIS — Polyak 1990 / SWA Izmailov 2018 recipe via `torch.optim.swa_utils.AveragedModel`; shadow EMA buffer updated each opt step `ema = 0.999·ema + 0.001·w`; evaluates with EMA weights; ZERO training-time perturbation = predicts cruise basin preservation; targets best=last by collecting late-training quality; +0 trainable params; first weight-averaging experiment in launch; 155th axis). Total closed: 152. Winners: 22. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-29]: Closed #2993 fern wd=1e-3 LOSS (val 30.5954 +1.85% vs NEW baseline; test 26.0329 +3.26%; in_dist +7.55% WORST-HIT; cruise +3.27% LOSS = 7th post-#2964 cruise breakage; **re_rand -3.21% SOLE WIN SIGNAL**). 151st taxon: WD-MAGNITUDE-AXIS-CLOSED-AT-3E-4 / RE_RAND-WANTS-MORE-CONDITIONING-REGULARIZATION. DECISIVE FINDINGS: (1) wd bracketing tight: 5e-5 (too weak), **3e-4 WIN baseline**, 1e-3 +1.85% LOSS — optimum at 3e-4. (2) Capacity-limited NOT reg-limited; bias-limited at 407k. (3) Re_rand WIN -3.21% is sole split-level improvement — model overfits Re-conditioning patterns; points to targeted intervention rather than global regularization. (4) Cruise broken 7/7 times post-#2964 — pattern accelerates. Per student suggestion #3 verbatim. Assigned #3014 fern film-input-dropout-p-0.2 (FRESH AXIS — nn.Dropout(p=0.2) on FiLM conditioning vector [log_Re, AoA0, AoA1] BEFORE film_mlp; targets re_rand failure mode without globally regularizing other splits; zero new params; first FiLM-internal stochastic regularization in launch; orthogonal to data-aug closures; 153rd axis). Total closed: 151. Winners: 22. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-28]: Closed #2995 tanjiro learnable-T-warm-start LOSS (val 31.0655 +3.42% vs NEW baseline; test 26.2443 +4.10%; cruise +3.76% LOSS = 6th post-#2964 cruise breakage). 150th taxon: T-WARM-START-CROSS-ARCHITECTURE-NO-TRANSFER / T-AXIS-COMPREHENSIVELY-CLOSED. DECISIVE FINDINGS (exceptional student write-up): (1) Cross-architecture warm-start does NOT transfer — T values migrated 8-40% per block from warm-start [0.94, 0.60, 0.73, 0.44] to NEW equilibrium [0.86, 0.81, 0.80, 0.62] within first 30 epochs; drift comparable to uniform-init drift; warm-start premise FAILED. (2) T migrated UP not stable — B1 (+33.9%), B3 (+40.2%) drifted UP from warm-start; inverse of premise. (3) Post-norm+γ=1.0 equilibrium FLATTER & WARMER (span 0.24, mean 0.77 vs warm-start span 0.50, mean 0.68); post-norm narrows residual magnitudes → reduces logit dynamic range → pushes T toward unity. (4) T-AXIS COMPREHENSIVELY CLOSED ACROSS ALL VARIANTS: per-block schedule LOSS #2969, T=2.0 LOSS #2944, T=0.25 LOSS #2955, dropout-on-routing LOSS #2958, warm-start LOSS this PR. T-LEARNABILITY with init=0.5 is final operating point. Assigned #3015 tanjiro fourier-pe-l6 (FRESH INPUT-REPRESENTATION AXIS — NeRF-style Fourier positional encoding; L=6 frequencies × 2 spatial dims × 2 sin/cos = +24 input channels; +2,304 params at input projection; AUGMENTS raw coords not replaces (respects #2976 closure "coord precision is load-bearing"); first input-representation experiment in launch; 154th axis). Total closed: 150. Winners: 22.

- 2026-05-14 [Round 138 close-27]: Closed #2997 askeladd n_layers=5 LOSS (interpreted from PR title "n_layers=6"; baseline is actually n=4 not n=5 as PR body stated, student correctly clarified). val 30.8711 +2.77% vs NEW baseline; test 26.7141 +5.97%; test_in_dist +14.51% WORST-HIT; reached only 51/60 epochs (30-min cap fired). 149th taxon: DEPTH-CAPACITY-AT-N_LAYERS-5 / BUDGET-CAPPED-AT-30MIN. DECISIVE FINDINGS: (1) Capacity is NOT bottleneck under 30-min budget — added +92k params (407k → 499k); per-epoch wall grew +15%; budget cap fired at ep51 (vs ep60 baseline) before extra capacity amortizes. (2) Per-epoch wall grows linearly with blocks (~15%/block) — deeper models REACH FEWER EPOCHS, dominating any per-epoch improvement. (3) OOD splits barely moved (cruise +0.59%, re_rand +0.06%) — extra capacity went to in_dist regression; depth alone doesn't help OOD at this scale. (4) best_epoch=51=terminal — same underfit signature, just shifted earlier on epoch axis. Capacity-axis is BUDGET-CAPPED at 30min/60ep envelope. Per student suggestion #4 verbatim. Assigned #3013 askeladd weight-tied-universal-block (FRESH AXIS — Universal Transformer / ALBERT recipe; 1 unique TransolverBlock applied 4 times instead of 4 unique blocks; -74% block params (407k → ~107k); SAME compute per epoch and SAME wall-clock budget; tests "parameter sharing as implicit regularization + iterative refinement" hypothesis; clean capacity-vs-compute separation; first weight-sharing experiment in launch; 152nd axis). Total closed: 149. Winners: 22.

- 2026-05-14 [Round 138 close-26]: Closed #2994 frieren lion-lr-1e-4 LOSS (val 32.5593 +8.39% vs NEW baseline; test 27.4533 +8.90%; ALL splits LOSS; cruise +13.30% LOSS = 5th post-#2964 cruise breakage). 148th taxon: LION-LR-AXIS-CLOSED-IN-BOTH-DIRECTIONS-AT-60EP / OOD-PREFERS-LARGER-STEPS-IN-POST-NORM-GEOMETRY. DECISIVE FINDINGS: (1) Hypothesis decisively FALSIFIED BY DIRECTIONAL INVERSION — OOD splits (cruise +13.30%, rc +11.32%) regressed MOST under smaller lr; predicted tighter basin instead became broader. (2) `best=last` is BUDGET SIGNATURE NOT STEPSIZE SIGNATURE — lr=1e-4 also ends best=last (ep59); 60ep budget insufficient at smaller lr. (3) OOD splits want MORE gradient signal not less — post-norm+γ=1.0 geometry benefits from LARGER steps (landscape exploration). (4) **CRUISE META-SIGNAL BROKEN 5th TIME post-#2964** — pattern: #2961 aux-sup, #2978 AoA, #2986 AdamW, #2988 scalar γ, #2994 lr=1e-4. Cruise WIN structurally fragile to ANY substantive perturbation including pure-LR scans. (5) **LION LR-AXIS DECISIVELY CLOSED BOTH DIRECTIONS**: 2e-4 LOSS, 1.75e-4 marginal, **1.5e-4 WIN baseline**, 1e-4 +8.39% LOSS — bracket tight, optimum at or near 1.5e-4 in 60ep budget. Per student suggestion #4 verbatim (optimizer-family pivot) — but optimizer-family axis already CLOSED (#2974/#2986). Assigned #3010 frieren droppath-p-0.1 (FRESH STOCHASTIC-DEPTH AXIS — Huang 2016/Swin/ViT/ConvNeXt recipe; zero new params; constant p_drop=0.1 across all 4 blocks; applied INSIDE residual `x = LN(x + DropPath(branch(x)))`; preserves post-norm + γ=1.0; orthogonal to all closed and in-flight axes; 151st axis; first stochastic-depth experiment in launch). Total closed: 148. Winners: 22. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-25]: Closed #2988 alphonse per-block-scalar-gamma-init-1.0 LOSS (val 31.5126 +4.91% vs NEW baseline; test 26.0892 +3.49%; cruise +5.10% LOSS — 4th post-#2964 cruise breakage). 147th taxon: γ-ADAPTIVITY-AXIS-DECISIVELY-CLOSED-ACROSS-ALL-3-GRANULARITIES. DECISIVE FINDINGS: (1) γ DRIFTS DOWN FROM 1.0 NOT UP — γ_attn mean=0.78, γ_mlp mean=0.67 at ep60; model self-shrinks branch contributions but val/test BOTH WORSE than fixed γ=1.0. (2) 3-POINT γ-ADAPTIVITY SWEEP COMPLETE — fixed γ=1.0 (0 params) BEATS BOTH learnable scalar (8 params, decisive LOSS) and learnable per-channel (768 params, mild LOSS). γ-axis closed at post-norm across full granularity range. (3) MLP γ DRIFTS LOWER THAN ATTN γ (0.67 vs 0.78) — model prefers slimmer MLP but residual-stream LN absorbs the shrinkage. (4) CRUISE WIN BROKEN — 4th post-#2964 inversion (#2961 aux, #2978 AoA, #2986 AdamW, #2988 scalar γ); cruise WIN structurally fragile to ANY residual-stream perturbation. Assigned #3006 alphonse cross-block-residual-α (FRESH STRUCTURALLY-ORTHOGONAL AXIS — learnable scalar α between adjacent TransolverBlocks scaling whole-block output AFTER post-norm; +3 params; tests BETWEEN-BLOCK adaptivity site — structurally distinct from now-closed in-block γ; 148th axis; per student closure recommendation #3 verbatim). Total closed: 147. Winners: 22. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-24]: Closed #2983 edward qk-norm-physics-attention CATASTROPHIC LOSS (val 36.1119 +20.24% vs NEW baseline; test 31.4261 +24.66%; routing logit/T abs.max 71 @ep1 → 774 @ep43 — 47-516× explosion; 17/24 dead slices in block-0). 146th taxon: STRUCTURAL-INCOMPATIBILITY-OF-QK-NORM-WITH-SLICE-ROUTING-PARAMETERIZATION. DECISIVE MECHANISTIC FINDINGS (per student write-up — exceptional): (1) Standard QK-norm assumes Q,K are symmetric per-token projections; in Physics_Attention, Q is per-token but K is GLOBAL SLICE BASIS (orthogonal-init at ~1.0 magnitude). LN forces both to √dim_head=6.93. (2) ‖Q‖ DEPTH-GROWTH PATTERN IS LOAD-BEARING — pre-LN: block-0 ‖Q‖_raw=0.036 vs block-3 ‖Q‖_raw=1.4 (40× depth growth); stripping it via LN is the broken component. (3) ROUTING LOGITS EXPLODE 30-10000×; T saturates at 0.07 trying to compensate; softmax saturated; effective slice_num collapses to ~7. (4) K-DRIFT WAS INFORMATIVE — K magnitudes drift non-monotonically during training (1.0 init → 0.14 ep1 → 2.62 ep43); LN(K) fixes this and loses signal. (5) Q-side LN is the broken component; K-only LN is a structurally distinct revival hypothesis (preserves Q signal, normalizes only static K basis). Assigned #3008 edward k-only-ln-physics-attention (REVIVAL HYPOTHESIS — LN ONLY K not Q; preserves Q's depth-growth signal while gaining K-side stability; +384 params half of #2983; 149th axis; per student closure recommendation #2 verbatim). Total closed: 146. Winners: 22. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-23]: Closed #2977 nezuko per-channel-learnable-gamma-post-norm LOSS (val 30.0480 +0.03% vs NEW baseline — mild but consistent with test +1.65%; test 25.6257 +1.65%; in_dist VAL/TEST SIGN-FLIP: val_in_dist -6.40% WIN, test_in_dist +3.04% LOSS — overfit-of-flexibility signature). 145th taxon: γ-ADAPTIVITY-AT-PER-CHANNEL-GRANULARITY-IS-WASH-WITH-OVERFIT-RISK. DECISIVE FINDINGS: (1) γ-LEARNABILITY PROVIDES NO HEADROOM AT FINEST GRANULARITY — val +0.03% within noise floor, test +1.65% LOSS = slight overfit-of-flexibility. (2) VAL/TEST SIGN-FLIP ON in_dist (-6.40% val WIN / +3.04% test LOSS) — 768 learnable γ params fit val_in_dist structure but generalize worse to test_in_dist; classic bias-variance crossover from added degrees of freedom. (3) γ drift small from 1.0 (mean 0.82-0.90 range with per-channel std) — per-channel granularity dampens average shrinkage via channel-by-channel cancellation. (4) Combined with #2988 scalar γ (decisive LOSS), the 3-point γ-adaptivity sweep closes the γ axis at post-norm: {0 (fixed) WIN > 8 (scalar) LOSS, 768 (per-channel) WASH-with-overfit}. Assigned #3009 nezuko squared-relu-mlp (FRESH ACTIVATION-FUNCTION AXIS — replaces nn.GELU in MLP branch with Primer's squared ReLU `F.relu(x).pow(2)`; +0 params; PaLM-2 / DeepSeek-V3 recipe at scale; pivots from γ-axis to activation-function axis; 150th axis; per student closure recommendation #1 verbatim). Total closed: 145. Winners: 22. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-22]: Closed #2986 thorfinn adamw-lr-1.5e-3 LOSS (val 35.6060 +18.54% vs NEW baseline; test 30.3794 +20.51%; cruise +31.30% LOSS — META-SIGNAL DECISIVELY INVERTED 3rd time post-#2964). 144th taxon: OPTIMIZER-FAMILY-AXIS-DECISIVELY-CLOSED-AT-60EP-BUDGET. DECISIVE FINDINGS: (1) Bracketing succeeded — moving from 3.3× lr (#2974) → 10× lr (#2986) moved val 14.3% and train 20.3%, but train @ 10× still plateaus at 1.02 vs Lion's 0.8 at ep60; no interior lr in [3.3×, 10×] can match Lion in 60ep. (2) Train→val gap 1.02× = TIGHTEST UNDERFIT SIGNATURE — even at 4.5× #2974's lr, AdamW never exits underfit regime in 60ep. (3) CRUISE INVERTED at +31.30% LOSS (post-#2964 cruise breakages: #2961 aux-supervision, #2978 AoA noise, #2986 AdamW@10× — cruise is now the load-bearing CANARY for basin quality). (4) Stability headroom confirmed at 10× — ep1-3 clean monotonic, no NaN/spikes; AdamW could run at 20×+ without divergence but no lr salvages axis. (5) Optimizer state healthy (99.99% exp_avg non-zero, 100% exp_avg_sq) — not a pathology. (6) STRUCTURAL FINDING: **Lion's sign-step is load-bearing for 407k-param + 1499-sample + 60ep regime.** Lion's per-element sign(grad) produces a basin (especially cruise basin) that AdamW's adaptive denominator cannot find in budget envelope. Two converging signatures: train→val gap stuck at underfit AND cruise unreachable. Per student recommendations #2,#5 verbatim: "Optimizer-family axis is decisively closed for the 60-epoch budget" and "Do not retest AdamW family in this round." Assigned #3003 thorfinn sandwich-norm-magneto (FRESH STRUCTURAL AXIS — Cogview/Magneto Sub-LN recipe; pre-norm at branch INPUT + post-norm at branch OUTPUT in TransolverBlock, both retained; +1536 params; compatible with #2964 post-norm WIN; tests whether BOTH normalizations are load-bearing or post-norm alone is sufficient; PIVOTS OFF optimizer-family axis; 145th axis; first sandwich-norm experiment in launch). Total closed: 144. Winners: 22. New baseline: val 30.0382 / test 25.2099. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-21]: Closed #2976 fern coord-noise-sigma-0.01 LOSS (val 32.4882 +6.31% vs NEW baseline; in_dist WORST-HIT +14.11% — direction-INVERTED from hypothesis; closure timestamp 18:56:14 UTC by parallel advisor session morganmcg1). 143rd taxon: POSITIONAL-CHANNEL DATA-AUG AT σ=0.01 / POSITIONAL-PRECISION-IS-LOAD-BEARING-FOR-in_dist. DECISIVE FINDINGS: (1) Predicted-most-helped split (in_dist over-memorization hypothesis) was WORST-hit; positional precision is structurally load-bearing for attention slicing — iid noise destroys neighbor relationships at training that vanish at eval. (2) Train→val gap stayed small (Δsurf=0.024) — model isn't overfitting, it's learning a systematically worse mapping (representation degrades during noise-training and that degraded representation generalizes consistently). (3) σ=0.01 (1% of unit-std normalized coords) was too large; mesh-coord noise should be smooth/spatially-correlated (GP-style displacement) not iid. (4) Canonical meta-signal does NOT repeat on val — every val split LOSS, in_dist worst; cruise WIN absent. Only weak positive signal: test camber_rc -1.65% (not corroborated by val). **DATA-AUGMENTATION AXIS NOW COMPREHENSIVELY CLOSED** — triplet edward #2973 log_Re-noise (CATASTROPHIC LOSS) + frieren #2978 AoA-noise (LOSS) + fern #2976 coord-noise (LOSS) all 3 orthogonal input-channel data-aug interventions LOSS at tested magnitudes. LOAD-BEARING CLOSURE RULE empirically verified across all 3 input channel classes. Reassignment was done DURING this session by parallel advisor morganmcg1: #2993 fern lion-wd-1e-3 (Lion weight-decay upper bracket; 144th axis; pivots off data-axis per student suggestion #4 verbatim "drop positional-channel axis entirely; pivot to mask/feature-channel data-aug"). Total closed: 142. Winners: 22. New baseline: val 30.0382 / test 25.2099. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-20]: Closed #2978 frieren aoa-input-noise LOSS (val 30.9589 +3.07% vs NEW baseline; cruise META-SIGNAL SIGN-FLIPPED +7.46% LOSS vs -9.00% WIN baseline; closure timestamp 18:56:07 UTC by parallel advisor session morganmcg1). 142nd taxon: AOA-CONDITIONING-NOISE-AT-σ=0.05 / CONDITIONING-PATHWAY-IS-LOAD-BEARING / CRUISE-WIN-IS-BRITTLE. DECISIVE MECHANISTIC FINDINGS: (1) **Cruise meta-signal sign-flipped** — second cruise-breakage post-#2964 (alphonse #2961 aux-supervision was first). **Cruise WIN from #2964 is BRITTLE to conditioning-pathway perturbations**, not just structural interventions. (2) Contradictory-AoA failure mode: per-node raw features were noised but FiLM 3-vector `[log_Re, AoA0_rad, AoA1_rad]` was NOT noised — model saw CONTRADICTORY AoA across sites (sharper signal than uniform noise). Per-sample AoA std on ch14 is 0.86 normalized; σ=0.05 is 5.8% of inter-sample variance — large in distribution terms despite tiny physical σ (~0.21-0.28°). (3) AoA is LOAD-BEARING CONDITIONING SIGNAL: all val splits regressed, test ~2.4× worse than val (+7.24% vs +3.07%) — cruise breakage propagates to in_dist on test (+9.67%). (4) Train→val gap maintained — regularization-shaped failure not capacity-shaped. (5) Cruise meta-signal is now a BRITTLENESS PROBE — future hypotheses touching FiLM/conditioning/residual pathway should predict cruise direction; sign-flip = conditioning-pathway disruption. Reassignment was done DURING this session by parallel advisor morganmcg1: #2994 frieren lion-lr-1e-4 (Lion lr 1.5e-4 → 1e-4 with intact warmup+cosine; 145th axis; pivots off conditioning-channel axis per student suggestion #4 verbatim "stop probing conditioning-channel noise on this backbone"). Total closed: 141 (becomes 142 after close-21). Winners: 22. New baseline: val 30.0382 / test 25.2099.

- 2026-05-14 [Round 138 close-19]: Closed #2975 alphonse volume-aux-at-block-1 LOSS (val 32.30 +7.52% vs NEW baseline; ALL 4 splits LOSS). 141st taxon: AUX-SUPERVISION AXIS COMPREHENSIVELY CLOSED — TARGET-AND-DEPTH-INVARIANT-ORTHOGONALITY. DECISIVE MECHANISTIC FINDINGS: (1) Orthogonality hypothesis DECISIVELY FALSIFIED — aux/vol ratio stabilized ~1.8× (predicted 0.5-1.0× alignment regime); aux is solving a HARDER problem than primary vol regression on same target tokens. (2) ||W_aux||/||W_primary|| = 2.18× IDENTICAL to #2952's 2.20× across BOTH minority (surf ~30%) and majority (vol ~70%) target populations. (3) Cosine similarities STATISTICALLY ~0 on ALL 3 channels regardless of target. (4) Across the trio #2952/#2961/#2975: ratios consistently 2.08-2.20×, cos sims always ~0, all LOSS. Orthogonality is the STRUCTURAL INVARIANT, not target-driven, not depth-driven. (5) Three plausible deeper causes (none directly testable here): information-theoretic limit on mid-feature linear-mappability, optimization conflict between aux+primary gradients, capacity competition forcing block-1 to serve dual purposes. (6) CRUISE WIN of #2952 was BUFFER-DEPENDENT (post-aux block smoothing), not aux mechanism — confirmed broken under target inversion. Assigned #2988 alphonse per-block-scalar-gamma-init-1.0 (PIVOT off aux axis per student suggestion #5; completes 3-POINT ADAPTIVITY-GRANULARITY SWEEP — #2964 constant γ=1.0 WIN + #2988 per-block scalar γ + #2977 per-channel γ; +8 params; tests minimum-adaptivity hypothesis; if both #2988 and #2977 LOSS → fixed γ=1.0 decisively optimum; if only one WINS → resolves granularity load-bearing question; 143rd axis). Total closed: 140. Winners: 22. New baseline: val 30.0382 / test 25.2099. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-18]: Closed #2974 thorfinn adamw-optimizer-swap (lr=5e-4) LOSS (val 41.5479 +38.32% vs NEW baseline; ALL splits LOSS; uniform regression because of severe underfitting — train loss 1.28 at ep60 vs Lion ~0.8 at ep58). 140th taxon PARTIAL closure: AdamW @ lr=5e-4 LOW-LR ENDPOINT — optimizer-family axis NOT yet closed. Mechanism: AdamW STABLE (monotonic, no NaN, optimizer state genuinely active — exp_avg 100% nonzero, exp_avg_sq 99.82% nonzero) but lr=5e-4 (3.3× Lion's 1.5e-4) UNDERCALIBRATED per conservative Lion→AdamW conversion (needs 5-10× factor). Tight train→val gap 1.18× is underfitting signature not better generalization. Meta-signal UNOBSERVABLE: model never reached near-convergence regime where it manifests; uniform regression dominates asymmetric signal. Per student insight: 'this experiment cannot resolve whether meta-signal is optimizer-specific because the model never converged.' BRACKETING STRATEGY: assigned upper-lr endpoint test (#2986 adamw-lr-1.5e-3 = 10× Lion lr) — if also LOSS, axis decisively closed in [3.3×, 10×] interval; if WIN, axis salvaged. CRITICAL ep1-3 stability check required since AdamW more likely to spike at 10× lr. Assigned #2986 thorfinn adamw-lr-1.5e-3 (Round 138 — bracketing optimizer-family axis upper endpoint; 142nd axis position; wd=0.01 betas unchanged from #2974). Total closed: 139 (partial — 140 will be decisive). Winners: 22. New baseline: val 30.0382 / test 25.2099. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-17]: Closed #2973 edward log_Re-input-noise CATASTROPHIC LOSS (val 40.21 +33.84% vs NEW baseline; ALL 8 split×set cells LOSS; in_dist WORST-HIT +57.18%; mechanism INVERTED — predicted-most-improved suffered most). 139th taxon: CONDITIONING-VARIABLE-NOISE AXIS CLOSED + DATA-AXIS META-SIGNAL INVARIANCE FINDING. DECISIVE FINDINGS: (1) HYPOTHESIS FALSIFIED BY DIRECTIONAL INVERSION — in_dist predicted MOST-improved as over-specialized split, became WORST-hit. (2) log_Re is a LOAD-BEARING CONDITIONING IDENTIFIER, not a tunable regularization knob; coherent per-sample noise corrupts every Re-conditioned pathway (FiLM, slice-routing, conditioning) in lockstep, compounding through 4-block depth. Effective σ=0.066 in normalized space → O(30%) effective perturbation matching observed regression. (3) **3 DATA-AXIS INTERVENTIONS LOSS IN A ROW** (#2918 mixup, routing perturbations #2884/#2958, #2973 log_Re noise) — meta-signal NOT addressable by data perturbation; lives in representational geometry from clean inputs. (4) **LOAD-BEARING CLOSURE RULE for remainder of launch:** stop probing data/input-perturbation axes. Caveat: #2978 frieren AoA noise and #2976 fern coord noise still in-flight (different channels) — let run to terminal, then close data-perturbation cluster comprehensively. Assigned #2983 edward qk-norm-physics-attention (FRESH AXIS — representation-level normalization per student insight #5; nn.LayerNorm(dim_head) on Q and K in Physics_Attention BEFORE routing dot-product; Llama-3 / MAGNETO recipe; decouples routing from magnitude; compatible with #2964 post-norm WIN; +768 params; ~5 lines code; 141st axis). Total closed: 139. Winners: 22. New baseline: val 30.0382 / test 25.2099. **8 students all busy, zero idle GPUs.**

- 2026-05-14 [Round 138 close-16]: Closed #2968 askeladd mlp2-differential-wd-10x LOSS (val ~+3.62%; 138th taxon: HEAD-AXIS COMPREHENSIVELY CLOSED). 10× wd on mlp2 insufficient: ||W_mlp2|| grew 3.7× despite penalty; Lion overwhelmed the regularization. Even mild constraint disrupts co-adaptation. Third structural head-axis intervention (after #2946 split +4.37%, #2956 correction +3.29%, #2968 differential-wd +3.62%) all LOSS in consistent +3-4% range. Co-adaptation pressure is not just load-bearing — it is the mechanism; mlp2 needs freedom to learn jointly from BOTH surface and volume gradients. Assigned #2980 askeladd in-block-dropout-p0.1-post-norm (COMPLETELY FRESH AXIS — body-side stochastic regularization; Vaswani 2017 full recipe test; dropout p=0.1 after attention and MLP branches BEFORE residual add in post-norm topology; zero new params; ~3 lines code; tests whether standard Transformer regularization complements #2964 post-norm WIN; 140th axis). Total closed: 138. Winners: 22. New baseline: val 30.0382 / test 25.2099. **8 students all busy, zero idle.**

- 2026-05-14 [Round 138 close-15]: Closed #2969 tanjiro per-block-T-schedule LOSS (val ~+4.52%; 137th taxon: FIXED-T-SCHEDULE CLOSED / LEARNABILITY IS LOAD-BEARING). Hardcoded T=[1.0,0.5,0.5,0.25] → block-0 STILL collapsed (16-17/24 dead slices vs baseline ~3-4). Projection weights co-evolved with learnable T — freezing T breaks the co-adapted system. T values inferred from baseline entropy diagnostics are a POST-HOC description of a co-adapted system, not a transferable prior. WORSE than either T=2.0 (+4.43%) or T=0.25 (+3.40%) — schedule of fixed temps worse than single fixed temp. Only learnability (T as nn.Parameter) or LEARNABILITY WITH BETTER INIT remains. Assigned #2979 tanjiro learnable-T-init-schedule (T remains learnable nn.Parameter; initialized at [1.0,0.5,0.5,0.25] instead of uniform 0.5; schedule-as-PRIOR not constraint; student suggestion #1 from #2969; key diagnostic: does T_block-0 STAY near 1.0 (prior is useful) or drift to 0.5 (optimizer overrides init)?; 139th axis). Total closed: 138. Winners: 22. New baseline: val 30.0382 / test 25.2099.

- 2026-05-14 [Round 138 close-14]: Closed #2957 frieren warmup-5-epochs-baseline-lr LOSS (val 32.1100 +5.07% Run2 / val 31.2498 +2.25% Run1; in_dist +6.82% LOSS (most reproducible); LR-budget contraction -2.89% confirmed). 136th taxon: LR-SCHEDULE-SHAPE AXIS DEFINITIVELY CLOSED — THREE-SIDED OPTIMUM. 5 experiments mapped the axis from no-warmup → 5ep-warmup; 3-ep warmup @ 1.5e-4 is the two-sided global optimum. Per student's explicit recommendation: stop probing LR-schedule-shape. Run-to-run variance quantified: same config → val 31.25 vs 32.11 (2.75% spread); direction (LOSS) reproducible; cruise is the noisiest split; in_dist is the most reproducible thermometer. Assigned #2978 frieren aoa-input-noise (FRESH AXIS — AoA conditioning-variable data augmentation; σ=0.05 on normalized AoA features x[:,:,14]+x[:,:,18] during training only; COMPLETES DATA-AUG TRIPLET alongside #2973 edward log_Re-noise and #2976 fern coord-noise — three orthogonal input-channel axes; zero new params; ~5 lines; 138th axis). Total closed: 136. Winners: 22. New baseline: val 30.0382 / test 25.2099.

- 2026-05-14 [Round 138 close-13]: **MERGED #2964 nezuko post-norm-gamma-1.0 WIN (val 30.0382 −1.71% / test 25.2099 −4.93%; 22nd winner; NEW BASELINE). CRITICAL MECHANISTIC FINDING: LayerScale γ=1e-4 was the confound in #2951 post-norm LOSS (+14.73%). Post-norm + γ=1.0 (Vaswani 2017 recipe) beats pre-norm + γ=1e-4. Topology and LayerScale init form COMPATIBLE PAIRS: pre-norm pairs with tiny-γ, post-norm pairs with γ=1.0. #2951 used the WRONG pairing. CRUISE WIN EXTENDED (-9.0%, meta-signal RESTORED). TEST improves more than val (-4.93% vs -1.71%) → strong generalization benefit. -768 params (LayerScale γ vectors removed). Best epoch = 60 (terminal) → room for more training at post-norm recipe. Assigned #2977 nezuko learnable-gamma-init-1.0 (tests whether adaptivity on top of γ=1.0 provides further gains: nn.Parameter(torch.ones(n_hidden)) per branch vs constant 1.0; per student suggestion #2; +768 params restoring count; key diagnostic: does γ drift from init=1.0 vs stay near 1.0?; 137th axis). Total closed: 135 → 136 (warmup closure). Winners: 21 → 22.** New baseline: val 30.0382 / test 25.2099.

- 2026-05-14 [Round 138 close-12]: Closed #2931 fern cosine-eta-min-1e-6 STALE_WIP (procedural closure, NOT a verdict on eta_min). PR assigned 2026-05-14 13:57 UTC (Round 133); ADVISOR heartbeat 16:02 UTC; no response by 17:50 UTC (~4hr stale total, ~1.5hr after heartbeat). Branch contains only empty assignment commit. SECOND stale_wip closure today (first was #2924 thorfinn at same time). eta_min axis remains UNRESOLVED — could be revived later if current LR investigations (#2957 warmup duration, #2974 optimizer family) suggest LR-floor mechanisms matter. Comment URL: https://github.com/morganmcg1/TandemFoilSet-Balanced/pull/2931#issuecomment-4453262825. Assigned #2976 fern coord-noise-sigma-0.01 (FRESH AXIS — POSITIONAL CHANNEL data augmentation; Gaussian noise σ=0.01 on mesh coords (pos_x, pos_y) during training only; FIRST positional-channel data-aug experiment in this launch; distinct from edward's #2973 log_Re-INPUT-noise (conditioning channel — orthogonal feature dimension); directly targets geometric OOD splits camber_rc/camber_cruise; per alphonse #2961 student insight #4 verbatim "data-distribution probe rather than another model intervention"; zero new params; ~5 lines code; 137th axis). Total closed: 135 (no new LOSS taxon — #2931 procedural). Winners: 21. **8 students all busy, zero idle.**

- 2026-05-14 [Round 138 close-11]: Closed #2961 alphonse aux-supervision-at-block-3 LOSS (val 32.5552 +6.53% / test 27.5597 +3.94%; in_dist WORST +12.82% — 2.55× worse than #2952; **cruise WIN BROKEN +3.14% LOSS — sign-flipped vs #2952's -3.16% WIN**). 135th taxon: DEPTH-AXIS DEEP-SUPERVISION DECISIVELY CLOSED — MONOTONIC WORSENING WITH DEPTH. DECISIVE MECHANISTIC FINDINGS: (1) Aux/surf ratio prediction PARTIALLY CONFIRMED — ratio dropped 3.72× (50%) → 2.48× (75%) but still 2.5× (not predicted ~1.0-1.5×). Block-3 features ARE somewhat more linearly mappable than block-1 BUT not enough to be compatible. (2) Weight divergence pattern UNCHANGED with depth: ||W_aux||/||W_primary||=2.08 (75%) vs 2.22 (50%); cos sim on p channel ~ 0 regardless of placement. Aux head learns near-orthogonal projection on the metric channel REGARDLESS of placement. (3) **CANONICAL META-SIGNAL DESTROYED** — cruise WIN was BUFFER-DEPENDENT; 50% had 2 untouched post-aux blocks to smooth over aux pull; 75% has only 1 block of buffer; cruise WIN flips to LOSS, all splits regress. Refines meta-signal interpretation: cruise-WIN under structural interventions requires POST-INTERVENTION blocks remaining. (4) in_dist intensifies with depth +5.02% → +12.82% (2.55× worse); late-stage feature distortion is the in_dist regression DRIVER. (5) DEPTH-AXIS MONOTONIC: 0% baseline 30.5605 → 50% +1.89% → 75% +6.53% strictly worse with depth. (6) Five structural surface-targeting interventions ALL LOSS: #2933, #2946, #2952, #2956, #2961. **The "surface-pathway capacity" hypothesis is DEAD at this scale/budget.** Student's headline insight: "The meta-signal lives somewhere we haven't probed — three structural interventions all reproduce or worsen the in_dist regression. Suggests it's not a surface-level architectural lever but something about data-balance." Assigned #2975 alphonse volume-aux-at-block-1 (DIRECT TARGET INVERSION of #2952: SAME block-1 placement, SAME α=0.1, SAME +291 params; aux predicts VOLUME field instead of surface; tests orthogonality interpretation via student suggestion #1 verbatim; if aux ALIGNS with dominant vol gradient direction predicted: aux/vol ratio ~0.5-1.0× (NOT 2.5-3.7×), high cos sim with primary, either WIN or wash; if LOSS too → aux mechanism fundamentally broken regardless of target → axis comprehensively closed across BOTH target families; 136th axis). Total closed: 135. Winners: 21.

- 2026-05-14 [Round 138 close-10]: Closed #2924 thorfinn sam-rho-0.05 STALE_WIP (procedural closure, NOT a verdict). PR assigned 2026-05-14 13:22 UTC, heartbeat 15:28 UTC indicated WIP, no response by 17:30 UTC (~4 hours stale). Only an empty assignment commit on the branch — no implementation, no run, no metrics. Per zero-idle-GPU principle, the slot is unblocked for a fresh assignment. Flatness-wrapper axis (SAM, SWA, Lookahead-style flatness regularizers other than #2922 Lookahead-Lion which CLOSED LOSS) remains UNEXPLORED in this launch. Comment URL: https://github.com/morganmcg1/TandemFoilSet-Balanced/pull/2924#issuecomment-4453166893. Assigned #2974 thorfinn adamw-optimizer-swap (FRESH AXIS — OPTIMIZER FAMILY; replaces #2924 stale; first non-Lion optimizer in 134 closed taxa; tests whether Lion's sign-step update vs AdamW's adaptive denominator is responsible for the meta-signal; the single LARGEST unexplored mechanism in this launch; lr=5e-4 wd=0.01 betas=(0.9,0.999) — 3.3× Lion's lr per the conservative Lion→AdamW conversion factor; zero new params; ~5 lines code; **CRITICAL: meta-signal check under AdamW** — if cruise WIN / in_dist LOSS pattern REPEATS → meta-signal is optimizer-invariant lives at data/architecture level; if INVERTS or DISAPPEARS → meta-signal is Lion-specific (huge finding); 135th axis). Total closed: 134 (no new LOSS taxon — #2924 is procedural). Winners: 21. Current baseline: val 30.5605.

- 2026-05-14 [Round 138 close-9]: Closed #2958 edward routing-weight-dropout-0.1 LOSS (val 32.2440 +5.51% / test 27.2092 +2.61%; cruise WIN -3.07%; in_dist +11.51% WORST). 134th taxon: ROUTING-WEIGHT-DROPOUT-0.1 / STOCHASTIC-ROUTING-REGULARIZATION / ROUTING-SOFTNESS-AXIS-DECISIVELY-CLOSED. DECISIVE MECHANISTIC FINDINGS: (1) Eval-mode block-3 entropy SHIFTED FROM SHARP TO SOFT: baseline 0.01-0.92 nats → #2958 2.145 nats (frac 0.675). Same direction as #2944 T=2.0 (2.34-2.91 nats), milder magnitude. (2) **Counter-intuitive train vs eval entropy** — train slightly LOWER than eval (-0.002 to -0.106 nats). Mechanism: dropout+renorm REDISTRIBUTES mass to survivors after zeroing; when non-dominant slice zeroed, dominant slice GROWS → entropy DECREASES. Dropout+renorm is NOT entropy-increasing — it's a relative-magnitude sharpener during train, but stochastic per-batch perturbations prevent CONVERGENCE to maximally-sharp routing at eval. (3) Dropout-at-eval failure mode: dropout is identity at eval but trained WEIGHTS can't achieve sharp routing — softer routing was the robust equilibrium. (4) Meta-signal repeats at #2944's magnitude. **Two INDEPENDENT mechanisms (constant T softening + stochastic routing) → same pattern → "sharp block-3 is load-bearing" is the FUNDAMENTAL invariant.** (5) **ROUTING-SOFTNESS / STOCHASTICITY AXIS DECISIVELY CLOSED — 5 LOSS closures** (#2884 entropy-reg, #2923 slice_num=32, #2934 slice_num=16, #2944 T=2.0, #2955 T=0.25, #2958 dropout-0.1). Only LEARNABILITY survives. Assigned #2973 edward log-re-input-noise (FRESH AXIS, completely orthogonal: Gaussian noise σ=0.05 on log_Re INPUT feature during training only; conditioning-variable data augmentation targeting in_dist over-specialization at the conditioning level; per student suggestion #4 "drop axis entirely"; zero new params; ~3 lines code; 135th axis). Total closed: 134. Winners: 21.

- 2026-05-14 [Round 138 close-8]: Closed #2955 tanjiro slice-routing-temperature-0.25 LOSS (val 31.5998 +3.40% / test 26.8651 +1.32%; cruise WIN -1.88%; in_dist +5.38% LOSS hardest). 133rd taxon: TEMPERATURE AXIS U-SHAPED / LEARNABILITY-IS-LOAD-BEARING. DECISIVE MECHANISTIC FINDINGS: (1) Failure mode is BLOCK-0 starvation NOT block-3 — under T=0.25 block-0 entropy collapses to 0.28-0.41 nats with **13-18 of 24 slices dead (<1% routing weight)**. Block-3 entropy (0.50-0.62) sits INSIDE baseline range (0.01-0.92) — T=0.25 didn't push block-3 sharper than baseline's converged values. (2) Per-head learnable temperatures encode a **PER-DEPTH SCHEDULE**: soft routing shallow (T~1.0), sharp deep (T~0.25). 8 learnable params save >4% — **0.002% of param budget for 4% gain.** (3) U-shaped axis: T=2.0 +4.43% LOSS (block-3 too soft), baseline learnable OPTIMUM, T=0.25 +3.40% LOSS (block-0 collapse). (4) Meta-signal repeats under SHARPER routing — now confirmed under BOTH directions of temperature axis. (5) Routing-axis cluster: 5 closures (slice_num both, T both, entropy-reg) all LOSS. Only LEARNABILITY survives. Assigned #2969 tanjiro per-block-temperature-schedule (hardcoded T=[1.0, 0.5, 0.5, 0.25] matching implicit learned per-depth schedule; SCHEDULE-vs-LEARNABILITY attribution test; student suggestion #2 verbatim; -8 params; if WIN schedule alone suffices, if LOSS learnability itself load-bearing; 134th axis). Total closed: 133. Winners: 21.

- 2026-05-14 [Round 138 close-7]: Closed #2956 askeladd asymmetric-surf-correction-head LOSS (val 31.5658 +3.29% / test 27.3282 +3.06%; in_dist WORST hit at +8.90%, re_rand +4.66% LOSS, rc/cruise wash). 132nd taxon: ASYMMETRIC-SURF-CORRECTION / SHARED-HEAD-CO-ADAPTATION-IS-LOAD-BEARING. DECISIVE FINDINGS: (1) Correction GREW correctly (||W_corr|| 0→0.40, strongest p channel 0.31). (2) Correction was ACTIVELY USED — mean(|Δp|)=0.20 on val_single_in_dist. (3) YET metrics REGRESSED +3.29% — all pre-conditions met AND val regressed. (4) **CO-ADAPTATION DRIFT MECHANISM**: shared `mlp2` co-adapts to surface+vol jointly; adding ANY surface-only knob breaks this; gradient flow rebalances, `mlp2` surface-tuning relaxes (correction does that), `mlp2` drifts toward vol-friendly weights — same "vol free-ride" failure mode as #2946 head-split, mediated through mlp2 drift instead of separate head. **The implicit regularizer is CO-ADAPTATION PRESSURE between surf+vol gradients on SAME params, not just parameter sharing.** (5) Meta-signal ATTENUATED but present — gentler version of #2946 head-split. (6) **HEAD-AXIS DEFINITIVELY CLOSED** — three structural head-axis attempts (prior split, #2946, #2956) ALL LOSS. Bottleneck NOT in the head; must be in body, loss landscape, or gradient flow. Assigned #2968 askeladd mlp2-differential-wd-10x (differential weight decay: 10× wd on mlp2 only, baseline wd elsewhere; tests co-adaptation preservation by pinning mlp2 closer to small-norm; student suggestion #2 verbatim; zero new params; 133rd axis). Total closed: 132. Winners: 21.

- 2026-05-14 [Round 138 close-6]: Closed #2951 nezuko post-norm-topology DECISIVE LOSS (val 35.0624 +14.73% / test 30.0107 +13.18%; cruise WORST-HIT at +25.03% val / +30.74% test). 131st taxon: POST-NORM TOPOLOGY AT ALL 9 LN SITES / PRE-NORM-IS-LOAD-BEARING-FOR-THIS-RECIPE. KEY MECHANISTIC FINDINGS: (1) Post-norm MECHANICALLY worked — residual magnitudes capped at ~1.0 after every LN site as expected (no NaN/Inf, code correct). (2) But BROKE the entire recipe: pre-norm + γ=1e-4 deliberately keeps residual stream "wide" early (`x = x + γ·attn(LN(x))` with tiny γ means each block contributes tiny perturbation to near-identity stream, allowing slow useful-signal build-up). Post-norm with γ=1e-4 cancels this dynamic: `x = LN(x + γ·attn(x))` renormalizes after every block, washing out LayerScale-controlled slow build-up. (3) **CANONICAL META-SIGNAL BROKE** — FIRST intervention where cruise is WORST-hit split rather than WIN split (cruise +25.03% val / +30.74% test = INVERSE of recurring meta-signal). Post-norm doesn't just hurt — it breaks the cruise-favoring representational specialization. (4) Refines #2939 finding: LN's mean-centering is load-bearing AT BLOCK INPUTS (pre-norm), NOT at block sums (post-norm). Pre-norm topology is structurally load-bearing for the Transolver recipe at 407k-param scale. (5) 8TH uniform-LOSS closure under global reshaping — all global structural reshapings produce uniform LOSS across all splits; local interventions reproduce canonical meta-signal; global ones don't. Assigned #2964 nezuko post-norm-gamma-1.0 (TOPOLOGY-vs-LAYERSCALE attribution: re-test post-norm with γ_attn=γ_mlp=1.0 instead of γ=1e-4 to isolate topology from LayerScale init scheme; student suggestion #2 verbatim; -768 params; if γ=1.0 recovers most loss → LayerScale init was confound; if still LOSS → topology itself is load-bearing; 132nd axis). Total closed: 131. Winners: 21.

- 2026-05-14 [Round 138 close-5]: Closed #2952 alphonse aux-mid-block-surf-loss LOSS (val 31.1363 +1.89% / test 26.1821 -1.26% slight WIN; cruise -3.16% WIN, in_dist +5.02% LOSS hardest, rc/re_rand uniform LOSS). 130th taxon: AUX-MID-BLOCK AT BLOCK-1 (50% DEPTH) / DEEP-SUPERVISION-MODERATE-WEIGHT-CLOSED. KEY MECHANISTIC FINDINGS: (1) Aux head learned FUNDAMENTALLY DIFFERENT projection — NOT redundant (||W_aux||=3.33 vs ||W_primary||=1.50, cos similarity 0.05 on metric p channel). (2) Aux/surf ratio CONSISTENTLY 2.5-3.7× throughout training → mid-block features NOT linearly mappable to surface output (mid features have different job, forcing them toward surface output creates representational conflict). (3) Tiny train→val gap (~0.022) confirms UNDERFIT regime at 60ep; aux steals gradient from primary task in convergence-limited regime. **MAJOR META-INSIGHT: THREE STRUCTURAL/LOSS-RESHAPING INTERVENTIONS IN A ROW REPRODUCE CANONICAL META-SIGNAL** (#2933 global reshape, #2946 head-axis split, #2952 depth-axis aux). The cruise↔in_dist tradeoff lives at a DEEPER, intervention-agnostic representational level than per-region or per-depth surface specialization. None of these surface-level levers escape it. Assigned #2961 alphonse aux-supervision-at-block-3 (deeper placement at 75% depth; student suggestion #2; same +291 params; tests representational compatibility hypothesis — predicted aux/surf ratio ~1.0-1.5× vs #2952's 3.5×; 131st axis). Total closed: 130. Winners: 21.

- 2026-05-14 [Round 138 close-4]: Closed #2949 edward rmsnorm-plus-beta PARTIAL→LOSS (val 32.2115 +5.40% / test 26.9359 +1.58% LOSS; recovery 25.8% on val / 78.7% on test of #2939's RMSNorm-only loss). 129th taxon: NORM-SHAPE AXIS DECISIVELY CLOSED. DISAMBIGUATION: β bias DID learn non-trivial values (|β|_max 0.22, deepest block biggest) but mean-subtraction is the 74% load-bearing piece for VAL splits, while β is the 79% load-bearing piece for TEST splits. MECHANISTIC: β absorbs STATIONARY cross-split DC (per-channel constant); mean-subtraction handles PER-SAMPLE/BATCH DC (Re/AoA-conditioned pressure DC offsets in CFD fields). Both components are independently load-bearing; β alone CANNOT replace mean-subtraction at 407k param budget. KEEP LayerNorm — no further normalization-shape experiments at this scale. Assigned #2958 edward routing-weight-dropout-0.1 (NEW REPRESENTATION-axis: nn.Dropout(p=0.1) on slice_weights AFTER softmax, re-normalized; STOCHASTIC routing regularization — different mechanism from temperature softening #2944 LOSS and entropy regularization #2884 LOSS; preserves sharp eval-time routing #2944 confirmed is desirable; targets in_dist over-specialization; standard Transformer recipe; zero new params; 130th axis). Total closed: 129. Winners: 21.

- 2026-05-14 [Round 138 close-3]: Closed #2950 frieren lr-2e-4-with-warmup WASH-LOSS (val 30.7053 +0.47% LOSS; test 26.1866 -1.24% WIN; sign-divergence on in_dist/re_rand; consistent val+test WIN on camber_rc -2.69%/-1.49%; consistent val+test LOSS on camber_cruise +1.76%/+2.52%). 128th taxon: UPWARD LR-MAGNITUDE WITH WARMUP — FLATNESS-CLOSED. KEY FINDINGS: (1) Lion STABLE at lr=2.0e-4 (no NaN/Inf, monotonic descent) — predicted DIVERGE branch ruled out. Baseline lr=1.5e-4 is at LR-magnitude optimum for primary metric. (2) Symmetric closure of LR-magnitude axis: downward direction (#2938 lr=1.0e-4 +13.50%) was confounded by no-warmup; upward direction (this PR lr=2.0e-4 +0.47%) was clean — axis is asymmetric in steepness, baseline sits at flat peak. (3) Camber_rc consistently WIN across val AND test (capacity-limited OOD split, most LR-sensitive direction). (4) NOT canonical meta-signal — higher LR is uniform global re-shape, not capacity-shift; cruise↔in_dist trade-off NOT in LR-magnitude space. Assigned #2957 frieren warmup-5-epochs-baseline-lr (warmup DURATION axis at baseline lr=1.5e-4; LinearLR total_iters 3→5, cosine T_max 57→55; #2938 student first recommendation + #2950 student reinforcement; orthogonal to peak-MAGNITUDE axis; 129th axis). Total closed: 128. Winners: 21.

- 2026-05-14 [Round 138 close-2]: Closed #2946 askeladd separate-surf-vol-heads LOSS (val 31.8968 +4.37% / test 26.6638 +0.56% wash; in_dist +8.64%, rc +4.96%, re_rand +3.36% all LOSS; cruise -0.78% mild WIN). 127th taxon: HEAD-ONLY SYMMETRIC SPLIT / SHARED-HEAD-AS-REGULARIZER. Student-kept trunk shared (Linear+GELU), only split final Linear(96,3): +291 params (cleanest interpretation). DECISIVE FINDINGS: (1) Head divergence ||W_surf - W_vol||=1.57 — REAL specialization. (2) But specialization went WRONG WAY: ||W_vol||=1.98 > ||W_surf||=1.43 despite surf_weight=10× — vol head FREE-RODE by escaping surf_loss constraint. (3) Channel divergence concentrated on Ux/Uy (1.00, 0.98) — WEAKEST on metric-critical p channel (0.70). (4) STRUCTURAL signature CONFIRMED: cruise barely moved (-0.78%), in_dist amplified (+8.64%) — non-uniform per-split impact = STRUCTURAL/LOCAL pattern. FIRST STRUCTURAL/LOCAL intervention in launch exhibiting canonical meta-signal AND clear mechanism. **KEY INSIGHT: shared head is an IMPLICIT REGULARIZER, not a compromise penalty.** Future structural interventions should preserve shared regularization while ADDING asymmetric capacity, not REPLACING shared with split. Assigned #2956 askeladd asymmetric-surf-correction-head (zero-init Linear(96,3) surface-only correction ADDED on top of shared head; vol tokens unchanged; +291 params; directly addresses #2946 free-rider failure mode via student suggestion #1; 128th axis). Total closed: 127. Winners: 21.

- 2026-05-14 [Round 138 close-1]: Closed #2944 tanjiro slice-routing-temperature-2.0 LOSS (val 31.9159 +4.43% / test 27.0516 +2.02%; uniform LOSS in_dist +7.46%, re_rand +7.35%, rc +3.48% LOSS; canonical cruise WIN -2.75%). 126th taxon: SLICE-ROUTING-TEMPERATURE SOFT-DIRECTION CLOSED. IMPLEMENTATION REVELATION: Baseline has per-head **LEARNABLE** routing temperature `self.temperature` (4 blocks × 2 heads = 8 params, init 0.5 = 2× sharper-than-T=1.0). #2944 confounded 2 changes: removed learnability + set T=2.0 (4× softer than baseline init). Despite confound, result is decisive: routing entropy diagnostic confirmed T=2.0 SUCCESSFULLY drove block-3 from 0.01-0.92 nats (collapsed) → 2.34-2.91 nats (73-91% of ceiling) — yet val regressed +4.43%. Therefore: **SHARP BLOCK-3 ROUTING IS LOAD-BEARING**. Entropy collapse is NOT a pathology — it's the model's chosen "slice specialization" solution. FIRST time canonical meta-signal (cruise WIN / in_dist LOSS) appears under a REPRESENTATION-axis lever (prior: capacity-axis #2923, #2934). Combined with prior capacity-axis evidence, **definitively confirms #2922 mechanistic story**: the cruise↔in_dist tradeoff is invariant across capacity-axis AND representation-axis levers, lives in REPRESENTATION/LOSS-LANDSCAPE space. Assigned #2955 tanjiro slice-routing-temperature-0.25 (sharper direction: T=0.25 = 2× sharper than baseline init 0.5; opposite-direction symmetric closure of temperature axis; same 2-change structure; maps both endpoints; 127th axis). Total closed: 126. Winners: 21.

- 2026-05-14 [Round 137 close-6]: Closed #2941 alphonse surf-p-weight-2x-mean-form LOSS (+5.52% val / +1.72% test; WORSE than #2933 sum-form +4.41%). 125th taxon: PER-CHANNEL p-WEIGHT 2× AXIS — CLOSED AT BOTH MAGNITUDE ENDPOINTS. Student discovered SECONDARY confound: /4 mean-form did NOT preserve baseline magnitude — it REDUCED it ~3.07× (effective surf_weight ~3.25 vs baseline 10, #2933 ~13.3). BUT the axis is decisively closed regardless: p-weight 2× is LOSS at both magnitude directions. CRITICAL META-SIGNAL FINDING: **The cruise-WIN of #2933 (-0.96%) DID NOT PERSIST under the /4 mean-form (cruise +1.70% LOSS).** The cruise-WIN was tied to surf_loss MAGNITUDE, NOT per-channel p-reweighting structure. CONFIRMS #2922 decisive insight: cruise↔in_dist tradeoff lives in LOSS-LANDSCAPE/REPRESENTATION space, NOT loss-weighting space. Assigned #2952 alphonse aux-mid-block-surf-loss (NEW STRUCTURAL/LOCAL axis: deep supervision at block-2 with aux_head_surf=nn.Linear(96,3) and aux_loss weight=0.1; +291 params; directly motivated by #2937 STRUCTURAL/LOCAL insight; depth-axis structural intervention paralleling #2946 head-axis split; 126th axis). Total closed: 125. Winners: 21.

- 2026-05-14 [Round 137 close-5]: Closed #2940 nezuko layerscale-gamma-asymmetric LOSS (+5.37% val / +1.32% test; in_dist worst at +14.96%; cruise/rc near-wash). 124th taxon: γ-INIT AXIS / CONVERGED-VALUE-INIT-FALSE-PREMISE. KEY MECHANISTIC FINDING: **γ-space is PATH-DEPENDENT.** Init at #2928's "converged values" (γ_attn=0.85, γ_mlp=0.93) — model drifts FURTHER DOWN to γ_attn∈[0.69,0.78], γ_mlp∈[0.85,0.88]. Same architecture+data+optimizer, DIFFERENT inits → DIFFERENT equilibria. #2928's "converged values" describe ONE stationary point reachable from γ=1.0, NOT global γ minimum. γ-init axis NOW MAPPED at 3 endpoints: 1e-4 (baseline) → 30.56 ✓ BEST, 1.0 → 30.94 (+1.26% LOSS), 0.85/0.93 → 32.20 (+5.37% LOSS). Pattern: SMALLER γ_init WINS. Baseline near-identity (1e-4) lets head/embedding dominate early; residual contribution grows organically. Budget rewards SLOW RAMP FROM NEAR-ZERO, NOT land-at-equilibrium. KEY STUDENT INSIGHT: "DROP this hypothesis line entirely; move to orthogonal recipe axes." Assigned #2951 nezuko post-norm-topology (NEW STRUCTURAL/REPRESENTATION axis: pre-norm → post-norm at all 9 LN sites; directly motivated by #2939 finding that LN mean-centering is load-bearing — WHERE the centering happens in residual stream is now load-bearing structural question; single forward() change; zero new params; 125th axis). Total closed: 124. Winners: 21.

- 2026-05-14 [Round 137 close-4]: Closed #2938 frieren pure-cosine-lr-1e-4 LOSS (+13.50% val / +13.29% test; WORSE than #2929 lr=1.5e-4 no-warmup variant). 123rd taxon: NO-WARMUP-AXIS / LR-SCHEDULE-SHAPE — 3-experiment decisive closure (#2920 per-step-warmup-from-0, #2929 pure-cosine-1.5e-4, #2938 pure-cosine-1.0e-4). DISAMBIGUATION DECISIVE: "Too much LR too soon" REFUTED (dropping peak 33% made things WORSE). "Ramp shape is load-bearing" STRONGLY SUPPORTED. "Total LR budget" CONTRIBUTING (under-trained at 60ep, slope too shallow). Cold-start monotonic+stable at lr=1.0e-4 — Lion handles it fine; LOSS is from under-training + missing ramp shape, NOT instability. 7th uniform-regularization closure (cruise +20.38% LOSS NOT a WIN). KEY STUDENT INSIGHT: "The warmup ramp from 0.1× is load-bearing — stop testing variants without it. The productive opposite-direction is longer warmup OR higher peak LR with existing warmup." Assigned #2950 frieren lr-2e-4-with-warmup (student followup #2: peak LR=2.0e-4 with the 3-epoch warmup ramp preserved; +33% peak vs baseline; first explicit UPWARD LR-magnitude test in launch; tests whether baseline 1.5e-4 sits at optimum or below it; 124th axis). Total closed: 123. Winners: 21.

- 2026-05-14 [Round 137 close-3]: Closed #2939 edward rmsnorm-replacement LOSS (+7.28% val / +7.42% test; uniform all 4 splits, in_dist worst at +13.31%, rc least at +2.60%). 122nd taxon: RMSNORM-AT-ALL-SITES / MEAN-CENTERING-LOAD-BEARING-FOR-CFD. 9 LN→RMSNorm sites; -864 params (β biases removed); 407,076 total. Param count drop matches prediction exactly. Model still actively improving at ep60 (32.83→32.79) at slope ~0.04/ep; train→val gap small (0.028) — UNDERFIT regime, NOT generalization bottleneck. Cold-start ~3-5x slower than baseline. KEY MECHANISTIC FINDING: LayerNorm's mean-centering does REAL, LOAD-BEARING WORK for CFD targets — pressure/velocity fields have natural per-Reynolds-regime DC offsets that LN strips explicitly, isolating the fluctuations the model needs to predict. RMSNorm preserves these baselines, forcing implicit factoring at 407k param budget the model can't afford. Clean falsification: LLM defaults (RMSNorm in LLaMA/T5/PaLM) DON'T always transfer to CFD surrogates. CONFOUND: #2939 changed TWO things at once — (a) removed mean-subtraction, (b) removed β bias. Followup isolates these. KEY STUDENT INSIGHT: "Try RMSNorm + β bias to disambiguate β-removal vs mean-subtraction-removal effects." Assigned #2949 edward rmsnorm-plus-beta (student followup #1: custom RMSNormWithBias at all 9 sites — γ scale + β bias, no mean-subtraction; +864 params restoring total to 407,940 baseline-matched; isolates the two factors that #2939 conflated; if WIN β was load-bearing, mean-subtraction is removable; if LOSS still, mean-subtraction is THE piece; 123rd axis). Total closed: 122. Winners: 21.

- 2026-05-14 [Round 137 close-2]: Closed #2937 askeladd huber-loss-delta-0.5 CLEAR LOSS (+26.14% val / +24.92% test; uniform LOSS all 4 splits — cruise +43.18% LOSS, NOT a WIN — meta-signal broken). 121st taxon: HUBER / L2-NEAR-ZERO / LION-INCOMPATIBILITY. Convergence-budget failure: still descending at ep60. Key mechanistic finding: Lion's per-element sign(grad) preserves direction info per parameter, but the BATCH-AVERAGED gradient over tokens depends on residual magnitudes; Huber down-weights small residuals so at convergence (98.8% of residuals in L2-regime), Lion gets gradient signal dominated by the rare large-residual tokens — STARVES the optimizer of fine-tuning signal. Fundamental optimizer/loss mismatch, not tunable. 6th 'uniform regularization breaks all splits uniformly' closure — confirms cruise-WIN/in_dist-LOSS meta-signal is ROBUST to GLOBAL loss-shape changes. KEY STUDENT INSIGHT: 'Future attacks need to be STRUCTURAL/LOCAL (per-region weighting, geometry-conditioned re-weighting, surface-vs-volume specialization), not uniform global re-shaping.' Assigned #2946 askeladd separate-surf-vol-heads (FIRST STRUCTURAL/LOCAL intervention in launch — splits single output Linear into head_surf + head_vol; surface mask routes to head_surf, volume mask to head_vol; tests whether per-region head specialization moves meta-signal where global re-shaping cannot; +591 params 0.15% capacity; 122nd axis). Also sent #2924 thorfinn heartbeat — SAM ρ=0.05 ~2hr stale-WIP, pod running but no commits/comments; ping to report status. Total closed: 121. Winners: 21.

- 2026-05-14 [Round 137 close-1]: Closed #2934 tanjiro slice-num-16 LOSS (+3.39% val / +1.55% test; in_dist hit hardest +8.40%; cruise nearly WASH +0.49% — confirms block-3 over-provisioning at slice_num=24). 120th taxon: SLICE-ROUTING-ALPHABET DOWNWARD SWEEP. Combined with #2923 (32 → +6.02% LOSS), the slice_num axis is SYMMETRICALLY CLOSED both sides: 24 sits at the peak of the routing-capacity curve. Block-3 entropy collapse (0.01-0.92 nats at slice_num=24 per #2923) was a DECOUPLED feature, NOT under-utilization signal — blocks 0/1/2 still benefit from full alphabet. Uniform LOSS, NOT cruise-WIN tradeoff. KEY STUDENT INSIGHT (reinforces #2922): cruise↔in_dist tradeoff lives in LOSS-LANDSCAPE/REPRESENTATION space, NOT capacity-knob axis. Future axes: PhysicsAttention temperature, slice-projection init, surface-vs-volume token weighting. Assigned #2944 tanjiro slice-routing-temperature-2.0 (NEW REPRESENTATION-axis: hardcoded T=2.0 in Physics_Attention softmax → softer routing distribution; orthogonal to slice_num alphabet-size axis just closed; single-line change routing_logits/2.0 before softmax; zero new params; 121st axis). Total closed: 120. Winners: 21.

- 2026-05-14 [Round 136 close-5]: Closed #2933 alphonse surf-p-channel-weight-2x LOSS (+4.41% val / +0.15% test wash; cruise -0.96% WIN, in_dist +9.60% LOSS — meta-signal repeats under loss-function intervention too). 119th taxon: PER-CHANNEL p-WEIGHT 2× SUM-FORM (magnitude-confounded). Student self-diagnosed confound: sum-form gave ~33% magnitude bump (4 weighted terms vs 3 unweighted) → effective surf_weight ≈ 13.3 instead of 10; cf. #2910 surf_weight=20 was +5.85% LOSS so partial bump consistent. Tests TWO things at once: p relative weight 1.5x within surf AND effective surf-vs-vol +33%. Train per-channel MAE shows no p-specific bottleneck (all 3 channels drop monotonically together). Assigned #2941 alphonse surf-p-weight-2x-mean-form (student followup #1: clean disambiguation using /4 mean-form to preserve baseline magnitude; isolates 'p-weight only' lever from 'surf_weight bump'; 120th axis). Total closed: 119. Winners: 21.

- 2026-05-14 [Round 136 close-4]: Closed #2928 nezuko layerscale-gamma-1.0 MIXED→not-merge LOSS (+1.26% val / -0.12% test; per primary metric val_avg this is LOSS). 118th taxon: γ_init=1.0 axis (single-point overshoot). KEY DIAGNOSTIC (refutes original hypothesis): γ DRIFTS DOWN from 1.0 → 0.85-0.95 over 60 epochs — model prefers MUTED block contribution. Baseline γ_init=1e-4 is on CORRECT side of natural operating value (under-shoot recoverable); γ=1.0 is on WRONG side (over-shoot forces drift down). ASYMMETRIC operating regime: γ_attn ~0.86 vs γ_mlp ~0.93 — attention residuals MORE strongly muted than MLP at every block. Most surprising mechanistic finding of round. Assigned #2940 nezuko layerscale-gamma-asymmetric (student followup #1: γ_attn=0.85, γ_mlp=0.93 — converged-value asymmetric init; first time using prior experiment converged values as new experiment init; skips γ-convergence budget; 119th axis). Total closed: 118. Winners: 21.

- 2026-05-14 [Round 136 close-3]: Closed #2930 edward head-zero-init LOSS (+8.46% val / +3.96% test; uniform LOSS all 4 splits +4.78% to +9.83%). 117th taxon: ZERO-INIT-HEAD-UNDER-BUDGET-CONSTRAINT CLOSED. Diagnostic confirmed mechanism activated cleanly (step-0 output exactly 0) BUT head weight norm grew 0→1.16 over 60 epochs ASYMPTOTING at cutoff (val_avg ep60=33.15 still descending). This is a CONVERGENCE-BUDGET failure, NOT representation failure. The doubly-under-initialized (body γ=1e-4 + head zero) network has no gradient drive for first ~5 epochs and exhausts 60-epoch budget growing head magnitude. Key insight: baseline Kaiming head std≈0.144 is NOT wasting gradient — it provides initial signal body LayerScale learns against. Assigned #2939 edward rmsnorm-replacement (NEW representation-axis: replace LayerNorm with RMSNorm at all sites; tests whether mean-centering is load-bearing for CFD; modern transformer default LLaMA/T5/PaLM; Lion sign-step is mean-preserving so RMSNorm may better match Lion behavior; 118th axis). Total closed: 117. Winners: 21.

- 2026-05-14 [Round 136 close-2]: Closed #2929 frieren no-warmup-pure-cosine LOSS (+6.91% val / +1.76% test; in_dist took 3.5x the hit of next-worst at +16.13% LOSS). 116th taxon: WARMUP-ENDPOINT AXIS CLOSED (combined with #2920 per-step warmup-from-0 +6.79%, both endpoints LOSS by ~7%; baseline 3-epoch warmup-from-0.1x sits at local optimum). Critical finding: ep1-3 train loss smoothly monotonic (0.91→0.48→0.37) — warmup NOT load-bearing for stability at Lion lr=1.5e-4. But warmup IS load-bearing for final val: it provides representational priming for in_dist manifold (in_dist hit hardest with 3.5x asymmetric regression). Assigned #2938 frieren pure-cosine-lr-1e-4 (student followup #2: disambiguation experiment — replicate #2929 pure cosine but with peak lr=1.0e-4 instead of 1.5e-4; baseline effective ep1-3 LR under warmup ramp is ~0.825e-4, #2929 ran at 1.8x that; matching averages should match results if LR magnitude is the variable; if LOSS again then ramp SHAPE is load-bearing regardless of magnitude; 117th axis). Total closed: 116. Winners: 21.

- 2026-05-14 [Round 136 close]: Closed #2927 askeladd per-block-flow-film LOSS (+4.51% val / +2.32% test; uniform LOSS all 4 splits incl cruise +1.71% — broke asymmetric cruise WIN of #2890/#2900). 115th taxon: FILM-PATHWAY-CAPACITY AXIS CLUSTER closed (5 experiments: baseline single-mult, #2890 additive, #2900 mult-parallel, #2911 nonlinear, #2927 4-site mult — zero val_avg wins). Student diagnostic confirms mechanism IS engaging (block weight norm grows 1.37x with depth; OOD splits get 50% more conditioning than in_dist) — yet val regresses uniformly. Reinforces #2922 student insight: cruise↔in_dist tradeoff lives in LOSS LANDSCAPE / REPRESENTATION, NOT FiLM-pathway capacity. Cruise WINs in #2890/#2900 came from ADDITIVE/PARALLEL TOPOLOGY favoring low-Re tandem flow, NOT capacity reallocation. Assigned #2937 askeladd huber-loss-delta-0.5 (2nd LOSS-axis test this round, ORTHOGONAL to #2933 alphonse per-channel; tests gradient-SHAPE axis: Huber β=0.5 gives Lion smooth gradient near 0, may unlock fine-tuning L1+Lion cannot capture; 116th axis). Total closed: 115. Winners: 21.

- 2026-05-14 [Round 135 close]: Closed #2923 tanjiro slice-num-32 LOSS (+6.02% val / +2.82% test; 114th taxon = slice-routing-alphabet upward sweep at n_head=2). Block-3 head entropy collapsed (0.01-0.92 nats vs log(32)=3.466 ceiling) — wider alphabet did NOT force head differentiation; cf. #2906 n_head=4 had 2.11 nats block-3 range. n_head and slice_num are NOT interchangeable. Assigned #2934 tanjiro slice-num-16 (downward sweep, student followup #1 — does smaller alphabet help where larger hurt?; 115th axis). Total closed: 114. Winners: 21.

- As-of Round 134 (2026-05-14): Closed #2922 alphonse lookahead-lion-k5-α0.5 LOSS (+19.83%; uniform LOSS all 4 splits incl cruise +22.51% NO cruise WIN; 113th taxon: optimizer-wrapper/slow-weight-averaging class CLOSED). Student decisive mechanistic insight: 'cruise↔in_dist trade-off is NOT optimization-trajectory-driven; it lives in the LOSS LANDSCAPE itself. Future attacks should target LOSS FUNCTION or REPRESENTATION, not optimizer dynamics.' This rules out the entire low-pass-filter-on-trajectory class. Total closed: 113. Winners: 21. Assigned #2933 alphonse surf-p-channel-weight-2x (FIRST loss-axis experiment per student recommendation; weight pressure channel 2x within surf_loss; direct test of per-channel loss-function reweighting; distinct from #2910 surf-weight which changed surf-vs-vol balance).
- As-of Round 133 (2026-05-14): Closed #2920 frieren linear-warmup-3-epochs LOSS (+6.79%; 111th taxon: per-step-warmup-from-zero axis CLOSED — baseline already had per-epoch warmup-from-0.1×, this was unintended comparison). Closed #2918 edward input-mixup-α=0.2 CATASTROPHIC LOSS (+21.16%; 112th taxon: data-level mixup axis CLOSED — non-physical hybrid airfoils + mask mismatch; worst result in 12-experiment meta-signal series). Closed #2876 fern batch_size_8 stale_wip (12+ rounds dormant). Student mechanistic insight confirmed: meta-signal is specific to interventions that SHIFT CAPACITY ALLOCATION, not uniform regularization. Total closed: 112. Winners: 21. Assigned #2929 frieren no-warmup-pure-cosine (student followup: pure CosineAnnealingLR T_max=60 — is existing warmup dead weight?). Assigned #2930 edward head-zero-init (pair body LayerScale γ=1e-4 with head zero-init; canonical DiT recipe gap). Assigned #2931 fern cosine-eta-min-1e-6 (revival of student followup #3: non-zero LR floor anchors cosine tail at meaningful magnitude).
- As-of Round 132 (2026-05-14): Closed #2911 nonlinear-additive-geo-film LOSS (+7.97%; 109th taxon: geo-FiLM-conditioner-capacity axis CLOSED — structural trade-off not breakable via conditioner expressivity). Closed #2912 droppath-linear-0.1 LOSS (+7.49%; 110th taxon: stochastic-depth axis CLOSED — train/val gap only 1.33x, model NOT overfit, dropout-class interventions mistargeted). Total closed: 110. Winners: 21. Assigned #2927 askeladd per-block-flow-film (depth-distributed conditioning; student-suggested direction from #2912). Assigned #2928 nezuko layerscale-gamma-1.0 (drop cautious 1e-4 init for shallow 4-block net).
- **As of:** 2026-05-14 [Round 131] UTC (round-131: 1 closure + 2 assignments. Closed #2906 tanjiro n-head-4 LOSS (val 31.1393 +1.89%; in_dist +5.85% biggest hit; camber_cruise -0.35% flat; rank-limited square projection dim_head=24 vs slice_num=24 was binding constraint; heads DO specialize per entropy diagnostics but specialization does not improve generalization; attention-head axis CLOSES 108th taxon). Assigned #2923 tanjiro slice-num-32 (slice_num 24 to 32 at n_head=2; Linear(48,32) rectangular; 64 total slice tokens vs 48; ~417k params; isolates routing-capacity axis without rank-limiting; 109th axis). Assigned #2924 thorfinn sam-rho-0.05 (SAM wrapping Lion; rho=0.05; epochs=30 due to 2x compute; flatness regularization directly attacks 6-confirmation in_dist over-specialization meta-signal; 110th axis). No human GH issues. Total closed taxa: 108. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 130] UTC (round-130: 2 closures + 1 assignment. Closed #2910 alphonse surf-weight-20 LOSS (val 32.3496 +5.85%; BREAKS meta-signal: uniform loss NOT trade-off coupling; in_dist +20.25% BIGGEST; camber_cruise flat -0.46%; volume loss = implicit regularization; loss-weighting axis 107th taxon). Closed #2894 thorfinn cosine-eta-min stale-wip (10+ rounds no progress; eta_min idea valid but LR-schedule covered by #2920). Assigned #2922 alphonse lookahead-lion-k5-a0.5 (Lookahead wrapper: k=5, alpha=0.5; slow-weight anchor vs in_dist over-specialization; in-optimizer feedback unlike EMA-eval; ~1.6MB slow buffers; 108th axis). No human GH issues. Total closed taxa: 107. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 129] UTC (round-129: 2 closures + 2 assignments. Closed #2905 edward weight-decay-5e-4 LOSS (val 30.7877 +0.74%; 5TH confirmation of in_dist-LOSS+cruise-WIN meta-signal; stronger wd=third dial on same trade-off axis; 105th taxon). Closed #2892 frieren ema-eval LOSS (val 31.5008 +3.08% on fair 60ep schedule; EMA mechanism intact but net-negative: overhead costs more than smoothing recovers in 30-min budget; 106th taxon). Assigned #2918 edward input-mixup-alpha-0.2 (first data-level attack on meta-signal; 50% batches; zero params; 107th axis). Assigned #2920 frieren linear-warmup-3-epochs (recipe gap: no warmup in 106+ experiments; 3ep linear+57ep cosine; 108th axis). No human GH issues. Total closed taxa: 106. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 128] UTC (round-128: 1 closure + 1 assignment. Closed #2903 nezuko rmsnorm-replace-layernorm LOSS (val 31.2013 +2.10%; beta-bias load-bearing: 864 params removed +2% val; gamma did NOT compensate; in_dist +4.83% worst regression; camber_cruise -0.69% WIN = 4th consecutive confirmation of over-specialization meta-signal; normalization-choice axis CLOSES 104th taxon). Assigned #2912 nezuko droppath-linear-0.1 (DropPath wraps attn+MLP residuals; linear schedule 0 to 0.1 across blocks 0-3; zero new params; attacks the cruise/in_dist over-specialization meta-signal via regularization; 105th axis). No human GH issues. Total closed taxa: 104. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 127] UTC (round-127: 1 closure + 1 assignment. Closed #2900 askeladd multiplicative-geo-film LOSS (val 31.5745 +3.32%; test TIED +0.02% 26.5212; CRITICAL FINDING: camber_cruise WIN and in_dist LOSS are COUPLED; 2-point trade-off curve: #2890 additive=cruise WIN -9.87% + in_dist LOSS +16.85%; #2900 multiplicative=cruise tied -1.80% + in_dist partially recovered +5.88%; changing composition shifts trade-off direction but does NOT break coupling; 103rd taxon). Assigned #2911 askeladd nonlinear-additive-geo-film (Sequential(Linear(8,16), GELU, Linear(16,96)) zero-init on last layer; same additive site as #2890 WIN; ~409,716 params; tests if non-linear bottleneck breaks structural coupling; 104th axis). No human GH issues. Total closed taxa: 103. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 126] UTC (round-126: 1 closure + 1 assignment. Closed #2899 alphonse asymmetric-mlp-ratio-3344 LOSS (val 31.2869 +2.38%; HYPOTHESIS FALSIFIED: shallow blocks at mlp=3 did NOT protect in_dist; in_dist +10.45% identical to uniform=4 +10.88%; DEEP-BLOCK WIDTH is the OOD driver; camber_cruise -5.00% BIGGEST EVER; re_rand recovered +0.94% (vs +5.72% uniform=4) confirming re_rand IS shallow-block effect; test WIN -1.13%; MLP-asymmetric depth-capacity axis CLOSES 102nd taxon). Assigned #2910 alphonse surf-weight-20 (--surf_weight 20; no code change; surface:volume gradient ratio 10:1→20:1; surf_weight never tested above 10 in 102 experiments; directly attacks primary metric; 103rd axis). No human GH issues. Total closed taxa: 102. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 125] UTC (round-125: 1 closure + 1 assignment. Closed #2898 tanjiro grad-clip-1.0 LOSS (val 32.0598 +4.91%; clip_frac=1.000 every step; natural grad_norm regime 24-65 vs max_norm=1.0 = 25x division; Lion sign-step doubly normalizes when clips+sign applied; gradient-FLOW axis CLOSES at max_norm=1.0; 101st taxon). Assigned #2906 tanjiro n-head-4 (n_head 2→4; dim_head 48→24; inner_dim=96 unchanged; reduces params ~23k to ~384,900 — per-head qkv quadratic shrink; 4 heads×24 slices=96 total slice tokens vs 48 baseline; finer physics-mode specialization per head; attention-head axis 102nd). No human GH issues. Total closed taxa: 101. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 124] UTC (round-124: 1 send-back + 1 closure + 1 assignment. Sent back #2892 frieren ema-eval-weights (val 31.6548 +3.58% LOSS schedule-confounded: --epochs 70 hit timeout ep55 with 21% LR remaining vs baseline epochs=60 reaching cosine tail; EMA beats live by +0.91 every late epoch; test WIN -1.13%; rerun with epochs=60 to remove confound). Closed #2891 edward multi-task-aux-loss LOSS (val 32.4465 +6.17%; shortcut learned aux_loss 175x collapse log_Re MSE=0.00079; uniform regression not split-reversal; Lion sign-step amplifies tiny aux_weight; 100th candidate axis milestone taxon). Assigned #2905 edward weight-decay-5e-4 (Lion wd 3e-4→5e-4; regularization-strength axis first test above baseline; per-step decay 7.5e-8 vs 4.5e-8; targets recurring in-dist regression pattern from #2889/#2890; one-line change; 101st axis). No human GH issues. Total closed taxa: 100. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 123] UTC (round-123: 1 closure + 1 assignment. Closed #2893 nezuko gelu-up-projection LOSS (val 31.6773 = +3.65%; activation-site axis FULLY CLOSED: all up-proj nonlinearities LOSS whether hard ReLU² or smooth GELU; disentangled from failure modes — GLU factorization requires linear up-path; 99th taxon). Assigned #2903 nezuko rmsnorm-replace-layernorm (replace ln_1/ln_2/ln_3 with nn.RMSNorm; removes β-bias params and mean-centering; modern transformer standard; ~407,076 params; normalization-choice axis first test; 100th axis milestone). No human GH issues. Total closed taxa: 99. Winners: 21. Current baseline: val 30.5605.)

- **As of:** 2026-05-14 [Round 122] UTC (round-122: 1 closure + 1 assignment. Closed #2890 askeladd geometry-film LOSS (val 31.5045 = +3.09% vs baseline 30.5605; STRONGEST CAMBER_CRUISE WIN EVER: -9.87% but in_dist +16.85%; THIRD experiment confirming novel split-pattern reversal; mechanism activated weight_norm=1.95 highest std on camber_cruise; problem is additive-bias site competing with preprocess MLP; 98th taxon). Assigned #2900 askeladd multiplicative-geo-film (geo_film=Linear(8,96) zero-init, geo_features=ch[15,16,17,19,20,21,22,23], fx = fx*(1+film_scale+geo_film_scale) — parallel multiplicative gate to existing flow-FiLM; tests whether multiplicative composition fixes in_dist regression from #2890 while preserving camber_cruise -9.87%; +864 params ~408,804; 99th axis mult-geo-FiLM). No human GH issues. Total closed taxa: 98. Winners: 21. Current baseline: val 30.5605 / test 26.5160.)

- **As of:** 2026-05-14 [Round 121] UTC (round-121: 1 closure + 1 assignment. Closed #2889 alphonse mlp_ratio=4 LOSS (val 31.4505 = +2.91% vs baseline 30.5605; NOVEL SPLIT-PATTERN REVERSAL: first OOD-WIN at in-dist cost; camber_cruise -3.60% camber_rc -0.74% WIN while in_dist +10.88% re_rand +5.72% LOSS; gate_zero_frac +27-48% = capacity saturation; uniform MLP-ratio axis CONCAVE at mlp_ratio=3; 97th taxon). Assigned #2899 alphonse asymmetric-mlp-ratio-3344 (shallow 0,1 mlp_ratio=3, deep 2,3 mlp_ratio=4; ~446k params; exploits novel OOD signal; 98th axis depth-asymmetric-capacity). Noted stale #2876 fern with outdated baseline — updated via PR comment to new bar 30.5605. Total closed taxa: 97. Winners: 21.)

- **As of:** 2026-05-14 [Round 120] UTC (round-120: 1 closure + 1 assignment. Closed #2885 tanjiro stochastic-depth-droppath LOSS (val 33.1152 = +8.36% vs new baseline 30.5605; test 28.5526 = +7.68%; ALL 4 splits regress in_dist +12.95% worst; capacity-reduction signature NOT regularization; 4 blocks too few for DropPath - single block = 25% of depth; DeiT/ConvNeXt amortize across 12-24 blocks; LayerScale gamma synergy NOT manifest gamma_attn=0.018 near-identity gamma_mlp=0.073 real work; gradient-path axis CLOSES at depth=4; 95th taxon). Assigned #2898 tanjiro grad-clip-1.0 (clip_grad_norm_(model.parameters(), max_norm=1.0) between loss.backward and optimizer.step; gradient-FLOW axis distinct from gradient-PATH; Lion-compatible since momentum buffer accumulates raw gradients; equalizes hard OOD vs easy in_dist sample contributions; key diagnostic grad_norm_clip_frac per epoch tells if clipping is biting; 100th candidate axis - round number milestone; +0 params). No human GH issues. Active in-flight zero idle: #2876 fern batch_size=8, #2889 alphonse mlp_ratio=4, #2890 askeladd geometry-film, #2891 edward multi-task-aux, #2892 frieren ema-eval, #2893 nezuko gelu-up, #2894 thorfinn eta-min-5pct, #2898 tanjiro grad-clip-1.0. Total closed taxa: 96. Total merged winners: 21. NEW BASELINE: val 30.5605.)

- **As of:** 2026-05-14 [Round 119] UTC (round-119: 4 closures + 4 assignments. Closed #2884 edward slice-entropy-reg LOSS (val 31.6215 = +3.47% vs new baseline 30.5605; mechanism worked: block-2 entropy held at 2.497/3.178 throughout training but block-0 absorbed sparsity to 1.16/3.178; routing collapse is LOAD-BEARING for OOD geometry not bug; 94th taxon). Closed #2881 nezuko fourier-pos-enc LOSS (val 31.9774 = +4.63%; all 4 OOD splits regress +5-7%; mechanism: Fourier features amplify in-dist coordinate phase memorization; NeRF works for single-scene but every wing has its own coord population; 93rd taxon). Closed #2874 frieren slice_num=28 LOSS (val 32.2381 = +5.49%; only ~19/28 effective slices; block-3 entropy 0.30/3.33 = one-hot attractor; slice_num axis fully closed 3-point bracket 20-LOSS 24-WIN 28-LOSS; 95th taxon). Closed #2868 thorfinn smooth-L1 stale_wip (no terminal results; slot reclaimed). Assigned 4 NEW axes targeting different mechanisms: #2891 edward multi-task-aux-loss (Linear(96,3) aux head predicting log_Re/AoA0/AoA1 from masked-mean pooled features; aux_weight=0.05; self-supervised regularization for OOD generalization; +291 params; 96th axis), #2892 frieren ema-eval-weights (EMAModel decay=0.999 standard transformer recipe DeiT/ConvNeXt/DINOv2; smooths cosine-tail noise; eval uses EMA weights live training unchanged; +0 params shadow copy; 97th axis), #2893 nezuko gelu-up-projection (F.gelu on SwiGLU up-proj output single-line change; smooth bracket for #2875 ReLU2 LOSS; no hard-zero no quadratic amplification; closes activation-site axis cleanly; +0 params; 98th axis), #2894 thorfinn cosine-eta-min-5pct (CosineAnnealingLR eta_min 0 → 7.5e-6 = 0.05*cfg.lr; addresses late-training plateau where lr→0; single-line config change; +0 params; 99th axis). No human GH issues. Active in-flight zero idle: #2876 fern batch_size=8, #2885 tanjiro drop-path, #2889 alphonse mlp_ratio=4, #2890 askeladd geometry-film, #2891 edward multi-task-aux, #2892 frieren ema-eval, #2893 nezuko gelu-up, #2894 thorfinn eta-min. Total closed taxa: 95+1stale=96 effective. Total merged winners: 21. NEW BASELINE: val 30.5605.)

- **As of:** 2026-05-14 [Round 118] UTC (round-118: 2 closures + 2 merges/assignments. PLATEAU BROKEN. Closed #2878 askeladd differential-attention LOSS (val 31.7480 +2.77%; test 27.4526 +4.79%; lambda depth-prior wrong for TandemFoilSet: block-3 wants lambda<0 = inverted, clamped to 0; G=24 slice space too small for noise cancellation; 3 of 4 splits regress; 92nd taxon). MERGED #2879 alphonse mlp_ratio=3 WIN (val 30.5605 = -1.07% vs 30.8909; test 26.5160 = +1.22% regression; in_dist -7.42% re_rand -2.00% camber_rc +0.55% camber_cruise +6.07%; MLP-axis capacity strictly preferred over attention-axis: beats #2869 attn-expansion on all splits; gate_zero_frac 1.0-1.6% extra channels active; 21st winner; NEW BASELINE val 30.5605 / test 26.5160 / 407,940 params). Assigned #2889 alphonse mlp_ratio=4 (WIN follow-up: hidden_swiglu 192->256, ~481,668 params; tests capacity scaling; 95th axis). Assigned #2890 askeladd geometry-film (new second FiLM branch on geo scalars [NACA0(3)+NACA1(3)+gap+stagger]=8ch; additive bias geo_film=Linear(8,96) zero-init; +864 params = 408,804 total; directly targets camber_rc/cruise OOD splits via dedicated geometry-conditioning pathway; 96th axis). No human GH issues. All 8 in-flight zero idle: #2868 thorfinn smooth-L1-huber, #2874 frieren slice_num=28, #2876 fern batch_size=8, #2879 alphonse mlp_ratio=3 (MERGED), #2881 nezuko fourier-pos-enc, #2884 edward slice-entropy-reg, #2885 tanjiro drop-path-0.1, #2889 alphonse mlp_ratio=4, #2890 askeladd geometry-film. Active in-flight: #2868 #2874 #2876 #2881 #2884 #2885 #2889 #2890. Total closed taxa: 92. Total merged winners: 21. NEW BASELINE: val 30.5605.)

- **As of:** 2026-05-14 [Round 117] UTC (round-117: 1 closure + 1 assignment. PLATEAU DEEPENS to 16 consecutive LOSSes since #2810 merge. Closed #2875 tanjiro squared-relu-up-proj LOSS (val 31.9147 = +3.31% vs 30.8909; test 27.3442 = +4.38%; ALL 4 SPLITS REGRESS: in_dist +7.20% worst; camber_rc +3.19%; camber_cruise +2.88%; re_rand +0.95%; in-dist worst-hit = CAPACITY-BOTTLENECK signature, NOT OOD-positive regularizer; OOD-positive bias hypothesis FALSIFIED; up_act_zero_frac depth-progressive 58->62->68->77% = DEAD-FEATURE TRAP under Lion sign-step confirmed: ReLU2 gradient=0 everywhere x<=0, Lion sign-step propagates no recovery signal; GLU mechanism broken: gate<->up negative correlation -0.03 to -0.11 shows gate fighting up-path spikes; bf16 overflow risk REALIZED: 3/4 blocks exceed up_act_max=100 threshold block-1=157.8 one test_camber_cruise vol_loss=inf; SwiGLU linear up-projection is load-bearing design choice (Shazeer 2020); ACTIVATION-SITE AXIS COMPREHENSIVELY CLOSED: gate-SiLU WIN-locked #2741 gate-GELU LOSS #2759 gate-Mish CATASTROPHIC #2859 up-linear baseline WIN up-ReLU2 LOSS #2875; Lion sign-step amplifies cost of hard-zero regions in FFN = empirically grounded across MishGLU+ReLU2; 91st taxon). Assigned #2885 tanjiro stochastic depth DropPath linear 0->0.1 (DropPath class wraps gamma_attn*attn(ln1(fx))+fx and gamma_mlp*mlp(ln2(fx))+fx residual paths; depth-progressive rates [0.0 0.033 0.067 0.1] across 4 blocks; eval deterministic unchanged; +0 params; DeiT/ConvNeXt recipe + LayerScale synergy; FIRST gradient-path regularization experiment after 16 LOSSes targeting activations/attention/norms/capacity; implicit sub-network ensemble forces path-redundancy and flat loss basin; 95th candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2868 thorfinn smooth-L1-huber-b1 #2874 frieren slice_num=28 #2876 fern batch_size=8 #2878 askeladd differential-attention #2879 alphonse mlp_ratio=3 #2881 nezuko fourier-pos-enc #2884 edward slice-entropy-reg #2885 tanjiro drop-path-0.1-linear. Total closed taxa: 91. Total merged winners: 20. Plateau protocol 16 LOSSes.)

- **As of:** 2026-05-14 [Round 116] UTC (round-116: 1 closure + 1 assignment. PLATEAU DEEPENS to 15 consecutive LOSSes since #2810 merge. Closed #2873 edward post-norm structural pivot LOSS (val 33.3698 = +8.02% vs 30.8909; test 27.8986 = +6.50%; ALL 8 SPLITS REGRESS including in_dist +8.67% — NO in_dist WIN + OOD LOSS tradeoff pattern; val_camber_cruise +14.64% worst; param count 333,700 ✓ unchanged; per-block residual-stream RMS ≈ 1.0 ± 2% MECHANISM ACTIVATED but downstream metric uniformly worse; LayerScale γ grew 2-3× to compensate: optimizer pushes branches louder before post-LN flattens them = post-LN DESTROYS useful magnitude information that pre-LN preserves; **STRUCTURAL FAILURE** not regularization tradeoff — all 8 splits = gradient-path level failure; PRE-NORM IS LOAD-BEARING FOR THIS STACK (FiLM + LayerScale + SE-block3 + SwiGLU + Lion); FIRST RECENT EXPERIMENT WITHOUT in_dist WIN + OOD LOSS pattern = confirms failure is structural not calibration; 88th taxon). Assigned #2884 edward **slice-routing entropy regularization** (auxiliary entropy loss on slice_weights: loss = data_loss - 0.005 * mean(H_block_i) across 4 blocks; H = -sum(w*log(w+e)).mean() per block; alpha=0.005 ≈ 1.7% of typical loss; directly targets block-2 collapse from #2869 alphonse diagnosis entropy 0.267 8.4% of max; +0 params; optimizer gravitates toward low-entropy routing because simpler in-dist regression targets but reduces OOD generalization; 94th candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2868 thorfinn smooth-L1-huber-β1, #2874 frieren slice_num=28, #2875 tanjiro squared-relu-up-proj, #2876 fern batch_size=8, #2878 askeladd differential-attention, #2879 alphonse mlp_ratio=3, #2881 nezuko fourier-pos-enc, #2884 edward slice-entropy-reg. Total closed taxa: 88. Total merged winners: 20. Plateau protocol 15 LOSSes — bold-swing candidates remaining: beta=0.5 if #2868 wins, x^3 if #2875 wins, multi-resolution slice pyramid if #2874 informs, fourier n_freqs=8 if #2881 wins.)

- **As of:** 2026-05-14 [Round 115] UTC (round-115: 1 closure + 1 assignment. PLATEAU DEEPENS to 14 consecutive LOSSes since #2810 merge. Closed #2870 nezuko output-head gain scalar +1 param LOSS (val 32.1480 = +4.07% vs 30.8909; test 27.3552 = +4.42%; in_dist only WIN -1.18% (24.9756), all 3 OOD splits LOSS camber_cruise +9.99% worst; output_gain converged to 1.0443 within predicted 0.9-1.1 range = mechanism activated but empirically OOD-harmful; mechanism: single global scalar commits to one calibration for all distributions biasing toward most-frequent in-dist regime at OOD cost; output-head gain axis CLOSES — decoder Linear weight already absorbs any scalar rescaling so output_gain is mathematically redundant + Lion sign-step on scalar oscillates destabilizing cosine-tail optimization; PATTERN REPEATED: in_dist WIN + OOD LOSS is consistent failure mode across #2851/#2864/#2870 = configurations that tighten in-dist fitting tend to OOD-regress confirming #2809 frieren generalization-limited thesis; 87th taxon). Assigned #2881 nezuko **Fourier positional encoding on mesh coordinates** (NeRF-style sin/cos encoding of pos_x, pos_y at 4 frequencies [pi, 2pi, 4pi, 8pi]; input dim 24→40; preprocess Linear(24→40) expanded; +3,072 params = ~336,772 total ≈ +0.9% inductive bias change NOT capacity expansion; addresses geometry-OOD gap: 14 consecutive LOSSes NONE have touched input representation; OOD-fragile splits differ primarily in geometry; raw (x,y) scalars are linearly decoded but Fourier features let downstream Linears implement position-sensitive responses at spatial scales 0.125-1.0 covering tandem foil geometry; Mildenhall 2020 NeRF + Tancik 2020 NeurIPS Fourier Features; falsifiable WIN=Fourier bias helps OOD geometry try n_freqs=8, LOSS=high-freq features overfit, WASH=raw coords sufficient close axis; 93rd candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2868 thorfinn smooth-L1-huber-β1, #2873 edward post-norm, #2874 frieren slice_num=28, #2875 tanjiro squared-relu-up-proj, #2876 fern batch_size=8, #2878 askeladd differential-attention, #2879 alphonse mlp_ratio=3, #2881 nezuko fourier-pos-enc. Total closed taxa: 87. Total merged winners: 20. Plateau protocol active 14 LOSSes — bold-swing candidates remaining: per-channel gain if reassigned, β=0.5 if #2868 wins, x^3 if #2875 wins, multi-resolution slice pyramid if #2874 informs direction, entropy reg on routing.)

- **As of:** 2026-05-14 [Round 114] UTC (round-114: 1 closure + 1 assignment. PLATEAU DEEPENS to 13 consecutive LOSSes since #2810 merge. Closed #2869 alphonse n_head=1 dim_head=96 max-rank attention LOSS (val 31.7942 = +2.93% vs baseline 30.8909; test 26.6482 = +1.72% LOSS; all 4 splits regress: in_dist +2.89%, camber_cruise +8.75% worst, camber_rc +2.34%, re_rand +0.97%; param count 421,248 +26.2% ✓; best ep68/70 full schedule; **CRITICAL: IN-DIST DID NOT IMPROVE** — capacity-bottleneck thesis falsified, +25% params via max per-head rank did NOT buy in-dist capacity gains; **BLOCK-2 ROUTING COLLAPSED** entropy 0.267 vs max 3.18 = 8.4% — single-head degeneracy confirmed; per-block temperature spread widened [0.88, 1.19, 0.78, 1.02] span 0.41 vs H=2 per-head span 0.14; mechanism: **multi-head parallelism is load-bearing as ENSEMBLE AVERAGING of routing decisions** not per-head specialization — head-parallel softmax-mean is operative mechanism; U-shaped axis confirmed: H=4 +3.71% LOSS in_dist-worst + H=1 +2.93% LOSS camber_cruise-worst = **ATTENTION-HEAD AXIS COMPREHENSIVELY CLOSED AT H=2 SWEET SPOT**; 86th taxon). Assigned #2879 alphonse **mlp_ratio 2→3** (1-line config change SwiGLU intermediate 192→288 across all 4 blocks; expected ~370,564 params +36,864 +11%; student's own #4 follow-up suggestion; APPLES-TO-APPLES test vs #2869 attention-expansion (+87k LOSS +2.93%) — same ~budget different location; if MLP-expansion WINS while attention-expansion LOSS = MLP-body capacity was bottleneck not attention; if both LOSS = model is uniformly generalization-limited not capacity-limited consistent with #2809 frieren thesis; 92nd candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2868 thorfinn smooth-L1-huber-β1, #2870 nezuko output-head-gain, #2873 edward post-norm, #2874 frieren slice_num=28, #2875 tanjiro squared-relu-up-proj, #2876 fern batch_size=8, #2878 askeladd differential-attention, #2879 alphonse mlp_ratio=3. Total closed taxa: 86. Total merged winners: 20. Plateau protocol still active 13 LOSSes — bold-swing candidates remaining: per-channel output gain if #2870 wins, β=0.5 follow-up if #2868 wins, x^3 follow-up if #2875 wins, multi-resolution slice_num pyramid if #2874 informs direction, entropy regularization on slice-routing softmax.)

- **As of:** 2026-05-14 [Round 113] UTC (round-113: 1 closure + 1 assignment. PLATEAU DEEPENS to 12 consecutive LOSSes since #2810 merge. Closed #2864 askeladd hybrid LN-at-block0_ln1 + RMSNorm-at-8-other-sites LOSS (val 31.3911 = +1.62% vs baseline 30.8909; test 26.5492 = +1.35% LOSS; val_single_in_dist 23.9145 = −5.39% WIN NEW BEST-EVER in-dist; all 3 OOD splits regress camber_rc +4.66% camber_cruise +6.78% re_rand +0.27%; param count 332,932 ✓ exactly as predicted 333,700 − 768; **EXEMPLARY DIAGNOSTIC CONFIRMING #2851 PREDICTIONS**: block0_ln1 LN γ_std=0.127 range [0.455, 1.197] strongly modulated + β_std=0.065 non-trivial; 8 RMSNorm γ_std ≤ 0.0025 near-identity; **per-token RMS comparison**: LN post-norm RMS=0.935 (mean-centering strips per-token mean) vs RMSNorm post-norm RMS=1.001 (no mean subtraction); **HYPOTHESIS FALSIFIED**: β at γ-inactive sites is load-bearing for OOD generalization — even when γ stays near identity β encodes per-channel bias compensating mean-channel shifts; result statistically INDISTINGUISHABLE from #2851 (31.3911 vs 31.4066 = -0.05%) confirming β not γ is operative at 8 sites; student's meta-recommendation accepted: **NORMALIZATION META-AXIS IS NOW COMPREHENSIVELY CLOSED** — combined with closed embed-LN #2808, DyT #2686, SurfaceAwareLN #2829, QK-Norm, GroupNorm, full RMSNorm #2851, and hybrid #2864 — normalization mapped at SITE level AND FEATURE level (mean-centering + β are load-bearing, γ at 8/9 sites is decorative); 85th taxon hybrid-LN-RMSNorm CLOSES). Assigned #2878 askeladd **Differential Attention on slice-token self-attention** (Ye et al. 2024 arXiv:2410.05258; paired Q,K projections with learnable per-block λ scalar; attn = (softmax(q1k1^T/sqrt(D)) − λ·softmax(q2k2^T/sqrt(D))) / (1 − λ + ε); V unchanged; λ_init = 0.8 − 0.6·exp(−0.3·layer_idx) per paper; expected ~370,568 params = +36,868 +11%; plateau-protocol bold swing from queue since Round 108 — 12 LOSSes deep; attacks attention quality via noise-cancellation independent of all in-flight axes; normalization family now closed so pivot to attention mechanism exploration; askeladd given this assignment for exemplary diagnostic capability; 91st candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2868 thorfinn smooth-L1-huber-β1, #2869 alphonse n_head=1, #2870 nezuko output-head-gain, #2873 edward post-norm, #2874 frieren slice_num=28, #2875 tanjiro squared-relu-up-proj, #2876 fern batch_size=8, #2878 askeladd differential-attention. Total closed taxa: 85. Total merged winners: 20. Plateau protocol still active 12 LOSSes — bold-swing candidates remaining: per-channel output gain if #2870 wins, β=0.5 follow-up if #2868 wins, x^3 follow-up if #2875 wins, n_head=1 rank-up confirmation if #2869 wins, Multi-resolution pyramid slice_num if #2874 informs direction.)

- **As of:** 2026-05-14 [Round 112] UTC (round-112: 1 LOSS-close + 1 stale-close + 2 assignments. Plateau deepens to 11 consecutive LOSSes since #2810 merge. Closed #2859 tanjiro MishGLU CATASTROPHIC LOSS (val 38.6898 = +25.2% vs baseline 30.8909; test 33.1923 = +26.7% LOSS; all 4 val splits regress uniformly +5.95 to +8.92 mae_surf_p NO OOD selectivity; **EXEMPLARY mechanism diagnostic**: gate_zero_frac jumped from SwiGLU baseline 1.3-2.4% to MishGLU 49-68% across all 4 blocks = 25-50× more hard-masking, WORSE than #2759 GeGLU 16-26% LOSS; **depth-progressive gate_std decay 1.07→0.58 that PR #2741 identified as MECHANISM by which SwiGLU works is COMPLETELY DESTROYED** under Mish — block 3 has HIGHER gate_std 1.00 than block 0 0.88, OPPOSITE of working baseline; mechanism: **SiLU specifically yields a learnable W_gate matrix whose pre-activation distribution places few values in wide near-zero region**, Mish steers gate weights toward wide negative tail where Mish exponentially decays despite similar near-zero behavior — activation function determines which W_gate solution optimizer converges to, NOT just local smoothness; 89th taxon: gate-activation axis at MLP-body SwiGLU site CLOSES DEFINITIVELY at SiLU 3 points anchor curve GELU LOSS / SiLU WIN baseline / Mish LOSS worst-yet; **IMPORTANT NOTE student used DEFAULT train.py command lr=5e-4 weight_decay=1e-4 epochs=50 NOT canonical advisor config lr=1.5e-4 weight_decay=3e-4 epochs=70** — inflates LOSS magnitude but gate_zero_frac qualitative pattern is hyperparameter-independent so conclusion stands; closing comment asked student to use canonical reproduce command verbatim for future experiments). Closed #2849 fern cosine eta_min=1e-6 as **4th stale_wip per Round 91/99/100/103/109 pod-failure close convention** (created 2026-05-14T07:27:52Z ~2h ago, 0 commits 0 comments beyond initial assignment, fern pod has stalled twice now #2794 Round 103 SwiGLU-preprocess + #2849 Round 112 = chronically unreliable pod harness, NOT experimental falsification; cosine eta_min=1e-6 axis UNTESTED not falsified — may re-assign to a different student in future round). Assigned #2876 fern **batch_size 4→8** (Lion-batch-size sensitivity test, minimally simple 1-line config change to maximize pickup probability for chronically-stall-prone pod; Chen 2023 Lion paper explicitly recommends larger batches "Lion needs larger batch sizes than AdamW; we use bs=4096 in most experiments" — at bs=4 our Lion may be under-batched for stable sign-step direction estimation; memory headroom available 14GB peak vs 96GB available; falsifiable WIN means Lion was under-batched try bs=16 next, WASH means bs=4 already sufficient close axis, LOSS means Keskar-style small-batch implicit regularization was OOD-load-bearing close upward direction; 89th candidate axis). Assigned #2875 tanjiro **Squared ReLU on SwiGLU up-projection** (Primer 2022 Hua et al. inspired NEW activation site distinct from gate-axis just-closed at SiLU; keep SiLU gate UNCHANGED proven WIN #2741 add `F.relu(W_up(x)).pow(2)` on up-projection which currently has NO activation; complementary sparsity-without-gating mechanism — gate selects which channels get re-weighted continuous-valued + up-activation selects which features non-zero hard-sparse; quadratic large-x amplification may help OOD via compact-feature-use forcing; falsifiable WIN means Primer-style up-activation breaks plateau try GELU² next, WASH means up-projection activation doesn't help close axis, LOSS means hard-zero regions in up-projection break SwiGLU mechanism try smoother alternatives; +0 params; bf16-overflow check requested in diagnostic logging; reproduce command explicitly canonical lr=1.5e-4 weight_decay=3e-4 epochs=70 not train.py defaults; 90th candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2864 askeladd hybrid-LN-RMSNorm, #2868 thorfinn smooth-L1-huber-β1, #2869 alphonse n_head=1, #2870 nezuko output-head-gain, #2873 edward post-norm, #2874 frieren slice_num=28, #2875 tanjiro squared-relu-up-proj, #2876 fern batch_size=8. Total closed taxa: 83 (82 + 1 new #2859). Total merged winners: 20. Plateau protocol still active 11 LOSSes — bold-swing candidates remaining: Differential Attention (Ye 2024), per-channel output gain if #2870 wins, β=0.5 follow-up if #2868 wins, post-norm-with-FiLM-protection if #2873 informative.)

- **As of:** 2026-05-14 [Round 111] UTC (round-111: 2 closures + 2 assignments. Plateau deepens to 10 consecutive LOSSes since #2810 merge. Closed #2860 edward gradient clipping max_norm=1.0 LOSS (val 32.2927 = +4.54% vs baseline 30.8909; test 27.3077 = +4.24% LOSS; all 4 val splits regress OOD-hit-hardest cruise +11.25% re_rand +4.24% camber_rc +4.03% in_dist +1.45% mildest — opposite predicted in_dist-hit-hardest; **CRITICAL DIAGNOSTIC**: `clip_fired_frac_train_epoch = 1.0000` for ALL 62 epochs = 100% clip-fire rate, pre-clip gradient norms 22-72× threshold throughout training mean never below ~23 max peaks at 248, cosine-tail LR ~6.6e-6 grad norm 22-25 still 20-25× threshold; mechanism re-interpretation: Lion's sign-step is per-step scale-invariant BUT Lion's exp_avg buffer accumulates magnitudes `exp_avg_t = β1·exp_avg_{t-1} + (1-β1)·grad_t` — with 100% clipping every step's contribution scaled by 1.0/||grad_t|| ERASES relative-magnitude signal between training steps so Lion's 'trust steps consistently large in same direction' property is destroyed, model learns noisiest features at same rate as most informative features flattening Lion's effective trajectory; mechanistically DISTINCT from a true 'spike-clipping doesn't help' finding — actual spike-clipping at max_norm 50-100 (mean+2σ) would be a separate sub-axis testing only rare 5-10% spikes; 87th taxon: gradient clipping max_norm=1.0 CLOSES; combined with closed EMA/SWA/Lookahead/SAM/Mixup/DropPath/WeightStandardization/DeepSupervision/GradientCentralization = **training-procedure/numerical-stability meta-family COMPREHENSIVELY MAPPED** on this codebase). Closed #2852 frieren slice_num 24→20 LOSS (val 32.0286 = +3.69% test 27.8076 = +6.16% LOSS; param 332,916 = -784 confirmed; best ep57/70 timeout-truncated; **IMPORTANT NOTE**: student's per-split deltas used wrong reference baselines [cruise 33.93 vs actual 16.84, in_dist 23.59 vs actual 25.28, camber_rc 32.36 vs actual 45.82, re_rand 33.81 vs actual 35.62], so "camber-asymmetry cruise WIN −45% camber_rc LOSS +47%" narrative was artifact; recomputed against correct #2810: all 4 splits LOSS in_dist 25.295 +0.08% nearly flat, camber_rc 47.495 +3.66% LOSS, camber_cruise 18.621 +10.56% WORST hit NOT a win, re_rand 36.703 +3.05% LOSS — real signal is uniform LOSS with cruise most-sensitive to slice reduction; the slice_num downward trajectory 64→32 WIN #1846 + 32→24 WIN #2307 + 24→20 LOSS #2852 closes downward direction at this magnitude; 88th taxon: slice_num downward axis CLOSES; SE block-3 gate stats and LayerScale γ readouts useful independently of baseline confusion; closing comment ALSO ASKED student to anchor split-comparisons to BASELINE.md per-split reference values for future reports). Assigned #2873 edward **post-norm structural pivot** (pre-LN `x = x + γ*attn(LN(x))` → post-LN `x = LN(x + γ*attn(x))` at ln_1 and ln_2 sites across all 4 blocks, ln_3 last_layer unchanged; +0 params; rationale Liu et al 2020 post-norm gives better final performance once stability solved, original Transformer used post-norm, modern pre-norm was for >30-layer deep models; LayerScale γ=1e-4 init stabilizes the transition since attn(x) near zero → LN(x + tiny·attn(x)) ≈ LN(x) at init; OOD-bias hypothesis post-norm activation calibration may help generalization to OOD distributions by preventing in-dist DC-offset memorization in residual stream similar to #2851 mean-centering diagnostic; failure modes FiLM at block0 ln_1 site feeds FiLM-modulated x directly to attn no-normalization in post-norm vs pre-norm normalizes FiLM-perturbed x — may damage FiLM conditioning; falsifiable WIN means post-norm activation calibration breaks plateau, WASH means pre/post-norm sweet-spot locally flat at 4 blocks close axis, LOSS means pre-norm load-bearing close post-norm direction with FiLM conditioning as likely failure mode; 87th candidate axis). Assigned #2874 frieren **slice_num=28** upward direction test (clean axis-direction test of NEVER-tested upward direction +4 slices vs baseline 24 +16.7% routing capacity ~+800 params ~334,500 total; rationale slice_num trajectory has ONLY been pushed downward 64→32→24 WIN-WIN then 24→20 LOSS so upward direction is genuinely untested; falsifiable WIN means upward headroom try slice_num=32 next, WASH means 24 is sweet spot locally flat close axis comprehensively, LOSS means 32→24 was correct direction close upward direction; assignment notes student about anchoring to BASELINE.md exact reference values to avoid #2852 baseline-confusion; 88th candidate axis). No human GH issues actionable. All 8 in-flight zero idle: #2849 fern cosine eta_min=1e-6, #2859 tanjiro MishGLU, #2864 askeladd hybrid-LN-RMSNorm, #2868 thorfinn smooth-L1-huber-β1, #2869 alphonse n_head=1, #2870 nezuko output-head-gain, #2873 edward post-norm, #2874 frieren slice_num=28. Total closed taxa: 82. Total merged winners: 20. Plateau protocol still active 10 LOSSes — bold-swing candidates remaining: Squared ReLU (Primer 2022) in MLPs, Differential Attention (Ye 2024), per-channel output gain if #2870 wins, β=0.5 follow-up if #2868 wins.)

- **As of:** 2026-05-14 [Round 110] UTC (round-110: 2 closures + 2 assignments. Plateau deepens to 8 consecutive LOSSes since #2810 merge. Closed #2858 nezuko PhysicsAttention temperature_init 0.5→1.0 CATASTROPHIC LOSS (val 40.8972 = +32.41% vs baseline 30.8909; test 34.7288 = +32.57% LOSS; all 4 val splits regress catastrophically in_dist +38.32% camber_rc +22.99% camber_cruise +52.88% re_rand +30.64% — slice routing entropy collapsed to one-hot 1.7e-23/0.018/0.0/1.019 across blocks 0-3; **CRITICAL DIAGNOSTIC**: per-block-per-head temperature trajectory revealed block-1 head-0 trained all the way to T=-1.88 by ep50 — **SIGN-FLIPPED negative temperature** negates softmax logits causing geometric routing inversion; mechanism re-interpretation: **T=0.5 init was load-bearing NOT for sharpness but as positive-basin attractor** — starting at T=1.0 lets optimizer freely cross zero into negative-T degenerate basin where 1/T→∞ amplifies gradients explosively, default-positive basin acts as implicit attractor; structural property of parameterization not hyperparameter choice; 85th taxon temperature_init upward direction CLOSES; reinforces #2828/#2848 thread slice attention is structurally one-hot-attractor with limited OOD headroom; combined with in-flight #2856 alphonse n_head=4 SAME launch LOSS — temperature/routing sub-family REINFORCES #2809 frieren OOD generalization-limited thesis; not pursuing student-suggested T=0.25 sharper init follow-up at this time moving nezuko to fresh axis). Closed #2856 alphonse PhysicsAttention n_head 2→4 LOSS (val 32.0367 = +3.71% vs baseline 30.8909; test 26.6328 = +1.66% LOSS; in_dist 27.3958 +8.39% WORST hit, cruise 18.0142 +6.96%, camber_rc 46.9870 +2.55%, re_rand 35.7499 +0.37% nearly flat; **CRITICAL ARCHITECTURAL INSIGHT**: param count 310,668 = -23,032 -6.9% NOT the predicted ~0 change because per-head Linear(dim_head, dim_head) construction in PhysicsAttention means doubling n_head 2→4 QUARTERS QKV params per block — so #2856 was actually a CAPACITY-REDUCTION test not head-parallelism test; capacity-bottleneck signature in_dist hit worst confirms attention rank is in-dist capacity-bottlenecked at H=2; per-head temperatures stayed tight in [0.42, 0.56] with per-block std 0.029-0.055 NO head specialization observed; block-3 output showed LEAST divergence std=0.029 suggesting heads don't specialize at deeper blocks; falsifies per-head-temperature-specialization WIN-scenario mechanism; reinforces #2828/#2848 thread slice attention structurally one-hot-attractor; 79th taxon n_head=4 with current dim_head=n_hidden/n_head construction CLOSES at this magnitude; combined with #2858 temperature-init upward sub-axis CLOSES = attention sub-family attention-rank-and-routing-sharpness comprehensively mapped). Assigned #2869 alphonse **n_head=1 dim_head=96** as student-suggested follow-up #2 natural falsification test (maximum per-head rank within current n_hidden=96 budget; doubles dim_head 48→96 single-head MAX-rank attention; expected ~415,000 params = +25%; falsifiable predictions WIN means attention rank capacity-bottlenecked at H=2 breaks plateau, WASH means tradeoff between rank-gain and head-parallelism-loss balanced close attention-rank axis, LOSS means multi-head parallelism was load-bearing; 85th candidate axis; NEW bar val < 30.8909). Assigned #2870 nezuko **output-head residual gain** learnable scalar at final decoder output init=1.0 — FRESH AXIS from Round 108 plateau bold-swing queue away from nezuko's just-failed attention/routing area (+1 param total; tests whether output magnitude is uncalibrated after LayerScale residual-stream changes; Lion sign-step normalizes within-block but cannot tune final output scale through loss; falsifiable WIN means head over/under-emphasized try per-channel gain next, WASH means optimizer didn't find non-trivial gain close 1-scalar direction, LOSS means destabilization close axis; 86th candidate axis; NEW bar val < 30.8909). No human GH issues actionable. All 8 in-flight zero idle: #2849 fern cosine eta_min=1e-6, #2852 frieren slice_num=20, #2859 tanjiro MishGLU, #2860 edward grad_clip=1.0, #2864 askeladd hybrid-LN-RMSNorm, #2868 thorfinn smooth-L1-huber-β1, #2869 alphonse n_head=1, #2870 nezuko output-head-gain. Total closed taxa: 80 (78 + 2 new closures #2858 #2856). Total merged winners: 20. Plateau protocol still active 8 LOSSes since #2810 — bold-swing candidates remaining if plateau persists: Squared ReLU (Primer 2022) in MLPs, Differential Attention (Ye 2024), post-norm structural pivot, per-channel output gain follow-up if #2870 wins, β=0.5 follow-up if #2868 wins.)

- **As of:** 2026-05-14 [Round 109] UTC (round-109: 1 stale-close + 1 reassignment. Closed #2827 thorfinn LayerScale γ-init=1e-3 as 3rd stale_wip per Round 91/99/100/103 pod-failure close convention (created 2026-05-14T06:47:08Z ~7h+ ago, 0 commits 0 comments beyond initial assignment commit, thorfinn pod persistently failing to poll/pick up the experiment = likely GitHub API rate limit or pod harness failure mode NOT experimental falsification; LayerScale γ-init=1e-3 axis UNTESTED not falsified — may re-assign to a different student in a future round). Assigned #2868 thorfinn **Smooth L1 (Huber) loss-shape pivot at β=1.0** for BOTH volume and surface training loss while keeping validation/test metrics as L1 MAE for direct baseline comparison; surf_weight=10 unchanged; loss-shape axis (training loss SHAPE not weighting) NEVER tested this launch since the baseline was set; mechanism: Smooth L1 has smooth gradient through origin (quadratic for |r|<β, linear for |r|≥β) vs L1's discontinuous ±1 sign at 0 — may improve cosine-tail convergence where loss landscape is shallow and Lion's sign-step benefits from richer near-zero gradient structure that exp_avg integrates over time; OOD outlier behavior unchanged (large residuals identical to L1); critical reporting clarity in assignment that students MUST keep val/test MAE for direct baseline comparison; 84th candidate axis; +0 params; 3 falsifiable predictions WIN (val<30.8909) means smooth-near-zero matters → try β=0.5 next, WASH means residual distribution bulk-in-linear-regime → try β=0.1 next, LOSS (val>30.8909+1%) means small-residual smoothing hurts Lion → loss-shape axis CLOSES at this magnitude; NEW bar val < 30.8909. No human GH issues actionable. All 8 in-flight zero idle: #2849 fern cosine eta_min=1e-6, #2852 frieren slice_num=20, #2856 alphonse n_head=4, #2858 nezuko temperature_init=1.0, #2859 tanjiro MishGLU, #2860 edward grad_clip=1.0, #2864 askeladd hybrid-LN-RMSNorm, #2868 thorfinn smooth-L1-huber-β1. Total closed taxa: 78 (#2827 closed as stall not falsified, axis count unchanged). Total merged winners: 20. Plateau protocol active 7 LOSSes since #2810 — strategy continues with first loss-shape pivot in launch + 6 in-flight attention/numerical-stability/activation/normalization-feature sub-axes from rounds 104-108.)

- **As of:** 2026-05-14 [Round 108] UTC (round-108: 1 closure + 1 assignment. Plateau deepens to 7 consecutive LOSSes since #2810 merge. Closed #2851 askeladd RMSNorm at all 9 LayerNorm sites LOSS (val 31.4066 = +1.67% vs new baseline 30.8909; test 27.1997 = +3.83% LOSS; param count 332,836 ✓ exact predicted -864 from β removal; total epochs 67/70 timeout-truncated; sec/epoch ~27s unchanged — predicted 2-5% speedup did NOT materialize, 96-elem β removal in the noise; **striking in-dist WIN + OOD LOSS divergence**: val_single_in_dist 24.2105 = **−4.21% WIN BEST-EVER in-dist edging past #2810's 25.27 AND #2765's 24.97**, but val_geom_camber_rc +3.71%, val_geom_camber_cruise +5.58%, val_re_rand +1.39% = exact OPPOSITE of recent merge-pattern OOD-positive wins #2810/#2765/#2727/#2614; **exemplary student 3-mechanism diagnostic**: (1) γ-evolution pattern reveals 8/9 RMSNorm sites have γ ≈ 1.000 ± 0.002 essentially untouched, ONLY block0_ln1 immediately post-FiLM-injection shows learned modulation γ mean 0.91 std 0.098 range [0.625, 1.135] — LayerScale γ_attn~1e-3/γ_mlp~1e-1 makes residual stream near-identity → gradient signal back through norms is small/uniform → RMSNorm's diagonal γ has nothing to push against → **β was the load-bearing degree of freedom for 8/9 sites**, β provides per-channel bias that can shift residual stream irrespective of pre-norm magnitude, (2) **mean-centering and β are load-bearing for OOD generalization**: slice attention is permutation-invariant so per-token mean carries position-like info; mean-centering forces relative-channel-structure encoding useful for OOD, without it the network memorizes in-dist DC offset as extra discriminator → in-dist gain + OOD damage, (3) **LLaMA/T5/Mistral RMSNorm prior does not transfer**: those are causal LLMs with absolute/rotary positional embeddings where per-token mean carries no inductive load — Transolver slice attention is the only structure that can route per-mesh-node info, per-token mean IS the location signal; **78th taxon: RMSNorm-at-all-sites CLOSES**; combined with closed DyT #2686, embed-LN replacement #2808, surface-aware-LN #2829, QK-Norm, GroupNorm = normalization-axis comprehensively mapped at the SITE level, LN-feature-decomposition (mean-centering vs γ vs β) partially resolved: β is load-bearing). Assigned #2864 askeladd **hybrid LN-at-block0_ln1 + RMSNorm-at-8-other-sites** as direct architectural conversion of student's own diagnostic — keep full LayerNorm at the ONE site where γ showed active learning post-FiLM (block 0 ln_1 captures FiLM γ_c(Re,AoA) per-channel bias content via its own β), strip β at the 8 inactive-γ sites; expected param count 332,932 = baseline 333,700 − 768 (8 × 96); 3 falsifiable predictions: WIN means 8 inactive-γ sites genuinely don't need β save 768 params for free; WASH same conclusion with smaller margin; LOSS similar to #2851 means β is universally load-bearing regardless of γ activity at site = normalization-axis comprehensively closed; +0 new module code reuses RMSNorm class; 83rd candidate axis; NEW bar val < 30.8909. No human GH issues actionable. All 8 in-flight zero idle: #2827 thorfinn layerscale_init=1e-3, #2849 fern cosine eta_min=1e-6, #2852 frieren slice_num=20, #2856 alphonse n_head=4, #2858 nezuko temperature_init=1.0, #2859 tanjiro MishGLU, #2860 edward grad_clip=1.0, #2864 askeladd hybrid-LN-RMSNorm. Total closed taxa: 78. Total merged winners: 20. Plateau protocol still active 7 LOSSes since #2810 — next bold-swing candidates if plateau persists: Squared ReLU (Primer 2022) in MLPs, Differential Attention (Ye 2024), output-head residual gain, post-norm structural pivot, Huber/SmoothL1 loss-shape pivot.)

- **As of:** 2026-05-14 [Round 107] UTC (round-107: 2 closures + 2 assignments. PLATEAU CONFIRMED: 6 consecutive LOSSes since #2810 merge. Closed #2848 tanjiro SE bottleneck post-GELU Dropout p=0.1 LOSS (val 31.2952 = +1.31% vs new baseline 30.8909; test 26.4491 = +0.96% LOSS; in_dist 26.2293 +3.78% WORST hit OPPOSITE of regularization-helps-OOD prediction, camber_rc +2.15%, camber_cruise +1.10%, re_rand only WIN -1.40%; **gelu_zero_frac collapsed to 0.0 across all splits** vs expected 16-26% from #2759 — FALSIFIES implicit-sparsification mechanism reading of #2814 SE GELU→SiLU LOSS; optimizer pushed pre-act distribution into GELU's positive regime to recover signal-to-noise from dropout noise; SE gate selectivity COLLAPSED gate_std 0.20-0.34 → 0.045-0.132 with gate_min 0.001-0.02 → 0.10-0.38 — gate became near-uniformly half-open at ~0.5 doing almost no channel selection; dropout zeroing fc1→fc2 paths during training penalizes sharp channel-conditional gates → optimizer converges to soft robust near-identity gate; re-interpretation of #2814 is 'GELU's smoother gradient near zero matters' OR 'gate range distribution shifts under SiLU' NOT 'GELU 16-26% sparsity is useful'; 76th taxon: SE bottleneck channel dropout CLOSES; combined with closed DropPath #2722 + attention dropout #2828 = stochastic-channel-zeroing meta-axis 3-direction CLOSED across all major architectural sites). Closed #2820 edward EMA-of-weights decay=0.9999 LOSS (val 32.8847 = +6.46% LOSS vs new baseline 30.8909 — note PR was scoped against old baseline 31.3216 = +4.99% LOSS; test 27.4119 = +4.66% LOSS vs new baseline 26.1964; ALL 4 val splits regress; **mechanism worked perfectly** — EMA descended monotonically from ep7, lapped base by ep~25 with gap up to −27 at high-LR mid-training shrinking to −0.5/−1.7 at cosine endgame ep50-60, EMA val_avg ema−base = −0.16 and test ema−base = **−0.60 on all 4 splits** = stronger than val, EMA-best ep60 vs base-best ep58 = +2 epochs later AS PREDICTED; **CRITICAL: EMA-base L2 drift 2.60%** vs predicted 5-15% — Lion + cosine + this model size + 70 epochs already produces relatively flat solution so EMA headroom STRUCTURALLY BOUNDED; training-budget cost killed it: each epoch ~30s vs baseline ~28s due to double-validation passes (base + EMA) = 4 lost epochs, baseline reached 31.32 at ep64 you capped at ep60 with val_ema still descending toward ~32.0-32.5; 77th taxon: EMA-of-weights at decay=0.9999 CLOSES; combined with closed SWA #2567 28th + Lookahead #2740 55th + SAM #2761 60th + Mixup #2687 + DropPath #2722 + Weight Standardization #2739 + Deep Supervision #2778 + Gradient Centralization #2710 + prior closed EMA #2735 (different decay) = **training-procedure / weight-averaging / flat-minima-escape meta-family COMPREHENSIVELY MAPPED** on this codebase). Assigned #2859 tanjiro MishGLU SwiGLU gate SiLU → Mish (1-line act_fn=F.silu → F.mish in TransolverBlock construction line ~213; +0 params; 3rd direction in gate-activation sub-axis: GeGLU closed #2759 LOSS, SwiGLU merged #2741 WIN, MishGLU untested; Mish = x*tanh(softplus(x)) is smoother than SiLU asymmetric small bump at negative inputs; structurally distinct from in-flight n_head=4 #2856 + temperature_init=1.0 #2858 + slice_num=20 #2852 + RMSNorm #2851 + cosine eta_min #2849 + layerscale_init=1e-3 #2827; 81st candidate axis; NEW bar val < 30.8909). Assigned #2860 edward gradient clipping max_norm=1.0 (1-line addition torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0) after loss.backward() before optimizer.step(); +0 params; numerical-stability sub-axis within training-procedure family NEVER tested this launch; distinct from EMA/SWA/Lookahead/SAM/Mixup/DropPath/WeightStandardization/DeepSupervision/GradientCentralization closed family; Lion sign-step makes gradient magnitude supposedly irrelevant but exp_avg buffer accumulates magnitudes so rare gradient spikes can perturb exp_avg sign on subsequent steps; your #2820 EMA diagnostic 2.6% L2 drift confirms Lion+cosine produces flat trajectory; clipping may further smooth by removing rare gradient outliers; +0 params; 82nd candidate axis; NEW bar val < 30.8909). No human GH issues actionable. All 8 in-flight zero idle: #2827 thorfinn layerscale_init=1e-3, #2849 fern cosine eta_min=1e-6, #2851 askeladd RMSNorm, #2852 frieren slice_num=20, #2856 alphonse n_head=4, #2858 nezuko temperature_init=1.0, #2859 tanjiro MishGLU, #2860 edward grad_clip=1.0. Total closed taxa: 77. Total merged winners: 20. STRATEGY-TIER PIVOT continues from loss-weighting/FiLM/SE-internals to attention sub-axes + numerical stability + alternative activation directions. Plateau protocol active: 6 consecutive LOSSes maps where NOT to look — bold structural shifts coming as more in-flight results land.)

- **As of:** 2026-05-14 [Round 106] UTC (round-106: 2 closures + 2 assignments. Closed #2844 nezuko block-3-only FiLM LOSS (val 30.9665 = +0.245% vs new baseline 30.8909; test 26.4983 = +1.15% LOSS; in_dist 24.8407 NEW BEST-EVER -1.72% WIN edging past PR #2765's 24.97, re_rand 35.0787 -1.51% WIN; camber_rc 47.0321 +2.65% LOSS, camber_cruise 16.9145 +0.43% LOSS; FiLM-b3 head DID learn substantive correction weight_norm 3.10 bias_norm 0.77 |γ|=0.16-0.26 = 3-4× embedding FiLM but |γ| anti-correlated with OOD usefulness — camber_cruise largest |γ|=0.258 LOSSES; key insight: block 3 over-allocated with 5 modulators γ_attn+γ_mlp+SE-gate+residual+FiLM-b3, NO normalization downstream before mlp2, optimizer finds in_dist-fitting config that breaks camber_rc; contrast with merged embedding FiLM #2614 which lives BEFORE block-0 with 3 further blocks of LN+mixing regularizing the modulation; 74th taxon: FiLM-axis CLOSES across both sub-directions per-block #2813 4-way LOSS and block-3-only #2844 LOSS — merged embedding FiLM #2614 remains UNIQUE WINNER placement BEFORE the stack with downstream regularization is the only generalizing FiLM site). Closed #2842 alphonse surf_weight=15 LOSS (val 31.3919 = +1.62% vs new baseline 30.8909; test 26.6495 = +1.73% LOSS; exemplary split-decomposition: 3 of 4 splits WIN in_dist 25.0772 -0.20, camber_cruise 16.2802 -0.56, re_rand 35.0345 -0.58 but camber_rc 49.1755 +3.36 +7.32% catastrophic LOSS drags average; clean illustration of loss-weighting bias/variance trade — heavier surface weight reduces bias on bulk distribution amplifies variance on tails; 75th taxon: surf_weight UPWARD axis CLOSES at this magnitude 10→15; surf_weight=10 baseline at-or-near optimum from upward direction; student's suggested follow-ups surf_weight {11,12,13} DEFERRED — plateau-protocol favors fresh structural axes over loss-weighting fine-tuning). Assigned #2856 alphonse n_head 2→4 (attention parallelism sub-axis NEVER tested this launch; n_hidden=96 stays so dim_head halves 48→24 with auto-derivation; +0 params net Q/K/V/O Linears shape stable; could enable per-head specialization on different physics scales boundary-layer vs wake or surface vs volume routing patterns; slice-routing softmax already runs per-head with learnable per-head temperature so 4 heads = 4 independent routing channels; risk: dim_head=24 may be below typical Transformer min effective rank; 79th candidate axis; NEW bar val < 30.8909). Assigned #2858 nezuko PhysicsAttention slice-routing temperature init 0.5→1.0 (init value of existing learnable per-head per-block temperature parameter from 0.5 to 1.0 — 1-character change; current init=0.5 makes effective softmax input 2× sharper than default; init=1.0 = natural scale less sharp at init; tests whether sharper-than-default routing was correct inductive bias or design choice never swept; distinct from in-flight #2852 frieren slice_num=20 which is COUNT not SHARPNESS — orthogonal sub-axes within slice routing; +0 params; 80th candidate axis; NEW bar val < 30.8909). No human GH issues actionable. All 8 in-flight zero idle: #2820 edward EMA, #2827 thorfinn layerscale_init=1e-3, #2848 tanjiro SE bottleneck dropout, #2849 fern cosine eta_min=1e-6, #2851 askeladd RMSNorm, #2852 frieren slice_num=20, #2856 alphonse n_head=4, #2858 nezuko temperature_init=1.0. Total closed taxa: 75. Total merged winners: 20. STRATEGY-TIER PIVOT in effect since #2810 merge — moving from loss-weighting / FiLM tweaks to attention structural sub-axes for plateau escape.)

- **As of:** 2026-05-14 [Round 105] UTC (round-105: 1 closure + 1 assignment. Closed #2829 frieren surface-aware LayerNorm at decoder ln_3 LOSS (val 33.4647 = +6.85% vs old baseline 31.3216; +8.34% vs new baseline 30.8909; test 28.0942 = +5.99% LOSS; all 4 val splits regress 5.6-9.2% — no split benefits from the surface/volume normalization split; param count 333,795 = +95 net vs baseline 333,700 (added ln_surf 192 params); best ep66 timeout-truncated at 30-min cap, linear extrapolation suggests ~33.3 even at full 70 epochs = solid LOSS; mechanism per student diagnostic: γ vectors DID diverge as predicted L2(γ_surf − γ_vol)=2.67 cos(γ_surf, γ_vol)=0.97 — both directions similar but magnitudes differ; CRITICAL FAILURE: γ_surf std=0.0715 is 3.4× SMALLER than γ_vol std=0.2449 — γ_surf is UNDER-TRAINED because surface tokens are ~8% of mesh = 12× less gradient signal per parameter; the very split that was supposed to help mae_surf_p ended up giving the surface-pressure-relevant normalization a noisy under-fit γ vector; insight: param-doubling at a normalization site only helps when both sub-populations receive comparable gradient mass — token-frequency imbalance defeats the structural split; reinforces #2809 frieren "generalization-limited not capacity-limited" thesis — adding surface-specific capacity at LN-level cannot rescue under-gradient training; 73rd taxon: surface-aware LayerNorm CLOSES — combined with closed embed-LN #2808 (replacement), DyT #2686 (all-or-nothing replacement), RMSNorm in-flight #2851 (parameter-stripping replacement) — token-population-split sub-axis of normalization meta-axis CLOSES; surface-token specialization at intermediate-layer norm fails due to gradient-frequency asymmetry not absence of structural utility). Assigned #2852 frieren slice_num=20 (1-line model_config change slice_num=24→20; +0 params; continues slice-routing-quality axis with two prior wins #1846 slice_num 64→32 WIN + #2307 slice_num 32→24 WIN; Round 104 student diagnostic #2828 confirmed slice attention is information-routing not feature-extraction so each slice carries unique information — question is whether all 24 are equally necessary or 20 is sufficient and forces better generalization; model is generalization-limited (frieren #2809) not capacity-limited so trimming slice routing pathways may help; 78th candidate axis; NEW bar val < 30.8909). No human GH issues actionable. All 8 in-flight zero idle: #2820 edward EMA, #2827 thorfinn layerscale-init-1e-3, #2842 alphonse surf_weight=15, #2844 nezuko block-3-only FiLM, #2848 tanjiro SE bottleneck dropout, #2849 fern cosine eta_min=1e-6, #2851 askeladd RMSNorm, #2852 frieren slice_num=20. Total closed taxa: 73. Total merged winners: 20.)

- **As of:** 2026-05-14 [Round 104] UTC (round-104: 1 closure + 1 assignment. Closed #2828 askeladd attention dropout p=0.05 LOSS (val 32.5009 = +3.77% vs old baseline 31.3216; +5.21% vs new baseline 30.8909; test 27.9455 = +5.43% LOSS; all 4 val splits regress with in_dist +5.18% and cruise +7.10% hit hardest = exact OPPOSITE of stochastic-regularization hypothesis; hit timeout ep58, 6 epochs short of baseline best ep64, linear extrapolation ~31-31.5 still LOSS; dropout diagnostics CONFIRMED wiring correct: attn_out_var_train ≈ 2× attn_out_var_eval throughout, all 4 PhysicsAttention blocks asserted dropout.p=0.05 at startup, train/eval mode flag flips correctly; student exemplary 3-mechanism analysis: (1) slice attention with G=24 slice tokens is information-routing not redundant feature-extraction, dropping 5% of 24-edge attention probabilities corrupts slice-to-slice routing non-redundantly unlike ViT/BERT, (2) damage pattern in_dist+5.18% > OOD splits falsifies over-regularization — model is generalization-limited via STRUCTURAL bottleneck consistent with frieren #2809 diagnosis, (3) to_out Dropout fights LayerScale γ=1e-4 near-identity init = same failure mode as closed DropPath #2722 just finer granularity; 72nd taxon: attention dropout (both attn probabilities + to_out projection) CLOSES — combined with DropPath #2722 dropout-axis well-mapped — STRATEGIC PIVOT from regularization to structural inductive bias changes). Assigned #2851 askeladd RMSNorm replacement of LayerNorm at all 9 sites in TransolverBlock (ln_1+ln_2 per block × 4 blocks + ln_3 in last_layer = 9 sites; Zhang & Sennrich 2019 / LLaMA / T5 / Mistral / PaLM-2 converged standard; removes mean centering and β bias; -864 params = 332,836 total; structural change in askeladd's normalization specialty axis distinct from closed embed-LN/DyT/QK-Norm/GroupNorm and from in-flight surface-aware LN #2829 frieren; β removal cleans up LayerScale near-identity residual stream where β is mostly redundant; slice attention may need preserved per-token scale baseline that mean centering discards; 77th candidate axis; NEW bar val < 30.8909). No human GH issues actionable. All 8 in-flight zero idle: #2820 edward EMA, #2827 thorfinn layerscale-init-1e-3, #2829 frieren surface-aware ln3, #2842 alphonse surf_weight=15, #2844 nezuko block-3-only FiLM, #2848 tanjiro SE bottleneck dropout, #2849 fern cosine eta_min=1e-6, #2851 askeladd RMSNorm. Total closed taxa: 72. Total merged winners: 20.)

- **As of:** 2026-05-14 [Round 103] UTC (round-103: 1 stale-close + 1 reassignment. Closed #2794 fern SwiGLU-preprocess 2nd stale_wip per Round 91/99/100 pod-failure close convention (created 2026-05-14T05:08:32Z ~4h+ ago, 0 commits 0 comments beyond assignment commit, fern pod alive 1/1 but persistently failing to poll = likely GitHub API rate limit symptom, axis UNTESTED not falsified, may re-assign to different student in future round). Assigned #2849 fern cosine eta_min=1e-6 (single keyword argument addition to CosineAnnealingLR call at train.py line ~652; +0 params; +0 structural changes; maximally simple to maximize pickup probability for stall-prone pod; well-motivated by Chen 2023 Lion paper observation that Lion's sign-step lacks magnitude adaptation and benefits from non-zero minimum LR; baseline's best_ep=64/65 with LR≈3e-6 in cosine tail = effectively zero updates for Lion sign-step in final 5-6 epochs; eta_min=1e-6 = 0.67% of peak 1.5e-4 keeps Lion fine-tuning at cosine tail; NEVER swept this launch; 76th candidate axis; NEW bar val < 30.8909). No human GH issues actionable. All 8 in-flight zero idle: #2820 edward EMA, #2828 askeladd attention dropout, #2827 thorfinn layerscale-init-1e-3, #2829 frieren surface-aware ln3, #2842 alphonse surf_weight=15, #2844 nezuko block-3-only FiLM, #2848 tanjiro SE bottleneck dropout, #2849 fern cosine eta_min=1e-6. Total closed taxa: 71. Total merged winners: 20.)

- **As of:** 2026-05-14 [Round 102] UTC (round-102: 1 closure + 1 assignment + 1 leave-stale. Closed #2814 tanjiro SE GELU→SiLU inner activation LOSS (val 32.3147 = +3.17% vs old baseline 31.3216; +4.61% vs new baseline 30.8909; test 26.8163 = +1.17% LOSS; all 4 val splits regress; exemplary mechanism diagnostic: silu_zero_frac=0.0 across all 4 splits CONFIRMED SiLU eliminates GELU hard-masking completely as predicted, but the elimination LOSSed downstream metrics — implying GELU hard-masking at SE bottleneck is BENEFICIAL implicit sparsification not wasted capacity; pre_act_std 1.4-3.0 symmetric distribution with negative_frac 0.46-0.51 = ~half of 24 bottleneck channels output small negative SiLU values destabilizing near-veto OOD gating; SE gate_std 0.18-0.30 slightly higher gate_min 0.0004-0.026 preserved low; structurally distinct from MLP-body GeGLU #2759 LOSS — gated multiplicative vs 2-layer encoder feeding sigmoid gate; 71st taxon: SE bottleneck inner activation axis CLOSES at GELU; full SE axis now mapped width r=4 merged #2765 + inner activation GELU + pool function attn-pool merged #2810). Assigned #2848 tanjiro SE bottleneck post-GELU Dropout p=0.1 (insert nn.Dropout(p=0.1) between F.gelu(self.fc1(pool)) and self.fc2 in SqueezeExcitation; +0 params; directly tests implicit-sparsification hypothesis from #2814 diagnostic; if GELU hard-masking is beneficial regularization explicit dropout will stack/be-redundant; if NOT operative dropout will regress; structurally distinct from in-flight attention dropout #2828 = attention probabilities INSIDE sdpa softmax, this targets SE bottleneck channels; 75th candidate axis; NEW bar val < 30.8909). Left #2794 fern SwiGLU-preprocess as-is 1st stale_wip (created 2026-05-14T05:08:32Z ~2h ago, 0 commits 0 comments, fern pod likely hitting GitHub API rate limits per Round 91 #2728 + Round 99 #2780 convention 1st stale = leave for one more round). No human GH issues actionable (only old #1569 data bug fixed via #1532 workaround on advisor branch, and Willow #257 not Charlie). All 8 in-flight: #2820 edward EMA, #2828 askeladd attention dropout, #2827 thorfinn layerscale-init-1e-3, #2829 frieren surface-aware ln3, #2842 alphonse surf_weight=15, #2844 nezuko block-3-only FiLM, #2848 tanjiro SE bottleneck dropout, #2794 fern SwiGLU-preprocess [stale 1st]. Zero idle. Total closed taxa: 71. Total merged winners: 20.)

- **As of:** 2026-05-14 [Round 101] UTC (round-101: 1 merge + 1 closure + 2 assignments. MERGED #2810 alphonse SE block-3-only attn-pool WIN (val 30.8909 = −1.37% vs baseline 31.3216; test 26.1964 = −1.17%; camber_rc −2.49% WIN, camber_cruise −4.92% WIN; gate_std +13-19% all splits; mechanism: attn-pool near-uniform weights but perturbation of SE bottleneck dynamics lifts gate_std across splits; 20th merged winner; NEW baseline val 30.8909 / test 26.1964 / params 333,700). Closed #2813 nezuko per-block FiLM LOSS (val 31.5747 = +0.81% vs baseline 31.3216; test 25.7718 = −2.77%; in_dist val +1.68 decisive failure; depth-progressive film_scale_abs_mean confirmed 0.0762→0.1118 block 0→3 +47%; mechanism: over-conditioning intermediate representations with Re/AoA scalars at all 4 blocks hurts in_dist via intermediate-representation over-specialization; test improves because test distribution benefits from conditioning but in_dist on constrained training distribution pays the cost; 70th taxon: per-block FiLM 4-way CLOSES; block-3-only FiLM is the motivated follow-up — targeted final-output conditioning without intermediate corruption). Assigned #2844 nezuko block-3-only FiLM (single nn.Linear(3, 96) zero-init after block-3 SE gate, before mlp2 decoder; +384 params = 334,084 total; directly addresses per-block FiLM diagnostic: block 3 is the correct site for Re/AoA conditioning (SE active, deepest, γ_attn sign-flip observed); targets re_rand and camber OOD splits; 73rd candidate axis; NEW bar val < 30.8909). Assigned #2842 alphonse surf_weight=15.0 (single hyperparameter change surf_weight 10.0→15.0; surf_weight NEVER swept this launch since early rounds with MSE/Huber/AdamW; +0 params; hypothesis: stronger surface pressure supervision directly targets primary metric with current better-capacity model; 74th candidate axis; NEW bar val < 30.8909). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 70. Total merged winners: 20.)

- **As of:** 2026-05-14 [Round 100] UTC (round-100: 2 closures + 1 stale-close + 3 assignments. Closed #2808 askeladd pre-block-0 embed-ln LOSS (val 32.4969 = +3.75% vs baseline 31.3216; test 28.5529 = +7.72% LOSS; in_dist marginally better −0.53 but 3 OOD splits regress; smoking-gun diagnostic: input_var=0.9737 at pre-LN site — ALREADY at unit variance before LN! preprocess+placeholder+zero-init-FiLM stack leaves post-FiLM stream at 0.97; LN weight mean=0.9931 std=0.0705 barely drifted from identity init — LN is effectively inert; +192 params act as optimization noise; 68th taxon: LN-site insertion at post-FiLM-pre-block-0 CLOSES — empirically falsified by diagnostic not hyperparameter accident; normalization-meta-axis now mapped REPLACEMENT+GRANULARITY+SITE-INSERTION). Closed #2809 frieren mlp2-3layer LOSS (val 32.9390 = +5.17% vs baseline 31.3216; test 27.3432 = +3.16% LOSS; only in_dist improves -2.15% val/-0.52% test; ALL 3 OOD splits regress 4-8%; mechanism: decoder-depth adds non-residual capacity that gets allocated to in-distribution surface-pressure pattern matching (decoder lacks LayerScale γ residual bound); γ_attn_block3=0.00561 and γ_mlp_block3=0.0802 did NOT collapse — decoder did not absorb block-3 work, both encoder+decoder fight; frieren's key insight: "model is GENERALIZATION-LIMITED not capacity-limited" — probes adding OOD generalization without capacity are higher-yield; 69th taxon: decoder-depth saturated at 2 layers; combined with stale #2750 fern mlp2-wide192, decoder-depth axis mapped). Closed #2780 thorfinn Mish-mlp2 2nd stale_wip (created 2026-05-14T04:22:02Z, ~2h17m, 0 commits, 0 comments, pod alive 1/1 but hitting GitHub API rate limits per heartbeat logs; 2nd consecutive stale flag = pod-failure close per Round 91 #2728 convention; Mish-mlp2 axis remains UNTESTED not falsified). Assigned #2829 frieren surface-aware LayerNorm at decoder ln_3 (SurfaceAwareLN class: ln_3_surf+ln_3_vol, select per-token via is_surface mask, +192 params = 333,795 total; targets frieren's own insight "generalization-limited not capacity-limited" — gives surface tokens separate normalization scale from volume tokens at the decoder entry point; surface tokens ~8% of mesh nodes get their own γ_surf/β_surf uncoupled from dominant volume statistics; structurally distinct from closed normalization-replacement/granularity/site-insertion axes — SAME site as baseline ln_3 but SPLIT by token type; thread is_surface through data dict at 2 model-call sites + Transolver.forward + TransolverBlock.forward; 70th candidate axis; NEW bar val < 31.3216). Assigned #2828 askeladd attention dropout p=0.05 (set Transolver dropout kwarg from 0 to 0.05; propagates to F.scaled_dot_product_attention dropout_p AND nn.Dropout in to_out; standard transformer attention-probability regularization NEVER tested this launch; +0 params; targets generalization-limited finding — stochastic regularization via attention edges is orthogonal to DropPath residual zeroing; DropPath #2722 (p=0.1 CATASTROPHIC) broke LayerScale γ=1e-4 residual identity; attention dropout is INSIDE attn after softmax so residual stream identity preserved; 71st candidate axis; NEW bar val < 31.3216). Assigned #2827 thorfinn LayerScale γ init=1e-3 (1-character change layerscale_init=1e-4→1e-3 in TransolverBlock kwarg; final |γ| at convergence 0.005-0.08 = 50-800× init at 1e-4; DeiT-III/ViT-22B/ConvNeXt-V2 recommend 1e-3 for non-deep stacks; tests whether γ=1e-4 wastes optimizer budget on γ-growth phase; +0 params; maximally simple for stall-prone pod; 72nd candidate axis; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 69. Total merged winners: 19.)

- **As of:** 2026-05-14 [Round 99] UTC (round-99: 1 closure + 1 assignment. Closed #2799 edward long-range preprocess→block-3 skip LOSS (val 31.2873 = −0.11% vs baseline 31.3216 TECHNICALLY within threshold but BELOW NOISE FLOOR; test 27.0123 = +1.91% LOSS on paper-facing metric; per-split val: in_dist 24.0608 −3.65% WIN new best-ever, rc 46.2634 −1.54% WIN, cruise 17.1796 −3.09% WIN, re_rand 37.6451 +5.75% CATASTROPHIC LOSS; 3-of-4 val splits WIN but re_rand tanks decisively; smoking-gun mechanism: SE block-3 gate_std drops 8-14% on EVERY split vs baseline #2765 — skip gate fully activated abs_mean=0.057 96/96 channels nonzero ~5% skip/residual ratio, skip and SE compete for same per-channel modulation capacity at block 3; skip gate mixed signs 45+/51− confirming per-channel directionality; block-3 γ_attn flipped from strongly-negative-mean (block-3 SE signature in #2765) to near-zero — skip and γ_attn sharing modulation work; re_rand was the split with HIGHEST baseline SE gate_std (0.275) so it pays the biggest cost when SE loses variance to skip (+5.75%); val improvement is 0.034 absolute on 31.32 baseline = well below ~0.5% run-to-run noise floor; test regression is real and paper-facing; 67th taxon: long-range residual skip at SE-gated final-block input CLOSES — block 3 is over-allocated with 4 per-channel mechanisms (LayerScale γ, SE gate, FiLM residual, skip_gate); adding a fourth creates zero-sum trade). Left PR #2780 thorfinn Mish-mlp2 as-is (status:wip, created today 04:22, 0 comments — pod alive 1/1 but hitting GitHub API rate limits per logs, NOT abandoned; will pick up when rate limits reset). Assigned #2820 edward EMA-of-weights regularization (Polyak 1992/Izmailov 2018 SWA; maintain shadow state dict of float param tensors, update each optimizer.step() as θ_ema = 0.9999·θ_ema + 0.0001·θ_train; eval EMA model at every val pass; best-val checkpoint and test evaluated from EMA weights; +0 trainable params, ~5MB RAM shadow state; effective averaging window 10,000 steps ≈ 40 epochs covering post-warmup cosine-decay phase; zero architecture change; STRATEGY-TIER SHIFT per plateau protocol: rounds 95-99 consecutive non-improvements; moving from block-level architecture tweaks to training-procedure axis; Lion optimizer known to find sharp minima vs AdamW (Chen 2023); EMA is canonical antidote to optimizer-induced sharpness; predicted WIN: EMA val_avg 1-3% below baseline esp. on OOD splits (re_rand, camber*); predicted WASH: Lion+cosine trajectory already smooth at this scale; LOSS highly unlikely; edward's EMA prior at Round 90 #2735 used decay=0.9995 and found EMA-vs-raw ~-0.6% relative improvement with drift_ratio peaking ep5 then monotone decay — that run ALSO showed EMA-best was real but overshadowed by SAM 2x-compute budget halving; current assignment uses SAME decay=0.9999 suggestion from student diagnostic (drift_ratio collapsed at ep5 in prior run, suggested higher decay = longer window) + no SAM competing for budget; structurally distinct from closed SWA #2567 (batch-wise averaging, fixed LR phase) + closed Lookahead #2740 (outer slow-step, Lion-incompatible) + student's prior closed EMA #2735 (different decay, overshadowed); 69th candidate axis; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 67. Total merged winners: 19.)

- **As of:** 2026-05-14 [Round 98] UTC (round-98: 1 closure + 1 assignment. Closed #2798 tanjiro SE reduction=2 LOSS (val 32.1229 = +2.56% vs new baseline 31.3216 closes axis per program.md; test 26.3402 = −0.63% improved interesting test-val divergence; per-split val: in_dist 23.3693 −6.42% WIN new best-ever, camber_rc 50.0688 +6.55% LOSS, camber_cruise 17.8757 +0.84% LOSS, re_rand 37.1777 +4.44% LOSS; mechanism per student exemplary diagnostic: gate_std rose monotonically r=8→r=4→r=2 +9-16% over r=4 = more bottleneck capacity gives finer discrimination AS PREDICTED, gate_min REVERSED direction r=4→r=2 on all 4 splits = aggressive veto pattern lost AS PREDICTED for LOSS scenario, block 3 γ_attn flipped back to POSITIVE +0.001401 vs r=4 negative = SE loosened attention re-engages, SE moved from "discriminator + sharp veto" mode to "averager + scaling" mode; 66th taxon: SE bottleneck dimension axis now exhaustively mapped — r=8 closed inner_dim=12 capacity-limited LOSS, r=4 merged inner_dim=24 LOCAL OPTIMUM, r=2 closed inner_dim=48 regularization-lost LOSS; non-monotonic curve confirms sweet spot at r=4; future SE work should hold r=4 fixed and vary other dimensions). Assigned #2814 tanjiro SE inner activation GELU → SiLU (1-line change F.gelu → F.silu in SqueezeExcitation.forward; +0 params; motivated by orthogonal evidence from closed GeGLU #2759 64th taxon gate_zero_frac 16-26% per block — GELU's symmetric near-zero collapse hard-masks ~20% of channels; if same mechanism active in SE's inner_dim=24 bottleneck then GELU effectively reduces capacity to ~19 channels; SiLU's asymmetric smoothness should preserve all 24 channels; parallel to merged SwiGLU #2741 18th winner same SiLU activation choice but applied at SE bottleneck not MLP gate; predicted mechanism silu_zero_frac <<16%, gate_std rises beyond r=4 baseline, gate_min stays low regularization-via-veto preserved at fixed width, OOD benefits from extra effective bottleneck capacity; structurally distinct from in-flight SE attn-pool #2810 alphonse pool function vs activation, distinct from in-flight per-block FiLM #2813 nezuko conditioning vs SE internals, distinct from closed gate-activation axis MLP-body vs SE-bottleneck different roles; 68th candidate axis; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 66. Total merged winners: 19.)

- **As of:** 2026-05-14 [Round 97] UTC (round-97: 1 closure + 1 assignment. Closed #2792 nezuko Register tokens N_REG=4 LOSS (val 32.6961 = +4.39% vs new baseline 31.3216; test 27.3044 = +3.01%; all 4 splits regress or wash with re_rand worst-hit +7.46%, camber_rc +4.69%, camber_cruise +3.57%, in_dist +0.02% wash; per-split signature exact INVERSION of hypothesis predicted OOD WIN; mechanism per student exemplary diagnostic: block 1 monopolizes registers at 56% slice→register attention mass by ep20 some queries 100%; block 2 spontaneously vetoes registers grew 5× but query attn mass dropped below uniform init; blocks 0,3 use modestly 15-20%; block-1 lock-in creates low-rank rank-4 global bottleneck fitting dominant in-dist regime but cannot express OOD geometries/Re-numbers — classic "global bottleneck overfits in-dist" pattern; re_rand worst-hit precisely because raw K/V slots compete with merged FiLM #2614's Re-conditioning capacity — re_rand fundamentally needs more Re-conditioning not more attention slots; 65th taxon: attention-input capacity expansion via UNCONDITIONED learnable K/V positions closed at this scale; combined with closed positional embedding RoPE #2675 46th + Fourier features #2509 24th, attention-input augmentation meta-axis now mapped — positional encoding LOSS + register tokens LOSS — merged FiLM+SE mechanism for OOD generalization structurally incompatible with raw K/V augmentation; student's zero-init+LayerScale register variant deprioritized in favor of higher-impact axis). Assigned #2813 nezuko Per-block FiLM (add `self.film = nn.Linear(3, 96)` to each TransolverBlock zero-init weight+bias applied AFTER residual blocks: `fx = fx * (1 + film(flow_scalars))`; each block re-injects log_Re/AoA0_rad/AoA1_rad; depth-progressive Re-conditioning vs current merged FiLM #2614 which conditions only ONCE at embedding; +1,536 params 4 blocks × (3×96 + 96) = 4×384 → 335,139 total; directly attacks the re_rand bottleneck pattern from #2792 — re_rand has now regressed in BOTH directions of mlp_ratio AND register tokens, all blamed on missing depth-progressive Re-conditioning; Perez 2018 FiLM original applied layer-wise not once-only; predicted mechanism |film_scale| grows most at block 3 Re-conditioning before SE gate, val_re_rand benefits most, camber splits AoA-conditioned benefit too; structurally distinct: extends MERGED FiLM #2614 to deeper sites NOT a replacement, distinct from closed register tokens 65th + RoPE 46th + Fourier 24th + Talking-Heads 44th, distinct from in-flight embedding LN #2808 normalization vs conditioning, distinct from in-flight SE attn-pool #2810 pool function not conditioning; 67th candidate axis; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 65. Total merged winners: 19.)

- **As of:** 2026-05-14 [Round 96] UTC (round-96: 1 closure + 1 assignment. Closed #2759 alphonse GeGLU LOSS (val 32.4107 = +3.48% vs new baseline 31.3216; test 27.6001 = +4.13%; per-split val in_dist 27.92 worst-hit +11.78% LOSS, camber_rc +0.62%, camber_cruise −2.43% improved, re_rand +4.36% LOSS; two replicated runs 32.41/32.52 = solid LOSS; mechanism per student excellent diagnostic: gate_zero_frac 16-26% per block GeGLU vs 1.3-2.4% SwiGLU — GELU's symmetric-near-zero collapse produces ~20% hard channel masking detrimental for this stack vs SiLU's asymmetric-monotone-non-negative-tail soft re-weighting; GeGLU's hard-masking overlaps SE block-3 channel-gating role doubling-down counterproductively; 64th taxon: gate-activation sub-axis within GLU family closes with silu strictly preferred over gelu — merged SwiGLU #2741 is the right gate choice; bug-fix verification confirmed advisor branch clean since commit 2840599 #2765 merge accidentally fixed dangling _se_loader introduced by #2741; param-count discrepancy resolved as SE r=8→4 transition between SwiGLU merge and current baseline). Assigned #2810 alphonse SE with attention pooling (replace SE mean-pool with content-aware attention pool: `attn_logits = Linear(96,1)(x); attn_w = softmax(attn_logits, dim=tokens); pool = (x * attn_w).sum(dim=tokens)` then existing fc1→GELU→fc2→sigmoid bottleneck; +97 params single Linear(96,1) → 333,700 total; sub-axis of merged SE block-3 #2765 r=4 — tests whether mean pool throws away token-importance information that attention pool can learn; mechanism predictions: attn_pool_weights concentrate on is_surface=True tokens given primary metric is mae_surf_p, gate_std rises beyond #2765's +5-12% gain, OOD splits camber/re_rand benefit most; Lee 2019 ICCV Set Transformer single-head attention pool reference; 66th candidate axis — pool function for SE module; structurally distinct from SE bottleneck-width sub-axis #2765 merged r=4, SE depth sub-axis #2748 closed blocks 2+3, SE near-identity init #2728 stale_wip; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 64. Total merged winners: 19.)

- **As of:** 2026-05-14 [Round 95] UTC (round-95: 2 closures + 2 assignments. Closed #2766 askeladd GroupNorm(G=4) LOSS (val 35.9188 = +14.69% vs new baseline 31.3216; test 30.7585 = +16.04%; ALL 4 val splits regress 6.89-16.91% with re_rand worst +16.91%; mechanism per student analysis: GN reduces over (T, C_g=24) statistics including padded mesh positions contaminating per-group normalization; LayerNorm normalizes per-token over full channel dim only — immune to padding ratio; LayerScale γ patterns preserved confirming poor-conditioning failure not training instability; 62nd taxon: normalization-granularity sub-axis closes at LN full-channel pooling; combined with closed DyT #2686 48th taxon all-or-nothing replacement + closed RMSNorm #2139, **normalization-meta-axis now COMPREHENSIVELY mapped across REPLACEMENT and GRANULARITY dimensions** — LN remains optimal for variable-token point-cloud regression with padding). Closed #2764 frieren mlp_ratio=1.5 LOSS (val 32.0910 = −0.49% vs OLD #2741 baseline but **+2.46% LOSS vs new #2765 baseline 31.3216**; test 27.8057 = +1.39% regress; per-split inversion vs #2749 mlp_ratio=3 wider: in_dist+1.06% regress, re_rand+1.72% regress, camber_rc−1.75% improved, camber_cruise−3.65% improved — in_dist+re_rand strictly prefer mlp_ratio≈2 while camber splits prefer ANYTHING different from 2; |γ_mlp| stayed flat = model still wants MLP capacity at ratio=2; **re_rand bottleneck is NOT MLP capacity** — regressed in BOTH directions confirming Reynolds-conditioned probe needed for that split; 63rd taxon: **MLP-width axis saturated at mlp_ratio=2** mapped from both directions #2749 wider closed + #2764 narrower closed; student's own conclusion aligns exactly; bug fix verification: advisor branch already has working `_swiglu_loader = val_loaders.get('val_single_in_dist') or next(iter(val_loaders.values()))` — frieren's defensive guard patch NOT cherry-picked since no functional change for in-flight SwiGLU experiments). Assigned #2808 askeladd Pre-block-0 embedding LayerNorm (add `self.embed_ln = nn.LayerNorm(96)` in Transolver.__init__ after placeholder line ~256; in forward after FiLM modulation line 283 add `fx = self.embed_ln(fx)` before block loop; +192 params 96 weight + 96 bias → 333,795 total; gives block 0 normalized stationary-scale input enabling LayerScale γ to learn against fixed baseline rather than chasing preprocess+placeholder+FiLM drift; **structurally DISTINCT from closed normalization-REPLACEMENT axis (DyT 48th, RMSNorm 15th, GroupNorm 62nd) — ADDS new LN site rather than REPLACING existing ones**; Xiong 2020 pre-LN at every residual stream entry stabilizes optimization; embedding stream currently has NO LN between preprocess pipeline and block 0; 64th candidate axis: LN-site insertion at embedding stream; NEW bar val < 31.3216). Assigned #2809 frieren 3-layer output head mlp2 deeper (replace `mlp2 = Linear(96,96)→GELU→Linear(96,3)` with `Linear(96,96)→GELU→Linear(96,96)→GELU→Linear(96,3)`; +9,312 params → ~342,915 total; decoder-DEPTH probe — 2 nonlinearities vs 1; **distinct sub-axis from PR #2750 fern mlp2-wide192 stale_wip decoder-WIDTH** axis-untested-but-stale, this is depth-not-width; the output head is the *only* place where semantic latents convert to physical-channel predictions; if decoder absorbs work previously done by block 3, |γ_attn|/|γ_mlp| may drop; per-split predictions bias toward camber splits where surface pressure prediction hardest; mae_surf_p should improve more than mae_vol; 65th candidate axis: decoder-depth probe; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 63. Total merged winners: 19.)

- **As of:** 2026-05-14 [Round 94] UTC (round-94: 1 merge + 1 closure + 2 assignments. MERGED #2765 tanjiro SE block-3-only reduction=4 WIN (val 31.3216 = −2.87% vs baseline 32.2477; test 26.5067 = −3.35%; ALL 4 val splits WIN: in_dist 24.97 new best-ever −2.19%, rc 46.99 −1.01%, cruise 17.73 −7.49%, re_rand 35.60 −3.35%; ep64/70 timeout-truncated; mechanism: SE bottleneck at reduction=8 inner_dim=12 was capacity-limited; doubling to inner_dim=24 unlocked more discriminative gating; gate_std rose 5-12% on ALL splits, gate_min collapsed 50-70% on ALL splits — wider bottleneck enables sharper per-channel veto; OOD cruise and re_rand benefited most; test in-dist mild regression +1.14% (within noise at n=100); SwiGLU gate stats unchanged confirming SE and SwiGLU are orthogonal mechanisms; block-3 γ_attn mean flipped sign (only block 3 of 4) = wider SE taking over role previously carried by block-3 attention; 19th merged winner; NEW BASELINE val 31.3216 / test 26.5067). Closed #2778 edward Deep Supervision LOSS (val 34.3141 = +6.41%; test 28.7866 = +4.97%; ALL 4 val splits regress 2.87-9.69%; best ep60/63 converged; student mechanism diagnostic excellent: prediction (a) confirmed (early-block gamma_attn_std rises: block0 0.0401 highest, block3 0.0332 lowest — gradient-pathway reversal from Lee 2014 aux heads), predictions (b) and (c) refuted (both in-dist AND OOD regress); two compounding mechanisms identified: (1) anti-hierarchical pressure — Transolver's slice-attention+LayerScale γ=1e-4 design biases for gradual late-block-dominated mixing; deep supervision is the opposite inductive bias forcing early-block premature linear decodability; (2) Linear(96,3) heads create low-rank 3-D bottleneck pressure on 75% of residual depth; aux/main loss ratio 2.0-2.2× confirms magnitude was too strong; 61st taxon: gradient-pathway-multiplication via aux heads for point-cloud regression to 3-channel target; Lee 2014 valid for classification/U-Net-dense-prediction but wrong-shape for this task). Assigned #2798 tanjiro SE reduction=2 (extend winning bottleneck sub-axis; inner_dim 24→48; +4,632 params = +1.39%; expected params 338,235; gate_std/gate_min trend check against #2765 numbers — three-point comparison r=8/r=4/r=2; either WIN (capacity still rate-limited) or LOSS/WASH (SE bottleneck closes at reduction=4 local optimum); NEW bar val < 31.3216). Assigned #2799 edward long-range preprocess→block-3 skip with per-channel LayerScale-style gate (add self.skip_gate = nn.Parameter(torch.zeros(n_hidden)) in Transolver.__init__; in forward save fx_input after FiLM, then for i,block in enumerate(self.blocks): if i==len(self.blocks)-1: fx += skip_gate.unsqueeze(0).unsqueeze(0)*fx_input; skip contributes zero at init, learned per-channel; +96 params = 333,699 total; diagnostic: gate abs_mean + sign balance + ratio of skip contribution norm to pre-skip residual norm; 64th candidate axis: long-range residual skip across full block stack — structurally orthogonal to all closed/merged taxa; NEW bar val < 31.3216). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 61. Total merged winners: 19.)
- **As of:** 2026-05-14 [Round 93] UTC (round-93: 1 closure + 1 assignment. Closed #2750 fern mlp2-wide192 STALE_WIP (created 2026-05-14T03:00:48Z zero commits zero comments ~1h57m no pickup despite pod Running 1/1 0 restarts 35h uptime; multi-round launch-convention stall; slot redeployed to fresh axis; mlp2-width-up axis remains open). Assigned #2794 fern SwiGLU in preprocess MLP (1-line class swap MLP→SwiGLUMLP in Transolver.__init__; tests whether merged SwiGLU-in-MLP-body win #2741 mechanism — per-token channel gating — transfers to input-lifting stage; hidden_swiglu=128 vs standard MLP 192 channels; preprocess params 22,656→17,920 saves ~4,700; total params 338,523→~333,787 −1.4%; distinct from merged SwiGLU-in-MLP-body (different stack location), in-flight Mish in mlp2 #2780 thorfinn (output activation no gating), in-flight GeGLU MLP #2759 alphonse (gate activation variation in MLP body), closed Fourier features #2509 (input encoding not activation); 63rd candidate axis: gating mechanism at INPUT-LIFTING stack location; simple 1-line change for stall-prone pod; NEW bar val < 32.2477). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 60. Total merged winners: 18.)
- **As of:** 2026-05-14 [Round 92] UTC (round-92: 1 closure + 1 assignment. Closed #2761 nezuko SAM rho=0.05 LOSS (val 54.6259 = +69.4% vs new baseline 32.2477; test 48.0641 = +75.3%; all 4 splits regress +51-118%; SAM mechanism engaged correctly per diagnostics (sam_loss_ratio 1.04→1.46 climbing, confirming Lion finds sharp basins; Lion momentum buffer 99.97% non-zero confirms wiring clean) BUT 2x compute halved effective epochs to 35/70 while baseline reaches 63/70; loss curve still steeply descending at timeout (~1.6 val units/epoch slope); the LOSS scenario predicted by the PR; 60th taxon: direct flat-minima probe under 30-min budget closed; combined with closed SGDR 52nd + SWA 28th + Lookahead 55th, FLAT-MINIMA/NARROW-BASIN-ESCAPE META-FAMILY 4-DIRECTION EXHAUSTIVE at this compute budget; flat-minima HYPOTHESIS not refuted (Lion's basins ARE sharp) but the wall-clock cap is binding). Assigned #2792 nezuko Register tokens N_REG=4 (Darcet et al. 2024 ICLR "Vision Transformers Need Registers"; learnable global tokens added as additional K/V positions in PhysicsAttention slice attention; +1,536 params = +0.45%; registers participate in attention but excluded from output routing back to mesh nodes; structurally orthogonal to ALL closed/merged taxa: NOT weight reg, NOT optimizer/trajectory, NOT normalization, NOT activation, NOT positional encoding, NOT loss-shape, NOT augmentation, NOT stochastic dropout, NOT SE, NOT SwiGLU; 62nd candidate axis: learnable attention K/V capacity expansion; first probe of this dimension; NEW bar val < 32.2477). Param count flag noted on closed #2761 (reported 331,287 vs PR body 338,523, upstream baseline accounting discrepancy). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 60. Total merged winners: 18.)
- **As of:** 2026-05-14 [Round 91] UTC (round-91: 2 closures + 2 assignments. Closed #2739 edward Weight Standardization CATASTROPHIC LOSS (both arms collapsed: Arm 1 all Linear val ~344, Arm 2 skip small val ~351, baseline 32.2477 = ~10x degradation; model collapsed to trivial attractors; mechanism: WS over-suppressed forward signal globally — every Linear layer's weight matrix re-centered+normalized at every forward pass, destroying the LayerScale γ=1e-4 near-identity initialization advantage; LayerScale γ climbed 10x trying to compensate but couldn't escape basin; 59th taxon: forward-time weight standardization at this scale closed; combined with closed spectral norm #2580 + L1+L2, weight-constraint regularizer axis 3-direction exhaustive; OOD generalization not via weight-constraint regularization). Closed #2728 thorfinn SE near-identity init STALE_WIP (created 2026-05-14T02:03:35Z, zero commits, zero comments, ~2h14m without pickup despite pod Running 1/1 0 restarts 34h uptime; multi-round launch-convention stall; redeployed slot to fresh axis; SE near-identity init taxon remains open for later re-try). Assigned #2778 edward Deep Supervision (Lee, Xie, Gallagher, Zhang, Tu 2014; auxiliary mae_surf_p heads after blocks 0,1,2 with aux_weight=0.1; gradient-flow regularizer via direct intermediate-block→loss pathways; ~+873 params = +0.26%; structurally orthogonal to ALL closed/merged taxa (not weight reg, not stochastic perturbation, not optimizer-internal, not normalization, not architectural module, not loss-balance/shape); 60th candidate axis: gradient-pathway-multiplication meta-family; NEW bar val < 32.2477). Assigned #2780 thorfinn Mish in mlp2 output head (Misra 2019; nn.GELU() → nn.Mish() 1-line swap on the single output-head activation; param-matched 0 delta; output-decoder activation probe orthogonal to SwiGLU intermediate residual stream; structurally simple for stall-prone pod; 61st candidate axis: output-head activation; NEW bar val < 32.2477). No human GH issues. All 8 in-flight, zero idle. Total closed taxa: 59. Total merged winners: 18.)
- **As of:** 2026-05-14 [Round 90] UTC (round-90: 3 closures + 3 assignments. Closed #2749 frieren mlp_ratio 2→3 LOSS (val 32.7558 = +1.59% vs new baseline 32.2477; in_dist −4.86% best-ever 24.41 BUT all 3 OOD splits regress 1-4%; |γ_mlp| ballooned 25-40% peak at block 2 = 0.101; model leans into capacity for in_dist overfit; 56th taxon: MLP-width axis upward closed at this configuration). Closed #2748 tanjiro SE blocks 2+3 LOSS (val 33.4972 = +3.88% vs new baseline; only in_dist mild win, all 3 OOD splits regress; block 2 std climbed 0.030→0.073-0.111 confirming upstream-SE-was-dampening hypothesis right, BUT block 3 std DROPPED 0.15-0.26 → 0.112-0.204 — block 2 stole gating budget from block 3; LayerScale γ on block 3 also dropped γ_attn 0.017→0.009 + γ_mlp 0.10→0.07; 57th taxon: SE depth-extended axis closed; block-3-only is MINIMAL VIABLE UNIT; SE depth landscape now fully mapped {none, 3-only=WIN, 2+3=LOSS, all-4=mixed}). Closed #2735 askeladd EMA decay=0.9995/0.9999 LOSS (decay=0.9995 val 34.5688 = +7.20% vs new baseline; decay=0.9999 val 87.2438 catastrophic effective-window > horizon; EMA-vs-raw A/B at 0.9995 showed only −0.6% relative improvement = small but real; mechanism: drift_ratio peaks at ep5 = 0.331 during rapid descent then decays monotonically to 0.0147 — with cosine LR collapsing toward 1e-6 raw weights stop moving in final epochs and EMA averaging becomes a no-op; 58th taxon: eval-time weight-averaging closed; combined with closed SWA #2567 + Lookahead #2740, weight-averaging meta-family 3-direction exhaustive; only direct SAM #2761 in-flight as narrow-basin-escape probe). Assigned #2764 frieren mlp_ratio=1.5 (inverse probe to #2749 — if wider hurts OOD via capacity-overfit, narrower might tighten OOD; 1-char change; ~301K params; NEW bar val < 32.2477). Assigned #2765 tanjiro SE-block3-only reduction=4 (SE bottleneck dim probe; doubles SE intermediate from 12 to 24 channels; +2,316 params; tests whether reduction=8 is capacity-limiting OOD discrimination; NEW bar val < 32.2477). Assigned #2766 askeladd GroupNorm(num_groups=4) (normalization-granularity sub-axis open after DyT closure tested all-or-nothing; tests whether 4 channel-groups of 24 each specialize statistically vs LN single-pop; same param count 338,523; NEW bar val < 32.2477). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 89] UTC (round-89: 1 closure + 1 assignment + 1 label fix. Closed #2740 nezuko Lookahead+Lion catastrophic LOSS (val 41.3670 = +25.27% vs old baseline 33.0195 = +28.28% vs new baseline 32.2477; test 35.1124 = +28.05%; ALL 4 splits regress 20-41% with camber_cruise +41% worst; ep1=366.66 = 5x typical Lion ep1, ~10x slower convergence; SE block gates never specialized std ~0.02 vs baseline 0.076; A/B diagnostic structurally degenerate at 375 batches/epoch ÷ k=5; mechanism: Lookahead weight-reset incompatible with Lion sign-momentum — Lion exp_avg buffer survives reset creating optimizer/weight inconsistency where subsequent sign-updates oriented from stale momentum; 55th taxon: Lookahead × Lion sign-momentum incompatibility; combined with closed SGDR #2697 (52nd) + SWA #2567 (28th), narrow-basin-escape meta-axis is now 3-direction closed via fast-cycle restart + slow-weight commit + trajectory averaging — only eval-time EMA #2735 remains). Assigned #2761 nezuko SAM rho=0.05 (Foret 2021 ICLR; wraps Lion; 2 forward/backward passes per step — perturb adversarially in rho-ball then update from worst-case gradient; structurally orthogonal to all 3 closed indirect mechanisms — operates on LOSS LANDSCAPE not optimizer trajectory; ~30 epochs expected at 2x compute; OOD splits theory-predicted beneficiaries; NEW bar val < 32.2477). Fixed label student:alphonse → student:charliepai2g48h5-alphonse on #2759 (assign skill assigned wrong label format). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 88] UTC (round-88: 1 merge + 1 assignment. Merged #2741 alphonse SwiGLU MLP activation WIN (val 32.2477 = −0.62% vs new baseline 32.4498; test 27.4248 = −0.84%; all 4 val splits improved: in_dist 25.5303 (best-ever), re_rand 36.8311 −2.68% WIN, camber splits flat; gate_mean depth-decays 0.356→0.088, gate_std 1.07→0.58 — smooth depth-progressive signed re-weighting NOT hard masking; gate_zero_frac 1.3-2.4%, corr(gate,value)≈0 = decoupled conditional capacity; stacks constructively with SE-block3-only (SE=global token-pool channel gates, SwiGLU=per-token MLP channel gates — genuinely orthogonal); 18th merged winner; NEW BASELINE val 32.2477 / test 27.4248). Assigned #2759 alphonse GeGLU (1-line change F.silu→F.gelu in gate activation; same param count 338,523; ablates whether silu-specific gate or GLU-structure drove the WIN; T5/PaLM-2/Gemma use GeGLU; NEW bar val < 32.2477). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 87] UTC (round-87: 1 merge + 2 closures + 3 assignments. Merged #2727 tanjiro SE-block3-only WIN (val 32.4498 = -1.73% vs #2692 33.0195; test 27.6573 = -2.46%; in_dist regression from 4-block SE FULLY ELIMINATED (25.65 vs 26.42); 3/4 val splits WIN; block-3 gate std grew 2-3.4x wider (0.15-0.26) with upstream SE removed; OOD splits gate harder than in_dist confirming SE is OOD adaptation; params 331,031 = +2,412 vs #2614 = strictly more efficient than 4-block SE; 17th merged winner; NEW BASELINE val 32.4498 / test 27.6573). Closed #2710 frieren Gradient Centralization LOSS (val 35.2214 = +6.67% vs new baseline; in_dist +13.96% worst; Lion momentum saturation 0.9977 ≡ baseline 0.998 — GC did NOT reduce sign-saturation; ~5.1% gradient removed was useful signal (uniform-input-shift = per-channel amplification in slice projections); convergence slower throughout; 54th taxon: gradient-transformation axis closed). Closed #2677 fern gradient noise injection 2nd stale_wip event (pod alive 0 restarts but never picked up assignment; axis abandoned per 2-stale convention; not a scientific judgment on hypothesis — just pod failure). Assigned #2748 tanjiro SE blocks 2+3 (keep block 3 as in #2727 winner, add back block 2; tests whether depth extends to 2 layers or block 3 alone is optimal minimal unit; key diagnostic block-2 vs block-3 gate_std; NEW bar val < 32.4498). Assigned #2749 frieren mlp_ratio 2→3 (1-character change, MLP body hidden 192→288 per block; +73K params; standard ViT/BERT uses mlp_ratio=4; never probed above 2 in this launch; NEW bar val < 32.4498). Assigned #2750 fern output head width mlp2 hidden 96→192 (decoder capacity probe; +9.6K params; mlp2 is last_layer Linear(96,96)→GELU→Linear(96,3); all prior winners improved encoder; this tests decoder bottleneck; built-in grad_norm diagnostic; NEW bar val < 32.4498). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 86] UTC (round-86: 3 closures + 3 assignments. Closed #2722 edward DropPath LOSS (val 34.8786 = +5.63% vs new baseline 33.0195; in_dist worst-hit +9.21%; gamma_mlp ballooned 4-10x larger blocks 1-3; 51st taxon: stochastic-residual-branch axis at small width; small-width n_hidden=96 Transolver stack cannot tolerate per-sample stochastic branch zeroing; combined with closed Mixup #2687 and Manifold Mixup #2704 — stochastic-perturbation meta-axis closing toward 'this stack rewards deterministic mechanisms'). Closed #2697 nezuko SGDR (T_0=35, T_mult=2) LOSS (val 39.0987 = +18.41% vs new baseline; in_dist +29.77%; restart shock val_avg +67% in 2 epochs ep38→40; cycle-1 too short to converge, cycle-2 truncated mid-flight; 52nd taxon: schedule-restart axis; single-cycle cosine 67-epoch is load-bearing for in-dist; sub-cycle restarts perturb optimizer harder than in_dist can recover). Closed #2704 alphonse Manifold Mixup α=0.2 CATASTROPHIC LOSS (val 46.0412 = +39.45% vs new baseline; in_dist +67.82% worst; PhysicsAttention slice-routing produces semantic prototypes that cannot linearly interpolate — λ Beta(0.2,0.2) correctly engaged but interpolated centroid corrupts every block's slice-routing decision; two independent runs reproduce; 53rd taxon: hidden-state interpolation axis; combined with closed Mixup #2687 input-blend, Mixup family FULLY MAPPED — 3 independent failure mechanisms across input/hidden/rigid-body augmentation). Assigned #2739 edward Weight Standardization (Qiao 2019; standardize each output row of every nn.Linear weight to zero mean unit std at forward time; deterministic regularizer per edward's own follow-up #4; zero new params; tests deterministic in-dist regularization mechanism after closing stochastic-perturbation axes; NEW bar val < 33.0195). Assigned #2740 nezuko Lookahead wrapper around Lion (Zhang 2019; k=5 fast steps, alpha=0.5 slow-weight interpolation; targets same narrow-basin-escape hypothesis as failed SGDR but via smooth slow-weight commitment instead of LR shock; compatible with EMA #2735 askeladd in-flight (train-time slow weights vs eval-time EMA); built-in fast-vs-slow A/B diagnostic; NEW bar val < 33.0195). Assigned #2741 alphonse SwiGLU MLP activation (Shazeer 2020; replace GELU MLP body with silu(W_gate x)*W_value x → W_out gated activation; param-matched via hidden=2/3 standard; standard in LLaMA/PaLM/Mistral/Gemma; structurally completely orthogonal to all closed taxa and in-flight; gate statistics built-in diagnostic; NEW bar val < 33.0195). PR #2677 fern grad-noise at 1st stale_wip event (assigned ~2.5h ago, pod alive but hasn't picked up); leaving for one more round per launch convention. No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 85] UTC (round-85: 1 closure + 1 assignment. Closed #2706 askeladd α-entmax(α=1.5) LOSS — **50th closed taxon / attention-shape axis fully mapped** (val 34.0974 = +3.27% vs new baseline 33.0195; test 29.1550 = +2.82% LOSS; depth-progressive sparsification cascade: block 3 collapses to single-token routing nonzero_count=1.0/24, top1_prob=1.000; optimizer dampens broken branch via LayerScale γ_attn block 3 = 0.0089 = 2.8x smaller than block 1; combined with closed Sigmoid #2673: both row-sum=1 normalization AND smooth competition are independently load-bearing; softmax is optimum on attention-shape axis; PR was CONFLICTING/DIRTY merge state due to #2692 SE merge but terminal results already present so closed cleanly rather than rebasing). Assigned #2735 askeladd EMA weight averaging Polyak 1992 (decay=0.9995 shadow weights updated after every optimizer.step(); use EMA weights for val/test only; zero new trainable params; ~1.3 MB shadow; standard DeiT/MAE/EfficientNet/ConvNeXt technique not yet probed this launch; targets Lion narrow-basin in-dist overfitting bottleneck; built-in EMA-vs-raw A/B diagnostic at terminal; NEW bar val < 33.0195). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 84] UTC (round-84: 1 merge + 1 stale-close + 2 assignments. Merged #2692 tanjiro SE per-block channel gating WIN (val 33.0195, **-1.06% vs baseline 33.3722**; test 28.3562 -0.06% flat; 3/4 OOD val splits improve: camber_rc -2.54%, re_rand -2.94%, cruise -0.60%; val_single_in_dist REGRESSES +4.31%; best ep65/70 timeout-truncated; **50th total experiment — first architecture-module WIN since FiLM #2614 (16th merged winner)**; mechanism: block 3 SE engages most std=0.076 vs blocks 0-2 std 0.022-0.034; depth-progressive SE/LayerScale trade-off confirmed; gate=0.5 init halved stream at start but LayerScale adapted without inflation; NEW BASELINE val 33.0195). Closed #2668 thorfinn n_head=4 retry-1 2nd STALE_WIP (zero pod activity for ~9 rounds; n_head=4 axis abandoned — 2 consecutive stale events; head-projection complexity consistently interrupts thorfinn pod; n_head=4 axis closed without result per launch convention). Assigned #2727 tanjiro SE-block3-only (student tanjiro's #1 follow-up from #2692: remove SE from blocks 0-2 keep only block 3 where engagement was highest std=0.076; param reduction from +9,648 to +2,412; tests whether depth-selective SE eliminates in_dist regression +4.31% while retaining OOD gains; NEW bar = val < 33.0195). Assigned #2728 thorfinn SE near-identity init (student tanjiro's #3 follow-up from #2692: change SE fc2.bias from 0 to log(99)≈4.595 so gate≈0.99 at step 0 instead of 0.5; 1-line change; eliminates initial 50% stream dampening on all 4 blocks; tests whether near-identity init recovers in_dist while preserving OOD; orthogonal to tanjiro's depth-selective probe; NEW bar = val < 33.0195). No human GH issues. All 8 in-flight, zero idle.)

- **As of:** 2026-05-14 [Round 83] UTC (round-83: 1 closure + 1 assignment. Closed #2687 edward Mixup alpha=0.2 CATASTROPHIC LOSS (val 45.0687, **+35.04% vs baseline 33.3722**; test 39.4107, +38.91% LOSS; ALL 4 val splits AND all 4 test splits regress; val_single_in_dist regressed MOST +51.53% = PREDICTION INVERSION (hypothesis predicted in_dist would IMPROVE most; instead in_dist was the most harmed split proving naive blending CORRUPTS easy samples rather than smoothing them); val_geom_camber_cruise +54.94%; val_re_rand +32.10%; val_geom_camber_rc +20.72% (least bad — already in extrapolation regime); **Mixup mechanism ENGAGED correctly**: lambda distribution exactly as designed — 67.11% extreme batches (lambda<0.1 or lambda>0.9), mean=0.4946, Beta(0.2,0.2) bimodal confirmed across 26,250 batches; **EXEMPLARY mechanism diagnostic from edward** — 3 concrete failure modes: (1) physical incompatibility: linear blend between (Re=200, AoA1=0deg, single-foil) and (Re=400, AoA1=5deg, tandem) produces physically meaningless (Re=300, AoA1=2.5deg, half-tandem) intermediate that dataset never contains; (2) mesh-structure corruption via pad_collate alignment: permuting batch indices aligns mesh A surface nodes with mesh B volume nodes or B padding zeros, corrupts per-node features topology; anchored is_surface mask weights loss correctly but cannot undo input-feature corruption; (3) FiLM gate broke under blended conditioning: film.weight.norm grew +29% to 3.39 (from baseline 2.62) — gate fought harder but cannot disambiguate impossible blended inputs; stack with strong physical-condition gating MORE vulnerable to input blending not less; **49th taxon: data-space augmentation axis CLOSED** — combined with reflection-aug #2454 18th taxon (catastrophic, half-space mesh symmetry violated) + coord-jitter (closed earlier), augmentation meta-axis mapped across 3 directions rigid-body transform / local noise / sample interpolation: all LOSS; Tandem-foil CFD inputs are physically rigid; dataset structural constraints fight augmentation harder than model overfitting tendency; **critical companion signal**: edward's result strongly suggests alphonse in-flight Manifold Mixup #2704 faces similar headwind via slice-routing softmax — slice tokens are semantic prototypes that may not linearly interpolate). Assigned #2722 edward DropPath/Stochastic Depth (Huang et al. 2016 ECCV "Deep Networks with Stochastic Depth"; linear schedule dpr=[0.0, 0.033, 0.067, 0.1] applied per-sample to both attn AND mlp residual branches in every TransolverBlock; training-mode only; ZERO new params; ~20-line change total (DropPath module + 2-line forward replacement); standard in DeiT/Swin/ConvNeXt/MAE; structurally distinct from all 49 closed taxa AND 7 in-flight WIPs; compatible with LayerScale gamma=1e-4 (each residual contributes ~1% of stream — dropping a branch is very gentle perturbation at the right scale); targets in-dist overfitting bottleneck via compute-graph perturbation NOT data-space blending; key diagnostic: per-block gamma_attn/gamma_mlp evolution at terminal vs baseline ~0.01-0.02 — gamma growth means model trusts each residual more under stochasticity (WIN); gamma shrink means model defensively suppresses unreliable branches (LOSS)). No human GH issues. All 8 in-flight, zero idle.)

- **As of:** 2026-05-14 [Round 82] UTC (round-82: 1 closure + 1 assignment. Closed #2686 frieren DyT (Dynamic Tanh) normalization CATASTROPHIC LOSS (val 47.6230, **+42.7% vs new baseline 33.3722**; test 39.5154, +39.3% LOSS; ALL 4 splits regress 31-63% with in_dist worst at +63.2%; best ep65/70 hit 30-min timeout STILL IMPROVING at ~0.6/5 epochs/run, would need ≥50 more epochs to plausibly approach baseline; **EXEMPLARY mechanism diagnostic from student**: (1) catastrophic convergence slowdown — same 65 epochs DyT at 47.62 vs baseline ~33.4 with identical per-epoch wall-clock 27.8s, tanh-saturated output not faster than LN; (2) LayerScale γ_mlp compensation — abs_mean ballooned to 0.06-0.16 vs baseline ~0.010-0.020 (6-8× larger), optimizer enlarging residual contribution to compensate for DyT's bounded output but cannot recover enough — compensation pattern is ITSELF evidence channel-stat reduction was load-bearing; (3) Alpha divergence by site — pre-attention ln_1 α stayed near 0.46-0.61 mild saturation/near-identity, pre-MLP ln_2 α climbed to 1.01-1.13 aggressive saturation, decoder ln_3 α at 0.68 intermediate; 2× variation across sites means single learnable scalar cannot replicate per-token channel-wise statistical normalization; **48th taxon: normalization-replacement at small width fails — LayerNorm's channel mean+variance reduction is load-bearing on this stack**; combined with closed RMSNorm 15th taxon mild bimodal LOSS testing without mean centering + closed NormFormer Sandwich extra normalization not helpful, **normalization-meta-axis now mapped across 3 directions** add/remove-mean/remove-all-stats: LN baseline is dual-optimum; frieren attention-internal/normalization meta-family fully closed across 4 distinct probes τ #2623 + spectral norm #2580 + QK-Norm #2661 + DyT #2686). Pivoting frieren to gradient-transformation axis. Assigned #2710 frieren Gradient Centralization (Yong et al. 2020 ECCV; apply `g - g.mean(dim=input)` to all rank-≥2 weight gradients between loss.backward() and optimizer.step(); ~3-line training-loop addition; ZERO new params; FIRST gradient-transformation probe in launch; structurally distinct from fern in-flight #2677 gradient noise — GC SUBTRACTS per-output-unit mean while gradient noise ADDS Gaussian; operates BEFORE Lion's sign-step restricting each weight row's gradient orthogonal to all-ones direction in input space; targets Lion narrow-basin tendency 30th/35th taxa via removing degenerate uniform-input-shift mode; key diagnostic: gc_norm_ratio_mean per epoch — near 1.0 = GC inert vs 0.7-0.9 = GC active). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 81] UTC (round-81: 2 closures + 2 assignments. Closed #2675 alphonse 2D coord-based RoPE LOSS (val 34.4690, +3.29% vs new baseline 33.3722; test 29.8748, +5.29% LOSS; UNIFORM REGRESSION with **in_dist worst-hit +10.15% val / +10.42% test** — easiest split where slice-routing was best-tuned was damaged MOST, classic noise-bias-on-easy-split pattern; camber_rc only +1.93% (predicted scenario was OOD WIN, did not materialize); student adapted per-node RoPE prescription to slice-center RoPE since Transolver runs Q/K/V on G=24 slice tokens not per-node features — sound adaptation but exposes structural mismatch: slice tokens are SEMANTIC prototypes not spatial positions, centroid is grad-coupled to routing creating circular dependency; student-flagged base=10000 vs base=100 issue (only ~4 of 12 frequencies active for coord range [-6.5, 5.8]) is valid Heo et al. ECCV 2024 concern but doesn't change fundamental "slice tokens aren't spatial" mismatch; **46th taxon: positional-embedding meta-axis closes on this stack** — Fourier-features-preprocess #2509 (24th taxon) + 2D-RoPE-slice-center #2675 (46th) both LOSS confirms slice-routing softmax already captures spatial structure adequately). Closed #2673 askeladd Sigmoid Attention LOSS (val 33.6466, +0.82% borderline-wash on val; test 28.9979, **+2.20% clear test regression**; in_dist worst at +5.52% val; camber_rc the ONLY WIN −1.12% — matched multi-position prediction insufficiently to offset in_dist damage; **EXEMPLARY mechanism diagnostic**: per-block sigmoid row_sum table revealed optimizer used sigmoid's freedom to DOWN-WEIGHT (row_sum 0.40-0.65 in blocks 0-2, well BELOW softmax's 1.0) NOT to attend broadly; block 3 collapsed to near-uniform 1/24 (std=0.0009, row_sum≈0.97) — block 3 attention neutralized, corroborated by smallest γ_attn=0.0117; max attention probability ≤ 0.107 across all blocks — sigmoid never produced sharp concentration; **47th taxon: non-softmax attention shape at G=24 slice tokens** — softmax row-normalization is a load-bearing inductive bias at this scale; Ramapuram et al. 2024 "multi-position simultaneously relevant" billions-of-params LM/ViT result does NOT transfer to 328K-param 4-block Transolver; combined with closed attention-internal 4-mechanism axis (τ, spectral norm, QK-Norm, Talking-Heads all WASH/LOSS), attention-mechanism axis now closed across **5 distinct mechanisms within softmax framework + 1 first non-softmax replacement**). Pivoting both students from saturated attention-shape and positional-embedding axes. Assigned #2704 alphonse Manifold Mixup α=0.2 (Verma et al. 2019 ICML; hidden-state linear interpolation at random TransolverBlock input k uniform on {0,1,2,3}; per-batch λ ∼ Beta(0.2,0.2); training-mode only; ZERO new params; ~10-line training-loop hook; FIRST hidden-state interpolation probe in launch; structurally distinct from edward in-flight #2687 input-space Mixup — Manifold Mixup operates at RANDOM HIDDEN LAYER not just input; complementarity is a feature: if input Mixup LOSES but Manifold Mixup WINS, learn something about where in network to apply linear interpolation; directly targets in-dist overfitting bottleneck identified by Lion-WD closure 43rd taxon; key diagnostic: λ distribution + k_layer histogram + per-split signature in_dist regression vs improvement). Assigned #2706 askeladd α-entmax α=1.5 attention (Peters et al. 2019 ACL; replace softmax(Q·K/√d) over slice tokens with entmax15; sparse with row-sum=1; KEEPS normalization sigmoid removed BUT allows exact zeros softmax forbids; structurally distinct from closed sigmoid #2673 by preserving normalization; structurally distinct from closed τ #2623 + spectral #2580 + QK-Norm #2661 + Talking-Heads #2669 by operating outside softmax framework; ISOLATES sparsity from normalization dimension; ZERO new params; ~3-line softmax → entmax15 swap; student's #3 follow-up recommendation from closed sigmoid PR; targets multi-scale CFD feature simultaneity at G=24 slice prototypes; key diagnostic: sparsity per block + top-1 prob + nonzero count). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 80] UTC (round-80: 1 closure + 1 assignment. Closed #2672 nezuko Kendall heteroscedastic uncertainty-weighted loss LOSS (val 33.6532, +0.84% vs NEW baseline 33.3722; test 29.1607, +2.78% LOSS; **MIXED per-split signature** — helped OOD splits camber_rc −3.06%, re_rand −2.15% but hurt easy splits in_dist +5.50%, cruise +10.12%; Kendall mechanism ENGAGED: terminal s_surf=−2.42 (eff_w_surf 5.61, was 10), s_vol=−1.75 (eff_w_vol 2.89, was 0.5); ratio surf/vol went from 10.0 → 1.94 — Kendall rebalanced 5× toward volumetric loss; **probabilistic NLL optimum DIVERGES from primary surface-pressure metric optimum** — heteroscedastic uncertainty target is "minimize joint NLL" not "minimize surface-pressure MAE"; algebraic mismatch: when surf has lower true noise than vol, NLL-optimal eff_w_surf < fixed-weight-optimum eff_w_surf; 45th taxon: **loss-balance meta-axis closed across all 3 directions** — fixed surf_weight=10 baseline OPTIMAL beats learnable Kendall #2672 + per-channel ch_w surf_loss-only #2496 + global per-channel ch_w #1428/#1871; combined with closed Huber β family (β=0.1, 0.25, 0.5, 1.0, 2.0) + L1-MERGED + berHu, the **loss-SHAPE meta-axis is also fully saturated**; student diagnostic exemplary on probabilistic-vs-MAE divergence). Pivoting nezuko from saturated loss-balance + Lion-internal axes to underexplored **schedule-restart axis** (only one schedule change since launch: merged warmup-3-cosine #2033). Assigned #2697 nezuko SGDR Cosine Warm Restarts (Loshchilov & Hutter 2017 ICLR; `CosineAnnealingWarmRestarts(T_0=35, T_mult=2)` replacing `CosineAnnealingLR(T_max=epochs-3)`; warmup-3 prefix retained; single warm restart at approximately ep38 resetting LR back to peak 1.5e-4; second cosine cycle decays toward 0 over remaining ~32 epochs; ZERO new params; one-line scheduler change; FIRST schedule-restart probe since warmup-3-cosine merged; targets Lion narrow-basin tendency (30th, 35th taxa) via mid-budget LR reset enabling escape from sharp minimum AND re-anneal into potentially wider basin in second cycle; structurally orthogonal to Lion-internal exhaustion AND all 45 closed taxa AND 7 in-flight WIP including pending fern gradient noise; key diagnostic: best epoch position — within first cycle (sharp-minimum survives), at restart boundary ~ep38 (warm-restart shake worked), or within second cycle ep>40 (re-anneal found wider basin)). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 79] UTC (round-79: 1 closure + 1 assignment. Closed #2669 tanjiro Talking-Heads Attention LOSS (val 34.5351, +3.48% vs NEW baseline 33.3722; test 28.8990, +1.85% LOSS; UNIFORM REGRESSION all 4 val splits with val_single_in_dist regressed MOST +8.17% — opposite of predicted OOD-friendly direction. **KEY MECHANISM DIAGNOSTIC: mixing matrices ENGAGED but failed to help:** logits_mixing |dev_from_I| per block [0.21, 0.14, 0.16, 0.20], probs_mixing |dev_from_I| [0.08, 0.09, 0.05, 0.02] — pre-softmax mixing 2-10× stronger than post-softmax; logits_mixing diagonals SHRANK BELOW 1.0 [0.58-0.94] meaning heads were ABSORBING each other's logits down-weighting each head's own pattern; at H=2 'sharing' between heads homogenizes them rather than expanding diversity, confirming theoretical concern that Talking-Heads is more useful at H≥4-8 where head specialization is stronger; identity-init wasn't enough — optimizer actively moved mixing toward harmful regime. **44th taxon: cross-head attention mixing at H=2 closed.** Attention-internal axis now COMPREHENSIVELY closed across 4 distinct mechanisms: τ #2623 (37th post-softmax shape) + spectral norm #2580 (32nd weight constraint) + QK-Norm #2661 (42nd score stabilization) + Talking-Heads #2669 (44th cross-head mixing); attention-internal stabilization is NOT the bottleneck on this stack at H=2). Pivoting tanjiro to structurally distinct architectural probe: channel-wise gating from global stream content (Squeeze-Excitation) — fundamentally different mechanism from per-token attention, per-channel LayerScale, or input-conditioned FiLM. Assigned #2692 tanjiro Squeeze-Excitation per-block (Hu, Shen & Sun 2018 CVPR; SE module at end of each TransolverBlock: mean_pool over tokens → Linear(96→12) → GELU → Linear(12→96) → sigmoid → broadcast multiply; reduction=8; +9,648 params total +2.9%; zero-init fc2 → gate=0.5 at training start; FIRST channel-wise gating CONDITIONED ON GLOBAL STREAM CONTENT in launch — distinct from LayerScale (per-channel CONSTANT gain), FiLM (input-conditioned not content-conditioned), and attention (per-token not global); structurally orthogonal to all 44 closed taxa AND all 7 in-flight WIP including pending DyT and Mixup). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 78] UTC (round-78: 2 closures + 2 assignments. Closed #2661 frieren QK-Norm WASH/LOSS (val 33.4391, +0.20% vs NEW baseline 33.3722 = WASH; test 29.1677 vs 28.3736 = +2.80% clear LOSS; 3/4 val splits IMPROVE — in_dist −2.58%, cruise −1.75%, re_rand −1.20% — but val_geom_camber_rc REGRESSES +3.50% dominating avg; test_geom_camber_rc regresses +5.62% — consistent camber_rc harm across val/test confirms QK-Norm specifically damages hard geometric-OOD generalization by removing Q/K magnitude info needed for extreme camber distinction; pre-norm ||Q||,||K|| grew 80-200× from init confirming mechanism worked but LayerScale γ_attn=0.010-0.018 already caps attention residual contribution to ~1-2% of stream making softmax saturation NON-BOTTLENECK; **42nd taxon: attention-internal score-stabilization closed completing trio τ #2623 (37th post-softmax shape) + spectral norm #2580 (32nd weight constraint) + QK-Norm #2661 (42nd score stabilization) — three independent mechanisms all WASH/LOSS proving Pre-LN + small LayerScale γ_attn IS sufficient attention regularization at this stack scale).** Student suggested follow-up insight: camber_rc consistently fails across attention-internal interventions → DATA SPARSITY problem not model regularization problem (front-foil M=6-8 held out; up-weighting M=2-5 and M=9 training neighbors might help geometric extrapolation more than any attention tweak). Closed #2658 edward Lion WD-down LOSS (val 34.8122, +4.31% vs NEW baseline; test 29.2347, +3.04%; UNIFORM REGRESSION all 4 val/test splits; val_single_in_dist regressed MOST +12.84% — seen single-foil distribution was what WD=3e-4 was protecting against overfitting; val_geom_camber_rc regressed LEAST +0.26% — OOD camber generalization is WD-INSENSITIVE; in-dist→OOD progressive regression confirms in-dist-favoring regularization not OOD-favoring; best epoch moved earlier 61 vs 70 but val/test both regressed; second attempt at same config gave val 34.4842 within run-to-run variance — result confirmed; **43rd taxon: Lion-internal optimization axes now EXHAUSTIVELY closed across all 4 dimensions — LR (36th, optimum 1.5e-4), β₁ (35th asymmetric peak at 0.90), β₂ (39th catastrophic at 0.999), WD (43rd, 3e-4 load-bearing for in-dist regularization); Lion optimizer SATURATED at lr=1.5e-4, betas=(0.9, 0.99), wd=3e-4)**. Both students pivot from saturated meta-axes. Assigned #2686 frieren DyT (Dynamic Tanh) normalization (Liu, Ba et al. 2024 NeurIPS 'Transformers without Normalization'; replace ALL nn.LayerNorm sites with DyT(d) = γ⊙tanh(α·x)+β; α scalar per LN site, init=0.5; +9-12 scalar params total; FIRST normalization-replacement probe in launch beyond closed RMSNorm; structurally distinct from NormFormer Sandwich (adds Post-LN), LayerScale (residual gain), and all attention-internal closures; removes channel-stat reduction in favor of bounded element-wise tanh; key diagnostic: α evolution per site at terminal + LayerScale γ_attn interaction). Assigned #2687 edward Mixup α=0.2 (Zhang et al. 2018 ICLR; per-batch λ ∼ Beta(0.2, 0.2), permute batch, blend x and y; training-mode only; ZERO new params; FIRST input-space LINEAR BLEND probe in launch; structurally distinct from closed reflection-aug #2454 (catastrophic, half-space mesh violation), coord-jitter (geometric perturbation), per-channel-loss (loss reweighting), and parameter-space averaging meta-family; directly targets in-dist overfitting bottleneck identified by Lion-WD closure's per-split signature; physical interpretation: synthetic samples on Re/AoA/geometry manifold force smooth interpolation). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 77] UTC (round-77: 1 closure + 1 assignment. Closed #2615 fern DropPath p=0.1 as 1st stale_wip (PR created 2026-05-13T22:16:56Z, ~24h+ zero pod activity from round-68 through round-76; only advisor placeholder commit; multi-launch precedent: DropPath was also abandoned in a prior launch after 4 stalls in round-39 → systematic implementation-pattern non-pickup; fern pod history pattern compounds: #2496 per-channel-surf 1st stale → #2557 retry-1 2nd stale axis-abandoned → #2615 DropPath 1st stale; hypothesis preserved at launch level but axis not retried this launch). Pivoting fern to maximally implementation-light optimization-internal probe never tried in launch. Assigned #2677 fern gradient noise injection (Neelakantan et al. 2015 'Adding Gradient Noise Improves Learning for Very Deep Networks'; annealed Gaussian noise σ_t = η/(1+t)^γ on parameter gradients BETWEEN loss.backward() and optimizer.step(); η=0.03, γ=0.55; σ at step 0 = 0.030, at step 100 ≈ 0.0040, at step 3500 ≈ 0.0005; ~3-line training-loop addition; ZERO new params; ZERO model changes; structurally orthogonal to all 41 closed taxa AND all 7 in-flight WIP — modifies gradient signal BEFORE optimizer-internal momentum/sign/normalization logic; targets Lion narrow-minimum tendency directly via early-epoch saddle-point escape; theoretically attacks camber_rc OOD bottleneck via flatter-basin convergence; per-step annealing aligns naturally with cosine cooldown; implementation-light maximizes pickup probability for fern pod). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 76] UTC (round-76: 1 closure + 1 assignment. Closed #2646 alphonse per-block FiLM LOSS (val 34.2522, +2.64% vs NEW baseline 33.3722; 4 independent FiLM gates one per block input vs single shared gate from merged #2614; 41st taxon: depth-specific FiLM dilution under residual-stream interruption. KEY DIAGNOSTIC: per-block FiLM weight norms learned heterogeneously [block_0: 1.52, block_1: 1.78, block_2: 2.04, block_3: 2.22] — depth-varying flow conditioning signal IS REAL with deeper blocks demanding stronger gating, but aggregate 4-gate budget exceeded single-gate without improving outcome; mechanism: residual stream never settles into flow-agnostic processing between blocks — each block continuously re-modulated by Re/AoA disrupts what merged #2614 single pre-block-0 gate achieved with one-shot conditioning at stream entry; per-split signature consistent: in_dist +3.8%, camber_rc +1.7%, cruise +2.4%, re_rand +2.9% — uniform regression confirms not OOD-specific damage; **flow-conditioning meta-family now fully mapped**: single pre-block-0 gate MERGED #2614 OPTIMUM, per-block depth-dilution LOSS #2646, output-side bias LOSS #2531, multiplicative output gate LOSS #2588 — conditioning works at residual-stream INPUT only, not depth-distributed and not output-side; 41st taxon closes the depth-distribution arm of flow-conditioning meta-family). Pivoting alphonse to structurally distinct axis untouched by any conditioning variant. Assigned #2675 alphonse 2D coord-based RoPE on Q, K projections (Su et al. 2021 extended to 2D mesh; rotate Q, K post-projection using (x, y) coord-frequency products; V unchanged; splits d_head=48 into 24 x-dim + 24 y-dim channels; frequency buffers via register_buffer; ZERO new parameters; **FIRST positional-embedding probe in launch** — Transolver has been geometry-blind across all 41 closed taxa; physically motivated by CFD spatial structure where attention should encode WHERE features are not just WHAT they are; structurally distinct from all closed flow/condition gates (output-side, residual-stream, multiplicative, depth-distributed) — operates at attention computation NOT residual stream; orthogonal to slice-routing softmax which is over slice prototypes not spatial positions). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 75] UTC (round-75: 2 closures + 2 assignments. Closed #2647 nezuko Lion β₂=0.999 CATASTROPHIC LOSS (val 59.4855, +78.25% vs NEW baseline 33.3722; test 52.2704, +84.22%; uniform regression all 4 splits 58-119%; in_dist +108.87% WORST, cruise +118.94% also catastrophic; best ep39/70 confirms convergence stopped halfway; mechanism: 1000-step EMA memory window incompatible with 70-epoch budget — only ~5 baseline refreshes per run vs ~50 with β₂=0.99; combined with sign-step truncation, miscommit cycles amplify dramatically; 39th taxon: Lion β₂-UP at long-memory regime fails catastrophically; **Lion-internal optimization axes now exhaustively closed** with β₁ (35), LR (36), β₂ (39), only in-flight #2658 WD remaining). Closed #2642 askeladd block-shared slice projection LOSS (val 35.2326, +5.57% vs NEW baseline; test 29.5146, +4.02%; uniform regression all 4 splits; camber_rc +4.39% fired in OPPOSITE direction of PR's 2% diagnostic; camber_cruise +13.36% WORST (matches narrow-minimum pattern from closed #2602 Lion LR-up); mechanism: per-block slice projections learn GENUINELY INDEPENDENT routings, not redundant copies; sharing wastes capacity proportional to depth; 40th taxon: slice-routing meta-axis fully saturated across COUNT (33), SHARPNESS (37), and DEPTH-SHARING (40)). Pivoting BOTH students away from saturated axes. Assigned #2672 nezuko Kendall heteroscedastic uncertainty-weighted multi-task loss (Kendall, Gal & Cipolla 2018 CVPR; replace fixed surf_weight=10 with learned log-variance scalars s_surf, s_vol; +2 params; FIRST probabilistic loss re-weighting probe in launch; structurally distinct from all closed fixed-weight surf/vol/per-channel weighting). Assigned #2673 askeladd Sigmoid Attention (Ramapuram et al. 2024 Apple; replace MHA softmax over Q·K positions with sigmoid(logits - log(N)); zero new params; slice-routing softmax UNCHANGED; FIRST non-softmax attention probe in launch; structurally distinct from all 40 closed taxa which kept canonical softmax). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 74] UTC (round-74: 1 closure + 1 stale-close + 2 assignments. Closed #2643 tanjiro bias-free Linears LOSS (val 34.4150, +3.13% vs NEW baseline 33.3722; test 29.6907, +4.64% LOSS; student measured vs old #2553 baseline showing +2.75% but vs NEW baseline clearly LOSS; 3/4 splits regress: in_dist +3.1%, camber_cruise +8.1% WORST, re_rand +6.6%; only camber_rc WINS −2.5% (hardest OOD); mechanism: at ~326K params n_hidden=96, per-output Linear biases are LOAD-BEARING — LLaMA-style "biases absorbed by upstream LN β" requires `W·β = b` pre-image, theoretically achievable at rank-96 but optimization-wise non-trivial at this small width; LLaMA convention validated at billions-of-params scale doesn't transfer; 38th taxon: parameter-pruning / bias-free axis closed at this scale; biases stay as default). Closed #2597 thorfinn n_head=4 as 1st stale_wip (only advisor placeholder commit from round-65 timestamp 2026-05-13T21:40:25Z, 8+ rounds zero activity; hypothesis preserved). Assigned #2668 thorfinn n_head=4 retry-1 on fresh branch (single-value change `n_head 2→4, dim_head 48→24, total inner dim 96 preserved`; structurally simpler than NormFormer which 2-staled before; targets multi-head granularity hypothesis for OOD interpolation). Assigned #2669 tanjiro Talking-Heads Attention (Shazeer et al. 2020; 2×2 logits_mixing + 2×2 probs_mixing Linear modules per block, identity-init; +32 params total; cross-head linear mixing pre- and post-softmax; structurally distinct from all 38 closed taxa — first ACROSS-head attention probe; compounds with in-flight #2661 QK-Norm if both win). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 73] UTC (round-73: 1 closure + 1 assignment. Closed #2623 frieren attention temperature τ LOSS (val 33.7249, +1.06% vs NEW baseline 33.3722; test 28.8200, +1.57%; student measured vs old #2553 which read as WASH-borderline but vs new baseline it's clearly LOSS; best ep66/70 timeout; per-block terminal τ values [0.81, 0.88, 0.83, 0.79] — ALL 4 blocks moved coherently below 1.0 mean ~0.83 indicating optimizer preferred softer routing, but val didn't move: in_dist −1.71% WIN, camber_rc flat +0.38% (failed predicted >2% WIN signal), camber_cruise +3.92% worst regressor; mechanism worked (τ engaged consistently) but no payoff; 37th taxon: slice-routing-sharpness axis closed; combined with closed slice_num bracket {16, 24, 32, 48, 96} at 24 and τ axis just closed, slice-routing meta-axis essentially saturated; no per-block heterogeneity signal — falsified block-3-special hypothesis). Assigned #2661 frieren QK-Norm (Henry et al. 2020 / Dehghani et al. 2023 ViT-22B; per-head LayerNorm on Q and K post-projection, V unchanged; +768 params; targets Q·K magnitude drift / softmax saturation; structurally distinct from τ (post-softmax shape) and spectral norm (weight constraint); standard in PaLM-E, ViT-22B, Gemma; completes attention-internal axis exploration). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 72] UTC (round-72: 1 closure + 1 assignment. Closed #2602 edward Lion lr=1.75e-4 LOSS (val 33.8115, +1.32% vs NEW baseline 33.3722, +0.95% vs old; test 29.0217, +2.28%; bracket {1, 1.5, 1.75, 2}e-4 = {36.40, 33.49, 33.81, 33.83} — sharp regression step between 1.5 and 1.75, not gradual; lr=1.5 on sharp boundary; 36th taxon: Lion LR-up axis fully closed at lr=1.5e-4. Critical per-split finding: val_single_in_dist IMPROVED at 1.75 (25.55 vs 25.77) and val_geom_camber_rc IMPROVED (50.23 vs 50.55), but val_geom_camber_cruise REGRESSED HARD +6.97% — dominant LOSS driver. camber_rc and camber_cruise have OPPOSING LR preferences; lr=1.5e-4 is the equilibrium. Combined with closed β₁ axis (35th taxon), Lion-internal LR + β₁ axes both saturated). Assigned #2658 edward Lion weight_decay 3e-4 → 1e-4 (1/3 lower; WD axis is structurally distinct from LR axis; never swept on Lion baseline; tests whether Lion paper's 3× AdamW WD rule-of-thumb is over-regularizing the budget-bound stack; orthogonal to in-flight #2647 β₂; single-value change). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 [Round 71] UTC (round-71: **MERGED #2614 alphonse FiLM feature-stream gate WIN** (val 33.3722, −0.36% vs #2553 33.4935; test 28.3736, −0.89%; uniform test WIN all 4 splits: in_dist −0.90%, camber_rc −1.04%, cruise ~0%, re_rand −1.16%; val_re_rand mild regression +2.12% = n=100 noise, test_re_rand confirms improvement −1.16%; FiLM weights learned non-trivially: film.norm=2.62 from 0, bias=0.85; modulation factor 0.92 on re_rand → gate down-scales Re-OOD by ~8%; +384 params total; flow conditioning at feature-stream NOT output; feature-conditioning meta-family NOT closed at this level). **NEW BASELINE 33.3722.** Closed #2613 nezuko Lion β₁=0.85 LOSS (val 35.8174, +6.94% vs NEW baseline 33.3722; test 31.2413, +10.13% LOSS; momentum non-zero fraction 0.9991 CONFIRMS faster reaction prediction; but faster flipping HURTS narrow-minimum settling in cosine cooldown; bracket {β₁=0.85: LOSS, β₁=0.90: OPTIMUM, β₁=0.95: LOSS} fully closed with asymmetric peak; up-side steeper than down-side; 35th taxon: Lion-β axis fully closed both directions). Assigned #2646 alphonse per-block FiLM (4 independent FiLM gates, one per block input, replacing single pre-block-0 gate from #2614; depth-specific flow conditioning; +1,536 params total; mirrors conditional batchnorm in GANs; compounds on merged FiLM WIN) and #2647 nezuko Lion β₂=0.999 (β₂-UP from 0.99 to 0.999; 10× longer memory window ~1000-step vs ~100-step; student suggestion #2 from β₁=0.85 results; first β₂ probe in launch; orthogonal to fully-closed β₁ axis). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 70] UTC (round-70: 2 closures + 2 assignments. Closed #2607 askeladd slice_num=48 LOSS (+4.13% val, +5.55% test; uniform regression with OOD cruise regressing MOST +9.66% — opposite of prediction; 33rd taxon: slice-up at slice_num>24 creates harder optimization landscape with no extra signal; slice-num axis fully closed at slice_num=24 across {16, 24, 48, 96}; student correctly identified my param estimate was 2× too high — slice projection is one Linear per BLOCK not per HEAD). Closed #2595 tanjiro SwiGLU LOSS (+4.74% val, +6.21% test; 34th taxon: gated-FFN family adds optimization burden in budget-bound regime; in-dist regressed MOST → optimization not generalization issue; γ_mlp grew 600-700× larger than baseline; mid-run timeout from likely shared-GPU contention but per-split signature decisive). Assigned #2642 askeladd block-shared slice projection (student suggestion #4 from #2607; ties in_project_slice across 4 blocks; −3,456 params; tests slice-routing redundancy across depth) and #2643 tanjiro bias-free Linears (LLaMA convention; bias=False on all attention+MLP Linears; LN affine + mlp2 unchanged; −2,688 params; structural distinct probe). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 69] UTC (round-69: Closed #2580 frieren spectral norm LOSS vs current baseline (+6.36% val, +12.2% test; student used wrong LR 1e-4 vs 1.5e-4 contract; even on favorable old-baseline comparison, val/test rank DISAGREE — val WIN on 3/4 splits but test +2.93% LOSS; camber_rc was the ONLY val regressor — the very split Bartlett margin-bound theory predicted to benefit MOST; σ_eff=1.0 pinned across all 16 wrapped layers confirms constraint binding; σ_raw 1.33-3.42 confirms model wants more spectral budget; 32nd taxon: attention-projection spectral norm regresses on Transolver+L1+Lion; function-class compression helps cruise/re_rand but hurts camber_rc OOD bottleneck and does not transfer to test). Assigned #2623 frieren learnable attention temperature τ per block (init=1.0; +4 params total; direct interpretable diagnostic on slice-routing sharpness; terminal τ values reveal whether slice_num=24 is over/under-routing; orthogonal to all in-flight). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 68] UTC (round-68: 3 closures + 3 assignments. Closed #2590 nezuko Lion β₁=0.95 LOSS (+10.4%, uniform regression; 30th taxon: persistent-momentum miscommit on sign-step; Lion narrow minimum is INTRINSIC to sign-step geometry — both β₁-up and β₁-down should not help if intrinsic). Closed #2588 alphonse multiplicative flow gate LOSS marginal (+0.46% val, +1.27% test; gate norm 0.91 confirms learned non-trivially; OPPOSITE per-split signature vs predicted — OOD splits regressed; 31st taxon: output-side flow-conditional readout meta-family closed across additive #2531 + multiplicative #2588). Closed #2557 fern per-channel loss as 2nd consecutive stale_wip (axis abandoned, NormFormer precedent). Assigned #2613 nezuko Lion β₁=0.85 β₁-down probe (student suggestion #2; bracket-completing symmetric counter-experiment; decides whether Lion-β family is fully closed). Assigned #2614 alphonse feature-stream FiLM gate (student suggestion #2 from #2588; conditioning at residual stream not output; +384 params; structurally different from closed output-side meta-family). Assigned #2615 fern Stochastic Depth / DropPath p=0.1 (Touvron 2021 CaiT completion; fresh regularization axis; orthogonal to spectral norm in-flight; zero params). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 67] UTC (round-67: Reviewed #2589 askeladd PaLM-parallel retry-1 LOSS (val 34.8652, +4.10% vs 33.4935; 29th taxon: parallel-composition regresses; γ_attn grew 2.7× (0.005→0.0136) — mechanism worked (decoupled gradient paths let attn branch scale up) but ALL 4 splits uniformly worse; MLP-post-attention correction is load-bearing: the sequential composition mlp(attn(x), x) matters more than gradient-path decoupling; EXEMPLARY SCIENCE but LOSS). Assigned #2607 askeladd slice_num=48 sweep (2× slice routing capacity 24→48; student's own suggestion; PhysicsAttention's architecturally unique part; untested intermediate between closed 96 and baseline 24; +9,216 params (+2.8%) from slice projection Linear; structurally distinct from all in-flight experiments; per-epoch cost ~5-10% higher; NEW bar = val < 33.4935). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 66] UTC (round-66: Reviewed #2583 edward Lion lr=2e-4 LOSS (val 33.8328, +1.01% vs 33.4935; key finding: per-split LR trade-off — OOD splits improved: camber_rc −0.74%, re_rand −3.39%; but in-dist +7.64% and cruise +5.08% REGRESSED; higher LR finds wider basin (OOD-friendly) but overshoots in-dist precision; net loss because in-dist regression dominates; best epoch still ep70; still budget-bound; LR-up axis CLOSED at 2e-4; optimum between 1.5e-4 and 1.75e-4). Assigned #2602 edward Lion lr=1.75e-4 midpoint to bracket the LR optimum precisely. No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 65] UTC (round-65: Closed #2541 thorfinn NormFormer Sandwich Norm retry-1 as 2nd consecutive stale_wip (after #2480 stale + #2541 stale; ~10 rounds without pickup; last updated 2026-05-13T19:33:36Z; only advisor placeholder commit). NormFormer axis abandoned (following same pattern as split-decoder: 3-stale abandon). Pivoted thorfinn to #2597 n_head=4 sweep (first attention-shape probe in this launch; n_head 2→4, dim_head 48→24; total attention dim preserved 4×24=96=2×48; single-value change; tests whether more attention heads with smaller per-head capacity unlocks richer multi-head behavior, particularly for OOD camber_rc split; NEW bar = val < 33.4935). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 64] UTC (round-64: Closed #2536 tanjiro split-decoder retry-2 as 3rd consecutive stale_wip (same hypothesis as #2396 stale + #2472 stale + #2536 stale; implementation complexity too high for consistent pod pickup; only advisor placeholder commit). Pivoting tanjiro from split-decoder axis (abandoned after 3 non-starts) to SwiGLU MLP: replace vanilla GELU-FFN with gated linear unit (Shazeer 2020; LLaMA/PaLM architecture). SwiGLU is a single-class swap with approximately same param count (d_hidden adjusted to 2/3 × mlp_ratio × d to compensate for extra W2 projection); zero split-decoder complexity; first gated-FFN probe in this launch. No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 63] UTC (round-63: Closed #2567 nezuko SWA late-start ep30-70 LOSS (swa_val 40.7580, +12.0% vs #2524 36.3994 / +21.7% vs NEW #2553 33.4935; 28th taxon: weight-averaging meta-family CLOSED — critical finding: drift ratio=2.09% caused 10.7% val degradation, i.e. 5× loss sensitivity per unit drift → Lion's sign-step creates NARROW not flat minimum; SWAD flat-minima hypothesis structurally violated; meta-family is now 3-LOSS: EMA #2544 + Lookahead #2550 + SWA #2567; student analysis exemplary; best-live val 36.8058 also missed baseline; 7/9 snapshots collected before timeout). Assigned #2590 nezuko Lion β₁=0.95 sweep (increase momentum persistence from 10-step to 20-step time constant; tests whether sign-direction noise near narrow minimum is addressable by more persistent momentum; single-value change; all other params unchanged; baseline to beat: val < 33.4935). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 62 addendum] UTC (round-62 addendum: Closed #2526 askeladd PaLM-parallel-attn-mlp as 2nd stale_wip (only placeholder commit from round-53, 9 rounds ago; last updated 2026-05-13T19:11:56Z; ~26h zero activity). PaLM-parallel hypothesis preserved (zero params, decouples γ_attn/γ_mlp gradient paths, ~5-10% epoch speedup, structurally unclosed). Reassigned as #2589 RETRY-1 on current Lion lr=1.5e-4 baseline (val < 33.4935). All 8 in-flight, zero idle.)
- **As of:** 2026-05-13 [Round 62] UTC (round-62: Closed #2531 alphonse flow-conditional output bias LOSS (val 47.1620, +11.4% vs old #2307 42.3455 / +40.8% vs NEW #2553 33.4935; all 4 splits regress +7-20%; weight-norm ratio 0.263 — additive double-counting signature confirmed independent of input source; 27th distinct closed-axis taxon: "additive output-side conditioning paths from broadcast scalars produce decoder-fork interference independent of input source" — cross-confirmed with latent-input PR #2503; gate earned weight but hurts overall fit; student's diagnosis exemplary). Assigned #2588 alphonse multiplicative flow gate (output = mlp2 * (1 + gate(Re, AoA0, AoA1)); gate zero-init → identity start; cannot double-count additively; structurally distinct from #2531 — if also fails, closes the flow-conditional readout axis entirely; NEW bar = val < 33.4935). No human GH issues. All 8 in-flight, zero idle.)
- **As of:** 2026-05-14 21:00 UTC (round-61: **MERGED #2553 edward Lion lr=1.5e-4 WIN** (val 33.4935, −8.05% vs #2524 36.3994; test 28.6279, −8.30%; all 4 splits improve uniformly: in_dist −9.60%, camber_rc −3.50%, cruise −14.36%, re_rand −8.90%; best=ep70/70 terminal — still monotonically descending at LR≈0; Scenario A FALSIFIED: higher LR did NOT accelerate convergence, it unlocked DEEPER minimum; best epoch moved LATER ep65→ep70 confirming wider landscape exploration pre-cooldown; model still budget-bound; first beat baseline at ep54 with 16 epochs to spare; 2nd consecutive Lion-family WIN). **NEW BASELINE 33.4935**. Assigned #2583 edward Lion lr=2e-4 (1.33× #2553; 2× #2524; continue LR-up direction since best epoch still ep70 = still budget-bound; all other params unchanged; baseline to beat: val < 33.4935). No human GH issues. All 8 students in-flight, zero idle, NEW baseline 33.4935.)
- **As of:** 2026-05-14 20:50 UTC (round-60: Closed #2550 frieren Lookahead(k=5, α=0.5) wraps AdamW (val 53.6305, +26.6% vs old 42.35 / +47.3% vs NEW 36.40 LOSS; uniform regression all 4 splits — cruise +42.5% WORST, camber_rc +13.2% LEAST; 70/70 epochs reached but val_avg monotonically descending at ep70=53.63 with LR≈0 — cosine cooldown WASTED; tail val_std last 10 ep=0.26 — smooth but around BAD plateau; CRITICAL mechanism analysis from student exemplary: over 1 cycle Δfast=g1+...+g5, outer step slow←slow+α·Δfast=0.5·Δfast, fast←slow → net per-cycle motion = 0.5·Δfast → effective step HALVED → 70 Lookahead epochs ≈ 35 effective AdamW epochs; per-split "regression-toward-mean" signature: easy splits regressed worst, hard splits regressed least — under-converged model can't differentiate, all splits drift to common ~50-60 plateau; OPPOSITE of predicted SWAD-style OOD-favoring pattern; 26th distinct closed-axis taxon: meta-optimizer halves effective step → under-convergence in budget-bound regime + cosine schedule mis-alignment; combined with #2544 EMA LOSS 25th taxon, the parameter-space-averaging meta-family is now 2-LOSS with one in-flight arm #2567 SWA-late; both LOSS variants share root cause: budget-bound regime can't afford mechanisms that delay effective convergence; base optimizer Lion just barely converges in 70 epochs). Assigned #2580 frieren spectral normalization on attention Linear layers (PIVOT away from weight-averaging family; 1-Lipschitz constraint on Q/K/V/proj projections via Miyato et al. 2018 SN; Bartlett et al. 2017 margin bounds → tighter OOD generalization bound; targets camber_rc 52.39 OOD bottleneck where Lion most improved performance; does NOT slow convergence — per-step weight normalization with no time-lag effect; structurally distinct from all 26 closed taxa: NOT LayerScale (per-channel gain), NOT RMSNorm (activation), NOT NormFormer in-flight (sandwich LN), NOT grad-clip (gradient norm), NOT SAM (gradient ascent); ~+5-15% per-epoch cost from power iteration; first Lipschitz-constraint regularization probe; NEW baseline to beat: val < 36.3994). No human GH issues. All 8 students in-flight, zero idle, baseline 36.3994.)
- **As of:** 2026-05-14 20:30 UTC (round-59: Closed #2544 nezuko EMA Polyak α=0.999 (val 48.19, +13.83% vs old 42.35 / +32.4% vs NEW 36.40; LOSS uniform all 4 splits — cruise worst +23.42%, camber_rc least +8.05%; 69/70 epochs timeout-capped, best=ep69; CRITICAL diagnostics: terminal drift ratio=0.0004 — EMA fully caught up to live (cosine LR near-zero ≡ zero weight movement); ep1 val_avg=379 — α^375≈0.687 cold-start contamination severe; head-to-head live=48.22 vs EMA=48.19 — 0.05% gap, both equally bad; best epoch=terminal every time, EMA always catching up, never plateaued; OOD pattern REVERSED from SWAD prediction: cruise +23.4% worst (expected least), camber_rc +8.05% least (expected most) — cold-start disproportionately hurt easy-splits; 25th distinct closed-axis taxon: parameter-space-averaging α-budget vs schedule mismatch + cold-start contamination; NOT a generic weight-averaging failure — discrete SWA-late is a clean structural retry). Assigned #2567 nezuko SWA-late (uniform discrete snapshot averaging at ep 30, 35, 40, 45, 50, 55, 60, 65, 70; late-start avoids cold-start entirely; uniform average fixes α-budget tradeoff; SWAD Cha et al. 2021 direct precedent for OOD; 9 snapshots × 328K params × 4 bytes = ~12MB CPU; evaluates only at terminal on the averaged snapshot; direct attack on camber_rc OOD bottleneck; NEW baseline to beat: val < 36.3994). No human GH issues. All 8 students in-flight, zero idle, baseline 36.3994.)
- **As of:** 2026-05-14 20:05 UTC (round-58: Closed #2496 fern per-channel-surf-p3 as 1st stale_wip (no commits since assignment at 18:00 UTC; only the advisor-assignment commit present; hypothesis preserved — per-channel surf loss ch_w=[1.0,1.0,3.0] is FIRST per-channel gradient allocation probe in launch; special interest now that Lion is on advisor: Lion sign-step means per-channel loss weights translate directly into per-channel sign-direction allocation in aggregated gradient sign; interaction of per-channel-loss × Lion-sign-step is novel and untested). Reassigned as #2557 fern per-channel-surf-p3-retry1 on fresh branch off current advisor (now has Lion merged; must beat NEW baseline 36.3994, NOT old 42.3455). No human GH issues. All 8 students in-flight, zero idle, baseline 36.3994.)
- **As of:** 2026-05-14 05:50 UTC (round-57: **MERGED #2524 edward Lion optimizer MASSIVE WIN** (val 36.3994, −14.05% vs 42.3455 baseline; test 31.2200, −18.92%). **NEW BASELINE 36.3994. LARGEST SINGLE-PR GAIN SINCE ROUND-1.** All 4 splits improve uniformly: in_dist −19.65%, camber_rc −13.88%, camber_cruise −14.35%, re_rand −9.69%. val_geom_camber_rc: 60.83→52.39 — LARGEST single-PR rc movement in launch. CRITICAL diagnostic: Lion momentum non-zero fraction=0.9986 — fully populated, no stuck/zero-update pathology. Mechanism: L1 loss already provides sign-based gradients; AdamW's 2nd-moment normalization approximately redundant; Lion sign(β₁m + (1-β₁)g) commits to sign-direction directly; no v_hat in bf16 = more stable than AdamW's 2nd-moment EMA in low-precision; test improves MORE than val (-18.92% vs -14.05%) → uniform step magnitudes provide slight regularization. Best ep=65/67 still improving at timeout → run cut before convergence. CRITICAL NOTE: Lion now on advisor branch; ALL subsequent PRs must beat new bar 36.3994. Assigned #2553 edward Lion lr=1.5e-4 sweep (1.5× baseline 1e-4; ep65/67 still improving at timeout in #2524 suggests LR slightly sub-optimal or cosine T_max too long; tests whether faster convergence within budget further improves result; if best epoch moves earlier AND val improves, LR was sub-optimal; if similar timing, cosine absorbs it; all other params identical). All 8 students in-flight, zero idle, NEW baseline 36.3994.)
- **As of:** 2026-05-14 05:30 UTC (round-56: Closed #2509 frieren Fourier feature encoding K=4 (val 48.2424, +13.94% LOSS — UNIFORM REGRESSION all 4 splits 5.93-22.93%; val_single_in_dist worst (+22.93%), val_geom_camber_rc LEAST (+5.93%); per-split signature DIAMETRIC OPPOSITE of predicted "high-frequency basis unlocks camber_rc" pattern; 54/70 epochs timeout-capped, best=ep50; mechanism: input dim expanded 24→40 (+67%, not +40% as PR body estimated) — 16 new periodic channels act as NOISE to PhysicsAttention slice-routing softmax; the orthogonal-init `in_project_slice` projection must route 40-dim node embeddings but FF channels don't correlate with geometric domain structure; preprocess Linear was NOT the bottleneck — 4 TransolverBlocks already learn adequate spatial-frequency basis from raw coords; saf channels (2-3) + dsdf rays (4-11) already subsume what NeRF-style coord encoding adds; 24th distinct closed-axis taxon: input-encoding-noise interferes with slice-routing softmax — input-encoding axis closes at K=0 (raw coords sufficient) for this stack at 24-channel input width; student's "slice-routing axis is more fertile" insight is on-point: `in_project_slice` sensitive to input scale/structure). Assigned #2550 frieren Lookahead optimizer k=5, α=0.5 (Zhang et al. 2019 NeurIPS slow-fast 2-loop scheme wrapping AdamW; fast weights = AdamW for k=5 inner steps, then slow weights interpolate: θ_slow += α(θ_fast - θ_slow); FIRST slow-fast meta-optimization probe in launch; cleanly distinct from in-flight #2544 EMA (continuous α=0.999 ~1000-step time constant vs discrete k=5 inner steps), in-flight #2524 Lion (sign-based base optimizer replacement, not meta-wrap), CLOSED SAM #2412 (gradient ascent perturbation), CLOSED LLRD #2497 + embed-UP #2457 (per-group LR scaling), and all 8 optimizer-family closures; ~+0% per-epoch cost; transient +1× slow-weights copy memory; targets generalization via flat-minima theorem). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 05:00 UTC (round-55: Closed #2495 nezuko aux-camber-head λ=0.1 RETRY-1 (val 46.9564, +10.89% LOSS — UNIFORM REGRESSION all 4 splits 3.97-23.76%; val_single_in_dist worst (+23.76%), val_geom_camber_rc LEAST (+3.97%); 58/70 epochs timeout-capped, best=ep57, terminal=ep58 1-ep bounce; CRITICAL aux-MSE trajectory: ep1=0.986 → ep3=0.085 (10× drop in 3 epochs) → ep57=0.0016; aux task TRIVIALLY SATISFIED — camber `x[:,0,15]` is already an input channel, hidden representation copies it through network; damage concentrated in epochs 1-3 where aux gradient λ×MSE ~0.099 is comparable to surf gradient signal, shifting representation toward redundant camber-encoding direction; per-split signature DIRECTIONALLY CONSISTENT with mechanism (camber_rc least hit; bimodal gap COMPRESSES from 71% → 44% but by hurting in_dist not helping rc); 23rd distinct closed-axis taxon: auxiliary-objective-with-trivially-satisfied-target — early-training inductive-bias damage; not λ-tunable (failure mode is structural). Closed #2480 thorfinn normformer-sandwich as 1st stale_wip (no commits since 2026-05-13T17:25:49Z, ~11h zero activity); hypothesis preserved — reassigned as #2541 RETRY-1 on fresh branch. Assigned #2544 nezuko EMA Polyak weight averaging (decay α=0.999 step-EMA on model parameters; evaluate using EMA snapshot; FIRST WEIGHT-AVERAGING PROBE in launch; Polyak-Ruppert 1992 + Izmailov et al. 2018 SWA + Cha et al. 2021 SWAD; theoretically targets OOD generalization via flat-minima hypothesis — directly relevant to val_geom_camber_rc 60.83 bottleneck; zero inference param-count change; ~+0.5-1% per-epoch cost; cleanly distinct from CLOSED SAM #2412 (gradient-ascent perturbation vs parameter smoothing), CLOSED LayerScale-init-asymmetry (per-channel learnable gain), and all 8 closed optimizer-family closures (meta-mechanism ON TOP of optimizer)). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 04:45 UTC (round-54 addendum: Closed #2472 tanjiro split-surf-vol-heads RETRY-1 as 2nd consecutive stale_wip (no commits since 2026-05-13T17:15:47Z, ~11h after assignment; original #2396 also stale; tanjiro pod has now 2 consecutive stalls on same hypothesis). Reassigned as #2536 RETRY-2 on fresh branch off current advisor (full stack inherited: L1 + compile + bf16 + scoring fix + sampler 2× single + slice_num=24 + warmup-3-cosine + n_head=2 + LayerScale γ=1e-4 + n_layers=4 + n_hidden=96 + mlp_ratio=2). Hypothesis still structurally fresh among 22 closed taxa. If RETRY-2 also stalls without committed results, axis will be deprioritized per same pattern as DropPath (abandoned after 4 stalls in round-39). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 04:30 UTC (round-54: Closed #2503 alphonse decoder residual skip (val 46.7497, +10.40% LOSS — UNIFORM REGRESSION all 4 splits 6.16-18.84%; val_single_in_dist worst (+18.84%) — DIAMETRIC OPPOSITE of predicted linear-component WIN; val_geom_camber_rc least (+6.16%); 70/70 epochs, best=ep66; CRITICAL diagnostic: mlp2_final_w.norm()=1.0836, skip_proj_w.norm()=0.4347, **ratio=0.40** — skip IS load-bearing (well above 0.1 ignored threshold) but training drove it to a HARMFUL equilibrium; bias zero-init didn't rescue: skip_proj.weight inflated despite zero-init baseline starting; mechanism: redundant readout pathway — mlp2 (96→GELU→96→3) already encodes smooth+sharp mixture efficiently; adding redundant linear path SPLIT gradient between two competing readout heads; optimizer settled on suboptimal 40-60 mlp2-skip mixture worse than mlp2 alone; 22nd distinct closed-axis taxon: redundant-readout-pathway interference — skip is load-bearing but harmful; readout-pathway architecture axis closes at mlp2 alone; matches "LOSS double-counting" prediction arm exactly; composability with in-flight #2472 split-heads is moot for this skip form). Assigned #2531 alphonse flow-conditional output bias (flow_bias = nn.Sequential(Linear(3, 16), GELU, Linear(16, 3)) with zero-init final Linear; flow_conditions = x[:, 0:1, [13, 14, 18]] (Re, AoA, fun_dim); output = mlp2(ln_3(fx)) + flow_bias(flow_conditions); +115 params (~+0.035%); first flow-condition-conditional OUTPUT-bias probe; physically motivated by freestream + perturbation decomposition for incompressible NS; cleanly distinct from CLOSED FiLM #2410 (per-block modulation, this is single-shot output bias) and CLOSED decoder-skip #2503 (latent-conditioned, this is flow-condition-conditioned); zero-init prevents immediate weight-splitting failure mode of decoder-skip; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 04:10 UTC (round-53: Closed #2502 askeladd attention temperature init τ=0.25 (val 45.4153, +7.25% LOSS — UNIFORM REGRESSION all 4 splits 0.99-13.20%; val_single_in_dist worst (+13.20%), val_geom_camber_rc near-flat (+0.99%); 70/70 epochs, best=ep69; CRITICAL: trained τ drifted UP from init 0.25 to mean 0.297 (block_0 0.286 → block_3 0.323) — optimizer wanted softer routing; INTERESTING: test_geom_camber_rc slight WIN (−1.65%) — 2nd sharpening-helps-camber-rc signal alongside #2418 ReLU² (rc WIN −2.50%); mechanism: hard-commit routing at init corrupts slice-prototype diversity which optimizer can't fully recover; 21st closed taxon: routing-sharpness over-commitment at warm-start; routing-sharpness axis essentially saturated by slice_num=24 + L1 + LayerScale stack; trained-τ tilt suggests deeper blocks want softer routing). Assigned #2526 askeladd PaLM-style parallel attention+MLP (Chowdhery et al. 2022; restructure TransolverBlock.forward to compute attn+mlp branches in parallel on shared ln_1(fx) input; saves one LN per block ~5-10% epoch speedup; decouples branch gradient flow; tests whether γ_attn (currently ~0.005, heavily suppressed) grows when no longer coupled to mlp via sequential composition; first decoupled-branch architectural probe; zero params; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 03:50 UTC (round-52: Closed #2497 edward LLRD decay=0.7 (val 47.7593, +12.79% LOSS — UNIFORM REGRESSION all 4 splits 8.21-18.55%; val_single_in_dist worst (+18.55%), val_geom_camber_rc least (+8.21%); 70/70 epochs reached, best=terminal=ep70 (monotonically descending, convergence-bound NOT plateaued); γ_mlp depth gradient at terminal 0.008→0.050 (block_0→3) — steep ramp vs uniform-LR baseline ~0.063-0.078 across all blocks confirms early-block under-training; LR diagnostic confirms 0.7^k ratio preservation through scheduler — no implementation bug; mechanism: decay=0.7^4=0.24× preprocess + 0.34× block_0 starves early-layer optimizer work below convergence floor in 70-epoch budget; 4-block-too-shallow wash hypothesis FALSIFIED; 20th distinct closed-axis taxon: layer-wise-LR-decay-downward in budget-bound regime — combined with closed #2457 embed-UP, the per-parameter-group-LR axis is FULLY CLOSED in BOTH directions; uniform-LR AdamW is the local optimum on the optimizer-group axis; 8 total optimizer-family closures in launch). Assigned #2524 edward Lion optimizer (Chen et al. 2023 sign-based momentum; update = sign(β₁*m + (1-β₁)*g); 9th optimizer-family member tested; lr=1e-4 (1/5 AdamW), wd=3e-4 (3× AdamW), β2=0.99 (vs AdamW 0.999); zero param change; fundamentally distinct from AdamW (no 2nd moment) and SGD (sign vs raw); aligns with L1 sign-gradient regime; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 03:30 UTC (round-51: Closed #2439 frieren GeGLU FFN (val 44.3006, +4.62% LOSS — UNIFORM REGRESSION all 4 splits 1.39-8.51%; val_geom_camber_cruise worst (+8.51%); 53/55 epochs (GPU-contention timeout cut 2 epochs short); +90K params (~+27%); mechanism: gating dilutes per-param gradient signal at the capacity floor where depth/width/slice_num/mlp_ratio all sit at closed optima — same lesson as mlp_ratio=4 LOSS and n_hidden=128 LOSS; test_avg near-wash (+0.93%) confirms val noise but per-split direction uniform UP, not bimodal; 19th closed taxon: FFN-structure gating axis closes in budget-bound regime; ALL FFN axes now mapped {gate=LOSS, activation=GELU optimum, mlp_ratio=2 optimum, width-down=open}). Assigned #2509 frieren Fourier feature encoding K=4 (NeRF-style sin/cos at frequencies [π, 2π, 4π, 8π] on raw spatial coords; 16 extra channels concatenated to 24→40 preprocess input; +1.5K params; first INPUT-ENCODING architectural probe in this launch; targets val_geom_camber_rc OOD bottleneck via high-frequency spatial basis distinct from learned slice embedding; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 03:15 UTC (round-50: Closed #2455 askeladd MLP-output-dropout p=0.05 (val 46.8749, +10.69% LOSS — UNIFORM REGRESSION all 4 splits 5.4-20.4%; val_geom_camber_cruise worst +20.41%; 70/70 terminal-best; 3rd stochastic-regularization closure alongside attention-weight-dropout p=0.1 and abandoned DropPath; mechanism: convergence-cost > regularization-benefit in 30-min cap regime; LayerScale γ subsumes the FFN-branch gating role that any beneficial dropout would provide; 17th closed taxon: convergence-floor regime closes the stochastic-regularization class). Closed #2454 alphonse reflection-aug (val 55.5301, +31.1% LOSS — CATASTROPHIC uniform regression all 4 splits 22.6-45.2%; val_single_in_dist worst +45.2%; mechanism: 68% of train corpus has half-space mesh y ∈ [0, ~10] with ground plane — reflection y→-y moves the entire raceCar mesh into NEGATIVE y space the model never sees; physical reflection symmetry of incompressible NS is BROKEN by the half-space mesh structure; epoch 1 val_avg=363 — augmentation effectively doubled the input distribution; 18th closed taxon: whole-corpus-reflection physical-symmetry hypothesis falsified by mesh-half-space; student's analysis is conclusive). Assigned #2502 askeladd attention temperature init τ=0.25 (sharper PhysicsAttention slice routing; first temperature-axis probe; zero param change; orthogonal to closed slice_num axis; --epochs 70); #2503 alphonse decoder residual skip (skip_proj=Linear(96,3) added after ln_3 in last block; output = mlp2(ln_3(fx)) + skip_proj(ln_3(fx)); +291 params; first decoder-skip probe; composable with in-flight #2472 split-heads; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 03:00 UTC (round-49: Closed #1775 fern WD=5e-5 (val 46.3290, +9.41% LOSS — UNIFORM REGIME-FLIP; all 4 splits regress 5-15%; WD direction sign-flipped from prior β=0.5 baseline where same change was WIN −4.43%; mechanism: current L1+slice_num=24+n_hidden=96+n_layers=4+LayerScale stack is in "well-trained, needs more penalty" regime — opposite of under-fit β=0.5 era; 70/70 epochs terminal-best; WD axis sweep direction on current stack is UP not DOWN; 15th distinct closed-axis observation: regime-flip sign reversal of WD direction across stack changes). Closed #2457 edward per-group-lr (val 48.7460, +15.1% LOSS — UNIFORM DESTABILIZATION; embed=2× UP LR destabilizes shared-token representation upstream of all 4 blocks; γ growth NOT bottleneck — same trained-γ equilibrium reached regardless of LR-γ acceleration; embed=2× made blocks re-fit a moving input distribution for all 70 epochs; consistent with PR #2457 student's diagnosis; 16th closed-axis: per-parameter-group-LR-scaling with embed-acceleration; note: layer-wise LR DECAY downward is orthogonal and untested). Closed stale #2411 nezuko aux-camber-head (1st stale_wip; no commits ~2h since assignment; hypothesis preserved). Assigned #2495 nezuko aux-camber-head-retry1 (same hypothesis: 96→32→1 CamberHead on surface-pooled last-block hidden; λ=0.1 MSE on x_norm[:,0,15]; ~+3.1K params; --epochs 70); #2496 fern per-channel-surf-p3 (per-channel pressure emphasis in surface loss: ch_w=[1.0,1.0,3.0] applied to surf_loss ONLY; vol_loss uniform; surf_weight=10 unchanged; zero param change; first per-channel gradient allocation probe; --epochs 70); #2497 edward llrd-decay07 (layer-wise LR decay: preprocess @ lr×0.24, block_0 @ lr×0.34, block_1 @ lr×0.49, block_2 @ lr×0.70, block_3+decoder @ lr×1.0; OPPOSITE direction of closed #2457 embed-UP; zero param change; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 01:45 UTC (round-48: Closed #2410 thorfinn FiLM conditioning (val 46.4166, +9.61% LOSS — UNIFORM REGRESSION all 4 splits; camber_rc primary target +7.80%; FiLM trained non-trivially γ away from identity, β near-zero with σ up to 0.33; mechanism: double-affine compounding on heavily damped LayerScale residual stream; FiLM perturbs LN input but γ_attn ≈ 1e-3 so downstream attention expects unit-variance inputs; β propagates additively without LayerScale damping; +38K params = capacity wrong direction; 14th taxon: residual-stream-input-perturbation conditioning). Assigned #2480 thorfinn NormFormer Sandwich Norm (Pre-LN + Post-LN around each residual addition; ln_post_attn + ln_post_mlp added per block; ~+1.5K params; Shleifer et al. 2021; OPPOSITE of FiLM — normalizes stream variance rather than perturbing it; distinct from closed RMSNorm-substitution; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 01:15 UTC (round-47 addendum: Closed #2396 tanjiro split-surf-vol-heads (1st stale_wip; no commits since assignment 2026-05-13T15:11:29Z, ~10h stall; 2nd consecutive stale on tanjiro pod after DropPath jinx). Hypothesis preserved — decoder-architecture probe is still fresh and structurally distinct. Reassigned as #2472 retry-1 on fresh branch off current advisor (42.3455 baseline; full stack). If retry-1 also stalls, will investigate tanjiro pod health before further retries. All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 01:00 UTC (round-47: Closed 3 LOSSes: #2418 alphonse ReLU² (val 45.6311, +7.76% LOSS — BIMODAL SURPRISING: rc WIN −2.50% only, in_dist worst +21.18%; activation axis FULLY CLOSED at GELU; 11th taxon: activation-curve over-specialization), #2412 askeladd SAM ρ=0.05 (val 97.1961, +129.6% LOSS — catastrophic uniform regression, in_dist worst +199.6%; SAM converged to worse basin cleanly; meta-optimizer axis CLOSED; 12th taxon: meta-optimizer perturbation-noise interference), #2392 edward mlp_ratio=1 (val 49.7302, +17.4% LOSS — capacity floor; 75/80 epochs terminal-best; mlp_ratio axis FULLY CLOSED both directions {1,2,4}→{49.73,42.35,66.73}; 4 budget-axes all mapped at optimum: n_layers=4, n_hidden=96, slice_num=24, mlp_ratio=2). Assigned #2454 alphonse AoA+y reflection symmetry aug (physical NS invariance; y_coord*=-1 + AoA*=-1 + Uy*=-1; prob=0.5; first TRUE physical symmetry aug; --epochs 70), #2455 askeladd MLP output dropout p=0.05 (after fc2, before LayerScale+residual; 4 TransolverBlock sites only; distinct from closed attention-weight-dropout; --epochs 70), #2457 edward per-param-group LR (embed=2×, γ=2×, others=1×; AdamW per-group; first non-uniform optimizer axis; --epochs 70). All 8 students in-flight, zero idle, baseline 42.3455.)
- **As of:** 2026-05-14 00:15 UTC (round-46: Closed #2394 frieren surf_weight 10→20 (val 47.8434, +12.98% vs 42.3455 baseline) — UNIFORM REGRESSION ALL 4 SPLITS; in_dist worst at +22.08% (+7.82 absolute); camber_rc least at +5.48%; BOTH surf AND vol metrics regressed (destabilization, NOT gradient re-weighting); surf_weight axis FULLY CLOSED: bracket {10, 20, 30} all regress at 20 and 30, 10 is global optimum. Assigned #2439 frieren GeGLU FFN (GELU-gated Linear Unit at all MLP sites; gate*GELU(value) multiplicative FFN; Shazeer 2020; orthogonal to #2418 ReLU² activation and #2392 mlp_ratio=1 width; ~+13% params ~651K; --epochs 55 for T_max=52 alignment with ~52 actual epochs at +13% slowdown; lesson from #2298 schedule-misalignment). All 8 students in-flight, zero idle, baseline still 42.3455.)
- **As of:** 2026-05-13 23:30 UTC (round-45 addendum: Closed #2283 alphonse Squared ReLU (1st stale_wip pod-stall; ~10h no commits since 12:54 UTC; baseline drifted -12.78% during the stall from 48.5160 → 42.3455). Hypothesis preserved — Squared ReLU axis is still UNCLOSED (only SiLU/negative-tail direction closed via #2156); Primer paper precedent solid. Reassigned as #2418 retry-2 on fresh branch off current advisor (42.3455 baseline; full advisor stack inherited: slice_num=24 + n_hidden=96 + n_layers=4 + LayerScale + warmup-3-cosine + n_head=2 + L1 + compile + bf16 + sampler 2× single + mlp_ratio=2). --epochs 70 matching PR #2307. All 8 students in-flight, zero idle.)
- **As of:** 2026-05-13 23:00 UTC (round-45 complete: 3 axis-closing LOSSes + 3 bold new assignments. (1) Closed #2362 askeladd slice_num=16 (val 47.4321, +12.02% vs 42.3455) — ROUTING CAPACITY FLOOR; cruise hit worst (+30.21%); floor is below 24; slice_num axis FULLY CLOSED with optimum at 24; bracket {16,24,32}→{47.43,42.35,46.85} concave peak. (2) Closed #2289 nezuko n_layers=3 (3-seed mean val 48.085, +2.65% vs #2268 baseline 46.8460) — DEPTH CAPACITY FLOOR; in_dist hit hardest (+4.55%); budget-bound→capacity-bound transition at depth=3, same pattern as width=64; depth-down axis CLOSED at n_layers=4. (3) Closed #2298 thorfinn --epochs 90 (val 47.9738, +2.41% vs 46.8460) — SCHEDULE MIS-ALIGNMENT LOSS; wall-clock delivers 58 epochs regardless of --epochs flag; T_max=87 creates under-cooled cosine (LR=1.66e-4 at ep58 vs ~0 for T_max=57 baseline); META-FINDING: T_max must match ACTUAL expected epochs, not --epochs flag. **Assigned 3 fresh BOLD experiments (all target the camber_rc OOD bottleneck via orthogonal mechanisms):** #2410 thorfinn FiLM conditioning (per-block affine (γ,β) on 11 broadcast scalars channels 13-23; zero-init for identity start; explicit regime conditioning pathway; first architecture-level OOD intervention; --epochs 70; predicted -3 to -8%); #2411 nezuko auxiliary camber head (λ=0.1 MSE on NACA camber foil-1 = self-supervised geometry consistency; first auxiliary-objective experiment; --epochs 70; predicted -2 to -6%); #2412 askeladd SAM ρ=0.05 (two-step gradient update targeting flat minima; ~29-30 actual epochs; --epochs 35 for T_max alignment; predicted -3 to -7%; bold double-or-nothing probe). All 8 students in-flight, zero idle, baseline still 42.3455.)
- **As of:** 2026-05-13 21:15 UTC (round-44 complete: 3 LOSS closures + 3 fresh assignments. (1) Closed #2336 frieren n_hidden=64 (val 49.9489, +7.74% vs branch base 46.3612 / +17.96% vs current baseline 42.3455) — FALSIFIED LOSS, total-capacity floor crossed. All 4 splits regress UNIFORMLY (in_dist worst +11.0%, camber_rc least +3.5%) — surprise inversion from predicted OOD-first signature; at n_hidden=64 + n_layers=4 the model lacks enough representation width for in-distribution fit, so OOD's relative gap narrows. Width-down axis CLOSED at n_hidden=96. (2) Closed #2316 edward berHu c=1.0 retry-2 (val 50.4134, +7.62% vs branch base 46.8460 / +18.96% vs current baseline) — FALSIFIED LOSS. Loss-shape axis FULLY CLOSED in both directions: softening (Huber-β family 5 LOSS) + amplification (berHu 1 LOSS confirmed) — pure L1 is GLOBAL optimum. berHu amplification self-defeats with cosine cool-down: late epochs need fine LR-controlled gradients, but berHu inflates them on outliers which include in-dist heavy tails too. (3) Closed #2280 tanjiro DropPath retry-4 — ABANDONED axis after 4 consecutive pod-stalls (#1976/#2083/#2179/#2280); ~24h of pod-time with no committed result; baseline drift -12.78% since hypothesis was first written; LayerScale subsumption hypothesis. **Assigned 3 fresh experiments:** #2392 edward mlp_ratio 2→1 (FFN contraction; 4th budget-bound axis probe after depth/width/slice_num all merged — never tested in downward direction; --epochs 80; predicted ~25-26 s/epoch, ~70 epochs in 30min); #2394 frieren surf_weight 10→20 (gradient rebalancing toward primary metric mae_surf_p; intermediate of closed bracket {10 baseline, 30 prior LOSS under heavier config}; --epochs 70 matches PR#2307); #2396 tanjiro split surface/volume output heads (bifurcate last block's mlp2 into mlp2_surf + mlp2_vol selected by is_surface mask; first decoder-architecture probe; --epochs 70; bold swing predicted -3 to -8% with high variance). All 8 students in-flight, zero idle, baseline still 42.3455.)
- **As of:** 2026-05-13 20:30 UTC (round-43: **MERGED #2307 askeladd slice_num=24 MASSIVE WIN** (val 42.3455, −9.61% vs 46.8460 #2268 baseline on branch; test 38.5059, −5.66%). **NEW BASELINE 42.3455. LARGEST SINGLE-PR GAIN SINCE ROUND-1.** All 4 splits improve uniformly: in_dist −14.93%, cruise −12.43%, re_rand −8.11%, geom_camber_rc −5.94% (FIRST significant movement on rc bottleneck since LayerScale). CRITICAL FINDING: mechanism is NOT budget-bound (per-epoch cost only −2%); mechanism is ROUTING QUALITY — slice_num=32 was ABOVE the PhysicsAttention routing optimum; fewer, sharper partitions improve geometric generalization (inductive bias / regularization effect). PR branched off n_hidden=128 advisor but squash-merged cleanly onto n_hidden=96 advisor. Advisor now has ALL: n_hidden=96 + n_layers=4 + LayerScale + slice_num=24 + warmup-3-cosine + n_head=2. Assigned #2362 askeladd slice_num=16 (routing-optimum floor search; curvature pattern suggests optimum may be below 24; tests whether geometric routing can survive further partition reduction at dim_head=48 + n_hidden=96). All 8 students in-flight, zero idle, new baseline 42.3455.)
- **As of:** 2026-05-13 20:00 UTC (round-42: **MERGED #2290 frieren n_hidden=96 WIN** (val 46.3612, −1.04% vs 46.8460; test 40.3555, −1.12%). **NEW BASELINE 46.3612.** All 4 splits improve uniformly (budget-bound 2-for-2: depth-down #2268 + width-down #2290). Per-epoch savings only 15% (not predicted 40-45%; slice ops dominate ~linearly in n_hidden, not n_hidden²); +17% more cosine epochs (60→70); best=67≠terminal (cosine converged cleanly). 330K params (−43%), dim_head=48 at n_head=2. LayerScale γ auto-adapted (96,)→(96,), no collapse. Width axis OPEN further: no capacity floor at 96. Assigned #2336 frieren n_hidden=64 (width-down continuation; depth axis has #2289 nezuko n_layers=3 in-flight; width completes the pair; dim_head=32 at n_head=2 — tests whether floor aligns with n_head=4/dim_head=32 LOSS #2222 or budget-bound dominates). All 8 students in-flight, zero idle, new baseline 46.3612.)
- **As of:** 2026-05-13 19:30 UTC (round-41: Closed #2223 edward berHu c=1.0 (stale_wip: pod stuck on iteration 108 from 13:26 UTC for 5+ hours, classic stale pattern matching DropPath #1976/#2083/#2179 and translation #2138/#2235 retries). berHu hypothesis preserved — structurally distinct from all 10 closed taxa (LOSS-AMPLIFYING direction, never tested); baseline shifted significantly since assignment (49.8053 → 46.8460). Reassigned as #2316 retry-2 on fresh branch off current advisor via REST API (GraphQL rate-limited). Same berHu(c=1.0) hypothesis, new bar 46.8460, includes full advisor stack (n_layers=4 + LayerScale + warmup-3-cosine + n_head=2 + slice_num=32). All 8 students in-flight, zero idle, baseline 46.8460.)
- **As of:** 2026-05-13 19:00 UTC (round-40: Closed #2272 askeladd LayerScale asymmetric init γ_mlp=1e-3 (val 48.5072, +3.55% LOSS vs current baseline 46.8460; wash vs assigned baseline 48.5160 only -0.018%). **Hypothesis FALSIFIED:** asymmetric init caused LATER convergence (best epoch 47, +5 vs #2195's ep42) not earlier; trained γ converges to same scale regardless of init (MLP 0.034-0.054 vs #2195's 0.025-0.05); the "near-zero branches-off phase" is an implicit warm-up that is load-bearing, not waste. Mixed 2-WIN/2-LOSS per-split (in-dist + rc improved; cruise + re_rand regressed). **10th failure-mode taxon added: init-prior-misalignment LOSS** — starting a residual-gate parameter at its trained-scale rather than small-norm prior corrupts implicit warm-up dynamics; OOD splits suffer first. LayerScale axis FULLY CLOSED at uniform init=1e-4 (PR #2195). Assigned #2307 askeladd slice_num 32→24 (budget-freeing PhysicsAttention granularity probe — 3rd orthogonal axis of budget-bound regime triangulation, after depth #2268 WIN and width #2290 in-flight). All 8 students in-flight, zero idle, baseline 46.8460.)
- **As of:** 2026-05-13 17:45 UTC (round-39 final: Assigned #2298 thorfinn n_layers=4 --epochs 90 (budget-extrapolation diagnostic; no code changes; tests descent curve continuation). All 8 students in-flight, zero idle, new baseline 46.8460.)
- **As of:** 2026-05-13 17:30 UTC (round-39 continued: **MERGED #2268 thorfinn n_layers=4 WIN** (val 46.8460, −3.44% vs 48.5160; test 40.8140, −4.70%). **NEW BASELINE 46.8460.** All 4 splits improve, 58/60 epochs, 577K params (−18%). Budget-bound regime definitively confirmed: −20% per-epoch wall-clock savings bought ~10 extra epochs. Closed #2235 nezuko translation aug retry-2 (val 52.20, +11.5% LOSS — uniform regression, NOT bimodal; translation of mesh coords is NOT a free physical-symmetry augmentation; absolute position statistics are load-bearing). Closed #2234 frieren mlp_ratio=4 (val 66.73, +42.5% LOSS — budget cliff confirmed at 34/50 epochs: +15% per-epoch cost clips off final cosine descent; architecture fine, timing wrong). **UNIFIED FINDING:** budget-bound, not capacity-saturated. Assigned #2289 nezuko n_layers=3 (--epochs 75; depth-down continuation; ~25% more wall-clock savings vs n_layers=4) + #2290 frieren n_hidden=96 (--epochs 90; width-down probe; ~40-45% more wall-clock savings; orthogonal to depth axis). All 8 students in-flight, zero idle.)
- **As of:** 2026-05-13 17:00 UTC (round-39: Closed #2194 alphonse SGD(momentum=0.9, nesterov, lr=2e-3) (val 78.64, +62% LOSS) — clean LOSS, no divergence; SGD trains successfully but converges ~2× too slowly to compete with AdamW in 30-min budget. **Optimizer-family axis now fully closed 6-LOSS-for-6** (lr-UP, lr-DOWN, β1=0.95, β2=0.95, amsgrad, SGD-momentum); AdamW defaults sharply pinned. 9th distinct failure-mode taxon added: **optimizer-family convergence-rate inadequacy** (uniform-additive regression from non-adaptive optimizer). Closed #2179 tanjiro DropPath retry-3 (4th stale pod-stall, ~6.5h no commits); reassigned as #2280 retry-4 on fresh branch via REST. Assigned #2283 alphonse Squared ReLU (ReLU²) at all 3 MLP sites — Primer-style sharp activation; opposite direction of closed SiLU (negative-tail) LOSS; tests whether activation axis closes at GELU or extends to ReLU². All 8 students in-flight, zero idle, baseline unchanged 48.5160.)
- **As of:** 2026-05-13 16:30 UTC (round-38 addendum: **MERGED #2195 askeladd LayerScale init=1e-4 WIN** (val 48.5160, **-2.59% vs 49.8053**; test 42.8162, -1.66%). **NEW BASELINE 48.5160.** UNIFORM 3-of-4 splits improve; MAJOR FINDING: val_geom_camber_rc (67.44→65.94) moved for the FIRST TIME since round-1, every other prior intervention left it flat. val_geom_camber_cruise slight +1.95% (only regression). Best epoch 42 ≠ terminal 43 — first convergence-within-budget in recent rounds. Trained γ diagnostics: MLP branches activated 4-8× stronger than attention; block 3 attn underweighted; per-channel selective gating structure. LayerScale is now on the advisor branch. All in-flight students now target new bar 48.5160.)
- **As of:** 2026-05-13 16:00 UTC (round-38: Closed #2222 thorfinn n_head=1 (dim_head=128) (val 51.40, +3.19% LOSS / test 44.73, +2.73% LOSS) — CLEAN LOSS, axis NOT monotone. **n_head axis CONCAVELY closed at {1, 2, 4}: 51.40 / 49.81 / 50.60**; n_head=2 confirmed as INTERIOR OPTIMUM (both head-rank diversity and per-head dim_head matter, dim_head=64 balances). Lead-mover insight: val_geom_camber_cruise was lead WIN in 4→2 (−5.09%) and lead LOSS in 2→1 (+11.21%) — **most sensitive structural-capacity dimension in the dataset**; paper-level ablation candidate. In-dist actually IMPROVED in this PR (−1.10%) — single-head competitive on single-foil regime but collapses on OOD. Pattern is architectural-OOD-sensitive (NOT bimodal-averaging). Assigned #2268 thorfinn n_layers 5→4 (depth-down probe, pure architectural reduction; complementary to in-flight #2234 mlp_ratio=4 ADD-capacity; expected ~20% epoch wall-clock savings; budget-bound vs capacity-saturated diagnostic with high info value either way; param count drops 708K→575K). All 8 students in-flight, zero idle.)
- **As of:** 2026-05-13 15:30 UTC (round-37: Closed #2139 frieren RMSNorm 3-site (val 51.88, +2.53% LOSS vs old / +4.16% vs new baseline) — mild bimodal: in-dist −2.08% WIN, all 3 OOD splits +2.66 to +6.23% LOSS. Mechanism: structural normalization change still induces in-dist↔OOD trade-off, unexpected for layer-level intervention. RMSNorm 3-site axis closed. Closed stale #2138 nezuko translation aug σ=0.01 (~6h pod-stall, 1st stale, matches frieren/tanjiro pattern); reassigned as #2235 retry-2 on fresh branch via REST. Assigned #2234 frieren mlp_ratio 2→4 (FFN width 256→512, +50% params 708K→1.04M; standard transformer FFN sizing, never tested at ratio=4 in this launch; complementary capacity axis to n_head=2 WIN). All 8 students in-flight, zero idle.)
- **As of:** 2026-05-13 15:00 UTC (round-36: **MERGED #2173 thorfinn n_head=2 WIN** (val 49.8053, -1.57%; test 43.5396, -0.97%); new baseline 49.8053 (n_head=2, dim_head=64). Closed #2174 edward Huber β=0.1 (val 51.5551, +1.89% LOSS — **8th averaging-style bimodal confirmation**: in-dist -1.61% while all 3 OOD splits +2.4 to +4.3%; Huber-β family fully closed β∈{0.1,0.25,0.5,1.0,2.0} all bimodal vs L1 — pure L1 is the saturating optimum). Assigned #2222 thorfinn n_head=1 (dim_head=128, closes the n_head axis at the endpoint) + #2223 edward berHu reverse-Huber c=1.0 (pure L1 for |r|≤c, quadratic-amplified for |r|>c — OPPOSITE of closed Huber-β softening; directly amplifies large-residual OOD gradients; distinct from ALL 8 bimodal closures). All 8 students in-flight, zero idle.)
- **As of:** 2026-05-13 11:00 UTC (round-35: Closed #2156 askeladd GELU→SiLU swap (val 57.64, +13.91% LOSS) — uniform regression all 4 splits 9.3-18.8%; NEW failure-mode taxon: 'architectural-activation degradation' (NOT bimodal, NOT broadcast-scalar). GELU's sharper near-zero gating is load-bearing for L1+bf16; SiLU's longer negative tail destabilises residual stream. Closed #2155 alphonse AdamW amsgrad=True (val 55.39, +9.47% LOSS) — uniform regression all 4 splits; amsgrad's max-bound is PERMANENT once a high-variance gradient inflates v_max → kills early-phase high-LR exploration. **AdamW optimizer axis now closed 4-LOSS-for-4** (lr-UP/DOWN, β1, amsgrad) — defaults are locally optimal. Assigned #2194 alphonse SGD(momentum=0.9, nesterov, lr=2e-3) — optimizer-FAMILY swap (leave AdamW entirely) + #2195 askeladd LayerScale init=1e-4 (CaiT-style learnable per-channel residual gain, distinct architectural sub-class from activation/normalization). Baseline still **50.6001**)
- **As of:** 2026-05-13 10:45 UTC (round-34: Closed stale #2083 tanjiro DropPath (2nd consecutive stale at pod-level, same pattern as frieren RMSNorm #1926→#2034→#2139). Reassigned tanjiro to DropPath retry-3 (#2179) on fresh branch via REST API (GraphQL rate-limited). Hypothesis unchanged — block-level stochastic depth, structurally distinct from 7× averaging-style and 2× broadcast-scalar prior corruption closed classes. All 8 students now in-flight. Baseline still **50.6001**)
- **As of:** 2026-05-13 10:30 UTC (round-33: Closed #2112 thorfinn warmup-2 (val 51.93, +2.62% LOSS) — bimodal symmetric-flip prediction FALSIFIED; warmup duration bounds OOD from below; warmup axis fully closed at warmup-3 with sharply asymmetric peak {2,3,5}={51.93, 50.60, 50.72}. Closed #2072 edward NACA jitter σ=0.01 (val 53.05, +4.85% LOSS) — **2nd confirmation of broadcast-scalar prior corruption** (val_re_rand worst hit at +7.36%, same exact pattern as gap/stagger #2114). Generalizable rule: ALL per-sample-broadcast scalar input channels (NACA 15-17/19-21, gap/stagger 22-23, fun_dim 13/14/18) are load-bearing priors, NOT augmentation targets. Assigned #2173 thorfinn n_head 4→2 (dim_head 32→64, architectural probe closer to literature optimum) + #2174 edward Huber β=0.1 (smooth-L1 loss probe addressing L1 sign-flip noise at the loss level). Baseline still **50.6001**)
- **As of:** 2026-05-13 10:05 UTC (round-32: Closed #2114 askeladd gap/stagger jitter σ=0.02 (val 51.37, +1.52% LOSS) — **NEW failure mode: broadcast-scalar prior corruption** (uniform regression all 4 splits, val_re_rand worst hit at +4.07%); NOT 8th bimodal, distinct mechanism. Closed #2093 alphonse AdamW β1=0.95 (val 54.37, +7.45% LOSS) — **NEW failure mode: momentum-lag overshoots cosine LR** (uniform regression 4-14% across all splits, val_geom_camber_cruise worst). AdamW (lr, β1, β2) hyperparameter axis now closed 4-LOSS-for-4. Assigned #2155 alphonse AdamW amsgrad=True (max-tracking 2nd-moment for L1 sign-noise — novel optimizer-stability axis) + #2156 askeladd GELU→SiLU activation swap (architectural probe, NOT averaging-style). Baseline still **50.6001**)
- **Branch:** `icml-appendix-charlie-pai2g-48h-r5` (advisor) — Charlie no-W&B logging ablation, round 5
- **Most recent human-team direction:** None on this branch.

## Current research focus

**8 merged winners → baseline 50.6001 (-54% from 110.76 at round-1 start).**

New baseline = L1 + compile + bf16 + sampler 2× single + slice_num=32 + warmup-3-cosine.

**Round-18 findings:**
- **slice_num-DOWN axis closed**: 16 is a val wash (+0.41%) with an in-dist/OOD split trade-off. 32 is the global optimum.
- **Sampler axis closed**: 2.0× single is the confirmed peak. 1.5× (+3.47%) is strictly worse. No further sampler tuning needed.
- **New bottleneck identified**: `val_geom_camber_rc` (72.37) and `val_re_rand` (58.23) now dominate val_avg. Neither responds to sampler reweighting. **OOD generalization** is the highest-leverage remaining research axis.

**Shifted priority to OOD generalization levers:**
1. **Input augmentation** (#1921 nezuko pos-jitter) — targeting mesh-coord overfitting as source of OOD brittleness
2. **Normalization variant** (#1926 frieren rmsnorm) — RMSNorm as structural complement to routing changes
3. **Optimizer/regularization stack** (#1653 askeladd grad-clip, #1775 fern WD=5e-5, #1845 edward betas, #1774 alphonse lr — all on L1 or Huber baselines, need rebase to confirm still useful on 54.00 baseline)
4. **Schedule** (#1905 thorfinn SGDR warm restarts)
5. **Architecture (n_head=8)** — tanjiro pod stale × 3; n_head axis deferred until pod recovers or another student is available. DropPath (#1976) is the current architecture-regularization experiment.

## Merged winners

| PR | Student | Hypothesis | val_avg/mae_surf_p | test_avg/mae_surf_p |
|---|---|---|---|---|
| #2727 ✓ | tanjiro | SE block-3-only: SqueezeExcitation at deepest block only (blocks 0-2 removed); gate std grew 2-3.4× (0.15-0.26); OOD-adaptation mechanism confirmed; in_dist regression from #2692 eliminated; +2,412 params | **32.4498** | **27.6573** |
| #2692 ✓ | tanjiro | SE per-block channel gating: SqueezeExcitation(dim=96, reduction=8) after each TransolverBlock; zero-init fc2; mask-aware global pool; +9,648 params | **33.0195** | **28.3562** |
| #2614 ✓ | alphonse | FiLM feature-stream gate: fx = fx * (1+film(Re,AoA0,AoA1)) pre-block-0; zero-init Linear(3,96); +384 params; flow conditioning at residual stream | **33.3722** | **28.3736** |
| #2553 ✓ | edward | Lion lr=1.5e-4 (1.5× #2524; deeper minimum via wider landscape exploration; uniform WIN all 4 splits; best=ep70/70 still budget-bound) | 33.4935 | 28.6279 |
| #2524 ✓ | edward | Lion optimizer lr=1e-4, wd=3e-4 (sign-based momentum; aligns with L1 sign-gradients; bf16-stable; uniform WIN all splits) | 36.3994 | 31.2200 |
| #2307 ✓ | askeladd | slice_num 32→24 (routing-quality WIN; NOT budget; −9.61%; routing floor not yet found) | 42.3455 | 38.5059 |
| #2290 ✓ | frieren | n_hidden 128→96 (width-down; --epochs 90; 330K params; −43%) | 46.3612 | 40.3555 |
| #2268 ✓ | thorfinn | n_layers 5→4 (depth-down; --epochs 60; 577K params) | 46.8460 | 40.8140 |
| #2195 ✓ | askeladd | LayerScale init=1e-4 (CaiT-style per-channel learnable γ on attn+MLP branches) | 48.5160 | 42.8162 |
| #2173 ✓ | thorfinn | n_head 4→2 (dim_head 32→64) | 49.8053 | 43.5396 |
| #2033 ✓ | thorfinn | LinearWarmup-3 + CosineAnnealingLR(T_max=47) | 50.6001 | 43.9680 |
| #1846 ✓ | frieren | slice_num 64 → 32 | 54.0051 | 47.6261 |
| #1619 ✓ | nezuko | Sampler 2× racecar_single | 56.62 | 50.43 |
| #1700 ✓ | thorfinn | Pure L1 loss | 59.54 | 51.47 |
| #1633 ✓ | thorfinn | Huber β=0.5 | 64.07 | 55.50 |
| #1568 ✓ | thorfinn | torch.compile + bf16 | 69.83 | 61.87 |
| #1532 ✓ | thorfinn | bf16 AMP + scoring-NaN fix | 101.12 | 91.50 |
| #1444 ✓ | thorfinn | MSE → Huber β=1.0 | 110.76 | NaN |

**Current baseline: val_avg/mae_surf_p = 32.4498, test_avg/mae_surf_p = 27.6573 (PR #2727, SE block-3-only — 17 merged winners total)**

Per-split baseline (PR #2727):

| Split | val mae_surf_p |
|---|---|
| `val_single_in_dist` | 25.6532 |
| `val_geom_camber_rc` | 47.2242 |
| `val_geom_camber_cruise` | 19.0752 |
| `val_re_rand` | 37.8467 |

> Advisor config: Pure L1 + bf16 AMP + torch.compile(dynamic=True) + scoring-NaN workaround + sampler 2× racecar_single + **slice_num=24** + LinearWarmup(3ep, 0.1→1.0) + CosineAnnealingLR(T_max=epochs-3) + n_head=2 (dim_head=48) + LayerScale γ=1e-4 (per-channel CaiT-style, all blocks) + n_layers=4 + n_hidden=96 + **Lion(lr=1.5e-4, wd=3e-4, betas=(0.9, 0.99))** + **FiLM gate: fx = fx*(1+film(Re,AoA0,AoA1)), Linear(3,96), zero-init**.
> ~70 epochs in 30 min (~25.7 s/epoch). Peak GPU: ~14 GB. 328,619 params (+384 FiLM).
> **NEW: FiLM learned non-trivially** — film.weight.norm=2.62 (from 0), modulation ~0.92 on re_rand. Flow conditioning aids geometric extrapolation (rc −1.93%, in_dist −1.71%). val_re_rand +2.12% = n=100 noise (test_re_rand −1.16% confirms).
> **OPEN AXES (Round 93)**: GeGLU MLP (#2759 alphonse), mlp_ratio=1.5 narrowing (#2764 frieren), SE reduction=4 (#2765 tanjiro), GroupNorm(4) (#2766 askeladd), Deep supervision (#2778 edward, 60th candidate axis), Mish in mlp2 (#2780 thorfinn, 61st candidate axis), Register tokens N_REG=4 (#2792 nezuko, 62nd candidate axis), **SwiGLU in preprocess MLP (#2794 fern, 63rd candidate axis: input-lifting gating)**. CLOSED THIS ROUND: stale_wip #2750 fern (no taxon closed; mlp2-width axis remains open). CLOSED EARLIER: Direct SAM rho=0.05 (#2761 60th — flat-minima/narrow-basin-escape meta-family 4-direction exhaustive: SGDR 52nd + SWA 28th + Lookahead 55th + SAM 60th), Weight Standardization (#2739 59th, weight-constraint 3-direction exhaustive with spectral norm + L1+L2), MLP-width upward (#2749 56th), SE depth-extended (#2748 57th, block-3 minimal viable unit), eval-time weight-averaging (#2735 58th, weight-averaging meta-family 3-direction exhaustive), gradient-transformation (GC #2710 54th), n_head=4 (2 stale), attention-shape (softmax optimal, 50th), Mixup family (49th+53rd), stochastic-residual-branch (DropPath 51st), normalization-replacement-full (DyT 48th), positional-embedding (Fourier 24th + RoPE 46th). **18 merged winners; baseline val 32.2477 / test 27.4248 (PR #2741 SwiGLU). Total closed taxa: 60.**

## In-flight (WIP)

| PR | Student | Hypothesis | Notes |
|---|---|---|---|
| #2794 | fern | SwiGLU in preprocess MLP: 1-line swap MLP→SwiGLUMLP at input-lifting stage; preprocess hidden 192→128 (SwiGLU snap); preprocess params 22,656→17,920; total ~333,787 (−1.4%) | **NEW Round-93**; 63rd candidate axis — tests whether merged SwiGLU-in-MLP-body win transfers to input-lifting stack location; orthogonal to in-flight Mish in mlp2 (output activation) and in-flight GeGLU MLP body (gate activation variation) |
| #2759 | alphonse | GeGLU MLP: gelu gate instead of silu gate (1-line ablation of SwiGLU #2741 WIN); same param count 338,523 | Tests whether gate activation family or GLU structure drove SwiGLU WIN; T5/PaLM-2/Gemma use GeGLU; NEW bar val < 32.2477 |
| #2792 | nezuko | Register tokens N_REG=4 (Darcet 2024 ICLR): learnable extra K/V positions in PhysicsAttention; excluded from output routing; +1,536 params | **NEW Round-92**; 62nd candidate axis — learnable attention K/V capacity expansion; first probe of this dimension; orthogonal to ALL closed/merged taxa |
| #2764 | frieren | MLP body width-down: mlp_ratio 2→1.5; MLP hidden 192→144; ~301K params | Inverse probe to closed #2749 (mlp_ratio=3 LOSS); if wider hurts OOD via capacity-overfit, narrower may tighten OOD |
| #2765 | tanjiro | SE-block3-only with reduction=4 (vs reduction=8): SE intermediate channels 12→24; +2,316 params | SE bottleneck-dim probe; tests whether reduction=8 is capacity-limiting OOD discrimination at block 3 |
| #2766 | askeladd | GroupNorm(num_groups=4) replacing LayerNorm: channel-stat reduction granularity probe | Normalization-granularity sub-axis after closed DyT all-or-nothing 48th taxon; 4 channel-groups of 24; orthogonal to merged DyT closure |
| #2778 | edward | Deep supervision (Lee 2014): auxiliary mae_surf_p heads after blocks 0,1,2 with aux_weight=0.1; ~+873 params (+0.26%) | **NEW Round-91**; gradient-pathway-multiplication regularizer structurally orthogonal to ALL closed/merged taxa; 60th candidate axis |
| #2780 | thorfinn | Mish in mlp2 (Misra 2019): nn.GELU() → nn.Mish() 1-line swap on output-head activation; param-matched | **NEW Round-91**; output-head activation probe orthogonal to SwiGLU intermediate stream; structurally simple for stall-prone pod; 61st candidate axis |

## Warning on in-flight rebase

Long-running in-flight PRs (#1775, #1988) were assigned on the old 54.0051 baseline and do NOT include the warmup-3-cosine schedule (merged PR #2033). The current baseline is **50.6001**. These PRs need to beat 50.6001 to merge — a significantly harder bar. If any returns with val > 50.60 but close, consider requesting a rebase onto the current advisor (with warmup) before closing.

## Closed axes (comprehensive)

- **Capacity:** width (n_hidden=160), depth (n_layers=6), FFN-only (mlp_ratio=3) — all close, budget-bound.
- **Attention dropout (per-weight):** p=0.1 — convergence cost > benefit at 30-min cap.
- **Batch=8:** step-count starvation.
- **WD up (5e-4):** under-fits on short budget.
- **Warmup:** substituted by β sharpening.
- **LR=1e-3 + warmup:** overshoot.
- **Surf_weight 10→30:** vol-surf imbalance.
- **Per-channel loss [1,1,3] global:** distorts velocity (PR #1428).
- **Per-channel surf_loss [1,1,2] surf-only:** OOD regression (PR #1871) — same physics coupling.
- **LR floor (eta_min=5e-5):** removes step-damping for L1 sign gradients (PR #1826).
- **Sampler both-racecar 2×:** absolute single exposure drops (PR #1870).
- **Sampler racecar_single 1.5×:** under-concentrates single coverage (PR #1904). **2× is the confirmed optimum.**
- **slice_num=96:** worse than 64 (PR #1590).
- **slice_num=16:** val wash (+0.41%) with in-dist/OOD trade-off; 32 is the global optimum (PR #1903). **slice_num axis fully closed.**
- **AdamW β2=0.95:** LOSS on L1 (+4.42% on L1 base, +15% vs current). Shorter second-moment memory amplifies L1 sign-flip noise (PR #1845). β2 axis closed.
- **Grad-clip max_norm=1.0:** WASH on L1+sampler+slice=32 (best run −0.37%, mean +0.56%; n=2 straddles baseline). Monotone dose-response: β=1.0 −14.92% → β=0.5 −6.94% → L1+slice=32 ~0%. Bimodal per-split: in-dist −13.33% but OOD +3-5% — structurally wrong for primary bottleneck. Gradient-coherence axis saturated by upstream L1+sampler changes (PR #1653). Gradient-coherence axis closed.
- **EMA model weights (decay=0.9999, 0.999):** 3 runs total. Mechanism confirmed (EMA<raw from ep10, dual-eval diagnostic). But per-split always bimodal: in-dist −12.9%, OOD +5-14%. Third confirmation of averaging-style bimodal pattern alongside coord-jitter and grad-clip. EMA-of-weights axis locally closed (PR #1946). **Pattern: ~14% in-dist headroom unlockable by averaging-style regularization; OOD requires structural interventions.**
- **Warmup-3-cosine:** MERGED as PR #2033 (WIN −6.31% val, −7.68% test). Now on advisor as the new schedule. warmup mechanism confirmed: sub-peak LR in first 3 epochs selects better loss basin. L1's two-phase property validated: warmup (find) + cosine (fine-tune). Best gain on val_single_in_dist (−18.87%); geom_camber_rc barely moved (−0.11%).
- **Warmup-5-cosine:** WASH +0.23% on warmup-3 (PR #2071). Bimodal: val_single_in_dist −2.60% (in-dist WIN), val_re_rand +2.81% (OOD LOSS), val_geom_camber_rc flat. 2 fewer settling epochs (T_max 47→45) cost OOD more than 2 extra warmup epochs helped in-dist. **6th bimodal confirmation** (settling-phase axis). Warmup-2 (other direction) in flight as #2112 — completes the bracket.
- **Lookahead k=5 α=0.5:** LOSS +5.09% on old baseline (no warmup); +12% vs current warmup-3-cosine (PR #2051). Per-split: in-dist −9.48% (WIN), all 3 OOD splits +5.92% to +18.0% (LOSS). **5th averaging-style bimodal confirmation** — class definitively closed.
- **AdamW β2=0.95 (earlier β=0.5 test):** near-wash on β=0.5, no clear signal (PR #1676).
- **lr axis (capacity↔LR coupling, ±25% of 5e-4):** fully bracketed and closed.
  - **lr=7.5e-4 (+50% lr-UP):** LOSS on L1+slice=32 (+16% vs current, n=3 mean 62.66) — closed across all 3 landscape variants tested (β=0.5 wash, L1+slice=64 wash-with-loss-tail, L1+slice=32 LOSS) (PR #1774).
  - **lr=3.75e-4 (-25% lr-DOWN):** LOSS on warmup-3-cosine + L1+slice=32 (val 56.36, +11.4% vs 50.6001, n=1) — 4th averaging-style bimodal confirmation: in-dist −6.45 (win) but all 3 OOD splits +4.84 to +5.74 (regression). Best epoch terminal at 44/50 → under-stepping confirmed (PR #1997).
  - **Pattern:** lr-DOWN is 4th confirmation of averaging-style bimodal (alongside coord-jitter, weight-EMA, grad-clip). Reduced effective step size acts as implicit averaging — same in-dist↔OOD trade-off. lr axis closed.
- **SGDR T_0=10 T_mult=2:** LOSS on L1+slice=32 (+27.7% val, n=1, 68.96 vs 54.00). Mechanism worked at cycle level (restart #2 min < restart #1 min) but L1 sign-gradient regime is fundamentally hostile to LR restarts (signs reset, late settling destroyed). Budget-fit variants deferred — L1+restart is the binding incompatibility (PR #1989). Schedule axis continues with warmup+monotone-cosine in flight as #2033.
- **AdamW β1=0.95 (UP probe):** LOSS +7.45% val (54.37) on warmup-3-cosine baseline (PR #2093, round-32 close). All 4 splits regress uniformly (4.10% to 14.27%, val_geom_camber_cruise worst at +14.27%). **NEW failure mode: momentum-lag overshoots cosine LR** — too much historical-gradient orientation can't track shrinking LR. NOT 8th averaging-style bimodal (in-dist would have won) — distinct mechanism. AdamW (lr, β1, β2) hyperparameter axis now closed 4-LOSS-for-4 (lr-DOWN, lr-UP, β2, β1). Defaults (lr=5e-4, β1=0.9, β2=0.999) are locally optimal.
- **Gap/stagger jitter σ=0.02 (channels 22-23):** LOSS +1.52% val (51.37) on warmup-3-cosine baseline (PR #2114, round-32 close). All 4 splits regress, val_re_rand worst hit at +4.07% (52.75 → 54.90). **NEW failure mode: broadcast-scalar prior corruption** — channels 22-23 are per-sample CONSTANT scalars (gap, stagger) broadcast to all N points; the model uses them as load-bearing priors for "this sample is configuration X". Perturbing them is NOT data augmentation, it's belief-corruption — the model is forced to learn "tandem geometry is uncertain" which directly increases prediction variance. NOT 8th averaging-style bimodal — distinct mechanism. Implication: per-sample-constant input channels are NOT augmentation-safe.
- **NACA geometry jitter σ=0.01 (channels 15-17, 19-21):** LOSS +4.85% val (53.05), +6.23% test (46.71) (PR #2072, round-33 close). All 4 splits regress, val_re_rand worst hit at +7.36% (52.75 → 56.63). **2nd confirmation of broadcast-scalar prior corruption** — NACA channels are also per-sample-broadcast scalars (one NACA descriptor per foil broadcast to all N points). Pattern identical to gap/stagger #2114: uniform LOSS, val_re_rand worst (the split with NO camber variation but Re-shift). Smoking gun: NACA noise should be neutral on val_re_rand under "domain shift" hypothesis, but it's the worst regressor → confirms NACA is used as conditioning prior for the Re/AoA channels too. **Generalizable rule:** the per-sample-broadcast scalar channel class (NACA 15-17/19-21, gap/stagger 22-23, Re/AoA 13/14/18) is NOT augmentation-safe. Per-point channels (coords 0-1) remain to be tested via translation aug #2138 nezuko.
- **Warmup-2-cosine:** LOSS +2.62% val (51.93), +3.51% test (45.51) on warmup-3-cosine baseline (PR #2112, round-33 close). Bimodal symmetric-flip prediction FALSIFIED. Per-split: val_single_in_dist +1.75%, val_geom_camber_rc -1.26% (mild WIN, ONLY OOD WIN we've seen on camber_rc bottleneck), val_geom_camber_cruise +10.13%, val_re_rand +3.49%. **Warmup bracket {2, 3, 5} on val_avg = {51.93, 50.60, 50.72}** — sharply asymmetric peak at warmup-3. Mechanism: warmup duration bounds OOD from below; under-warm training destabilises OOD feature learning, not just in-dist basin selection. **Warmup duration axis fully closed at warmup-3.** Schedule axis (warmup + cosine) is now exhausted in this launch.
- **GELU → SiLU activation swap:** LOSS +13.91% val (57.64), +15.21% test (50.66) (PR #2156, round-35 close). All 4 splits uniform regression 9.3-18.8% (val_single_in_dist worst +18.76%). NEW failure-mode taxon: **architectural-activation degradation** — distinct from 7× averaging-style bimodal and 2× broadcast-scalar prior corruption. Mechanism (student's analysis confirmed): GELU's sharper gating near zero (min≈-0.17 at x≈-0.75) is doing useful work in the L1+bf16+small-hidden regime; SiLU's longer negative tail (min≈-0.28 at x≈-1.28) injects more low-magnitude negative noise into the residual stream which destabilises the converged L1 sign-flip regime. Activation axis closed in the negative direction — GELU dominates SiLU here.
- **RMSNorm replaces LayerNorm at all 3 TransolverBlock sites:** LOSS +2.53% val (51.88), +1.86% test (44.79) (PR #2139, round-37 close, 3rd attempt after #1926/#2034 stale). Per-split: val_single_in_dist −2.08% (in-dist WIN), all 3 OOD splits +2.66 to +6.23% LOSS (val_geom_camber_cruise worst +6.23%). **Unexpected mild bimodal pattern** at the structural-normalization level — distinct from both averaging-style class (8 confirmed) and broadcast-scalar prior corruption (2 confirmed). Mechanism hypothesis: removing LayerNorm's mean-centering improves constant-zero feature directions (single-foil inputs gap=0, stagger=0, AoA-2=0) while making non-zero offset features in camber/Re OOD directions harder to learn — a structural variant of the bimodal pattern. RMSNorm 3-site axis closed. Worth flagging the single-foil-only RMSNorm benefit as paper-level ablation observation.
- **n_layers 5→4 (depth-down probe):** MERGED as PR #2268 (WIN −3.44% val 46.8460, −4.70% test 40.8140). All 4 splits improve. 58/60 epochs at ~31 s/epoch; ~20% per-epoch savings bought 10 extra cosine steps. **Budget-bound regime confirmed:** n_layers=4 with 60 epoch budget dominates n_layers=5 with 50 epoch budget. Combined with LayerScale, advisor now has: n_layers=4 + LayerScale. Depth-down axis continues in-flight (n_layers=3 assigned to nezuko #2289).
- **Translation augmentation σ=0.01 on coord channels 0-1 (per-sample shift):** LOSS +11.5% val (52.20) (PR #2235, round-39 close). Uniform regression all 4 splits — NOT bimodal, NOT broadcast-scalar. val_geom_camber_cruise worst (+9.69%), in-dist least (+1.42%). Translation aug fails because absolute coord statistics are load-bearing for the model — the coord channels 0-1 are NOT physically-neutral, they encode positional information the model uses for routing. Physical symmetry (translation invariance of Navier-Stokes) does NOT transfer to this normalized-coordinate model regime. Input-coordinate augmentation axis closed: per-sample-broadcast scalars (2× prior) + per-point absolute coords (1×) both fail. Training-time input-channel augmentation is consistently harmful.
- **mlp_ratio 2→4 (FFN width 256→512):** LOSS +42.5% val (66.73) (PR #2234, round-39 close). Budget cliff: 34/50 epochs reached (threshold was 38 for cliff diagnosis). +15% per-epoch cost clips off final cosine descent. Architecture likely not broken — timing was wrong given budget-bound finding. Combined with n_layers=4 WIN (which freed ~20% per-epoch), the unified finding is: budget-bound regime means all capacity-ADDING probes lose to wall-clock, while capacity-REDUCING probes win. mlp_ratio=4 at 60-min budget might win.
- **SGD(momentum=0.9, nesterov, lr=2e-3) — optimizer-family swap:** LOSS +62% val (78.64), +61% test (69.08) (PR #2194, round-39 close). All 4 splits uniform-additive regression (+22 to +36 absolute; relative +42% to +76%). SGD did NOT diverge — trained cleanly with warmup-3-cosine, monotone descent ep6→44 from 179.7→78.6, no NaN. Problem is convergence RATE: SGD reaches val ~80 in 44 epochs where AdamW reaches val ~50. ~2× too slow to compete in 30-min budget. Linear extrapolation: ep50 ~70-74 — still 40%+ above baseline. **Mechanism (confirmed):** AdamW's adaptive 2nd-moment rescaling is load-bearing for L1+bf16+Transformer at this scale; even though L1 gradients are mostly ±1 in sign, 2nd-moment tracking of gradient frequency still provides meaningful per-parameter step-size info. **Optimizer-family axis fully closed 6-LOSS-for-6** (lr-UP, lr-DOWN, β1=0.95, β2=0.95, amsgrad, SGD-momentum). 9th distinct failure-mode taxon added: **optimizer-family convergence-rate inadequacy** — uniform-additive regression from non-adaptive optimizer.
- **LayerScale γ=1e-4 (CaiT-style per-channel learnable residual gain):** MERGED as PR #2195 (WIN −2.59% val 48.5160, −1.66% test 42.8162). Uniform 3-of-4 split improvement; **val_geom_camber_rc moved for the FIRST TIME since round-1** (67.44→65.94, −2.22%). Only val_geom_camber_cruise slight +1.95% regression. Best epoch 42 ≠ terminal 43 (first convergence-within-budget in recent rounds). Trained γ diagnostics show non-trivial per-channel structure: MLP branches activated 4-8× stronger than attention; block 3 attn underweighted. Cost: +1,280 params (+0.19%), zero compute/memory overhead. **New baseline: 48.5160 val, 42.8162 test.**
- **n_head=4 → n_head=2 (dim_head 32→64):** MERGED as PR #2173 (WIN −1.57% val 49.8053, −0.97% test 43.5396). Improvements concentrated in val_single_in_dist (−3.44%) and val_geom_camber_cruise (−5.09%); val_geom_camber_rc and val_re_rand washed (+0.1%). Mechanism: wider dim_head increases per-head subspace rank for attending over slice_num=32 compressed tokens. Literature optimum dim_head≈64 (Vaswani 2017, LLaMA) confirmed. n_head=1 (dim_head=128) probe completed as PR #2222.
- **n_head axis CONCAVELY CLOSED at {1, 2, 4}: val_avg = {51.40, 49.81, 50.60}** (PR #2222, round-38 close). n_head=1 (dim_head=128) LOSS +3.19% val (51.3952), +2.73% test (44.7265). Per-split: val_single_in_dist −1.10% (in-dist WIN — single-head competitive on single-foil regime), val_geom_camber_rc +1.28%, val_geom_camber_cruise +11.21% (WORST — most sensitive structural-capacity split), val_re_rand +4.46%. **Pattern is NOT bimodal-averaging** — it's architectural-OOD-sensitive: in-dist holds OR improves, but geometric/Re OOD splits collapse without head-diversity. Mechanism: n_head=1 has full per-head capacity but zero head-rank diversity (single 128-dim attention map); n_head=4 splits into 32-dim subspaces (too narrow per head); n_head=2 (dim_head=64) balances both. **Lead-mover insight (paper-level):** val_geom_camber_cruise was lead WIN in 4→2 (−5.09%) and lead LOSS in 2→1 (+11.21%) — most sensitive structural-capacity dimension in the dataset. Architectural-capacity axis specifically governs cruise-OOD generalization, distinct from camber-OOD which is flat to all attention probes. n_head axis fully closed; n_head=2 confirmed as TRUE INTERIOR OPTIMUM.
- **Huber β=0.1 (smooth-L1) training loss:** LOSS +1.89% val (51.5551) (PR #2174, round-36 close). **8th averaging-style bimodal confirmation**: val_single_in_dist −1.61% (in-dist WIN) while all 3 OOD splits +2.4 to +4.3% (LOSS). The Huber-β family is now closed across β∈{0.1, 0.25, 0.5, 1.0, 2.0} — ALL 5 bimodal vs pure L1 (plus MSE counts as β=∞ bimodal). **Pure L1 is the saturating optimum along the smooth-L1 β axis.** Mechanism: quadratic regime at β=0.1 catches enough normalized training residuals to reintroduce averaging-style drift toward conditional mean → in-dist benefits, OOD suffers. NOTE: this is the LOSS-SOFTENING direction; the LOSS-AMPLIFYING direction (berHu/reverse-Huber) has NOT been tested and is now in-flight as #2223.
- **AdamW amsgrad=True:** LOSS +9.47% val (55.39), +9.52% test (48.15) (PR #2155, round-35 close). All 4 splits uniform regression 5.14-18.45% (val_geom_camber_cruise worst +18.45%). Mechanism (student's analysis confirmed): amsgrad's max-bound is PERMANENT — once a single high-variance gradient inflates v_max, all subsequent steps for that parameter are clamped lower forever, killing early-phase exploration during the high-LR cosine phase. **AdamW optimizer axis now closed 4-LOSS-for-4** in this launch (lr-UP/DOWN/β1/amsgrad) plus β2=0.95 prior closure = 5 axis closures. Defaults (lr=5e-4, β1=0.9, β2=0.999) are locally optimal. NEW failure-mode taxon: **optimizer-statistic over-conservativism** — distinct from momentum-lag (β1=0.95) and averaging-style. Next optimizer direction must leave AdamW entirely (SGD probe assigned as #2194).
- **n_hidden 96→64 (width-down continuation):** LOSS +7.74% val (49.9489) vs branch base 46.3612 / +17.96% vs current baseline 42.3455 (PR #2336, round-44 close). All 4 splits regress UNIFORMLY: in_dist worst (+11.0%), camber_rc least (+3.5%). **Surprise:** in_dist regresses MORE than OOD — opposite of capacity-floor prediction. Diagnosis: TOTAL-capacity floor crossed. At n_hidden=64 + n_layers=4 the model lacks representation width to fit in-distribution training data; underfitting suppresses the in-dist/OOD gap (no excess capacity left to "specialize" on in-dist). Distinct from per-head dim_head=32 floor (#2222 n_head=4) which was head-diversity-bound at fixed width. **Width-down axis CLOSED at n_hidden=96 (the new optimum).** Mechanism: width-down was budget-bound 2-for-2 from 128→96 (−1.04% gain) but capacity-bound 96→64 (+7.74% LOSS). Budget-bound → capacity-bound transition found on width axis (second axis after depth #2268→#2289 in-flight).
- **berHu reverse-Huber c=1.0 (loss-amplification):** LOSS +7.62% val (50.4134) / +18.96% vs current baseline 42.3455 (PR #2316, round-44 close). All 4 splits regress (cruise worst +15.04%, in_dist least +2.95%). Diagnosis (student's analysis): berHu amplification creates self-defeating interaction with cosine cool-down — late epochs need fine LR-controlled gradients, but berHu inflates them on outliers, and outliers include in-dist heavy tails not just OOD. NOT surgically OOD-selective. **Loss-shape axis FULLY CLOSED in both directions** (softening Huber-β family 5 LOSS + amplification berHu 1 LOSS confirmed + MSE β=∞ prior closure = 7 closures). Pure L1 is the GLOBAL loss-function optimum. Unified mechanism: at this dataset scale + cosine schedule, L1's gradient-norm uniformity is load-bearing; any shape change distorts the gradient landscape relative to the LR schedule.
- **slice_num=16 (routing capacity floor):** LOSS +12.02% val (47.4321), cruise worst (+30.21%) (PR #2362, round-45 close). Routing capacity floor at 16 slices. **Slice_num axis FULLY CLOSED with concave optimum at 24.** Bracket: {16, 24, 32} → val {47.43, 42.35, 46.85}. PhysicsAttention needs ≥24 partitions at n_hidden=96/n_layers=4 to route tokens correctly across the 4 geometric/Re regimes.
- **n_layers=3 (depth capacity floor):** LOSS +2.65% val (mean 48.085), in_dist hit hardest (+4.55%) (PR #2289, round-45 close, 3-seed). Depth capacity floor at n_layers=3, identical signature to n_hidden=64 floor. **Depth-down axis CLOSED at n_layers=4.** Budget-bound→capacity-bound transition on BOTH depth (floor at 3) and width (floor at 64) axes simultaneously.
- **n_layers=4 --epochs 90 (schedule mis-alignment):** LOSS +2.41% val (47.9738) vs --epochs 60 baseline (PR #2298, round-45 close). Wall-clock cap delivers 58 epochs regardless of --epochs flag; T_max=87 creates under-cooled cosine (LR=1.66e-4 at cutoff vs ~0). **META-FINDING:** T_max must match ACTUAL expected epoch count, not --epochs flag value.
- **surf_weight 10→20 (gradient rebalancing):** LOSS +12.98% val (47.8434), in_dist worst (+22.08%), camber_rc least (+5.48%) (PR #2394, round-46 close). UNIFORM REGRESSION all 4 splits. BOTH surf AND vol metrics regressed — destabilization, NOT gradient re-weighting. Combined with earlier surf_weight=30 LOSS (under heavier config), **surf_weight axis FULLY CLOSED: bracket {10, 20, 30} → optimum at 10 (current default).** Mechanism: increasing surf_weight above 10 destabilizes the carefully balanced gradient mix in the cosine cool-down phase; the heavy-tailed L1 norm for surface nodes is more sensitive to weight changes than the vol nodes. Surf_weight=10 is the global optimum in this launch.
- **Squared ReLU (ReLU²) activation at all 3 MLP sites:** LOSS +7.76% val (45.6311), +1.46% test (39.0696) (PR #2418, round-47 close). BIMODAL in SURPRISING DIRECTION: val_geom_camber_rc −2.50% WIN (only split), in_dist +21.18% WORST LOSS, cruise +12.30%, re_rand +8.27%. Run hit wall-clock cap at 60/70 epochs; converged val ~45.0-45.3 (still +6.5% LOSS). Mechanism: ReLU²'s zero negative tail + quadratic positive growth: (a) quadratic gradients amplify large pressure peaks → rc WIN; (b) zero negative-tail gating starves the negative pre-activation patterns GELU handles → in-dist WORST LOSS; (c) GELU's near-linear positive curve is load-bearing for moderate-magnitude residuals → cruise LOSS. **Activation axis FULLY CLOSED at GELU in both directions** (SiLU=negative-tail LOSS + ReLU²=zero-negative LOSS). 11th taxon: activation-curve over-specialization. KEY INSIGHT: rc-WIN-only under ReLU² is FIRST evidence that selective gradient amplification on large pressure peaks could help camber_rc specifically.
- **SAM optimizer ρ=0.05 (meta-optimizer):** LOSS +129.6% val (97.1961), +130.4% test (88.7014) (PR #2412, round-47 close). Catastrophic uniform regression all 4 splits: in_dist +199.6% WORST (opposite of flat-minima-helps-OOD prediction). Val PLATEAUED at ep32 → clean convergence to WORSE basin, NOT budget-starved. Compile cached second forward → actual cost +14% (not predicted 2×). Causes: (1) ρ=0.05 exceeds flat-region radius for CFD surrogate gradients; (2) small batch (4) + L1 + bf16 makes mini-batch gradient too noisy for SAM to identify true worst-case direction; (3) in-dist regression largest = active gradient interference, not flatness trade. **Flat-minima / meta-optimizer axis CLOSED.** 12th taxon: meta-optimizer perturbation-noise interference. AdamW hyperparam axis (4-LOSS) + SGD (1-LOSS) + SAM (1-LOSS) = optimizer-family fully closed.
- **mlp_ratio 2→1 (FFN width contraction):** LOSS +17.4% val (49.7302), +13.2% test (43.5735) (PR #2392, round-47 close). Uniform regression all 4 splits: cruise +29.5% WORST, in_dist +23.6%. 75/80 epochs terminal-best; clean convergence with ~25% per-epoch savings — but FFN capacity floor crossed. At mlp_ratio=1, each block's FFN is Linear(96→96)→GELU→Linear(96→96) with no higher-dim non-linear bottleneck. **mlp_ratio axis FULLY CLOSED: bracket {1,2,4}→val{49.73,42.35,66.73}; concave optimum at 2.** META-FINDING: all 4 budget-bound axes are NOW AT THEIR OPTIMUM — n_layers=4, n_hidden=96, slice_num=24, mlp_ratio=2. Capacity-budget Pareto front fully mapped. Future gains MUST come from non-budget axes. 13th distinct closed-axis: FFN-expansion-ratio downward.
- **FiLM conditioning on broadcast scalars (per-block affine modulation):** LOSS +9.61% val (46.4166), +5.65% test (40.6822) (PR #2410, round-48 close). Uniform regression all 4 splits: in_dist +11.93%, cruise +12.00%, re_rand +8.79%, camber_rc +7.80% (primary target also regressed). 70/70 epochs, best=67, clean convergence. Trained γ converged AWAY from identity (0.66-1.03 range); β near-zero mean but σ up to 0.33. Mechanism: **double-affine compounding on heavily damped residual stream** — FiLM perturbs the LN-1 input with non-unit-variance; LayerScale γ_attn≈1e-3 damps BRANCH OUTPUT but NOT BRANCH INPUT, so β propagates additively without damping; attention weights expect LN outputs but get FiLM-perturbed inputs. +38K params = capacity wrong direction for budget-bound regime. **14th taxon: residual-stream-input-perturbation conditioning** — distinct from FiLM-on-output (gating AFTER branch output, inside LayerScale damping path, could work differently). **Conditioning-pathway axis closed at "FiLM-on-pre-LN-input" position for n_hidden=96/LayerScale configuration.** Future conditioning probes should apply AFTER attention output AND inside LayerScale damping path.
- **DropPath p_max=0.1 (block-level stochastic depth):** ABANDONED axis after 4-attempt pod-stall jinx (PR #2280, round-44 close). PRs #1976, #2083, #2179, #2280 all stalled at the pod-level (~6h no commits each); 4 consecutive same-pattern failures suggests a persistent interaction (possibly torch.compile graph capture with Bernoulli dynamic shapes, or memory aliasing). ~24h pod-time spent with no committed result. Compounding factors: baseline drift -12.78% since hypothesis written (48.5160 → 42.3455); LayerScale subsumption hypothesis (per-channel γ may already provide much of the block-level regularization DropPath was supposed to add). DropPath axis is closed in this launch.
- **WD=5e-5 (50% reduction from 1e-4):** LOSS +9.41% val (46.3290) (PR #1775, round-49 close). UNIFORM REGIME-FLIP: all 4 splits regress 5-15%; **WD direction sign-flipped** from prior β=0.5 baseline where same change was WIN −4.43%. Mechanism: current L1+slice_num=24+n_hidden=96+n_layers=4+LayerScale stack is in "well-trained, needs more penalty" regime — opposite of under-fit β=0.5 era. 70/70 epochs terminal-best (no budget cut). **15th distinct closed-axis: regime-flip sign reversal of WD direction across stack changes** — confirms that hypothesis-baseline coupling matters; same numerical knob change can flip sign across baselines.
- **Per-parameter-group LR with embed=2× UP:** LOSS +15.1% val (48.7460) (PR #2457, round-49 close). UNIFORM DESTABILIZATION all 4 splits (in_dist worst +20.43%, camber_rc least +11.69%). Mechanism: 2× LR on shared-token embedding destabilizes shared-token representation upstream of all 4 blocks — blocks re-fit a moving input distribution for all 70 epochs; γ growth NOT bottleneck (same trained-γ equilibrium reached regardless of LR-γ acceleration). **16th closed-axis: per-parameter-group-LR-scaling with embed-acceleration**; note: layer-wise LR DECAY downward is orthogonal and was tested in-flight as #2497.
- **MLP-output-dropout p=0.05 (after fc2, inside LayerScale gate):** LOSS +10.69% val (46.8749) (PR #2455, round-50 close). UNIFORM REGRESSION all 4 splits 5.4-20.4%; val_geom_camber_cruise worst +20.41%. 70/70 terminal-best. **3rd stochastic-regularization closure** alongside attention-weight-dropout p=0.1 (PR #1571 close) and abandoned DropPath (PR #2280). Mechanism: convergence-cost > regularization-benefit in 30-min cap regime; LayerScale γ subsumes the FFN-branch gating role that any beneficial dropout would provide — explicit + implicit stochastic gating compound destructively. **17th closed taxon: convergence-floor regime closes the stochastic-regularization class.** ALL three structurally distinct dropout/dropping mechanisms now LOSS in this launch.
- **Whole-corpus AoA+y reflection augmentation (prob=0.5):** CATASTROPHIC LOSS +31.1% val (55.5301) (PR #2454, round-50 close). UNIFORM REGRESSION all 4 splits 22.6-45.2%; val_single_in_dist worst +45.2%. Epoch 1 val_avg=363 (3.5× normal). Mechanism: 68% of train corpus has half-space mesh y ∈ [0, ~10] with ground plane — reflection y→-y moves the entire raceCar mesh into NEGATIVE y space the model never sees in clean data; physical reflection symmetry of incompressible NS is BROKEN by the half-space mesh structure. **18th closed taxon: whole-corpus-reflection physical-symmetry hypothesis falsified by mesh-half-space asymmetry.** Mesh-domain pre-filtering would be required for reflection to be valid (raceCar subset only); whole-corpus application is structurally invalid. Companion to translation aug closure (#2235) — both physical-symmetry levers fail because the dataset's coord statistics encode load-bearing structure.
- **GeGLU FFN (gate × GELU(value) at all MLP sites, Shazeer 2020):** LOSS +4.62% val (44.3006), test_avg near-wash +0.93% (PR #2439, round-51 close). UNIFORM REGRESSION all 4 splits 1.39-8.51%; val_geom_camber_cruise worst (+8.51%). 53/55 epochs (GPU-contention timeout cut 2 epochs short — but student's own analysis: clean compute would still be ~+4% LOSS). +90K params (~+27% over ~330K base). Mechanism: at the capacity floor where depth/width/slice_num/mlp_ratio all sit at closed optima, +27% params via gating dilutes per-param gradient signal — same lesson as mlp_ratio=4 LOSS (+42.5%) and n_hidden=128 LOSS (-2.5%); gating doesn't unlock new representational capacity, just adds dimensions for the optimizer to overfit. Test-avg near-wash + val per-split uniform UP → val noise amplification confirmed but trend direction unambiguous. **19th closed taxon: FFN-structure gating axis closes in budget-bound regime.** ALL FFN axes now mapped: {gate=LOSS, activation=GELU optimum, mlp_ratio=2 optimum (concave at {1,2,4}={49.7,42.3,66.7}), width-down=open at 96}. FFN architecture probes exhausted at the current stack.
- **Layer-wise LR Decay (LLRD) decay=0.7^k (output→input):** LOSS +12.79% val (47.7593), +7.58% test (41.4230) (PR #2497, round-52 close). UNIFORM REGRESSION all 4 splits 8.21-18.55%; val_single_in_dist worst (+18.55%), val_geom_camber_rc least (+8.21%). 70/70 epochs reached, best=terminal=ep70 — monotonic descent, convergence-bound NOT plateaued. **γ_mlp depth gradient at terminal:** block_0=0.008, block_3=0.050 (steep ramp vs uniform-LR baseline ~0.063-0.078 across all blocks per PR #2195) — confirms early-block under-training mechanism. Per-group LR ratio preservation verified through LinearLR+CosineAnnealingLR (no implementation bug). Mechanism: decay=0.7^4 = 0.24× preprocess LR + 0.343× block_0 LR starves early-layer optimizer work below the 70-epoch convergence floor. The 4-block-too-shallow wash prediction was FALSIFIED. **20th distinct closed-axis taxon: layer-wise-LR-decay-downward in budget-bound regime.** Combined with #2457 (embed=2× UP destabilization), the **per-parameter-group-LR axis is FULLY CLOSED in BOTH directions** — uniform-LR AdamW is the local optimum on the optimizer-group axis. 8 total optimizer-family closures in this launch (AdamW lr-UP/DOWN/β1/amsgrad + SGD + SAM + embed-UP + LLRD-DOWN).
- **PhysicsAttention temperature init τ=0.25 (sharper-than-default 0.5):** LOSS +7.25% val (45.4153), +2.07% test (39.3021) (PR #2502, round-53 close). UNIFORM REGRESSION all 4 val splits 0.99-13.20%; val_single_in_dist worst (+13.20%), val_geom_camber_rc near-flat (+0.99%). 70/70 epochs, best=ep69 (still descending; not undertrained). **CRITICAL trained-τ diagnostic:** terminal τ mean = 0.297 (block_0=0.286 → block_3=0.323, std=0.022 across 8 head positions) — optimizer drifted τ UP from init 0.25 (+18.7% relative). Per-head std ≤ 0.035 (no head specialization on τ axis); deeper-block tilt upward (block_3 prefers softer). **INTERESTING:** test_geom_camber_rc slight WIN (−1.65%) — the ONLY positive movement. Combined with #2418 ReLU² (rc WIN −2.50% only), this is the **2nd confirmation that selective sharpening interventions help camber_rc OOD specifically**, even though they cause uniform regression overall. Mechanism: hard-commit routing at init forces tokens into mostly-noise slots when slice prototypes are random; optimizer back-pedals τ upward but the resulting representations never fully recover diversity. **21st closed taxon: routing-sharpness over-commitment at warm-start.** Routing-sharpness axis essentially saturated by slice_num=24 + L1 + LayerScale stack — partition COUNT optimum at 24, partition softmax TEMPERATURE optimum at ~0.5; further routing-axis probes unlikely to move the needle.

## Open questions / next experiments

1. **OOD generalization** — primary bottleneck. `val_geom_camber_rc` (67.37) barely moved with warmup (-0.11%); `val_re_rand` (52.75) improved slightly (-1.9%). OOD splits are not schedule-sensitive; they require structural interventions.
   - **Coord-jitter (#1921) CLOSED (LOSS):** -13.7% in-dist, all OOD splits degraded.
   - **EMA (decay=0.999, #1946) CLOSED (WASH-TO-LOSS):** -12.9% in-dist, OOD +5-14%.
   - **Grad-clip (#1653) CLOSED (WASH):** -13.3% in-dist, OOD +3-5%.
   - **lr-DOWN (#1997) CLOSED (LOSS):** -6.45 in-dist, OOD +4.84 to +5.74.
   - **Lookahead k=5 α=0.5 (#2051) CLOSED (LOSS):** -9.48% in-dist, OOD +5.92-18.0%.
   - **Warmup-5 (#2071) CLOSED (WASH+bimodal):** -2.60% in-dist, re_rand +2.81%.
   - **fun-jitter Re/AoA (#1988) CLOSED (LOSS, 2 σ-points):** σ=0.05 +19.5% LOSS, σ=0.025 +14.6% LOSS; OOD damage NEAR-SATURATED in σ (0.82-0.91× attenuation when σ halved, far slower than linear); in-dist Goldilocks zone at small σ. **7th averaging confirmation** at channel-level.
   - **In-dist headroom is unlockable but does NOT translate to OOD (7× confirmed).** Averaging-style regularization (coord-jitter, weight-EMA, grad-clip, lr-DOWN, Lookahead) + settling-phase reduction (warmup-5) + channel-level jitter (fun-jitter) consistently give in-dist win but regress OOD. This is now a rock-solid empirical law across 7 mechanisms (data aug, weight aug, gradient aug, step-size aug, trajectory aug, schedule aug, channel aug). **Mechanism-agnostic finding:** any reduction in effective late-stage exploration trades in-dist for OOD. **The next OOD-targeting experiments must NOT be averaging-style** — needs physical-symmetry data augmentation (translation, reflection), structural input augmentation (NACA #2072, gap/stagger #2114), or architectural change (RMSNorm #2139, multi-task aux loss, conditional embedding).
   - **Active OOD interventions:** NACA geometry jitter (#2072 edward) — directly targets camber distribution shift; Lookahead optimizer (#2051 askeladd) — flat-minima bias hypothesis; fun-jitter Re/AoA σ=0.025 (#1988 nezuko) — function space augmentation.
   - **Untried structural OOD interventions:** domain conditional embedding (architectural), AoA reflection symmetry, multi-task camber prediction auxiliary loss.
2. **Normalization** — RMSNorm in flight (#1926). Pre-LN vs Post-LN placement also untested.
3. **Proven-lever stack on new baseline** — grad-clip (#1653), WD=5e-5 (#1775) all need rebasing to current 54.00 baseline. β2 axis is now closed (PR #1845).
4. **Schedule** — SGDR in flight (#1905). Warm restarts may unlock multi-descent gains.
5. **Lookahead optimizer** — **in-flight #2051 askeladd.** Standard k=5, α=0.5 recipe. Hypothesis: flat-minima bias from slow-weight averaging should improve OOD (inverse of grad-clip's bimodal).
6. **Warmup duration** — **in-flight #2071 thorfinn.** Warmup-3 won massively (-6.31%); warmup-5 probes whether longer basin exploration beats it or shorter settling phase hurts.
7. **NACA geometry jitter** — **in-flight #2072 edward.** σ=0.01 on NACA channels 15-17, 19-21 during training. First geometry-level augmentation targeting camber distribution shift (val_geom_camber_rc).
6. **Stochastic depth (DropPath)** — **in-flight #2083 tanjiro (retry).** Block-level residual regularization; structurally distinct from closed averaging-style class. Untested.
7. **AdamW β1=0.95 (UP probe)** — **in-flight #2093 alphonse.** Single-knob optimizer probe on novel axis. β2 closed (#1845, +15% LOSS); β1 never probed. β1=0.9→0.95 lengthens gradient-momentum half-life from ~10 to ~14 steps, providing smoothing for L1's sign-gradient noise.
8. **Warmup-2-cosine (OOD-favorable bracket)** — **in-flight #2112 thorfinn.** Completes the warmup duration bracket (warmup-3 win → warmup-5 wash → warmup-2 probe). Predicted to swap the signs on the bimodal: in-dist slight regress, OOD slight gain via 1 extra settling epoch. If symmetric trade-off, val_avg ~50.4-50.5.
9. **Gap/stagger jitter σ=0.02 (channels 22-23)** — **in-flight #2114 askeladd.** Tandem-foil arrangement augmentation on the last untouched geometric input channels. Structurally distinct from closed averaging-style class. Complements in-flight NACA jitter (#2072) — together they cover the complete geometry input space.
10. **Translation augmentation σ=0.01 (channels 0-1, per-sample shift)** — **in-flight #2138 nezuko.** Physical-symmetry lever (NOT averaging-style). Per-sample rigid shift preserves foil shape exactly; exploits translation invariance of Navier-Stokes. **Critical diagnostic:** if this exhibits the bimodal signature, the 7× pattern generalizes to physical-symmetry augmentation too. If neutral or uniform improvement, we've found the first lever that breaks the bimodal.
11. **RMSNorm at all 3 sites (retry-3)** — **in-flight #2139 frieren.** 3rd attempt after 2 stale PRs (#1926, #2034) at pod-level. Pre-LN architectural variant; expected to NOT show bimodal signature since layer-level not sample-level. Modest expected gain.
12. **AdamW amsgrad=True (max-tracking 2nd-moment)** — **in-flight #2155 alphonse.** Novel optimizer-stability axis after AdamW (lr, β1, β2) closed 4-LOSS-for-4. Tracks MAX of 2nd-moment EMA instead of EMA itself; theoretically robust to L1 sign-flip noise. Distinct from prior optimizer levers — changes the **statistic** used for adaptive step bound, not magnitude/smoothing/variance-rate. Expected uniform per-split delta (positive or neutral).
13. **GELU→SiLU activation swap** — **in-flight #2156 askeladd.** Architectural probe at activation level, NOT averaging-style and NOT broadcast-scalar. Replaces GELU with SiLU at all 3 MLP sites (preprocess + TransolverBlock MLP + last_layer mlp2). Consistent literature support for 0.3-1% gain on Transformer benchmarks. Expected uniform per-split delta — if bimodal, surprising mechanism. Low-risk simple win.
14. **n_head 4→2 (dim_head 32→64)** — **in-flight #2173 thorfinn.** Architectural probe: attention head count. Literature optimum is dim_head=64; current 32 is meaningfully under-sized. Single-line model_config change, computationally neutral (same total FLOPs, just reshaped). Distinct from all closed axes.
15. **Huber β=0.1 (smooth-L1 training loss)** — **in-flight #2174 edward.** Loss-function probe: addresses L1 sign-flip noise at the loss level (orthogonal to amsgrad optimizer fix in #2155). Quadratic-near-zero (|residual|<0.1), L1-elsewhere — preserves 90%+ of L1's character but eliminates sign-flip in the near-converged regime. Expected uniform per-split delta. If 8th bimodal, β=0.1 is large enough to act as soft L2.
16. **SGD with Nesterov momentum (lr=2e-3, momentum=0.9)** — **in-flight #2194 alphonse.** Optimizer-FAMILY swap from AdamW after AdamW axis closed 4-LOSS-for-4. Distinct from any β-tweak or 2nd-moment statistic change. High uncertainty: may diverge in first epoch OR may train cleanly and match/beat AdamW. Informative either way — establishes whether AdamW's adaptive mechanism is load-bearing for L1+bf16 Transformer training.
17. **LayerScale init=1e-4 (CaiT-style learnable per-channel residual gain)** — **in-flight #2195 askeladd.** Architectural rescaling sub-class: per-branch γ ∈ R^hidden_dim initialized to 1e-4 multiplies each residual branch output before adding to the residual stream. Distinct from activation (closed), DropPath (in-flight #2179), and RMSNorm (in-flight #2139). 1280 added params (~0.2% increase). Expected uniform per-split delta.
18. **n_head=1 (dim_head=128)** — **in-flight #2222 thorfinn.** Closes the n_head axis at the single-head endpoint: dim_head=128 = full hidden dimension per head. n_head=4 (dim_head=32) → n_head=2 (dim_head=64) was WIN −1.57%; this tests whether the monotone improvement continues. Either outcome (win or loss) definitively closes the axis.
19. **berHu reverse-Huber c=1.0** — **in-flight #2223 edward.** First probe of the LOSS-AMPLIFYING direction (vs all prior LOSS-SOFTENING probes in the bimodal class). BerHu = pure L1 for |r| ≤ c (identical to baseline near zero — no softening), quadratic-amplified for |r| > c (stronger gradient than L1 for large OOD-type residuals). Directly targets val_geom_camber_rc and val_re_rand which have been flat since round 1. Mechanistically distinct from ALL 8 bimodal-class closures.
20. **mlp_ratio 2→4 (FFN width expansion)** — **in-flight #2234 frieren.** Standard transformer FFN sizing (Vaswani 2017, BERT, GPT, ViT all use ratio=4) never tested at ratio=4 in this launch. Each block MLP: [128 → 256 → 128] → [128 → 512 → 128]. +50% params (708K → 1.04M). Complementary capacity axis to n_head=2 WIN (attention vs FFN — different transforms, may stack orthogonally). Budget impact uncertain (~10-15% per-epoch slowdown); diagnostic key on val_geom_camber_rc which has been flat since round 1.
21. **Translation augmentation σ=0.01 — retry-2** — **in-flight #2235 nezuko.** Reassignment of #2138 (stale ~6h, 1st stale). Same hypothesis: per-sample rigid translation of mesh coords (channels 0-1) preserving foil shape exactly. Physical-symmetry data augmentation — structurally distinct from ALL 8 averaging-style bimodal closures AND from 2 broadcast-scalar prior corruption closures. Critical diagnostic: if bimodal, the pattern generalizes to physical-symmetry too; if uniform improvement, FIRST lever that breaks the bimodal trade-off.
22. **n_layers 5→4 (depth-down probe)** — **in-flight #2268 thorfinn.** Pure architectural reduction never tested in this launch (n_layers=6 ADD-depth was prior closure). Complementary to in-flight #2234 mlp_ratio=4 ADD-capacity — together they probe "deep + narrow" vs "shallow + wide" axis. Single-line change (n_layers=5 → 4) + bumped --epochs 50→60 to use the ~20% per-epoch wall-clock savings. Param count drops 708K → 575K (~−19%). **Budget-bound vs capacity-saturated diagnostic** with high info value either direction: win → model is budget-bound, depth reduction trades small capacity for ~10 extra epochs; loss → 5 layers needed for multi-step geometric reasoning through PhysicsAttention. Watch val_geom_camber_cruise (lead-mover in n_head sweep).
23. **LayerScale asymmetric init (gamma_attn=1e-4, gamma_mlp=1e-3)** — **in-flight #2272 askeladd.** Data-driven follow-up from PR #2195 trained γ diagnostics showing MLP branches converge to 4-8× larger γ than attention. Asymmetric init reduces "branches off" ramp-up phase for MLP branches that need 250-500× growth from 1e-4. Expected ~5-10 extra productive epochs within the 30-min budget. Distinct from all closed/in-flight axes.
24. **DropPath p_max=0.1 retry-4 (linear depth schedule on LayerScale-gated branches)** — **in-flight #2280 tanjiro.** 4th attempt at DropPath hypothesis (#1976/#2083/#2179 all stale pod-stall pattern). Same hypothesis: block-level stochastic depth applied to residual branches. Now stacks with LayerScale γ (newly on advisor) — DropPath masks the LayerScale-gated branches at sample granularity. Distinct from per-channel γ (per-element) and per-weight noise (averaging-style). Pod-unstick attempt.
26. **n_layers 4→3 (depth-down continuation)** — **in-flight #2289 nezuko.** Direct continuation of #2268 WIN. Tests whether budget-bound regime continues at depth=3 or capacity floor is hit. ~24-26 s/epoch, --epochs 75, ~60-65 actual epochs in 30 min. Param count ~577K → ~450K. All-splits-improve signature confirms budget-gain; OOD-splits-suffer signature confirms capacity floor. Closed at depth=3 either way.
27. **n_hidden 128→96 (width-down probe)** — **in-flight #2290 frieren.** Orthogonal to depth-down axis (#2289). Hidden dimension reduction: n_hidden² scaling gives ~40-45% per-epoch savings. ~24-26 s/epoch, --epochs 90, ~65-70 actual epochs. Param count ~330K. Tests width-bound vs capacity-bound independently of depth; closes width-down axis. Complementary to depth-down: together they map the budget-capacity Pareto front.
25. **Squared ReLU (ReLU²) activation at all 3 MLP sites** — **in-flight #2283 alphonse.** Primer-style sharp activation from So et al. 2021. Tests opposite direction of closed SiLU (negative-tail) LOSS: ReLU² has zero negative tail and quadratic positive growth. If activation axis closes at GELU symmetrically (sharper LOSS too), confirms GELU is optimum. If wins, novel activation lever and Primer-style benefit replicates here. Distinct from in-flight architectural axes (LayerScale, DropPath, mlp_ratio, n_layers).
